# Exists so G15 has a *declared* parameter type to check against. The
# argument-type check reads RBS/RBI only -- Ruby source declares no
# parameter types -- so without a project `sig/` there is nothing for it
# to be right or wrong about.
class ArgumentProbe
  def resize: (Integer size) -> void
end

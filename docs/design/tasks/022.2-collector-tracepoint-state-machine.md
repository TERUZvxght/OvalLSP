# Task 022.2 (extended): Observation::Collector TracePoint state machine

Companion note, written after independent review rounds 20–22 each found a
genuine correctness bug in `Ovallsp::Observation::Collector`'s TracePoint
handling, with every fix revealing that the *previous* fix's premise was
still wrong (round 20: unbalanced push/pop across non-workspace callees;
round 21: an unwound frame's fabricated `nil` return; round 22: `:raise`
itself does not mean a frame ended). That pattern — three consecutive
rounds, same file, each correcting the last one's assumption — was treated
as evidence the design itself, not just its edge-case coverage, needed
re-examining before another incremental patch went in.

This note is the state model, transition table, and invariants that came
out of that exercise, and the reasoning for why the fix is a structural
extraction (`CallStackMachine`) rather than one more conditional in
`Collector` itself.

## Why a separate state machine

Before this pass, `Collector` mixed three concerns in one class: translating
real `TracePoint` events into abstract call/return/raise transitions,
looking up and caching per-method metadata (symbol_id, fingerprint,
parameter names), and aggregating recorded evidence. The actual bug surface
in rounds 20–22 was entirely in the first concern — a handful of methods
(`handle_call`, `handle_return`, `handle_raise`, `unwind_to`,
`return_type_for`) implementing an ad hoc stack machine, verified only by
constructing specific Ruby call shapes and running them through the real
`TracePoint` API.

That made every fix expensive to verify (spin up real Ruby processes/
threads/fibers, trigger an actual CRuby event, read the aggregate) and
impossible to fuzz (no way to hand the "stack machine" a synthetic event
sequence directly). `Ovallsp::Observation::CallStackMachine`
(`core/lib/ovallsp/observation/call_stack_machine.rb`) is that first
concern pulled out whole: a plain Ruby object with three inputs — `#push`,
`#pop_matching`, `#note_raise` — no `TracePoint`, no threads, no I/O, so
its invariants can be checked by feeding it a sequence of symbolic events
directly (`spec/ovallsp/observation/call_stack_machine_spec.rb`), including
generated/fuzzed sequences that never construct real Ruby call shapes at
all.

`Collector` is now purely the glue: one `CallStackMachine` per fiber
(fiber-local, same reasoning as before — see the machine's own docs),
translating `tp.event` into the machine's three inputs, plus the parts
that were never in question — method-info caching, symbol/fingerprint
computation, and aggregate recording.

## Invariants the state machine must hold

These are the properties `call_stack_machine_spec.rb` pins directly, independent
of any particular Ruby call shape:

- **I1 — One machine per fiber.** A machine's stack is reachable only from
  the fiber that pushed onto it. `Collector` enforces this by storing one
  machine per fiber in fiber-local storage; the machine itself has no
  thread/fiber awareness at all (it doesn't need any — see I8).
- **I2 — `#push` never raises and always succeeds.** Every observed
  `:call`, tracked or not, becomes exactly one `#push`. A method outside
  the workspace still gets a frame (with a caller-supplied "not tracked"
  payload) so the stack stays balanced; only *recording* is skipped later,
  not pushing.
- **I3 — `#pop_matching` closes the frame on *top* of the stack, and only
  when its key matches.** A `:return` can only ever be about the
  *innermost* live frame: CRuby delivers exactly one `:return` per frame,
  in strict inner-to-outer order — recursion included, multi-frame
  unwinding included (row 7) — so nothing is ever left stacked above a
  frame when its own return arrives. If the top frame is not the one
  returning, then no frame the machine holds has ended, and closing one
  would be a guess.

  Corrected in round 30. This invariant previously read "finds the
  *nearest* frame whose key matches, and discards everything above the
  match", justified as defensive generality (I7) that real delivery never
  exercises. It was not defensive — see row 11a and the round-30 section
  below: CRuby really does deliver a mid-stack `:return` for a frame it
  never announced a `:call` for, and when that stray key was also live
  further down, the scan answered with the outer, still-running frame and
  silently destroyed every live frame above it.
- **I4 — `#pop_matching` whose key does not match the top of the stack is
  a no-op** that returns `nil` and leaves the stack untouched, rather than
  falling back to "pop whatever's on top anyway" (or to "scan for it
  deeper down"). This is the one case a bare pop gets wrong, and it is the
  mechanism round 20's shape actually depends on today — see "Which
  mechanism is load-bearing" below, which corrects an earlier version of
  this note that credited I2 instead.
- **I5 — `#note_raise` never touches the stack.** It only advances a
  monotonic per-machine `raise_epoch` counter. A raise is evidence that
  *some* frame *might* be abandoned later, not that any particular frame
  has ended — round 22's finding was that treating `:raise` as a close-out
  event is simply wrong, because CRuby fires `:raise` at the frame the
  exception originated in whether or not that frame ever unwinds.
- **I6 — A popped frame records the `raise_epoch` value at push time**, so
  the caller (`Collector#return_type_for`) can tell a frame that was
  abandoned by an exception (its epoch is stale relative to the *current*
  epoch at pop time) from one that returned normally (epoch unchanged).
  This is the only use `raise_epoch` has; the machine does not interpret
  it itself.
- **I7 — The machine never raises, for any input.** A caller feeding it
  malformed or unexpected event sequences (see the transition table's
  "unmatched return" and "return depth exceeds push depth" rows) gets a
  well-defined answer (usually `nil`), never an exception. `Collector`'s
  own `rescue StandardError` around the whole `handle` dispatch is a
  second, independent backstop — the machine itself does not depend on it.
- **I8 — The machine has no notion of time, threads, or process
  lifecycle.** It does not clean anything up on its own; a machine (and
  whatever frames are still on it) is simply garbage the moment nothing
  references it any more (the fiber that owned it has died). This is
  deliberate: rounds 17/18 already established, for `AgentProcessManager`'s
  registry, that "add a pruning mechanism" is the wrong fix when "hold
  nothing that isn't already needed" is available instead. See
  `Collector#current_stack`'s own docs for why fiber-local storage gives
  this for free.

## Which mechanism is load-bearing (review round 23 correction)

An earlier version of this note asserted that I2 ("push a frame for
*every* `:call`") is what fixes round 20, and that matching-by-identity
(I3/I4) is load-bearing only for table row 11's calls-already-in-flight.
Re-measured directly, by reverting each mechanism separately and running
`collector_spec.rb`, both halves of that were wrong:

| Reverted | `collector_spec.rb` result |
|---|---|
| `#pop_matching` → bare `@stack.pop`, I2 intact | **green** (23/23) |
| I2 → push only tracked calls, matching intact | **green** (23/23) |
| Both (= round 20's original code) | 3 failures, incl. both round-20 examples |

Re-measured in round 24 against the enlarged file (now 28 examples), with
the round-23 example in place: bare pop → **1** failure (that example, and
only it); I2 reverted → still **green**; both → 4 failures. So the gap is
closed for `#pop_matching`, and I2's status is unchanged: it remains
removable with the suite green, deliberately, because with matching correct
it is genuinely redundant rather than merely untested. An untracked key is
untracked *deterministically* (`compute_method_info` is a memoized function
of `[defined_class, method_id]`, so no key is ever both tracked and
untracked in one run), which means an unmatched `:return` can never collide
with a tracked frame's key and a missing untracked push therefore cannot
misattribute anything. I2's remaining value is bounding `#pop_matching`'s
scan and I7's defence in depth, not correctness.

So the two mechanisms are *mutually redundant* for every shape the suite
covered, and neither was independently pinned. Row 11 does not close the
gap either: a call already in flight when `#start` runs is strictly
*outermost*, so its stray `:return` arrives when everything pushed after
`#start` has already been popped — a bare pop finds an empty stack and is
harmlessly a no-op.

The shape that genuinely requires matching is a push `Collector`
**skipped**: `#handle_call` can raise before reaching `#push`
(`symbol_id_for` reads `defined_class.name`, and real code — ORMs,
proxy/delegator gems — does override `name`), and `#handle`'s blanket
rescue swallows it, while `#handle_return` pops unconditionally. The
unmatched `:return` then arrives in the *middle* of a live stack, and a
bare pop answers it with the caller's frame — recording the callee's
return value against the caller's symbol_id, which is round 20's bug
verbatim. Measured on a fixture whose callee returns Integer and whose
caller returns String: bare pop reports the caller as returning Integer.

Pinned by `collector_spec.rb`'s "keeps a live frame when a `:return`
arrives for a call that was never pushed", which fails with a bare pop
and passes with matching. I2 is kept as deliberate defense in depth
(I7's spirit), not as the load-bearing half — a note worth keeping,
because this class's own docs invite simplifying `#pop_matching` to a
bare pop on the grounds that it gets rows 1–10 right.

## TracePoint event / state transition table

Every row is a case this pass explicitly constructed and observed on Ruby
3.4.7 (CRuby), or is a `CallStackMachine`-level abstract case verified
without going through real `TracePoint` at all. "Owner" is which piece of
code is responsible for the row holding: the machine itself (structural,
pinned by `call_stack_machine_spec.rb`), or `Collector`'s translation logic
(pinned by `collector_spec.rb`).

| # | Event / situation | CRuby behavior (verified) | State transition | Owner |
|---|---|---|---|---|
| 1 | Ordinary method call → return | `:call` then `:return`, `return_value` = the real value | push on `:call`; matching pop on `:return`, real value trusted | Machine (push/pop) + Collector (value trust) |
| 2 | Nested calls (`a` calls `b` calls `c`) | `call a, call b, call c, return c, return b, return a` | Each `:return` finds its match already at the top — a bare LIFO pop would get this right too (see I3's note); matching-by-identity doesn't need to scan here | Machine |
| 3 | Recursion (direct: `f` calls `f`) | Each recursive `:call` pushes its own frame with the same key; each `:return` closes the innermost live one, in the same strict LIFO order as row 2 | Same as row 2 — the innermost invocation is always already on top when its `:return` arrives, so matching by identity and a bare LIFO pop agree; verified with real direct/mutual/`send`-based recursion (round 21) precisely to confirm this, not to exercise scanning | Machine |
| 4 | Mutual recursion (`a`→`b`→`a`) | Same as above, different keys interleaved | Same as row 3 — no cross-talk between the two keys' entries, and no scan needed | Machine |
| 5 | `b_call`/`b_return` (plain block yield/return) | **Not traced.** `Collector`'s `TracePoint.new` only requests `:call, :return, :raise`. A block invocation produces no event at all under this filter | No transition; the block's own body's *method* calls still fire normally | Deliberate scope limit (overhead budget), documented in `Collector` |
| 6 | `c_call`/`c_return` (C-implemented methods) | **Not traced**, same filter. Verified: a method whose entire body is C calls (`@name.freeze.upcase.to_sym`) still fires its own `:call`/`:return` correctly — the *absence* of C-method events cannot desync the stack, because nothing pushes for them in the first place | No transition for the C call; the enclosing Ruby method's own push/pop is unaffected | Deliberate scope limit, verified round 22 |
| 7 | Exception raised and propagates across N frames | `:raise` fires **once**, attributed to the frame it originated in (see row 9); each of the N unwound frames still gets exactly one `:return` with `return_value = nil`, in inner-to-outer order, as the exception unwinds past them | `:raise` → `#note_raise` only (no pop). Each unwound frame's `:return` → `#pop_matching` finds and closes it, tagged with its own `raise_epoch` for the fabricated-nil check | Machine (pop) + Collector (epoch check) |
| 8 | `ensure` block runs during unwind and itself calls a workspace method | The `ensure` body's calls push/pop normally, *after* the raise that triggered the unwind — so their frames carry the *current* (post-raise) epoch, not the pushing method's original one | Their `#push` happens after `#note_raise`, so their own returns are correctly trusted even though a raise is "in flight" for the frame(s) below them | Machine (epoch is per-push, not global-flag) + Collector's regression test for exactly this ordering |
| 9 | `rescue` catches a raise **in the same method body** that raised it (guard clause, `begin/rescue` in one method) | CRuby still fires `:raise` (attributed to that same frame) *and then* a normal `:return` with the method's real return value — the method's frame was never actually abandoned | `:raise` → `#note_raise` (no pop, so the frame survives); `:return` → `#pop_matching` finds it and returns the real value, trusted normally | Machine (no premature pop) — this was round 22's fix; rounds 20–21 both got this row wrong |
| 10 | A raise inside a block passed to a workspace method (`xs.each { raise Stop }`), rescued by an outer method | `:raise` attributed to the frame that *wrote* the block (per row 9's rule, since block bodies aren't separately traced — row 5); that frame is not popped by `:raise`; its own later `:return` closes it with the real value | Same as row 9 — `:raise` never pops, so nothing above the raising frame is prematurely discarded either | Machine + regression test (round 22, "raises_in_yielded_block") |
| 11 | Calls already in flight when `#start`/`TracePoint#enable` runs (Harness installs via `-r`, so RubyGems'/Bundler's own loader frames are mid-call) | Those frames' eventual `:return` fires with no matching `:call` ever having been pushed for them | `#pop_matching` finds the top does not match (its key was never pushed) → no-op, returns `nil` (I4) | Machine — this is exactly what "no match" means; no special-case needed |
| 11a | A method whose own **argument prologue raises** — an optional-argument default (`def m(a, b = might_raise)`), a keyword-argument default (`def m(a, b: might_raise)`), or a block evaluated inside either | `:call` fires only *after* the prologue has run, so that method gets **no `:call` at all** — and still gets its own `:return`, `return_value = nil`, as the exception unwinds past it. Verified on 3.4.7 for all three spellings. This is ordinary Ruby, not an exotic shape, and unlike row 11 the stray `:return` arrives in the *middle* of a live stack | `#pop_matching` sees a key that does not match the top → no-op (I4). Under the pre-round-30 "nearest match" rule this instead matched a *live outer invocation of the same method* and discarded every frame above it — round 30's defect | Machine (`#pop_matching`, fixed round 30) |
| 12 | Frames still open when `#stop`/`TracePoint#disable` runs | No more events fire once disabled; whatever's left on any fiber's machine is simply never popped | Nothing — the machine (and its stack) becomes unreachable garbage the moment the fiber holding it exits or is dropped (I8) | Machine (nothing to do) + `Collector`'s fiber-local storage (nothing to prune) |
| 13 | A `:return` with no logical `:call` at all, in the abstract sense — including one whose key is live somewhere below the top | Reachable from real Ruby via row 11a; also constructed directly in `call_stack_machine_spec.rb` | `#pop_matching` on an empty stack, or with a key that does not match the top → `nil`, no exception, stack untouched (I4, I7) | Machine, fuzz-tested |
| 14 | Method redefined mid-run (`class_eval` over an already-observed method), including the `alias_method :m_without_x, :m` + new `def m` monkey-patch shape | `method_id` is an interned Symbol *and is the name a method was **defined** as*, so both the redefinition and the aliased-away original report `method_id == :m` and reuse the same cache key. Measured round 31 on a 1-arg `m` observed once and then replaced by a 2-arg `m`: one aggregate, `samples: 5`, **one** parameter slot rather than two | Out of `CallStackMachine`'s scope entirely — this is `Collector#@method_cache`'s concern, not the stack machine's. Documented residue: the stale parameter-name list drops a slot (or degrades it to `Types::UNKNOWN` when the stale name is absent from the new binding) — under-collection, not fabrication. Round 31 corrected the old claim that `alias_method` dodges this by producing a different key; only `prepend` (row 14b) does | Collector (deliberately unchanged; see its own docs) |
| 14a | The *same* method dispatched through different receivers: an inherited class method (`Child.build` where `build` is defined on `Base`), or `super` between two singleton methods | `tp.defined_class` is `#<Class:Base>` for both; `tp.self` is `Child` for both — the receiver, not the definer. Verified on 3.4.7 | The symbol_id must be a function of `defined_class` alone (`#attached_object`), because `@method_cache` is keyed by `[defined_class, method_id]` and its docs assert every cached field is invariant per key. Deriving it from `tp.self` broke that: first receiver wins for the whole run — round 24's bug | Collector (`#symbol_id_for`, fixed round 24) |
| 14b | A module is `prepend`ed into the class a frame belongs to (an instrumentation gem patching app code, or the app patching a gem) | `tp.defined_class` is the class itself for the inner frame, but `prepend` puts the module *ahead of* that class in its own ancestor chain, so `defined_class.instance_method(method_id)` resolves to the **wrapper**, not to the frame. Verified on 3.4.7 | Resolve the definition whose `#owner` *is* `defined_class`, walking `#super_method` past any prepended wrapper. Everything `#compute_method_info` derives — the workspace-containment test, the fingerprint, the parameter-name list — must describe the frame, not whatever dispatch would find. Reading the wrapper was round 25's bug | Collector (`#defined_method`, fixed round 25) |
| 15 | Fiber switches away mid-call (an `Enumerator`, `Async`, or hand-rolled scheduler) and is later resumed, or is dropped without ever resuming | Each fiber has its own machine (I1); a suspended fiber's machine is untouched by any other fiber's events; a dropped fiber's machine (and any stranded frames) becomes garbage with it | No transition — this is exactly the isolation fiber-local storage buys structurally, not a case the machine has to detect | Collector (`current_stack`'s fiber-local key) |
| 16 | Two threads executing workspace methods concurrently | Each thread's fibers get their own machine (same mechanism as row 15 — a Thread's main execution context is itself a Fiber) | No cross-thread interaction at the machine level at all | Collector |
| 17 | The Collector's own code (inside `handle`, or inside a machine method) triggers a nested `TracePoint` event | **Does not happen.** A `TracePoint` handler block does not re-enter itself by default (verified: Ruby suspends tracing for the extent of the handler's own execution unless `TracePoint#allow_reentry` is used, which this code never calls) | N/A — no re-entrant events are ever delivered while `handle` is executing | CRuby's own `TracePoint` semantics, not something this code has to defend against; noted here so a future change doesn't need to rediscover it |

## What is *not* fixed, and why

Four residues survive this pass, all previously identified (rounds 21–22)
and now stated here as the single place they're tracked, all sharing the
same shape: CRuby delivers **no event of any kind** that distinguishes them
from a genuine `nil` return, so the state machine has nothing to key its
epoch check on.

- `throw`/`catch` unwinding a frame.
- `break` out of a block, ending the method that yielded to it.
- A non-local `return` from inside a block, ending the enclosing method.
- `Thread#kill` on a thread parked inside a workspace method.

All four produce `:call` then a bare `:return` with `return_value = nil`
and no `:raise` anywhere on that fiber — indistinguishable, at the level of
events this Collector subscribes to, from the method actually evaluating to
`nil`. Re-verified on Ruby 3.4.7 in review round 23, with one precision
correction: it is the *intermediate* frames these unwind past that get the
fabricated `nil` (`throw`'s middle frames, the method that yielded to a
`break`ing block, a method a `proc`'s non-local `return` unwound through).
The frame that actually catches or owns the transfer reports its real
value normally — `catch`'s own frame returns the thrown value, and the
method a non-local `return` returns *from* reports that value. Crucially,
every one of these cases stays **stack-balanced**: each `:call` still gets
exactly one `:return`, so none of them can strand a frame that a later
`#pop_matching` would wrongly match. The residue is bounded to a
fabricated `nil` in one union, as stated. Distinguishing them would require also tracing `:c_call`/`:b_return`
(both fire on every C call and every block return respectively, in *any*
Ruby process, workspace code or not), which was measured to cost enough
overhead to fail this module's own budget test
(`spec/ovallsp/observation/overhead_spec.rb`). All four are rarer in
practice than an ordinary `raise`, and the failure mode is bounded — one
fabricated `nil` added to one method's return-type union, not the
systematic wrong-attribution or lost-evidence shape rounds 20–22 fixed.

A fifth residue was added in round 30, from row 11a. When a method's
argument prologue raises, CRuby delivers that method's `:return` without
ever having delivered its `:call`. If the *same* method is already live on
the stack **and is on top** — i.e. it is directly recursive, and the
recursive invocation is the one whose default raised — that stray
`:return` is indistinguishable from the live frame's own, so the live
frame is closed one event early. The parameter types recorded are the live
frame's own and still correct; its return value is *discarded* rather than
fabricated, because the raise epoch has already advanced and
`#return_type_for` refuses the `nil` (I6). Measured: `recur(1)`, whose
base case raises from a default argument, reports its real `Integer`
parameter but `Unknown` instead of the `Symbol` it returns; `recur(5)`
reports `Symbol` with all 5 samples, since only the innermost invocation
is affected. Under-collection of one return type in one union — the same
bound as the four above, and strictly smaller than what the pre-round-30
rule cost (whole methods).

Note the non-recursive form of row 11a is *fixed*, not a residue: when the
stray `:return`'s key does not match the top, nothing is closed at all and
every live frame reports normally.

`Collector#@method_cache` staleness under method redefinition (table row
14) is likewise deliberately out of scope for this pass: it is not a
`CallStackMachine` concern, its failure mode is under-collection (a stale
parameter name degrades to `Types::UNKNOWN`), and it was already reviewed
and left alone in round 22.

## Round 24: two defects outside the stack machine, in the same class

Round 24 re-derived everything above (all 17 rows re-observed on 3.4.7, the
revert-and-measure table reproduced, the `UnnameableOwner` example
adversarially mutated) and found the machine itself and its coverage sound.
Both defects it did find were in `Collector`'s *other* two concerns —
exactly the parts the extraction above declared "never in question":

1. **`#symbol_id_for` derived a singleton method's owner from `tp.self`**,
   the receiver dispatched through, rather than the class the method is
   defined on (table row 14a). Three consequences: the observed evidence
   was filed under a symbol_id no static declaration matches (ParserService
   declares `def self.x` under its lexical class); `@method_cache`'s stated
   per-key-invariance premise was violated, so the *first* receiver to call
   an inherited class method decided the attribution for the whole run; and
   `super` between two singleton methods collapsed two different methods
   into one signature, unioning their parameter lists position-wise
   (measured: `parameter_types = [Integer, Symbol]` for a one-parameter
   method). Fixed by using `defined_class.attached_object`, which is a pure
   function of the cache key. Same family as rounds 20–22: confidently
   wrong evidence, from ordinary application code.
2. **`#workspace_method?` tested containment with a bare string prefix**,
   so any sibling directory whose name merely begins with the workspace
   root's was treated as inside it (`/srv/app` accepted
   `/srv/application/lib/x.rb`), and everything defined there was reported
   as the user's own workspace evidence. Same path-boundary class rounds
   1–2 fixed in `BundleEnvironment`; `ColdIndexer#real_path_stays_inside_root?`
   already used the correct idiom, and `Collector` was the outlier.

Round 24 also found that `@method_cache` itself was a mechanism nothing
pinned — deleting it outright left the entire suite green, including the
overhead example its own comment credited with guarding it (measured: 12x
slower, still 50x inside that example's deliberately loose bound). Given
finding 1, the cache is not merely an optimization — the run's whole
attribution is keyed off it. It is now pinned by a *count*-based example in
`overhead_spec.rb` (one source-file digest per method, never per call),
which cannot go flaky on slow hardware the way a duration bound would.

## Round 25: the third concern, `#defined_method`, was reading the wrong method

Round 25 re-verified rounds 23 and 24 independently (each fix reverted
separately, each measured symptom reproduced from scratch rather than taken
from the commit message, each new example adversarially mutated) and found
all of it sound and correctly targeted. The machine remains untouched.

Both defects it found are, again, in `Collector`'s translation layer rather
than in the machine — and this time in the one concern rounds 20–24 had all
taken for granted: **which method object a frame is actually about**.

1. **`#defined_method` resolved the frame's definition with
   `tp.defined_class.instance_method(tp.method_id)`** (table row 14b). That
   answers "what would dispatch find starting here", which is a different
   question from "what is this frame", and the two diverge for exactly one
   construct: `prepend`, which inserts a module *ahead of* a class in that
   class's own ancestor chain. Every field `#compute_method_info` derives
   then described the wrapper instead of the frame. Measured three ways at
   once:

   - A gem prepending into a workspace class (how ActiveSupport and every
     APM/instrumentation gem patch app code) made `#workspace_method?` read
     the *gem's* `source_location`, so the app's own method produced **no
     evidence at all** for the whole run.
   - The workspace prepending into a gem's class made the same test read
     the *workspace's* `source_location`, so the **gem's** method was
     collected and reported as the user's own evidence — round 24's
     containment defect again, reached by a different route and past the
     boundary fix that round installed.
   - The parameter-name list and the fingerprint came from the wrapper, so
     a one-parameter method was reported with the wrapper's two parameter
     slots (both `Types::UNKNOWN`, since the wrapper's names don't resolve
     in the real frame's binding) and fingerprinted against a file whose
     edits `Store#invalidate_changed` would then track *instead of* its own
     — evidence that outlives the edit that invalidates it.

   Fixed by walking `#super_method` down to the definition whose `#owner`
   is `defined_class` itself. This also completes round 24's premise rather
   than merely sitting beside it: the cached entry was previously not a
   function of `[defined_class, method_id]` even in the temporal sense, since
   prepending mid-run changed the answer for an unchanged key.

2. **`#positional_param_names` looked every slot up by name**, and Ruby
   permits the same name in two positional slots (`def m(_, _)`), binding
   only the first. `Binding#local_variable_get` therefore answered every such
   slot with the same value: measured, `m(1, "s")` reported
   `[Integer, Integer]`. Now those slots are kept (so every later slot stays
   at its own position) but carry `Types::UNKNOWN` — under-collection over
   fabrication, as everywhere else in this module.

Both are rounds 20–22's family once more: not lost evidence but confidently
wrong evidence, from ordinary application code.

Re-audited and found correct, for the record, so a later round need not
redo them: `#fingerprint_for` is symlink-safe by construction (the digest
is over file *content*, so which path it was read through cannot change it,
and `File.realpath` failing degrades to a `nil` fingerprint); `attached_object`
handles every input it can receive (a singleton class of a singleton class
yields an unnamed `Module` and a plain object yields a non-`Module`, both of
which already fall through to "no symbol_id"); refinements and anonymous
classes/modules likewise produce no evidence rather than wrong evidence; and
`include`/`extend`/`alias_method`/`define_method`/inheritance/private
methods were all measured to attribute correctly both before and after this
pass. `File.realpath` in `#initialize` is now also pinned as load-bearing
for the `@workspace_prefix` construction, not merely for symlinks: a root
handed in with a trailing separator would otherwise build a doubled one and
match nothing.

The remaining known imprecision, unchanged and deliberate: a `req`/`opt`
parameter that follows a `*rest` (`def m(a, *rest, b)`) is reported at the
slot index it occupies *after* rest parameters are dropped, not at its
index in the declaration. `ObservedSignature#parameter_types` has no
representation for rest at all (documented scope limit), and no consumer
joins it positionally against a declaration today — `ovallsp/showTypeEvidence`
renders the list as text. Giving rest parameters a representation is a
design change, not a review fix. Round 26 measured this residue and pinned
it with an example rather than leaving it as prose only (see below).

## Round 26: the shadowed-name guard asked its question of the projection

Round 26 re-derived rounds 23–25 independently — each fix reverted
separately, each claimed symptom reproduced from scratch rather than read
off the commit message. All of it holds. Reverting the `#super_method` walk
fails exactly 2 examples and reproduces all three of its symptoms (a
workspace method a gem prepends into vanishes; a gem method the workspace
prepends into is collected as the user's own; a one-parameter method is
reported with the wrapper's slot count — measured independently at 3 slots
for a 3-deep wrapper, all `Unknown`). Reverting the shadowed-name
deduplication fails exactly 1 and reproduces `[Integer, Integer]` for
`pair(1, "s")`. Dropping `File.realpath` from `#initialize` fails exactly 1.

The walk itself was held to an objective oracle — at a `:call` event
`tp.lineno` *is* the frame's own `def` line, so the resolved definition's
`source_location` must equal it — across 15 generated configurations and 54
frames: prepend chains 1–4 deep, a wrapper module with its own prepend, a
prepend on a superclass *and* on the subclass reached through it, `include`
plus prepend on both the module and the class, singleton-class prepend
chains, the same module prepended twice, `alias_method` over a prepend, and
`extend` of a prepended-into module. Every frame resolved to its own
definition, and the walk terminated in at most 4 steps in every case
(`#super_method` returns `nil` at the end of a finite ancestor chain, which
ends the `while` — verified on a subclass-`alias` frame, where no method in
the chain is owned by `defined_class` at all and the result is correctly no
evidence rather than the wrong method's).

The walk cannot land on a *different* method owned by `defined_class`: a
module owns at most one method per name, so if the frame's definition is
owned by `defined_class` it is the only candidate the walk can stop on.
The one construct that looked like it could break that — `alias` of a
prepended method, which makes `Klass.instance_method(:m2).owner` report the
class while the body comes from the wrapper — does not, because CRuby
already reports `tp.defined_class` as the *body's* origin module for alias
copies (measured).

The defect it did find is one kind boundary over from round 25's own:

**`#positional_param_names` counted the repeat over the list it returns
rather than over the declaration.** The property that licenses a by-name
read is "this slot is the only parameter with this name" — a fact about the
whole declaration — but the tally ran over the `req`/`opt` slots left after
filtering, so a name shared with a `*rest`, `**kw`, `&blk` or `key:`
parameter was never seen as a repeat. Ruby rejects duplicate parameter
names except `_`-prefixed ones, and `Binding#local_variable_get` answers
with the *leftmost* declaration, so the reachable wrong-evidence shape is
the one where the filtered-out duplicate is declared first — which only
`*rest` can be. Measured on `def m(*_a, _a)`, legal Ruby:
`m(1, 2, "the-real-tail")` reported `[Array]` for its single slot, the
trailing required parameter, which plainly holds a String. The repeat is
now counted over `method_obj.parameters` entire.

Narrow — `def m(*_a, _a)` is not code anyone writes — but the same family
as rounds 20–22, 24 and 25: not lost evidence but confidently wrong
evidence. Pinned by its own example, which rejects dropping the slot
instead of blanking it, emitting a slot for the rest parameter, and the
over-broad "blank every positional slot of any method that has a rest
parameter" — that last one being rejected by the new control example that
also pins the rest-index residue above.

Re-audited and found correct, so a later round need not redo them: `:call`
fires *after* the argument prologue, so an omitted optional argument reads
back as its evaluated default (`def opts(a, b = Defaults.side, c = a.to_s)`
called as `opts(1)` reports `[Integer, String, String]`, never `NilClass`),
and the default expression's own calls stay balanced on the stack — **the
second half of that is wrong when the default expression *raises*, which
round 26 did not try; see row 11a and round 30 below**; keyword
arguments mixed with positional ones do not disturb the positional
by-name reads; `tp.method_id` is the name a method was *defined* as, never
the name it was called as (`tp.callee_id` is that), so an `alias` files its
evidence against the definition it actually shares and `@method_cache`'s key
unifies both call names; a `method_missing`-based dynamic call is attributed
to `#method_missing` itself, which is the frame that really ran; a
singleton class of a singleton class, a refinement module and a
never-named anonymous class all still produce no evidence rather than wrong
evidence; and `@method_cache`'s per-key invariant now genuinely holds under
a mid-run `prepend` (measured: patching a class between two observed calls
leaves the wrapped method's own slot count, types and sample count intact,
and files the wrapper separately).

## Round 27: the aggregate's shape was decided by whichever call arrived first

Round 27 re-derived round 26 independently: reverting
`#positional_param_names`' tally to the filtered `req`/`opt` list fails
exactly 1 example and reproduces `[Array]` for
`trailing_after_rest(1, 2, "the-real-tail")`, confirmed a second time
outside the collector entirely (`def m(*_a, _a)` reports
`parameters == [[:rest, :_a], [:req, :_a]]` and
`binding.local_variable_get(:_a) == [1, 2]` inside the frame). The round-26
example is specific in both directions: dropping the shadowed slot instead
of blanking it fails it *and* round 25's; emitting a slot for the rest
parameter fails it *and* its control; while an unrelated revert (the
`#super_method` walk) fails 2 other examples and leaves it green.

Parameter handling was then swept exhaustively rather than sampled, since
that is where rounds 25 and 26 both lived — 15 declarations measured
end-to-end through a real `Collector`, and none produced wrong evidence:

- A name shared between two *non-positional* kinds is legal Ruby
  (`def m(a, **_x, &_x)`, `def m(a, *_x, &_x)`) and is a non-event here:
  neither kind occupies a positional slot, and the slot that does exist
  reports its own class.
- Anonymous parameters (Ruby 3.2+ bare `*`, `**`, `&`, and `...`) do **not**
  produce `name == nil` — CRuby reports them as the symbols `:*`, `:**`,
  `:&`, all distinct — so they can neither collide with each other nor be
  filtered out by the `name &&` guard. `def m(*, b)` correctly reports `b`'s
  own class, which is round 26's shape with the collision removed.
- Nameless parameters do occur, from destructuring (`def m(a, (b, c))` →
  `[[:req, :a], [:req]]`), including two at once; each keeps its slot and
  degrades to `Unknown`, and the `name &&` guard keeps two nil names from
  tallying as a repeat.
- A collision where the *positional* slot is declared first
  (`def m(_a, _a: 5)`, `def m(_a, &_a)`, `def m(_a = 1, *_a)`) blanks a slot
  whose by-name read would in fact have been correct. That is
  under-collection, this module's stated preference, not wrong evidence.
- Numbered block parameters (`_1`, `it`) inside a traced method body do not
  interact at all: blocks are not traced and the read happens at `:call`,
  before the body runs.

The defect found is in aggregation rather than in how any single call's
evidence is derived — a different shape from every round since 20:

**`#record` sized an aggregate's per-slot array once, from whichever
observation happened to arrive first.** A `symbol_id` is
`[kind, owner-name, method-name]`, so two *distinct* `Class` objects that
answer `#name` with the same string legitimately share one aggregate — which
is what a Rails/Zeitwerk constant reload, a bare `remove_const` + redefine,
and rspec-mocks' `stub_const` each produce inside a single run — and they
need not share an arity. When the later definition was wider,
`agg[:param_type_sets][index]` was `nil` and `nil << type` raised
`NoMethodError` straight into `#handle`'s blanket rescue, which exists so a
plugin's own bug cannot take down the user's test run, not to absorb this
class's own index errors. `#record` is not atomic either, so the aggregate
was left half-applied: every slot before the overflow had already taken the
new call's type while `samples` and the return type never did.

Measured on a class reloaded mid-run from `m(a)` to `m(a, b, c)`, called
once narrow and twice wide: a *single* slot reading
`Integer | String | Symbol` — a union of all three calls' slot-0 arguments —
reported next to `samples: 1`, with both wider calls' return types dropped
and two whole parameter slots reported as not existing. `samples` is the
confidence signal `ovallsp/showTypeEvidence` renders verbatim and sums into
`sampleCount` (`Server#type_evidence_response`), so this is rounds 20–22 and
24–26's family once more: not lost evidence but confidently wrong evidence.

The slot array now grows per slot, so its length is a function of every
observation rather than of the first. Holes are impossible by construction
rather than by a guard: indices are visited in ascending order from 0, so
slot *n* cannot be reached before slot *n − 1* exists, and `#results` never
sees a `nil` where it expects a `Set`. Pinned by two examples — the wider-
later one fails without the fix, and a narrower-later control rejects the
over-broad "resize to the latest call's arity", which would silently discard
every slot the newest definition no longer has.

`CallStackMachine` is untouched by this round.

## Round 28: `#parameters` is not the declaration

Round 28 re-derived round 27 independently rather than trusting its commit
message. Reverting `#record` to size `param_type_sets` once
(`Array.new(payload[:param_types].size) { Set.new }`) fails exactly 1
example, and the symptom reproduces outside the suite entirely: a class
reloaded mid-run from `m(a)` to `m(a, b, c)`, called once narrow and twice
wide, reports one slot reading `Integer | String | Symbol` next to
`samples: 1`; with the fix in place the same script reports 3 slots and
`samples: 3`. The round-27 examples are specific in both directions — a
half-fix that stops the `NoMethodError` while still truncating to the
first-seen slot count fails the wider-later example on its *slot count*
assertion, the over-broad "resize to the latest call's arity" fails the
narrower-later control, and an unrelated revert (round 26's tally) fails
round 26's example while leaving both of round 27's green.

The aggregation area round 27 opened was then swept rather than sampled,
and came back clean. A later *narrower* call correctly leaves the higher
slots' existing `Set`s alone rather than truncating them. `agg[:fingerprint]`
is set from the first call and never updated, but it cannot produce false
freshness in either direction: `Fingerprint` digests the *whole file* at
observation time plus the declaration's line, so "the source changed since
this was observed" always changes the digest, and
`Store#invalidate_changed` therefore only ever over-invalidates (drops
evidence) when the stored fingerprint is the other definition's — this
module's stated preference for under-collection. `#record`'s
`@mutex.synchronize` demonstrably covers the *entire* read-modify-write,
including `agg[:param_type_sets][index] ||= Set.new`: measured with 8
threads × 400 calls alternating between two distinct classes sharing one
`symbol_id` at arities 1 and 5, plus 2 threads calling `#results`
concurrently, the result is exactly 3200 samples and 5 slots every run. That
negative is only meaningful because the same harness is a real detector —
inserting an explicit `Thread.pass` mid-`#record` and removing the lock
yields 401/3200, and restoring the lock with the `Thread.pass` still in
place returns it to 3200/3200. An empty `Set` in `#results` is unreachable
(every slot is created and written in the same expression), and would
degrade to `Types::UNKNOWN` via `Types.normalize_union([])` anyway; no
consumer joins `parameter_types` positionally against a declaration
(`Server#show_type_evidence_result` renders it as text), so a variable-arity
aggregate breaks nothing downstream.

The defect found is in parameter handling again — rounds 25 and 26's exact
family, one boundary further out. Round 25 counted a repeated parameter name
within the filtered `req`/`opt` projection; round 26 widened that to the
whole `#parameters` list. But `#parameters` is itself an incomplete
description of the declaration: a **destructuring parameter is reported as a
bare `[:req]` with no name**, and the names it binds — ordinary method-body
locals, indistinguishable at the binding from a positional slot's own — are
reported nowhere at all. So a name the tally sees exactly once can still be
bound a second time inside a destructure, and Ruby binds the *rightmost*
such declaration.

Measured end to end through a real `Collector` on
`def shadowed_by_destructure(_a, (_a, _b))`, called as
`shadowed_by_destructure(42, ["x", "y"])`: `[String, Unknown]` — a
confidently wrong class for a slot 0 that plainly holds an `Integer`. Both
halves are everyday idioms (`_`/`_a` for "ignore this", and destructuring a
pair), legal *together* only because `_`-prefixed names are exempt from
Ruby's duplicate-name rule. That exemption is also what bounds the fix:
Ruby rejects a duplicated name across the destructure boundary too — both
`def m(a, (a, b))` and `def m(a, (b, a))` are `SyntaxError` — so a collision
`#parameters` cannot see is reachable for exactly the `_`-prefixed names and
no others.

`#positional_param_names` now blanks a positional slot when the declaration
contains any nameless positional parameter *and* the slot's own name is
`_`-prefixed. The guard is stated as the property it is actually asserting
("this declaration binds this name in exactly one place") rather than as a
list of the shapes found so far, and it is as tight as Ruby's own rule
allows: `def m(kept, (_x, _y))` still reports slot 0's own `Integer`, so the
fix cannot degrade every method that destructures. Pinned by two examples —
the shadowed one fails without the fix, a non-underscore control rejects the
over-broad "blank every slot of any method that destructures", and the
fixture uses `_a` rather than a bare `_` specifically so a fix narrowed to
the single name `:_` fails rather than passes.

### The same assumption, outside the collector

Adding the destructuring fixture to `spec/fixtures/observation/sample_classes.rb`
made `server_spec.rb`'s "logs and continues after a notification handler
raises" fail deterministically — a *second* `logger.error`, from a part of
the system this review was not looking at. `Server`'s default workspace root
is the repo itself, so the cold index reads that fixture file, and it logged:

```
cold index: failed to index .../spec/fixtures/observation/sample_classes.rb:
NoMethodError: undefined method 'name' for an instance of Prism::MultiTargetNode
```

`ParserService::Visitor#extract_parameters` read `#name` off every entry in
`parameters_node.requireds`, and a destructuring parameter is a
`Prism::MultiTargetNode`, which has none — the *same* assumption the
collector was making one layer down ("every positional parameter has a
name"), reached from the static side. The consequence is much larger there:
the `NoMethodError` propagates out of `#summarize`, so the file produces no
`FileSummary` at all and **every declaration in it disappears from the
index** — hover, go-to-definition, completion and diagnostics all go dark
for any workspace file containing one such method, silently apart from a log
line. `LocalInferencer` already guards its own block-parameter reads with
`respond_to?(:name)` for exactly this node type; this path did not.

Sweeping the same method turned up a second, quieter gap in it: `posts` —
the required parameters that follow a `*rest` — were never read at all, so
`def m(a, *rest, b)` reported `b` as not existing and signature help
rendered `m(a, rest)`.

Both are fixed in `#extract_parameters`, each pinned by its own example in
`parser_service_spec.rb` (the destructuring one asserts the *whole file's*
declarations, not just the one parameter list, so it fails on the collateral
loss rather than only on the parameter shape). A `nil` parameter name was
already an ordinary supported shape — an anonymous `*`/`**`/`&` produces one
— and every consumer already handles it, so the nameless slot is kept rather
than dropped, which is what keeps every later parameter at its own position.

`CallStackMachine` and `#record` are untouched by this round.

## Round 29: the collector held; the same habit had one more file to take down

Round 29 found **nothing in `Collector` or `CallStackMachine`** — the first
clean result for either since round 19. What it did find is round 28's
*second* defect one method further up the same file, and it is the same
severity: the whole file's index.

`ParserService::Visitor#visit_namespace` read
`node.constant_path.full_name` bare. That method does not return something
unhelpful for a constant path with a non-constant segment — it **raises**
`Prism::ConstantPathNode::DynamicPartsInConstantPathError`. And half-way
through typing `class Foo::Bar`, at `class Foo::`, `node.constant_path` is
not a `ConstantPathNode` at all but a `Prism::CallNode`, which has no
`#full_name` to raise from: it raises `NoMethodError` instead — round 28's
missing-`respond_to?` defect exactly, in the same class, on the same day.

Either way the exception propagates out of `#summarize`, so there is no
`FileSummary`: every declaration in the file disappears from the index and
hover/definition/completion/diagnostics go dark for it. Two reachable
shapes, both measured end to end through `ParserService`:

- `class self::Beta` / `module self::Beta` — legal, ordinary Ruby.
- `class Foo::` — a document mid-edit. This one matters most, because it is
  reached by *anybody typing a compound class name*, on a path whose entire
  premise is error tolerance ("even when the source has a syntax error,
  declarations before the error remain visible", Task 002), reparsed on
  every `didChange`. The index went dark exactly when hover and completion
  are most wanted.

The fix routes the read through `#raw_constant_name`, the `respond_to?` +
`rescue` guard this same file already uses for this same question in
`#record_superclass`, `#record_ancestor_call`,
`#record_method_call_candidate` and `#constant_full_name` ("動的な名前は推
測せず対象外"). An unresolvable namespace is skipped along with its subtree
rather than recorded under a guessed owner — under-collection over
fabrication, the same preference the collector states.

The four stack pushes moved out of `#visit_namespace` into
`#within_namespace`, so the `ensure` that pops them can only run when they
actually ran. This is the structural half, not cosmetic: the obvious fix — a
bare `return unless` above the old method's own `ensure` — pops four stacks
that were never pushed, and it *passes* a top-level-only regression example
(popping an empty `@owner_stack` is a no-op and the next namespace re-pushes
`@visibility_stack`). The pinning example therefore nests the skipped
namespace inside a `module Outer` with a sibling `def` after it, where that
implementation silently attributes the sibling to `nil` instead of
`::Outer`, with the file still indexing.

Everything else round 29 tried on the collector held: 14 parameter shapes
through a real `Collector` (nested destructures, a splat inside a
destructure, a destructure in `posts`, `&_a`/`**_a` collisions, `...`
forwarding) all reported either the right class or `Unknown`, never a wrong
one; Ruby's duplicate-name rule was re-verified to reject every
non-`_`-prefixed cross-boundary collision, which is what bounds round 28's
`_`-prefix restriction from being over-narrow; 1.8M generated
`CallStackMachine` operations over 3000 seeds matched an independent
reference model exactly; and throw/catch, `retry`, `ensure`-body calls,
recursion, `Enumerator` fibers and threads all produced the documented
results, including the two known residues (a self-rescued frame and a
`throw`-abandoned frame) and nothing beyond them.

## Round 30: the scan was not defensive generality — CRuby really does deliver that sequence

Round 30 re-derived round 29 independently: reverting `#visit_namespace`'s
`#raw_constant_name` guard fails exactly the 2 examples round 29 added, and
the whole-file index loss reproduces outside the suite on three distinct
exceptions rather than the two the commit message names —
`DynamicPartsInConstantPathError` for `class self::Beta`,
`MissingNodesInConstantPathError` for a bare `class Foo::`, and
`NoMethodError` (`Prism::CallNode` has no `#full_name`) for `class Foo::`
followed by a body. All three propagate out of `#summarize`; all three are
caught by the one guard. The "obvious fix" mutation behaves exactly as
claimed: a bare `return unless` above the old method's own `ensure` passes
the mid-edit example and *fails* the pinning one, attributing the sibling
`def` to `nil` instead of `::Outer`. An unrelated mutation
(`#namespace_name_location` always taking the whole path's location) fails
only its own example, in `rename/planner_spec.rb`, leaving round 29's green.

The defect round 30 found is in `CallStackMachine` itself — the first since
round 22 — and it is a *documented assumption*, not an unexamined corner.
`#pop_matching` scanned down from the top for the nearest frame with a
matching key and discarded everything above it. Both this note (I3/I4) and
the method's own comment justified the scan as defensive generality (I7)
that real `TracePoint` delivery is "not known to require". Round 26's
re-audit went further and recorded the relevant premise as verified: "the
default expression's own calls stay balanced on the stack".

They do — until the default expression **raises**. `:call` fires only after
the argument prologue has run, so a method whose optional-argument or
keyword-argument default raises gets no `:call` at all, and still gets its
own `:return` with `return_value = nil` as the exception unwinds past it
(new table row 11a, verified on 3.4.7 for `b = raise_me`, `b: raise_me`,
and a block evaluated inside a default). `def m(a, b = might_raise)` is
ordinary Ruby. That stray `:return` therefore arrives in the *middle* of a
live stack — and when the same method is live further down, the scan
matched that outer, still-running invocation and sliced off every frame
stacked above it.

Measured end to end through a real `Collector` on
`unannounced_outer(1) -> unannounced_middle -> unannounced_outer(0)`, whose
default raises: **`unannounced_middle` was absent from the results
entirely** — its frame was discarded with the outer one, so its own later
`:return` matched nothing — and `unannounced_outer` reported `Unknown`
instead of the `String` it returns. Silent, from ordinary code, and the
number of methods lost grows with the depth of the call chain between the
two invocations. In the same shape without the raise, both methods report
normally, so this is not a pre-existing gap in that fixture.

`#pop_matching` now closes the top frame and only when its key matches
(I3/I4 rewritten above). That is not a narrowing of the contract for
convenience — it is the contract CRuby's delivery actually licenses: a
`:return` can only be about the innermost live frame, so a key that does
not match the top is a frame this machine never held, and closing anything
would be a guess. Every case rows 1–12 exercise is unaffected, because the
match is always already on top there — which is what the old comment
itself said. The residual cost, the directly-recursive form of row 11a, is
recorded above as a fifth residue: one return type widened to `Unknown`,
never a wrong one.

Pinned in three places, all of which fail without the fix (22 failures
total): a machine-level example (a stray pop whose key is live below a
different top frame leaves both frames intact), all 20 generative seeds
(the reference model now implements the top-only contract, and with a
three-key alphabet "live but not on top" is a common draw), and an
end-to-end `collector_spec.rb` example asserting *both* methods, since
losing `middle` outright is the more serious half. The examples are
specific in both directions: a bare `@stack.pop` fails them **and** round
20's own example; a half-fix that answers only for a matching top but pops
unconditionally fails the same four; and the natural compromise — check the
top first, then fall back to the old scan — still fails both new examples
plus all 20 seeds. An unrelated revert (round 27's per-slot growth, both
halves) fails exactly its own example and leaves round 30's green.

`Collector` itself is untouched by this round.

## Round 31: the machine held; the cache was pinning the classes it described

Round 31 re-derived round 30 independently rather than trusting its commit
message. Reverting `#pop_matching` to the scan-and-discard version fails
exactly the 22 examples claimed, and the symptom reproduces on a **fresh**
fixture built from scratch — `outer(1) -> middle -> outer(0)`, where
`outer`'s own optional-argument default raises at the base case and
`middle` rescues it. With the scan: `middle` is absent from the results
entirely and `outer` reports `Unknown` instead of the `String` it returns.
With the top-only rule: all three methods report correctly
(`entry` → String, `middle` → Integer, `outer` → String, one sample each).

The fifth residue was verified to the letter rather than approximately, on
its own fresh fixture (`recur` both directly recursive *and* carrying a
raising default). `recur(1)` reports `params=[Integer, nil]` — its own real
parameter types — and `ret=Unknown`, `samples=1`; `recur(5)` reports
`ret=Symbol` with `samples=5`. Exactly as claimed: the live frame's own
parameters kept, only its return type lost, and only for the innermost
invocation. The raw event stream confirms row 11a directly — one `call
recur`, two `return recur` (the first `nil`, for an invocation CRuby never
announced).

The top-only restriction opens no new gap, for a reason that is structural
rather than enumerated: a top-only pop is a strict **subset** of what the
scan did (whenever the top matches, the scan's `rindex` finds that same top
frame), so the new rule can never *act* where the old one did not — it can
only decline to. The only way it could lose something the scan recovered is
a frame **stranded** on the machine — pushed, never returning — which is
also the load-bearing premise of I3 ("one `:return` per frame, in strict
inner-to-outer order"). That premise was tested rather than asserted: 14
control-flow shapes measured through a real `Collector`, reading the
machine's own stack directly, all balanced at depth 0 — `throw`/`catch`
across three frames, `break`, a non-local `return` from a block, `retry`,
an `ensure` body that calls out while the method raises, a raising optional
default, an `Enumerator` and a lazy enumerator (both fiber-backed), a
`Thread`, `method_missing`, `define_method`, and a `super` chain.

Exactly one shape strands frames: `SystemStackError`, where the handler
itself cannot run near stack exhaustion (measured: 10,906 frames left on
the machine after a 100,000-deep recursion aborted). Under the top-only
rule that costs nothing beyond the frames themselves — 20 subsequent
`plain` calls and 20 subsequent `with_throw` calls on the *same* machine,
stacked on top of that 10,906-frame residue, were all recorded completely
and correctly. Under the old scan they were exactly the fuel a later stray
`:return` needed to destroy an unbounded number of live frames, so this is
one more place the top-only rule is strictly safer rather than merely
narrower.

Round 20's shape was re-constructed in combination with recursion, since
that is where a top-only rule could plausibly regress it: a recursive
`recur` whose every recursion passes *through* a method on a class whose
`#name` raises, so `#symbol_id_for` raises, so `#handle_call` never reaches
`#push`. The result is identical to a fully-tracked control
(`String | Symbol`, 4 samples, both ways) — the stray `:return`'s key never
matches the top, so nothing is closed. The premise underneath that,
"an untracked key is untracked *deterministically*", was checked directly
rather than reasoned about: a `compute_method_info` that raises is never
memoized (the assignment never runs), so the same key can never be pushed
on one call and skipped on another, and a skipped key therefore can never
collide with a live frame of its own key.

`CallStackMachine` is untouched by this round. **The defect is in
`Collector`, and it is the same "bounded by construction" claim rounds
17/18 fixed for `AgentProcessManager`'s exit registry and round 20 fixed
for the per-thread stack registry one method below.**

`@method_cache`'s own docs asserted it was "bounded by construction — one
entry per method that actually exists in the process, not per call, per
thread, or per run". It was a plain `Hash` keyed by
`[defined_class, method_id]`, which holds `defined_class` **strongly**, so
the real bound is one entry per `Module` object a method was ever called
on. Modules are created at *runtime*, per example, by everything a real
suite does constantly: rspec-mocks defines each stubbed method on that
double's own singleton class, `stub_const` takes a fresh `Class.new`,
`Struct.new`/`Data.define` get called inside methods, `def obj.x` makes a
singleton class per object, and a Zeitwerk reload replaces every model
class.

Measured: 20,000 objects each given a singleton method inside one run were
**all still live after three full GCs**, against 0 for the identical churn
with no `Collector` installed — and they dropped to 0 the instant the
`Collector` itself was dropped, proving the cache key was their only
retainer. The entry need not buy anything either: the same churn with the
classes defined *outside* the workspace root produced 3,004 entries whose
cached value is `nil`, next to an empty `@aggregates`. This is a leak in
the **user's own test-suite process** — the one thing this whole module is
built not to disturb — and it is silent and scales with suite length.

Fixed by holding `defined_class` weakly: `@method_cache` is an
`ObjectSpace::WeakKeyMap` (the same tool and the same reasoning as
`AgentProcessManager`'s `@exit_registry`) in two levels,
`defined_class -> {method_id => info}`, because the weak key has to be the
collectable object itself rather than an `Array` wrapping it. An entry now
disappears exactly when the class it describes does — "hold nothing that
isn't already needed" (I8), rather than a pruning mechanism bolted on.
Three properties keep that honest: the inner value references nothing that
could reach the key back (a symbol_id of Strings, a digest String, a list
of Symbols); lookup semantics are unchanged, since `WeakKeyMap` matches by
`hash`/`eql?` exactly as the `Array` key's element comparison did (measured
on a class pair overriding both — identical collision under either); and a
raising `compute_method_info` is still never memoized, which
`#pop_matching`'s top-only rule depends on.

Pinned by three examples, all of which fail without the fix: the singleton
churn (200 retained pre-fix, ≤ 1 post-fix), the anonymous-class churn (200
pre-fix), and a memoization example that rejects the over-broad "just
delete the cache", which would satisfy both retention examples on its own.

### The other hedged claims, checked rather than trusted

- **I2 is still removable with the suite green.** Re-measured on the
  879-example suite: reverting `#handle_call` to push only tracked calls
  leaves 0 failures, unchanged from rounds 23/24. I2 remains deliberate
  defense in depth, not the load-bearing half.
- **Row 11 holds.** A call already in flight when `#start` runs has its
  stray `:return` arrive on an empty stack, as claimed.
- **The `alias_method` claim was wrong** — see row 14 above, corrected.
  This does not change behavior (row 14 stays a deliberate scope limit),
  but the justification is now what was measured rather than an assumption.
  If row 14 is ever closed, the cheap oracle is `tp.lineno`, which at a
  `:call` event *is* the frame's own `def` line (round 26 established this
  and used it as an oracle across 54 frames) — an O(1) integer compare per
  call rather than the `source_location` lookup the old comment costed it
  at. That is a design change, not a review fix.
- **`#results` during a live trace is safe.** `@mutex.synchronize` wraps
  the entire `#aggregates.map`, not just a read. Stress-measured with the
  trace still enabled: 8 writer threads × 500 calls each at two arities,
  overlapped by 3 threads calling `#results` in a tight loop (687,974
  completed iterations), yields exactly 4,000 samples per method and 5
  slots for the wide one, every run. Same-thread re-entry cannot hang
  either: `Mutex#synchronize` nested on one thread raises `ThreadError`,
  which is a `StandardError` and so lands in `#handle`'s blanket rescue.
- **Ractors are not reachable.** `TracePoint` is per-Ractor in CRuby: a
  workspace method called inside `Ractor.new` completes normally and
  produces no events for a `Collector` enabled on the main Ractor
  (measured: empty `@aggregates`, no exception). Were it ever reachable,
  `Ractor::IsolationError` is a `StandardError` and would degrade to "this
  one event produced no evidence".
- **`Fiber.new(blocking: true)`** isolates exactly like an ordinary fiber
  (measured: 10/10/20 samples across blocking fibers, plain fibers and the
  main fiber, all correctly attributed).

`BundleEnvironment` remains untouched since round 4 (`346993f`): zero
commits, zero diff.

## Unexpected-event-sequence policy

Per-case policy for anything the abstract machine receives that a real
`TracePoint` stream (rows 1–12) would never actually produce (row 13's
category) or that represents `Collector`-level bookkeeping rather than a
stack-shape problem:

| Situation | Policy |
|---|---|
| `#pop_matching`'s key does not match the top frame | Ignore: return `nil`, leave the stack untouched (I4) — including when that key *is* live further down (row 11a); closing a frame that is still running is a guess, and this module prefers under-collection |
| `#pop_matching` called on an empty stack | Ignore: return `nil` (a degenerate case of the above) |
| `#note_raise` called with no frames on the stack | Ignore: still advances the epoch (a raise from outside any tracked frame is legitimate — e.g. from a non-workspace method with nothing recorded above it — and a later frame's genuine-nil check still needs to see it) |
| Any exception inside `Collector`'s own event-handling code (method-info lookup, fingerprinting, param extraction) | Caught by `Collector#handle`'s blanket `rescue StandardError`, independent of the machine; degrades to "this one event produced no evidence", never crashes the observed process (unchanged by this pass) |
| An internal invariant genuinely violated (should be unreachable given the table above) | The machine still does not raise (I7) — there is no diagnostic-logging path today because every case in the table above is either handled structurally or explicitly out of scope; a future change that finds a genuinely new reachable case should extend the table, not add a rescue |

## Final release gate (replaces the "2 consecutive clean reviews" rule)

The review cadence for this area (rounds 20–31, twelve rounds, every one
finding something) was originally gated on two consecutive independent
reviews finding nothing at all. That rule was retired by explicit user
decision after round 31: the review agents can always widen their search
(more Ruby versions, more gems, more concurrency shapes), so "zero findings,
twice in a row" was at risk of becoming an unbounded search for
ever-narrower edge cases rather than a release criterion. What follows is
the finite replacement: a severity-classified catalog of everything found,
an explicit guarantee/no-guarantee statement, a bounded test matrix, and a
release gate stated as pass/fail conditions rather than "nothing found."

### Bug catalog, rounds 20–31, classified by severity

Severity classes, as specified for this gate:

- **P0** — crash, protocol violation, impact on the *target* process (the
  workspace's own test run, or Core's own process for the two `ParserService`
  findings), a security issue, or data destruction.
- **P1** — confidently wrong observation/index results for ordinary Ruby
  code (the dominant shape in this catalog: not lost evidence, fabricated or
  misattributed evidence).
- **P2** — wrong only for an unusual language feature or a rare event
  sequence (parameter-name collisions requiring `_`-prefixed duplicate
  names, for the most part).
- **P3** — theoretical, or a documentation/maintainability/optimization
  concern with no user-visible incorrectness.

| Round | Commit | Defect | Severity | Reachable via | Regression test |
|---|---|---|---|---|---|
| 20 | `9a3c77c` | Push only for tracked calls, pop unconditionally → caller's return type replaced by callee's, for any workspace method that calls a non-workspace one | **P1** | Any method body calling a gem/stdlib method — the ordinary shape of nearly every real method | `collector_spec.rb` "call/return balance across non-workspace callees" |
| 21 | `19b77c1` | Unwound frame's fabricated `nil` return trusted as genuine | **P1** | A method whose callee raises and isn't rescued internally | `collector_spec.rb` "returns fabricated by an exception unwinding a frame" |
| 22 | `9612e7e` | `:raise` treated as a close-out event, discarding a live (self-rescuing) frame and everything above it | **P1** | Guard clauses, `xs.each { raise Stop }` + rescue, `transaction { raise Rollback }` | `collector_spec.rb` "raises the raising method itself survives" |
| — | `fe43ec9` | (Refactor: extraction of `CallStackMachine`, no behavior change) | — | — | — |
| 23 | `a936a9c` | Round 20's exact defect was silently reintroducible with the post-extraction suite green (no test independently pinned matching-by-identity vs. push-for-every-call) | **P1** (regression-risk classification of what it reintroduces) | Same as round 20 | `collector_spec.rb` "keeps a live frame when a `:return` arrives for a call that was never pushed" |
| 24 | `c3e91d8` | (a) Singleton method owner derived from `tp.self` (receiver) instead of `defined_class.attached_object` | **P1** | Any inherited class method / `super` between singleton methods — the `ApplicationService.call` idiom every Rails app has | `collector_spec.rb` "subclass_first"/"keeps a super-calling override…" |
| 24 | `c3e91d8` | (b) `#workspace_method?` bare string-prefix containment (`/srv/app` matches `/srv/application/...`) | **P1** | Any sibling directory whose name extends the workspace root's (vendored gems, sibling engines, adjacent checkouts) | `collector_spec.rb` / `observation_neighbor` fixture |
| 25 | `f5b94be` | (a) `#defined_method` resolved via bare `instance_method`, reading the `prepend`-wrapper instead of the frame's own definition | **P1** | `ActiveSupport`/APM gems patching app code via `prepend`, or the app patching a gem the same way | `collector_spec.rb` prepend-chain examples |
| 25 | `f5b94be` | (b) Parameter-name dedup counted over the filtered req/opt projection, not the full declaration | **P2** | `def m(_, _)` — an unusual but legal signature | `collector_spec.rb` "pair(1,\"s\")" example |
| 26 | `6a38b89` | Round 25(b)'s dedup still counted over the wrong scope (whole `#parameters` list, still excluding `*rest`/`**kw`/`&blk`/`key:`) | **P2** | `def m(*_a, _a)` — narrower still | `collector_spec.rb` shadowed-rest example |
| 27 | `bf9dd06` | `#record` sized the aggregate's per-slot array once, from the first observation; a later, wider call (legitimate when two distinct classes share a `#name`) indexed out of bounds, raising into the blanket rescue and leaving the aggregate half-applied | **P1** | Zeitwerk/Rails constant reload, `remove_const`+redefine, rspec-mocks `stub_const` — ordinary dev/test workflow | `collector_spec.rb` wider-later / narrower-later examples |
| 28 | `1ceb706` | (a) Parameter-name dedup didn't see a name shadowed *inside* a destructuring parameter | **P2** | `def m(_a, (_a, _b))` — everyday idioms combined in an unusual way | `collector_spec.rb` "shadowed_by_destructure" |
| 28 | `1ceb706` | (b) `ParserService#extract_parameters` read `#name` off `requireds` unconditionally; a destructuring parameter has none → whole file lost from the index (sibling file, not Collector) | **P1** | Any workspace file containing one destructuring parameter | `parser_service_spec.rb` |
| 29 | `f501b99` | `ParserService#visit_namespace` read `constant_path.full_name` bare; raises for `class self::X` and (as `NoMethodError`) for `class Foo::` mid-edit → whole file lost from the index (sibling file) | **P1** | Any namespace path with a non-constant segment, and — critically — mid-edit while typing a compound class name | `parser_service_spec.rb` |
| 30 | `1844368` | `CallStackMachine#pop_matching` scanned past the top of the stack and could match a *live, still-running* outer invocation when a method's own argument-default raised, discarding every frame above it | **P1** | `def m(a, b = might_raise)` where `m` (or an ancestor call) is also live further down the stack — ordinary Ruby, no exotic construct | `call_stack_machine_spec.rb` + `collector_spec.rb` "unannounced_middle"/"unannounced_outer" |
| 31 | `635658c` | `@method_cache` held `defined_class` strongly, pinning every `Module` a method was ever called on (rspec-mocks doubles, `stub_const`, `Struct.new`/`Data.define`, per-object singleton methods, Zeitwerk reloads) for the life of the observation run | **P0** (impact on the target process: unbounded memory growth inside the user's own test-suite process) | Any suite using rspec-mocks/`stub_const` at any scale, or a long-running suite | `collector_spec.rb` singleton-churn / anonymous-class-churn examples |

Eleven distinct defects (round 23 is a coverage-gap classification of round
20's own defect, not a twelfth independent bug): **9 × P1, 1 × P0 (round
31), 2 × P2** (rounds 25b/26/28a are three sub-findings of the same
narrowing parameter-name issue, all P2). Zero P3 findings recorded — every
defect found across twelve rounds affected real, ordinary code, not a purely
theoretical concern.

### How the defects converged

Rounds 20–22 (pre-extraction): systemic — the stack bookkeeping was
unbalanced by construction, and every fix corrected the *previous* fix's own
premise. Rounds 23–24 (immediately post-extraction): the extraction's own
test coverage had gaps, and two more systemic-but-narrower defects (method
identity, containment) surfaced in the two concerns the extraction had
declared "never in question." Rounds 25–28: defects narrowed to specific
language constructs (`prepend`, parameter-name shadowing, first-of-two
levels of it) — still P1/P2, but each requiring a more specific code shape
than the last. Round 27 was a genuine change of shape (aggregation, not
per-call derivation). Rounds 28–29 surfaced two *sibling-file* defects (not
in `Collector`/`CallStackMachine` at all) via the same "assume every Prism
parameter/path node responds the same way" habit, both P1 due to
total-file-index-loss severity despite affecting only destructuring/
namespace-path edge cases. Round 30 found one more real defect in
`CallStackMachine` itself, disproving an explicit "defensive generality,
not load-bearing" claim two prior rounds had re-audited and accepted. Round
31 found the session's only P0: a resource leak, the same "bounded by
construction" class already fixed twice elsewhere in this codebase
(`AgentProcessManager`'s exit registry, rounds 17/18; the per-thread stack
registry, round 20).

Net trend: severity has not strictly decreased round-over-round (round 27
and round 31 are each arguably more consequential than the parameter-name
findings immediately before them), but *scope* has: every defect from round
25 onward requires an increasingly specific combination of language
features or runtime conditions to reach, rather than being reachable by
"call any method that calls any other method" (round 20's actual reach).

### Final guarantee scope

What `Collector`/`CallStackMachine` guarantee, and do not, as of `635658c`:

| Dimension | Guaranteed | Not guaranteed / residue |
|---|---|---|
| Ruby method call → return | Correct parameter/return types, for the actual frame regardless of `prepend` (row 14b) | — |
| C-method call/return | Not traced at all (deliberate scope limit); cannot desync the stack | — |
| Block call/return (`b_call`/`b_return`) | Not traced (deliberate scope limit, overhead budget) | A block that `break`s or non-locally `return`s abandons its enclosing method's frame with no distinguishing event — fabricated `nil` residue |
| Recursion (direct, mutual, via `send`) | Correctly attributed, innermost-first | If the recursive method's own argument default raises, the live (still-running) invocation on top closes one event early — return type lost, parameters kept (fifth residue) |
| Exceptions / non-local exits | `raise`/`rescue` in any of: same-frame, callee, block passed to a method — all correctly attributed, no fabrication, no collateral loss | `throw`/`catch`, `break`, non-local `return`, `Thread#kill` all abandon a frame with no event to key on — fabricated `nil` in that one union (four residues, all bounded, none fabricate a non-nil type) |
| Threads | Fully isolated (each gets its own fiber-local machine) | — |
| Fibers (incl. `Enumerator`, `blocking: true`) | Fully isolated; suspend/resume/drop-without-resuming all correct | — |
| `TracePoint` re-entrancy | Does not occur (CRuby suspends tracing during its own handler) | — |
| Frames pre-existing `#start` | Their stray `:return` finds an empty stack (outermost by construction) — harmless no-op | — |
| Frames open at `#stop` | Become unreachable garbage with the fiber that held them | — |
| `eval`/`define_method`/argument defaults | All produce ordinary, correctly-attributed frames; a raising default correctly produces no `:call` (row 11a) and is not mismatched against anything except the fifth residue above | — |
| `WeakKeyMap`/`object_id` reuse | `@method_cache`'s weak keys cannot be reused while referenced (an unreaped entry keeps its class alive only as long as the class itself is alive); `object_id`-derived fiber-local storage keys never collide (monotonic, unreused in this CRuby) | — |
| Unexpected/malformed event sequences | Never raise (I7); documented per-case policy above | — |
| Method redefinition mid-run (`class_eval`, `alias_method`) | — | Stale parameter names from before the redefinition (row 14) — degrades to `Types::UNKNOWN`/fewer slots, never fabricates a wrong non-Unknown type |
| `SystemStackError` (near stack exhaustion) | The machine itself never corrupts state | Frames stranded by the aborted unwind are never popped (harmless — they're simply never recorded against; subsequent, unrelated calls on the same machine are unaffected, measured) |

### Test matrix

Every row of the transition table (17 numbered rows, two lettered
sub-rows) and every dimension in the guarantee-scope table above has at
least one test exercising it:

| Coverage area | Test file | Method |
|---|---|---|
| Rows 1–4 (ordinary/nested/recursive/mutual calls) | `call_stack_machine_spec.rb`, `collector_spec.rb` | Hand-written + real-code (`factorial`, `mutual_a`/`mutual_b`) |
| Rows 5–6 (blocks/C methods not traced) | `collector_spec.rb` | `only_c_calls`, block-yielding fixtures |
| Rows 7–10, 11a (raise/rescue/ensure/guard-clause/yielded-block/raising-default shapes) | `collector_spec.rb` | `boom`, `rescues_internally`, `propagates_outside_raise`, `ensure_calls_nil_returner`, `guarded`, `raises_in_yielded_block`, `nil_or_outside_raise`, `unannounced_outer`/`unannounced_middle`, `recur` (fifth residue) |
| Row 11 (pre-existing frames) | `call_stack_machine_spec.rb` | Machine-level "no match anywhere" examples (real-code equivalent is the Harness's own `-r` install path, exercised implicitly by every `harness_spec.rb`/`runner_spec.rb` example) |
| Row 12 (post-stop frames) | `collector_spec.rb` | Retained-stack examples |
| Row 13 (defensive/malformed sequences) | `call_stack_machine_spec.rb` | Explicit unit examples + 20-seed generative fuzz (600 ops/seed) |
| Row 14/14a/14b (redefinition, receiver-vs-definer, `prepend`) | `collector_spec.rb` | `SubRegistry`/`Registry`, `OverridingRegistry`, prepend-chain fixtures (1–4 deep, both directions) |
| Row 15–16 (fibers, threads) | `collector_spec.rb` | `fiber_worker`, thread-churn examples |
| Row 17 (TracePoint non-reentrancy) | Established empirically each review round; not independently unit-tested (CRuby's own guarantee, not this code's) | — |
| `@method_cache` weak-reference bound | `collector_spec.rb` | Singleton-churn / anonymous-class-churn examples (bounded ≤ 1 / ≤ 50 after GC) |
| `#record` aggregation slot growth | `collector_spec.rb` | Wider-later / narrower-later class-reload examples |
| Parameter-name shadowing (all three levels: filtered list, full list, destructuring) | `collector_spec.rb` | `pair`, `trailing_after_rest`, `shadowed_by_destructure`, plus non-underscore/non-destructuring controls |
| Concurrency (multi-thread `#record`/`#results`) | `collector_spec.rb` | 8-writer/3-reader stress example |
| `ParserService` sibling defects (destructuring params, dynamic namespace paths) | `parser_service_spec.rb` | Destructuring-parameter example, `class self::X`/mid-edit `class Foo::` examples |
| Generative/property coverage | `call_stack_machine_spec.rb` | 20 fixed seeds × 600 ops each, differential-fuzzed against an independent reference model, asserting I2–I7 on every generated sequence |

No gap was identified between the transition table / guarantee-scope table
and this matrix as of `635658c`.

### Known limitations (P2/P3), carried forward rather than fixed

- **Parameter-name shadowing across an underscore-only collision that
  survives all three levels of the current guard** — none is currently
  known; the guard is stated as the general property ("this declaration
  binds this name in exactly one place") rather than as an enumerated list,
  specifically so a fourth level does not need discovering the hard way.
- **`Collector#@method_cache` staleness under `class_eval`/`alias_method`
  redefinition** (row 14) — under-collection only (stale parameter names →
  `Types::UNKNOWN`/fewer slots), never fabrication. Left alone since round
  22; the cheap fix if ever prioritized is `tp.lineno` as an O(1)
  invalidation oracle (round 26).
- **The four "no event to key on" residues** (`throw`/`catch`, `break`,
  non-local `return`, `Thread#kill`) and the **fifth residue** (recursive
  form of a raising default) — all bounded to one fabricated/discarded
  `nil` in one method's return-type union, never a wrong non-`Unknown`
  type. Fixing any of them requires tracing `:c_call`/`:b_return`, measured
  to fail this module's own overhead budget.
- **`SystemStackError` frame stranding** — harmless (measured: does not
  corrupt subsequent, unrelated recording on the same machine), left alone.
- **`ParserService`'s broader "assume every Prism node answers the same
  API" habit** — two instances found and fixed (rounds 28b, 29); not
  exhaustively swept across the entire file beyond the specific methods
  touched. Flagged as worth a dedicated pass if `ParserService` itself is
  ever the subject of its own review cadence, not fixed further here since
  it is out of this gate's scope (Collector/CallStackMachine).

## Where the tests live

- `core/spec/ovallsp/observation/call_stack_machine_spec.rb` — the state
  machine in isolation. Table-driven where the table above names a
  specific transition; a randomized/generative section constructs
  synthetic event sequences (bounded-depth pushes, pops with random keys,
  raises interleaved at random points) and asserts the machine-level
  invariants (I2–I7) hold for every generated sequence, without
  constructing any real Ruby call shape at all.
- `core/spec/ovallsp/observation/collector_spec.rb` — unchanged in intent
  from before this pass (real Ruby code, real `TracePoint`, asserting on
  `ObservedSignature` output), since this is exactly the boundary
  `CallStackMachine`'s extraction was meant to keep working: rounds 20–22's
  regression examples still pass unmodified and now additionally exercise
  the machine through `Collector`'s translation layer rather than
  `Collector`'s own ad hoc stack logic.

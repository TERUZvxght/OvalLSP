# 046 — 0.2.14: making the record true

**Branch: `feat/0.2.14`.** Measured at `6bc31b9`. Every count below was
re-derived at that revision, and where one differs from an auditor's, the
one here is the one that was run.

Asked for by the maintainer, in two parts: close the three gaps the
0.2.13 assessment left open, and audit the whole repository at a root
level for accumulated cruft — "指摘は作業量や手戻りの量を加味せず、とにかく
綺麗にすることにフォーカスを当ててください". Nine auditors over disjoint
areas produced 122 findings; each non-trivial one was then given to a
second agent whose instructions were to *refute* it. **15 survived
adversarial check, 9 were refuted, 28 low-severity carried forward.**

**That is 52 of the 122, and the other 70 have no recorded disposition
anywhere** — not here, not in the rounds below, not in a commit. The
figure is stated rather than left to be summed, because it was summed
by a reader two releases later and not by the person who wrote it.
`024.164`.

## The hypothesis, and the half of it that was wrong

The maintainer's hypothesis was that documentation has accumulated to the
point of quietly polluting the context of anyone working here.

**Half right, and the wrong half is the one that would have driven the
work.** "Too much documentation" predicts that the cure is volume
reduction. The evidence refuses that reading:

- The largest file in the tree — the register — is the one whose
  discipline is being *followed*. **66 of its 88 resolved entries** are
  cited from `core/lib`, `core/spec`, `vscode/src` or `scripts/`
  (re-derived in round 3; written as "61 of its 74" from a count nobody
  recomputed, and both halves had moved). Exactly
  **one** is cited from nowhere outside it.
- `core/lib` is 52.7% comment, and only **7 sentences** repeat across
  files. The heaviest-commented files are guards recording a specific
  incident.
- **Four "delete the accumulation" proposals were refuted**, each because
  the accumulation *was* the deliverable. `024.122`'s five successive
  figures are the record of a measurement going wrong, which
  `CLAUDE.md`'s "A measurement is a claim" section requires. `024.15` and
  `024.47` are named *in `CLAUDE.md`* as the mandated output of its own
  same-place rule.

What the evidence supports is sharper: **the pollution is not at the
front of the context window, it is at the end of every pointer.** The
1,753 lines an agent must read at session start are maintained and
largely true. The rot is in the files an agent reaches *because something
pointed at them*, and **nothing in this tree resolves a documentation
pointer.**

## Root causes

**RC-1 — frozen copies of sources that later became maintained.**
`RUBY_SEMANTIC_LSP_COMPLETE_DESIGN.md` (2,680 lines) concatenates README <!-- deleted -->
+ START_HERE + docs 01–11 as of 2026-07-25, is referenced by **nothing**,
carries the threading model 0.2.7 retracted and the plugin API that never
existed — and **contains none of section 0**, the trusted root every rule
in this repository is subordinate to.

*Round 3 re-derived this too, and it was understated. The file's four
`0.x` headings are `0.1-alpha`, `0.2-alpha`, `0.3-alpha`, `0.5-beta` —
release milestones in a roadmap list, matched by a regex looking for
section-0 headings. Its `01. Product Requirements` block begins at
`## 1. 背景`: no 開発理由, no 「完成」の定義, no accuracy-versus-shipping
principle. "Omits part of" was a regex artefact standing where "omits
all of" was true.* `make-final-review-bundle.sh` has no exclusion for it, so it
ships to every external reviewer under the name "COMPLETE DESIGN", beside
the maintained originals. Same class: `START_HERE.md`, <!-- deleted -->
`IMPLEMENTATION_SEQUENCE.md`, and the dependency manifest in three <!-- deleted -->
hand-maintained copies whose gate reads `目視確認済み`.

**RC-2 — briefs being read as descriptions.** `docs/design/docs/04`–`11`
are pre-implementation briefs from Task 001, and **37** citation lines
in `core/lib` point into the package.

*Round 3 re-derived this: it was written as 156, which is a count of
every `core/lib` line mentioning `docs/` at all (158), of which 115 cite
`docs/design/tasks/` — task records, not the design package. The
justification for rewriting three documents overstated the exposure
fourfold, and carried no marker anyone could check it against.* Three now describe a product that does
not exist: `05-protocol.md` (three agent requests specified and never
implemented, two shipped ones absent, §5/§6/§8 unimplemented and reading
as contract), `06-plugin-system.md` (**five registration methods, none of
which exists**; the shipped three are `register_declarations`,
`register_generic_rules`, `register_diagnostics`), `07-vscode-extension.md`
(**zero of eight command ids exist**, 8 of 10 settings absent). The
structural cause: `DOCUMENTATION_MAP.md`'s trigger table has **one** row
pointing into `docs/design/docs/`. Twelve documents, one trigger.

**RC-3 — pointers with no resolver.** **19 lines across 17 files** cite
five task filenames that have never existed in any commit. The whole
`plugins/` subsystem and the public SDK document point at one of them.
`024.87`'s **Area** names a file never committed, and it is the first
thing whoever picks up that open user-visible entry will act on.
`scripts/check_site_links.rb` exists and makes exactly this argument in
its own header — scoped to `site/` alone.

**RC-4 — registers whose entries have no second reader.** `024.64` is
open and its body asserts that running `activate()` in CI "was never
added… the reason is cost, not principle" — `ci.yml` has run it since
0.2.12, and `043`'s own *What shipped* table lists `024.64`. A 19-line
section of `024.57` sits inside `024.25`, opening "The table above" about
a table `024.25` does not contain. `024.90` is nine unrelated defects
under one number, and two of them are documented nowhere.
`AGENTS.md`'s work-in-progress paragraph has gone stale for the **third**
time, and the paragraph says so about itself.

**RC-5 — gates cited as green, run by nothing.**
`make-final-review-bundle.sh` (918 lines) is named by `RELEASE_CHECKLIST`
as the enforcement for five gate rows; its `BASELINE_COMMIT` does not
resolve (`fatal: Not a valid object name`) and no workflow, script or
spec invokes it. `test:integration:packaged` is marked ✅ in two rows and
run by no CI job. `verify_sbom_against_vsix.rb` is invoked by nothing.

**RC-6 — a discipline written in prose beside the tool that should
enforce it.** The 0.2.13 assessment's own finding, and it belongs here:
`scripts/corpus_diagnostics.rb` documents in a comment exactly the three
failures `026` records, and checks none of them.

## One release, or several

**This ships as 0.2.14. `0.3.0` stays the capability release the roadmap
already promises.** The maintainer's instruction was conditional — insert
it as 0.3.0 *if* the change is wide enough to deserve a minor version —
and the condition is not met, for a reason that is about meaning rather
than size:

**The minor version in this repository is a claim, and this change set
would make it false.** `045`, both roadmaps, both site roadmap pages and
both README matrices state that 0.3.0 is *the first release that may add
capability*. This change set adds zero capability and changes zero engine
answers. Numbering it 0.3.0 makes the version number itself a wrong
answer — the precise defect class the change set exists to remove.

Supporting: the shift costs **249 edits across 41 files** in two
languages including the public site, and `roadmap_parity_spec` and
`check_site_links` compare per-version item counts, so a half-finished
shift goes red in a way that says nothing about the cleanup. And every
mechanism-only release in this line shipped as a patch — 0.2.11, 0.2.12
(which shipped a whole new guard layer), 0.2.13.

**The structural half already has a home, and it is 0.3.0.** `024.R9` —
"This register outgrew its file, and 0.3.0 moves it" — is the recorded
place for the register restructure. Putting it in 0.2.14 would be
*adding* during a truth pass, which the review-cadence rule forbids.

| Unit | Contents | Release |
|---|---|---|
| **A — the truth pass** | Make every existing statement true or delete it | 0.2.14 |
| **B — the countermeasures** | Nine checks, each closing a class at occurrence ≥3 | 0.2.14 |
| **C — the register restructure** | Tombstones, relocation, the `024.90` split, the move | 0.3.0, as `024.R9`, *before* `024.85` |

### What this release claims, and the correction to it

**No behaviour changed. Two documented limitations did**, and the claim
this section originally made — *nothing a user can observe changed* —
was therefore false by the end of the pass:

- `024.130`'s paragraph was **removed** from `KNOWN_LIMITATIONS` in both
  languages. It described a limitation the product does not have.
- `024.131`'s was **corrected**: it said hover answers nothing, and
  hover answers `nil` for a local that is a `String`. Absent became
  wrong, which section 0 ranks the other way up.

Both are user-facing text, and both make it *more* true. Round 3 found
the mismatch between the claim and the pass; the claim is what changed,
because a release that discovers a published limitation is false does
not get to leave it published in order to keep a sentence tidy.

**The maintainer's ruling on this, recorded because it governs the rest
of the 0.2.x line:** what is being repaired here is precisely that
things which should already have been true were not, and *that* is what
made the version boundaries awkward to reason about. So the 0.2.x line
carries an explicit dispensation — **it may make some user-visible
change where the change is part of clearing up this class of problem.**
It is not a general licence to add capability in a patch; it is
permission to stop a patch release from being trapped by an accuracy
claim it made about itself.

**One deliberate exclusion, which the dispensation does *not* cover.**
The VSIX prune (≈4.4 MB of `.c`/`.h`/`.rbi` in `core/vendor`) changes
the **bytes users install**, which is a different category from
correcting what a document says. It stays in 0.3.0 with a
`verifyVendoredPayload` assertion that the two `.bundle` files survive.

**One amendment to `045`.** Its "the first task is `024.85`" sentence
becomes `024.R9` first: a register is cheapest to move when nothing is
appending to it, and 0.3.0 is a review loop that will append. `045`'s
argument for `024.85` orders the *capability* items and survives intact.

## Order, and why

**No deletion is safe until "nothing cites this" is a question the tree
can answer, and no pointer is worth fixing until the set of destinations
is final.** Both halves have already failed here — 19 citations to five
never-existent files survived because nobody could ask, and `AGENTS.md`'s
pointer broke *while being fixed*.

### A0 — done

`scripts/check_doc_links.rb` inspected **529 files** and found **663
documentation citations** at `26243e0`, the commit that added it.

*Stated as 527 and 661 until round 3, in the present tense, under a
header saying every count was re-derived at `6bc31b9`. Neither number
matches any revision: they were taken while the script was still
untracked, so it was not scanning itself — the same blind window as
`024.147`, and the reason that commit shipped red. Today's figures are
larger again because the checker's scope has since widened twice.* The census it produced on its first run
reproduced the audit exactly: **19 citations, 5 names, 17 files**.
(Corrected in 0.2.16: this said "every one in a source comment", and
re-running it — the A0-era checker restored from its own commit and
pointed at the tree of the day — shows eighteen `.rb` comments and a
nineteenth in a Markdown file — the plugin SDK document, deleted since
by `024.234`, so its path is described here rather than written: naming
it would be a citation of a file that no longer exists, which the
doc-link checker catches, and did. `024.178` is about a quantifier that was
wrong in four places; this was the fifth, in the document the entry's own
Area names.) Each named a document that exists under a
different filename —

| cited | actual |
|---|---|
| `007-active-record-model-and-local-inference.md` | `007-active-record-snapshot.md` |
| `013-unified-semantic-query-and-lsp-integration.md` | `013-unified-semantic-queries-and-lsp-features.md` |
| `016-guarded-rename-and-preview.md` | `016-guarded-rename.md` |
| `017-rails-dsl-extension.md` | `017-rails-dsl-expansion.md` |
| `018-static-runtime-plugin-api-and-sdk.md` | `018-plugin-api-and-sdk.md` |

— so all 19 were repointed rather than deleted, and the checker is green.
It runs in the suite (`core/spec/meta/doc_links_spec.rb`) and gates in CI.

**These five were fixed before the rest of A4**, out of the stated order,
and the reason is worth recording: a checker that is red on things it can
already resolve produces noise for every later step, so it has to be
green on the resolvable set before it can gate the pass that freezes the
rest of the destinations.

**A0 — the resolver, first and blocking.** `scripts/check_doc_links.rb`
resolves every `docs/**.md`, `tasks/*.md` and `adrs/*.md` path in tracked
content — **source comments included, since 18 of the 19 dangling ones
live there** (`024.178`; the nineteenth is in the public SDK document,
and the sentence said "all 19" until it was re-run) — with a
normalisation rule for the `docs/NN-*.md` shorthand (an established
shorthand, not an error; the occurrence count first written here was a
raw grep that also matched the fully-qualified form, and is retracted —
`024.179`).
Then record the census in this file, because every deletion in A2 rests
on it.

### A1 — in progress

Done: `024.87`'s Area (`built_in_generic_rules.rb`, never committed →
`generic_rule_registry.rb`), verified by **C3 built first and watched
firing on exactly that entry and no other** — which is the measurement
that the rest of the open entries' Areas are sound. `024.64` closed
against what shipped, with its Round 40 section deleted rather than
corrected because the claim had no surviving half:
`bin_vendor_bootstrap_wiring_spec.rb` had pinned that call before the
section was written. Its one surviving claim became `024.125`.

**Friction found while doing it, and fixed:** a checker that scans all
tracked content scans itself, so an example path written the way a real
one is written becomes a finding about the checker's own comment — and
then the sentence written to *explain* that became one too, because it
quoted the bad form in order to name it. The general rule is in
`check_doc_links.rb`'s header: make an example unspellable as a path
rather than exempting the file, because exempting it would stop checking
a file that carries four real citations.

**A1 — make the register true**, before anything reads it. `045` sends
0.3.0's first session into it, and a session that opens `024.64` today is
told a CI job that exists does not exist.

**A2 — delete the frozen copies**, after A0 proves nothing cites them.
Order matters: `05` and `06` exist byte-identically inside
`COMPLETE_DESIGN`, so deleting the copy first means A3 rewrites each
section once rather than twice — which is the whole failure mode.

**A3 — fix the design package**, with the three `DOCUMENTATION_MAP`
trigger rows in the *same* change, per the map's own rule.

**A4 — fix the pointers**, last, once the destinations are frozen.
Fixing a pointer while its target is still moving is `026`'s persisted
`cd` in prose.

**B — turn the checks on**, each observed failing first.

## What is deleted

| Path | Lines | Why |
|---|---|---|
| `docs/design/RUBY_SEMANTIC_LSP_COMPLETE_DESIGN.md` | **2,680** | Zero inbound references; omits part of section 0; carries the retracted threading model and the fictional plugin API; ships to every external reviewer | <!-- deleted -->
| `make-final-review-bundle.sh` | **918** | Baseline commit does not resolve; invoked by nothing; cited as the enforcement for five `RELEASE_CHECKLIST` rows |
| `docs/design/IMPLEMENTATION_SEQUENCE.md` | 63 | Zero inbound references; prescribes "1.0 = Task 022" with every task in it shipped | <!-- deleted -->
| `docs/design/START_HERE.md` | 42 | Step 1 is "copy this directory into a new repository"; its open questions all shipped. Drop the clause from both READMEs in the same commit | <!-- deleted -->
| `docs/design/docs/06-plugin-system.md` §§2–8 | 155 | Five registration methods that do not exist, a fictional manifest format, fictional types. Keep §1, §9, §10; point at `plugin-sdk.md`, which is accurate | <!-- deleted -->
| `docs/design/docs/07-vscode-extension.md` §3,5,6,7,10,11,12 | ~110 of 191 | Zero of eight command ids real; 8 of 10 settings absent; §12 superseded by ADR-0004. **§8 is rewritten, not kept** — `Static (untrusted workspace)` is not a producible status |
| `docs/design/docs/05-protocol.md` §6, §8 | 16 | Nothing implements `cancelRequest`; none of the four limits exists. The file stays — `agent_process_manager.rb`'s comment leans on §7, and the absence is itself part of the specification. (This row said "four source comments" until 0.2.16; it was one at every revision, and the other three `core/lib` citations of the file name no section at all — `024.168`.) |
| ~~`.github/workflows/ci.yml` `swallowed-failures` job~~ | 0 | **Kept.** The justification was false and was corrected; the job buys a separately-named red when the suite is failing for another reason. See A5 |
| ~~`.github/workflows/pages.yml` `pull_request:` block~~ | 0 | **Kept.** Deleting it leaves a site PR ungated before merge, and `site_version_guard_spec` pins it against 0.2.2's recorded blind spot. See A5 |
| 5 fixture files under `rails_minimal` | 5 files | Deletion-tested: each removed individually leaves the referencing specs at 0 failures |
| `engine.rb:1110–1120` | 11 | A comment about three methods deleted in 0.2.11, attached to no code. **`receiver_resolution#canonical_receiver_name` was kept** — ten call sites, argued delegation; the defect was two comment paragraphs run together. See A5 |
| `008.5` `## 残課題`, `008.6` `## 残っているKnown Issue` | 11 | The register's legend forbids a second collection point; these are the second and third. Promote their one surviving item first |

**3,975 lines and 5 fixture files deleted.** The design package was not
"rewritten" at volume: it is **+155 / −200 across nine files**, of which
four gained only a five-line header. Only `05`, `06` and `07` were
materially changed.

*And the change set as a whole is **+6,256 / −4,588** — a net increase of
1,668 lines. Round 3 derived all of this. The ledger's Lines column had
summed to 4,006 by counting three rows as deletions that were section
sizes **rewritten in place**: `05`'s §6/§8 both still exist (+25/−8),
`07`'s seven sections all still exist (+79/−68), and `06`'s §2 is
byte-identical and was kept. Round 1 had struck the two rows that were
kept entirely, and both carried Lines = 0, so that measurement never
reached the rows that did contribute. The closing phrase "this change set
is 4,000 lines of revert" is not what the diff shows and is removed.*

*The ledger above was written before the work and listed three rows that
were not deleted, with a total that summed them. Round 1 measured it.
Struck through rather than removed, because a plan that proposed a
deletion and a pass that refused it are both part of the record — and
the refusals are what produced A5's rule about naming what a reader
loses.*

## What is kept, where an auditor proposed deleting it

The refutations matter as much as the findings:

- **`024.122`, `024.15`, `024.47` in full.** The successive corrections
  *are* the entry, and `CLAUDE.md` names two of them as the required
  output of its own rule.
- **All 138 unticked acceptance boxes in tasks 001–022.** They are stage <!-- measured: unticked-acceptance-boxes-001-022 = 138 -->
  milestones rather than tracked obligations, and `RELEASE_CHECKLIST`
  quotes one verbatim as its own charter. *The reason first given here
  was that no box has ever been ticked in this repository's history.
  That is false — boxes are ticked across this tree's task files,
  including `008.5`, which is inside the stated range and is one of the
  two files this change set edited. No count is written here on purpose:
  it would be a number about this tree with no basis, which is the
  defect `024.160` is about. The decision to keep them stands on the
  sentence above; the reason given for it did not (`024.165`).*
- **`023.1` in full** — its 調査結果 holds the still-in-force decision to
  reuse `ovallsp.restartServer`, which appears in no ADR, and both
  `KNOWN_LIMITATIONS` files send **end users** here.
- **Both `034-*` files** — `035` delegates to the briefing in the present
  tense, and two of its findings exist nowhere else while `024.76` and
  `024.77` are open.
- **`03-semantic-engine.md`** — 12 citations point at sections that
  shipped verbatim, and its §9 is *stricter* than the engine. It gets a
  header sentence, not a rewrite: rewriting it as prose copies of
  `Data.define` would manufacture the duplication the countermeasure rule
  exists to remove.
- **`core/lib`'s 52.7% comment share.** Trim the misfiled blocks, not the
  ratio.
- **`CLAUDE.md`'s incident narratives (47% of the file).** The narrative
  is what makes each rule checkable, which the trusted-root argument
  requires.

  **`CLAUDE.md`'s sentence was softened** — "These rules were each
  written after a real incident" now says *most* were, and names the
  inference-derived ones as the ones most worth re-checking, since an
  inference carries the authority of an incident without having paid
  for it.

  **`AGENTS.md`'s paraphrase did not shrink; it grew by 15 words**, and
  this bullet asserted the opposite until round 1 measured it. Not done
  rather than not noticed: `CLAUDE.md` says *during a review loop, fix;
  do not add*, and restructuring the file a session reads first is an
  add — one whose premise (that the paraphrase is lossy in a way that
  matters) this pass has exactly one measurement for, `024.150`.
  Recorded there instead.

## The checks

Each closes a class at occurrence ≥3, which is `CLAUDE.md`'s own bar.

| | Class | Check | Fails on |
|---|---|---|---|
| **C1** | a doc path resolving to nothing | `check_doc_links.rb` + a meta spec | any unresolvable doc path in tracked content — source comments included, and relative Markdown links resolved against the citing file's own directory. *The relative half was added after round 1 measured 105 links outside the check while this row claimed all of them.* |
| **C2** | `AGENTS.md`'s work-in-progress pointer going stale | extend C1, fenced to that bullet | naming a task file that is not the highest on HEAD |
| **C3** | an open entry whose claim has decayed | every path in an **open** entry's `**Area:**` must exist; plus a release-time step to re-run the reproduction of every entry targeting the release being cut | `024.87` today |
| **C4** | two readers, one text, two grammars | extract `DeferredFindings` into `scripts/`; `metadata_of` reads it instead of scanning | a sub-numbered entry indexed under a dead anchor with the suite green |
| **C5** | a design document going stale with the code | three `DOCUMENTATION_MAP` rows, two of them machine-checked against `core/lib`'s method strings and `vscode/package.json` | the exact drift measured — for §5 that verification was weaker than this cell reads: the comparison was a regex sample of one file, and 0.2.16 found a template literal and a digit in a codicon name both slipping past it (`024.209`) |
| **C6** | a gate cited as green and run by nobody | every script or npm script in `RELEASE_CHECKLIST`'s evidence column must be invoked by `release.sh`, a workflow, or a spec | three today |
| **C7** | a generated artifact maintained by eye | regenerate `SBOM.md` in memory and assert the tracked file matches | the first dependency change reaching one copy |
| **C8** | a corpus run that does not report on itself | `corpus_diagnostics.rb` prints cwd, revision, dirty count, file-list sha256 and loaded VERSION; refuses an empty corpus; takes `--expect-control` | `026`'s three recorded false results |
| **C9** | committing on a partial run | `scripts/preflight.rb` + an installable `pre-commit`, documented in `CONTRIBUTING` | what happened twice in the 0.2.13 session |

### What they turned out to be

All nine are in. Five changed shape once built, and the changes are the
part worth reading:

- **C4** was to *delete* `metadata_of` along with the false comment
  above it. The method stayed and became a one-line delegation to
  `DeferredFindings.entries`, with the comment replaced by a true one:
  the defect was two grammars reading one text, and one reader calling
  the other closes that as well as deletion would while keeping the
  caller in `reindex_findings.rb` readable. Better than the row, and
  not the row — which is why it is listed here. *Both this bullet and
  the row above were corrected in 0.2.16 under `024.166`; the row had
  described a deletion that did not happen since the release shipped,
  and this list had four items.*
- **C2** was to verify `AGENTS.md` names the highest task file on HEAD.
  0.2.14 deleted the number instead — a pointer that must be edited every
  release is wrong most of the time — so the check is that the number
  does not come *back*. `agents_pointer_spec.rb`.
- **C6** needed comment-stripping to mean anything. Its first version
  searched for a cited script's name anywhere in the workflows and specs,
  and `release_script_guard_spec.rb` and `check_home_paths.rb` both
  *name* `make-final-review-bundle.sh` in comments explaining that it was
  deleted — so a plain text search found it and called the gate wired.
  **A check whose evidence is that a string appears somewhere is the same
  mistake as the table it is checking.**
- **C7** could not be a spec until the generator could compare without
  writing: it writes `docs/SBOM.md` in place, so a failing example would
  have left the tree modified. `--check` and `SBOM_PATH` exist for that.
- **C8** found a live hole while being built. Anything not a directory
  was taken as a file, so a mistyped corpus path became a corpus of one —
  the run reported `corpus-files=1`, found almost nothing, and looked
  like a run.

**Every one of the nine is checked by planting the thing it hunts**, not
by observing that it passes: C1 against a path no commit ever carried,
C5 against `07`'s actual pre-0.2.14 command list, C6 against a gate
citing a script nothing runs, C7 against a mutated licence field, C8
against a control that moved. A check that has only ever been seen
passing is `024.109`.

**And two of them tripped `024.126` while being written** — the
self-scanning trap, at its third and fourth occurrences. Both took the
same repair the entry already prescribes (make the example unspellable,
never exempt the file), which is the evidence that the rule sits at the
right level.

**Where a check deliberately does not go**, stated so the absence is a
decision rather than an oversight:

- **`024.90`'s grab-bag shape gets no spec.** Any rule counting bullets
  is guessing at authorial intent, and this repo has rolled back one
  countermeasure aimed at the wrong level (`024.47`). The fix is the
  split plus one legend sentence: *one entry states one defect, with one
  Area and one reproduction.*
- **`03`, `04`, `08`–`11` get a label, not a check.** One header
  sentence each: *this is a pre-implementation brief; where it and the
  code disagree, the code is the answer.*

**And a rule that changes rather than being enforced.** The register's
deletion rule — "a resolved entry is deleted once nothing cites it" —
fires on **zero of 74** entries, because the register is itself in the
tree. It guarantees monotonic growth. It becomes the tombstone form,
which the guard already accepts, and that is `024.R9`'s work in 0.3.0.

## The three gaps from the 0.2.13 assessment

Folded in above as **C8** (corpus self-reporting) and **C9** (the commit
gate). The third — `024.71`, the suite's cost — is **not** in 0.2.14: it
changes no statement's truth and is a precondition for `024.121`'s
whole-tree mutation run, so it belongs beside that work rather than
beside a truth pass. Recorded here so the omission is deliberate.

The two working-practice items the assessment found — `git checkout
<file>` discarding uncommitted work, and a completion check reading a
truncated output file — are not repository state and cannot be checked
by the repository. They go into `CLAUDE.md` as two lines. **No new
document**, since that is the failure this task is about.

## Execution

**A0** `check_doc_links.rb`; census recorded here.
**A1** `024.64`; a new entry for the packaged-integration gap; `024.68`
(+ move `024.69`'s resolution paragraph into it); `024.57`↔`024.25`;
`024.87`'s Area; `024.41`; `024.52`→`024.56`; `024.90` split into nine;
`WorkspaceIndex#search` promoted; `AGENTS.md`; `008.5`/`008.6`.
**A2** the four deletions + both README clauses.
**A3** `05`/`06`/`07` + three map rows, same change.
**A4** 19 pointer fixes across 17 files — **done inside A0**, for the
reason recorded there: a checker red on things it can already resolve
produces noise for every later step.
**A5** housekeeping; `PUBLISHING`'s publish command.

### A5 — two of the listed deletions were refuted by doing them

**Kept: `ci.yml`'s `swallowed-failures` job.** The finding was that its
stated justification — "this is where a push cannot get round it" — is
false, and it is: the `core` job runs the suite on the same triggers, so
the spec is no more evadable than the job. But the job buys something the
sentence never claimed. When the suite is red for an unrelated reason,
"Rescue verdicts" still reports *by name, in the checks list*, whether
that guarantee specifically holds. `doc-links` and `site-consistency`
are the same shape. **The false sentence was the defect; the job was
not.** Corrected in place.

**Kept: `pages.yml`'s `pull_request:` block.** "Runs three times on a
site PR" counted the cost and not what each run buys. Deleting it means
a site PR is not gated before merge, which is what a PR check is for —
and `site_version_guard_spec.rb` pins it deliberately, with 0.2.2's
recorded blind spot (a version bump moved `package.json` with no `site/`
change and the workflow never ran) as the reason.

*Both are worth recording because the audit that produced this list was
33 agents and it was right about the smell in each case. What it could
not do was weigh what a thing buys against what it costs, and a
cleanliness pass that only subtracts will eventually subtract something
load-bearing. The rule that came out of it: **before deleting redundant
enforcement, name what a reader loses when the other one is red.***

**Done in A5:** five `rails_minimal` fixtures, deletion-tested
individually against the four specs that reference the directory and
then against the full suite (3 controllers, 2 views — the other 7 each
took the suite to 26 failures). `engine.rb`'s tombstone comment about
three methods deleted in 0.2.11, attached to no code.
`receiver_resolution#canonical_receiver_name` **kept** — it has ten call
sites and its delegation is argued; the real defect was two comment
paragraphs run together with no break, the first describing the body the
method no longer has. `PUBLISHING` both languages → `024.141`.
**B** C1–C9, each observed failing first.

## Review rounds

**The finding list goes here as each round reports it.** `024.109` is
what the other arrangement cost — findings that lived only in a session
and were lost when it ended — and `CLAUDE.md` names writing them down as
the rule rather than the courtesy.

**Recorded departure.** `041` asks that one round use `drive`. This
change set alters no engine answer, so `drive` has nothing to compare
and cannot apply. Methods used are `diff`, `attack`, `reproduce`.

*Corrected in 0.2.16 under `024.162`. The
enumeration this departure originally gave was wrong in both halves,
and nobody re-derived it. What reproduces at `6bc31b9..6155cf4` is: 16
changed files under `core/lib` (+38/−47), of which **thirteen are
one-line citation repoints**, plus `semantic/query_service.rb` (3/3),
one comment rewrite (`semantic/receiver_resolution.rb`, 22/20) and one
deleted tombstone (`diagnostics/engine.rb`, 0/11). And the corpus
consumer did change in this very range — `scripts/corpus_diagnostics.rb`
is +125/−4 with a new 119-line spec, and C8 below records that building
it exposed a live hole where a mistyped path became a corpus of one,
which is what a `drive` round exercises. The conclusion survives on its
own measurement and is the reason the departure stands: zero
non-comment, non-blank changed lines under `core/lib` in that range
(the release commit adds two, and they are the version constant). The
restatement that used to sit in "Review cadence" below now points here
rather than repeating this, because both copies were wrong and a
justification kept in two places is wrong in one of them. The revisions
are named so these figures can be re-derived rather than believed —
`git diff --numstat 6bc31b9..6155cf4 -- core/lib` — and, the range being
fixed, they cannot drift the way the sentence they replace did.*

**How each round was asked.** Full tool access, the change set as
`main..HEAD`, `CLAUDE.md` and `AGENTS.md` given as the standard to hold
it to, and the instruction to *report anything you consider a defect,
whether or not it looks already known or deliberate*. No exclusion list,
no "concentrate on", no statement about what a clean report would look
like — `024.36` records what those three did to 0.1.15's counts.

Each finding is then given to two independent verifiers with different
lenses: one told to **refute** it and to default to refuted when
uncertain, one asked only whether it **reproduces**. A finding survives
only if neither kills it.

> **A note on how these are recorded.** A review finding's
> reproduction contains example data — probe filenames, synthetic
> register numbers, quoted `measured:` and deletion markers — and this
> tree's checks read tracked content, so recording one verbatim makes the
> record itself a finding. That happened here, at scale, the first time
> this list was written. The tokens below are therefore **neutralised**:
> a probe file reads `<probe>.md`, a synthetic entry reads `024.<n>`, and
> a quoted marker is described rather than spelled. `CLAUDE.md`'s section
> on this says why the alternative — exempting the file — is worse.

### Round 1 — `diff`

Five readers over the change set, each with a different starting point
(claims, checks, deletions, documents, code) and each free to report
anything anywhere. **84 raised, 30 distinct after deduplication, 18
survived adversarial verification, 12 were refuted with reasons.**

*This list was missing until round 3 found it missing — in the section
whose own opening sentence is "the finding list goes here as each round
reports it", above a citation of `024.109`, which is what the other
arrangement cost. Three of round 3's six agents reported it
independently. The counts had been committed; the content had not, so
none of the 36 findings that were neither duplicates nor confirmed could
be re-derived or re-opened. Written from the run's journal, which is
where a round's result lives (`024.149`).*

#### The 18 that survived verification, all fixed

> **These eighteen are truncated, and this note is the correction.**
> Every one of them is cut at 574–643 characters, mid-token, and the cut
> lands on the consequence clause and the reproduction — the part a
> reader needs. Commit `577704b` un-truncated round 2's seventy in the
> same change that wrote these eighteen with the same cut, so the
> paragraph below round 2 saying "Recorded in full and untruncated" is
> true of round 2 and of nothing else.
>
> The substance is not lost. Commit `8f1d4f4`'s message records this
> round in prose — 84 raised, 30 deduplicated, 18 survived, 12 refuted —
> and is where to read what these bullets stop short of saying. What is
> lost is the journal they were written from, so they cannot be
> re-derived in this form.
>
> Left as they are rather than reconstructed: a bullet completed from
> memory would read exactly like one that was never cut. `024.154`.


- **[medium]** docs/design/tasks/008.5-runtime-and-index-corrections.md:106-107 states that this section's three items became `024.136` `024.137` `024.138`. Only two did. `024.138`'s own provenance line (docs/design/tasks/024-deferred-review-findings.md:7380) says it came from **008.6**, not 008.5 — and 008.6's replacement paragraph already claims it. 008.5's third item, "Runtime Plugin機構（AASM等）は未着手のまま", became no entry: 024.139's disposition table (docs/design/tasks/024-deferred-review-findings.md:7416) rules it "**false** — Task 018 shipped it; `server_plugins_spec.rb` and `Plugins::CURRENT_PROTOCOL_VERSIO
- **[medium]** docs/DOCUMENTATION_MAP.md:41 — the checks column of the "A command id, setting, activation event, status-bar string, or workspace-trust declaration" row still reads "…the rest is unchecked; see `046`'s C5", but the same change set (commit 7c92b05) added core/spec/meta/design_doc_drift_spec.rb, whose four examples pin `07-vscode-extension.md`'s §3, §5, §6 and §7 against `vscode/package.json` and `vscode/src/clientPresentation.ts` — i.e. all four sections the clause calls unchecked. Every one of the five sections the row names is now machine-checked (§8 by `vscode/src/test/unit/workspaceTrust.te
- **[medium]** docs/design/docs/07-vscode-extension.md:78 claims `vscode/src/clientPresentation.ts` の `STATUS_LABELS` is the sole definition ("唯一の定義") of the five status-bar strings listed at lines 81-85, but STATUS_LABELS defines only four of them (vscode/src/clientPresentation.ts:80-85 — indexing, ready-static, ready-rails, agent-unavailable). The fifth, `$(error) OvalLSP: Configuration error` at doc line 85, is the separate constant STATUS_ERROR_TEXT at vscode/src/clientPresentation.ts:87. The source's own comment at line 76 says "The four states".  Demonstrate with:   /usr/bin/grep -n "STATUS_LABELS\|STA
- **[medium]** docs/design/tasks/046-0.2.14-making-the-record-true.md:270-273 states two corrections to the working-agreement files as dispositions of 0.2.14, and `git diff main..HEAD -- CLAUDE.md AGENTS.md` contains neither:  1. "What shrinks is **`AGENTS.md`'s 1,463-word lossy paraphrase**" — AGENTS.md is 1,463 words on main and 1,478 on HEAD. It grew by 15 words. The claim is false in the direction opposite to what it asserts, and the quoted figure matching main exactly rules out any ambiguity about the referent.  2. "`CLAUDE.md`'s \"These rules were each written after a real incident\" is not true of thr
- **[medium]** scripts/check_doc_links.rb:133-137 — the rescue's comment claims "not raising here would make an unreadable file look clean. Counted, and reported at the end", but nothing counts it and nothing reports it: `inspected` (line 121) is only incremented at line 140, after the skip, and the summary at line 158 prints only inspected/citations/recorded_deletions. The adjacent `next unless content.valid_encoding?` (line 132) drops a file the same way with no comment at all. A file dropped by either branch takes its citations with it and the gate still prints "every
- **[medium]** `docs/design/docs/05-protocol.md` §4 (line 94) and §5 (line 161) were not touched by this change set and still read as a contract for a protocol that does not exist, while the same change set added `docs/DOCUMENTATION_MAP.md:42` requiring that "a section describing something unimplemented says so in the section".  §4 documents nine requests. Three are implemented by nothing in the tree — `agent/routeLocation` (05-protocol.md:127), `agent/restartRequired` (:143), `agent/plugin/request` (:147) — and two that ship are absent: `agent/status` (`core/lib/ovallsp/runtime_agent/agent.rb:58`, sent from
- **[medium]** Narrowed: two of the three named documents are correct, and the file the finding names in FILE: is one of them. The real defect is in CLAUDE.md, which this change set added (7c92b05) and 024.148 then made stale without updating.  CLAUDE.md:441-442 — "It runs the full suite, the two real-Rails suites separately with a non-zero example-count assertion, and the five scripted checks" — is wrong three ways against `ruby scripts/preflight.rb --list`:  1. "the two real-Rails suites" — preflight runs three spec files (spec/integration/real_rails_spec.rb, spec/e2e/capabilities_spec.rb, spec/meta/client
- **[medium]** docs/design/tasks/046-0.2.14-making-the-record-true.md:235, :236 and :238 — the "What is deleted" ledger still lists `.github/workflows/ci.yml`'s `swallowed-failures` job (12 lines), `.github/workflows/pages.yml`'s `pull_request:` block (6 lines) and `receiver_resolution#canonical_receiver_name` as deleted, while A5 (:376, :386, :405) records all three as deliberately kept. All three are present at HEAD: ci.yml:327, pages.yml:36, receiver_resolution.rb:59 (12 call sites). The bolded total at :241, "**≈4,040 lines and 5 fixture files deleted**", sums the column exactly and therefore counts thos
- **[medium]** AGENTS.md:34 tells every session that opens on this branch "The release being prepared now is **0.3.0, on `feat/0.3.0`**", with its scope at `045-0.3.0-scope.md` (AGENTS.md:36-37) and 0.2.13/`044` as the last thing shipped (AGENTS.md:38-40). HEAD is `feat/0.2.14`, and `docs/design/tasks/046-0.2.14-making-the-record-true.md:103` — added by this change set — decides the opposite: "This ships as 0.2.14. `0.3.0` stays the capability release the roadmap already promises." The current release, its branch, and its task file appear nowhere in the paragraph AGENTS.md itself designates as the defence ag
- **[medium]** `core/spec/meta/measured_claims_spec.rb` is a third and fourth reader of `docs/design/tasks/024-deferred-review-findings.md` that C4 did not consolidate, and both diverge from `DeferredFindings` on the exact inputs C4's own record cites.  - `core/spec/meta/measured_claims_spec.rb:71` — `fields = yaml.scan(/^([a-z-]+):\s*(.*)$/).to_h` is the hand-rolled `key: value` scanner C4 deleted from `reindex_findings.rb`. On a quoted `status: "fixed"` — the divergence named verbatim at `scripts/reindex_findings.rb:47-49` — it yields `"fixed"` with the quotes, fails the `%w[fixed done]` test at line 72, a
- **[medium]** scripts/check_doc_links.rb:62-65 defines a "documentation path" as one literally beginning `docs/`, so 105 relative Markdown links in tracked Markdown (10 between task files, e.g. docs/design/tasks/036-road-to-1.0.0.md:211 `](042-second-enumeration.md)` and docs/ROADMAP.md:16 `](<a relative link to the register>)`) are outside the check entirely — yet the guarantee is stated without caveat in three places: scripts/check_doc_links.rb:7, the same sentence at the head of core/spec/meta/doc_links_spec.rb, and the countermeasure register at docs/design/tasks/046-0.2.14-making-the-record-
- **[low]** `docs/design/tasks/024-deferred-review-findings.md:6926` records `released-in: 0.3.0` for 024.124, but 024.124 was fixed in commit 6bc31b9 — the first commit of `main..HEAD` on branch `feat/0.2.14`, the release `046-0.2.14-making-the-record-true.md` names and measures at that same SHA — so it ships in 0.2.14, as the other thirteen entries fixed in this change set correctly say. The value is a leftover from before 046 fixed the release number: 6bc31b9 predates `fe05e3f 046: the plan for 0.2.14`. It should read `released-in: 0.2.14`. Nothing can catch it: `scripts/deferred_findings.rb:115-121` (
- **[low]** scripts/corpus_diagnostics.rb:105 — the comment introducing the parser branch reads `# `--expect-control unresolved-constant=9550`: a category the change ...`, but the parser two lines below requires the opposite punctuation on both sides: `--expect-control=` (line 110, `arg.start_with?`) and `CODE:N` (line 224, `expectation.split(":", 2)`). Following the comment gets the run refused: `bundle exec ruby ../scripts/corpus_diagnostics.rb --expect-control unresolved-constant=9550 <corpus>` exits 1 with "--expect-control, unresolved-constant=9550 does not exist
- **[low]** core/spec/meta/tmpdir_hygiene_spec.rb:25-42 is dead after the switch to a Prism visitor in 1bf897b: the `blockless_mktmpdir` regex (line 34), the `source_lines` helper (lines 40-42), and the two comments that exist only to explain them (lines 25-33 on the possessive quantifier, lines 36-39 on encoding-scrubbing) are referenced nowhere in the repo — `grep -rn "blockless_mktmpdir\|source_lines"` returns only their own definition lines. Deleting lines 25-42 leaves `bundle exec rspec spec/meta/tmpdir_hygiene_spec.rb` green (2 examples, 0 failures). Lines 29-33 are worse than merely unused: they do
- **[low]** scripts/documented_counts.rb:87 -- `puts "documented-counts: re-derived #{count} into #{stale.length} document(s)."` reports the number of documents that *needed* rewriting, not the number `DocumentedCounts.rewrite` actually changed, and the branch then exits 0. `rewrite` returns false when its gsub matched nothing (line 63), and that value is only used to decide the per-document line at line 86. A document whose pattern stopped matching is classed stale at line 73 (`stated` returns `[]`), is not written to at all, and is still counted in the success summa
- **[low]** `docs/design/tasks/046-0.2.14-making-the-record-true.md:336-338` states, in the section listing decisions rather than oversights, that "`03`, `04`, `08`–`11` get a label, not a check. One header sentence each: *this is a pre-implementation brief; where it and the code disagree, the code is the answer.*" — and `:261-263` offers the same header sentence as the reason `03-semantic-engine.md` is kept rather than rewritten. Neither exists. `head -4 docs/design/docs/{03-semantic-engine,04-runtime-agent,08-implementation-plan,09-test-strategy,10-ai-execution-guide,11-risk-register}.md` shows each fil
- **[low]** `core/spec/meta/release_script_guard_spec.rb:141` computes `block = block_containing(/files inspected/)` and never reads it, so the example named "makes the artifact check say what it inspected, and refuse an implausible count" pins only the refusal, not the reporting. `vscode/scripts/release.sh:188` — `echo "PASS: packaged-artifact path inspection (${INSPECTED} files inspected)"` — is pinned by nothing: `/usr/bin/sed -i '' '188d' vscode/scripts/release.sh && bundle exec rspec spec/meta/release_script_guard_spec.rb` still reports 8 examples, 0 failures. That line is the countermeasure's visibl
- **[low]** `register-open-defects` is a present-tense deriver bound to a past-tense sentence, and its only cite in the tree is `docs/design/tasks/037-0.2.7-concurrency-foundations.md:223` — a 0.2.7 record whose next clause is "plus this release's own rounds" and whose table lists 0.2.8/0.2.9 as future. So every release that opens or closes a register entry rewrites 0.2.7's number: it was 57 at the 0.2.7 merge (`git show f67e743:docs/design/tasks/037-0.2.7-concurrency-foundations.md | grep "open defects in the register"`), fell to 44 on `main`, and this change set's register expansion moved it to 55 (`git

#### The 12 refuted, and why

Recorded because a refutation is a result: a finding killed with a
reason is evidence the tree holds, and a reader who meets the same
suspicion later should find it already answered.

- C6 passes on the very gate RC-5 named: verify_sbom_against_vsix.rb is wired to nothing and the check is green
- The deleted-marker admits a renamed file's old path, contradicting the checker's own stated guarantee
- No hunk-by-hunk pinning sweep is recorded, though CLAUDE.md makes it mandatory before declaring a change set ready
- An unmeasured performance claim is published to end users while its own register entry says no measurement exists
- Two new user-visible open defects carry no target, so the release-time step this change set adds can never list them
- 024.90 ships as a defect fixed in 0.2.14 when nothing was fixed
- duplicate_headings only reads h1 and h2, which the spec never says
- 024.126's sweep is marked fixed over all six scanners while the same change set adds more with the defect
- docs/design/README.md, the entry point both top-level READMEs now point at alone, still declares Ruby 3.3+/Rails 7.1+
- 06 section 10, kept unrewritten, states a plugin contract the rewritten sections 3-8 table and the code both contradict
- C7 checks one of the three hand-maintained dependency manifests, and its fails-on claim overstates that
- The rationale for deleting make-final-review-bundle.sh accounts for only half of what it did

#### The 84 as raised, before deduplication

Titles only; the full text of each is in the round's journal. Kept
because the deduplication was mine and a reader may disagree with it.

- *[high]* The suite is red at HEAD, and the release commit and RELEASE_CHECKLIST both state it is green
- *[high]* C6's real assertion passes because a cited script names itself; two RELEASE_CHECKLIST gates are still enforced by nothing
- *[high]* HEAD ships a red suite, and the final commit message claims "2,374 examples 0 failures"
- *[high]* C6 reports a gate "wired" when the only thing naming the script is the script itself — two of the three gates it was built for still pass
- *[high]* The suite is red at HEAD: C6's own "would catch" example fails, because the spec scans itself
- *[high]* C6 passes on the very gate RC-5 named: `verify_sbom_against_vsix.rb` is wired to nothing and the check is green
- *[high]* C9's stated guarantee is false: preflight's skip detector passes on a run where every example was skipped
- *[high]* The change set is red: `release_gate_spec.rb`'s own planted string is inside the haystack it builds
- *[high]* C6 calls a gate "wired" because the cited script names itself; `verify_sbom_against_vsix.rb` is invoked by nothing
- *[high]* The suite is red at HEAD: C6's own planted-defect example fails
- *[high]* C6 reports two of the three gates it was built for as wired, for reasons other than the one it states
- *[high]* C9's "the skipped suites actually ran" check cannot fail in the case it exists for
- *[high]* C4's declared failure mode is not caught: a sub-numbered entry gets a dead anchor, a blank title and a duplicated number with the suite green
- *[medium]* `046`'s "What is deleted" table lists three things that were not deleted, contradicting the same document's own A5 section
- *[medium]* `046` states two corrections to CLAUDE.md and AGENTS.md that were not made, one of them measurably backwards
- *[medium]* DOCUMENTATION_MAP's new design-document row says the restatements are unchecked; the same change set added the check
- *[medium]* 07-vscode-extension.md §5 replaces a false claim about the status bar with a new false claim
- *[medium]* check_doc_links.rb silently drops any file it cannot decode, while its comment claims those files are counted and reported
- *[medium]* Three documents describe preflight's contents and none of them matches the eight checks it runs
- *[medium]* AGENTS.md still tells every session that the release being prepared is 0.3.0 on feat/0.3.0
- *[medium]* 046 records two edits to the working-agreement files that were never made
- *[medium]* "Every one of the nine is checked by planting the thing it hunts" is false for C9, C6 and C2
- *[medium]* RELEASE_CHECKLIST gate 15 names two steps as the hard fail for text-based leaks; neither inspects anything
- *[medium]* CONTRIBUTING now tells contributors to run only preflight before a PR, which runs no TypeScript check and skips the pinned-mutations gate
- *[medium]* 05-protocol.md §4 and §5 still specify three requests and four notifications that do not exist, and omit two that do
- *[medium]* C4 left a third parser of the register in place, and it diverges on the exact input C4's own example plants
- *[medium]* DOCUMENTATION_MAP's new C5 row says "the rest is unchecked" while the same change set adds the check
- *[medium]* 008.5's replacement paragraph is false and contradicts 024.139
- *[medium]* 046's "What is deleted" ledger lists three deletions that did not happen and two wrong totals
- *[medium]* C1 only matches paths literally prefixed `docs/`; 107 relative Markdown links and bare `tasks/`/`adrs/` citations are invisible
- *[medium]* 024.124 records itself as released in a release it was not fixed in, and its own check cannot see that
- *[medium]* scripts/preflight.rb is 178 lines of new behaviour with no test, and three of the nine checks have no planted case in the suite
- *[medium]* Both doc-link files justify not exempting themselves with a citation count that is zero
- *[medium]* `check_doc_links.rb` swallows an unreadable file and says it counts and reports it
- *[medium]* The stated countermeasure for the remaining pre-implementation briefs was never applied
- *[medium]* Two corrections `046` records as part of this release were not made: CLAUDE.md's sentence and AGENTS.md's size
- *[medium]* C3's real-file example passes vacuously if the Area extractor stops matching
- *[medium]* Three deliverables the plan states as done were not done
- *[medium]* "No box has ever been ticked in this repository's history" is false, in a file this change set edited
- *[medium]* 008.5's replacement paragraph misattributes an entry, and 024.139 says two sections were deleted that were not
- *[medium]* check_doc_links.rb silently drops unreadable files while its comment claims they are counted and reported
- *[medium]* The `[deletion marker]` marker admits a renamed file's old path, contradicting the checker's own stated guarantee
- *[medium]* An unmeasured performance claim is published to end users while its own register entry says no measurement exists
- *[medium]* 024.124 records itself as released in a version that does not exist, and nothing checks `released-in`
- *[medium]* No hunk-by-hunk pinning sweep is recorded, though CLAUDE.md makes it mandatory before declaring a change set ready
- *[medium]* C9 (preflight) is pinned by nothing, and the commit message's "each verified by planting" is false for three of the nine
- *[low]* tmpdir_hygiene_spec.rb leaves the regex it replaced, its helper, and eighteen lines of comment explaining both, all dead
- *[low]* release_script_guard_spec.rb computes a window it never uses, and that window is wrong
- *[low]* corpus_diagnostics.rb's comment spells the --expect-control flag in a form the parser rejects, and validates it only after the run
- *[low]* documented_counts.rb reports documents as re-derived when its pattern matched nothing and it wrote nothing
- *[low]* "seven rows named make-final-review-bundle.sh" contradicts `046`'s own count of five, in two places
- *[low]* The stated reason for keeping 05-protocol.md's section numbering overcounts its citations four to one
- *[low]* The tmpdir-hygiene rewrite left its replaced implementation and the comment justifying it in place
- *[low]* 07 §5 says STATUS_LABELS is the only definition, and one of the five strings it lists is not in STATUS_LABELS
- *[low]* corpus_diagnostics.rb documents a --expect-control syntax the script rejects
- *[low]* A measured-claim marker keeps rewriting a historical count, so 037 now asserts something nobody measured
- *[low]* C1's stated scope is wider than the regex: relative Markdown links are not checked
- *[low]* C3 skips open entries that are not kind: defect, have no Area, or name a path outside its prefix allowlist
- *[low]* release_gate_spec says seven rows named the deleted script; five did, and 046 says five
- *[low]* duplicate_headings only reads h1 and h2, which the spec never says
- *[low]* Four descriptions of preflight's check list, all introduced here, give four different counts and all omit the same check
- *[low]* 024.126's sweep is marked fixed over "all six scanners in the tree" while the same change set adds a seventh with the defect
- *[low]* The promised "pre-implementation brief" header was added to none of 03, 04, 08–11 — and 12 was never in the audit at all
- *[low]* docs/design/README.md — the entry point both top-level READMEs now point at alone — still declares Ruby 3.3+/Rails 7.1+
- *[low]* Dead code and a stale explanatory comment left in tmpdir_hygiene_spec after the switch to Prism
- *[low]* Two new user-visible open defects carry no `target:`, so the release-time step this change set adds can never list them
- *[low]* 024.90 ships as a defect "fixed" in 0.2.14 when nothing was fixed
- *[low]* Three of this change set's own numbers about the deleted release script disagree with each other and with the file
- *[low]* 06 §10, kept unrewritten, states a plugin contract the rewritten §3–8 table and the code both contradict
- *[low]* 07 §5's "STATUS_LABELS is the only definition" is inaccurate, and C5's extractor cannot see the case that makes it so
- *[low]* corpus_diagnostics.rb documents its own flag with the wrong syntax at the point that parses it
- *[low]* `046`'s deletion table still lists three items the same document later records as kept
- *[low]* Three documents give three different, all-wrong descriptions of what `preflight.rb` runs
- *[low]* `tmpdir_hygiene_spec.rb` left the replaced regex helper and its comment behind as dead code
- *[low]* C7 checks one of the three hand-maintained dependency manifests, and its "fails on" claim overstates that
- *[low]* The rationale for deleting `make-final-review-bundle.sh` accounts for only half of what it did
- *[low]* C5 has no planted counter-example although the plan says all nine do
- *[low]* `024.90` is recorded as `fixed` in `0.2.14` although all nine defects it held are open
- *[low]* `corpus_diagnostics.rb`'s inline comment spells the flag in a form the parser rejects
- *[low]* Three different counts of preflight's own checks, and CONTRIBUTING's enumeration omits one
- *[low]* 046's "What is deleted" table lists four things that were not deleted, and its ≈4,040-line total counts them
- *[low]* C1 checks only paths that literally begin with `docs/`, while the plan claims broader coverage
- *[low]* DOCUMENTATION_MAP's new row says the design document is unchecked when this release machine-checks four of its five sections
- *[low]* Two small counts in the new gate spec and plan disagree with the tree

### Round 2 — `attack`

Twelve attackers, one guarantee each, told to make it false while every
check stays green. **70 findings survived refutation**; 15 were killed.

**The working tree was dirty for almost all of this round, and it was
mine.** Round 2 ran 02:02–05:26; `024.149` was being written into the
register throughout and was not committed until 05:45. What was actually
verified was `git status --porcelain` *after* the round, which was empty
— and this section said "the tree was verified clean and at the same
HEAD afterwards" as though that covered the run. It does not, and the
sentence has been corrected to this one.

Three round-3 agents and one round-2 finding recorded the dirty tree
independently; one wrote *"Working tree was NOT clean when I finished,
and none of it is mine."*

**What it does and does not cost.** The attackers' subject was code —
scripts and specs — and my edit was to a task document, so the findings
themselves stand. What is not trustworthy is any **count of tracked
content** an attacker took: `check_doc_links` and `check_home_paths`
read that file, so two attackers running them minutes apart could get
different totals with nothing wrong. Numbers in round 2's findings are
re-derived before being acted on for that reason.

*This is the second time in one release, by the same author, after
writing the entry about the first. `024.149` carries it.*

**This round found roughly four times what round 1 did, and the reason is
the method.** `diff` reads what changed; `attack` asks whether a
guarantee *holds*, and almost every one of these is invisible to reading
because the code is correct — it is the code's **reachability** that is
not defended. `024.151` is the class.

*Recorded in full and untruncated — **round 2's seventy, and only
those**. The first version of this list cut every finding at 400
characters, mid-sentence, which round 3 found: a finding whose
reproduction is cut off is a finding nobody can act on, so it had been
recorded in form and not in substance. The same commit that fixed these
seventy wrote round 1's eighteen with the same cut, and this sentence
read as a claim about the whole record until `024.154`.*

**Dispositions.** 15 are fixed in this release, named in the commits
that fixed them. The remaining 55 are open under `024.151`, whose
Direction is two mechanisms rather than 55 patches. Absolute paths in
the original text have been made repo-relative — the attackers quoted
their own working directory, which put a real home path into a tracked
file twenty times.


#### `design-drift` — 5 confirmed

- **[high]** §5's status-bar check sees only single-quoted, icon-prefixed literals, so a new status string the extension really shows stays undocumented with everything green  
  `core/spec/meta/design_doc_drift_spec.rb` — The example extracts the "real" status strings with `source.scan(/'(\$\([a-z~-]+\) OvalLSP: [^']+)'/)`. Three independent conditions must all hold before a string is even a candidate: single quotes, a `$(...)` icon whose name is only `[a-z~-]`, and the literal `OvalLSP: `. Anything else is not compared against the document — it is silently absent from `real`, and set equality between the document's five lines and the five survivors still passes. There is no linter in the extension package (no eslint config anywhere in the tree, no lint script in vscode/package.json, no lint job in ci.yml), so quote style is entirely unconstrained; a double-quoted or template-literal status string is ordinary, compiling, idiomatic TypeScript that no check in this repository objects to. The guarantee the row in docs/DOCUMENTATION_MAP.md states — "§3, §5, §6, §7 against package.json and clientPresentation.ts — set equality both ways" — is therefore true of the manifest halves and false of the clientPresentation half: it is set equality against a regex sample of the file, not against the file's status strings. Note also that the check is already blind to one string the shipped extension produces: statusPresentation's fallback `` `OvalLSP: ${outcome.state}` `` at clientPresentation.ts:109, which clientPresentation.test.ts deliberately pins ('renders an unrecognised state by name, not as an error') and §5 does not mention while claiming the file is 唯一の定義 of five strings.  
  *Check:* In vscode/src/clientPresentation.ts add after STATUS_ERROR_TEXT: `export const STATUS_UNTRUSTED_TEXT = "$(lock) OvalLSP: Untrusted workspace";` and add `if (outcome.state === 'untrusted') { return { visible: true, text: STATUS_UNTRUSTED_TEXT }; }` before the final return of statusPresentation. Do not touch docs/design/docs/07-vscode-extension.md. Then `cd core && bundle exec rspec spec/meta/design_doc_drift_spec.rb` → 6 examples, 0 failures; and `cd vscode && npm run test:unit` → 202 passing. The same holds with the string written single-quoted but without the `$(...)` prefix, e.g. `'OvalLSP: Untrusted workspace (static only)'`. Suggested fix: extract the real list from the file's exported bindings rather than by quote character — e.g. scan for `OvalLSP: ` in any string form (`['"`]`) and fail on a match the icon-shaped regex did not also capture, so an unrecognised form is a failure rather than an omission.

- **[high]** §7's settings check drops any documented id containing a digit, hyphen or underscore, so the document can list settings that do not exist — C5's exact failure, reproduced under the check written to prevent it  
  `core/spec/meta/design_doc_drift_spec.rb` — The documented side is extracted with `.scan(/"(ovallsp\.[A-Za-z.]+)":/)`. The character class excludes digits, `-` and `_`, and because the following `"` is required the match simply fails rather than truncating — so a documented key like `"ovallsp.rails7Compatibility"`, `"ovallsp.agent-timeout"` or `"ovallsp.trace_server"` contributes nothing to `documented`, and set equality against the manifest still passes. A reader of 07 §7 is told these are what `contributes.configuration` defines; the spec's own header says zero of the commands and two of the settings in the previous version of that document were real, and this is the same class of falsehood surviving the check. The failure is silent in one direction and noisy in the other: if vscode/package.json ever gains a setting id with a digit, hyphen or underscore, the documented side can never contain it and the example fails for a reason that has nothing to do with drift — which invites weakening the regex further.  
  *Check:* Add to the fenced block under `## 7. Settings` in docs/design/docs/07-vscode-extension.md three lines: `"ovallsp.rails7Compatibility": true,`, `"ovallsp.agent-timeout": 30,`, `"ovallsp.trace_server": "off"`. Change nothing in vscode/package.json. Then `cd core && bundle exec rspec spec/meta/design_doc_drift_spec.rb` → 6 examples, 0 failures, with the document now listing eight settings of which three do not exist. Suggested fix: parse the fenced block as JSONC and compare its full key set (or at minimum widen to `[\w.-]+` and assert the extracted count equals the number of `"…":` keys in the block), so an unparseable key is a failure rather than an omission. Comparing the defaults as well would close the adjacent gap — only names are checked today, so a changed default in package.json leaves the document's shown default wrong with nothing noticing.

- **[medium]** §5 has no non-empty floor, so the status-bar comparison can pass on two empty lists — the vacuous shape the guard example was written to prevent, in the one section it does not cover  
  `core/spec/meta/design_doc_drift_spec.rb` — The final example, 'extracts a non-empty list from each place it reads', exists because "[e]ach example above compares two lists, and would pass on two empty ones". It asserts floors for §6's documented block, the manifest's commands and the manifest's settings — and nothing for §5's documented block, nothing for the strings extracted from clientPresentation.ts, and nothing for §3. §5 is the section whose extraction is most easily emptied, because it depends on a quote character in a package with no linter: a whole-file switch to double quotes (a Prettier pass, a new contributor's editor) empties `real`, and any reformatting of the fence empties `documented`. Emptying both is one commit, and the result is a green check certifying a document that lists no status strings at all against a file the check can no longer read.  
  *Check:* Change the five status literals in vscode/src/clientPresentation.ts from single to double quotes (values byte-identical, so `npm run test:unit` still passes — clientPresentation.test.ts compares string values, not quote style) and delete the contents of the fenced block under `## 5. Status Bar` in docs/design/docs/07-vscode-extension.md, leaving the empty fence. Then `cd core && bundle exec rspec spec/meta/design_doc_drift_spec.rb` → 6 examples, 0 failures with both sides `[]`. Suggested fix: extend the guard example with `expect(...§5 documented...length).to be >= 5` and a floor on the strings scanned out of clientPresentation.ts, and a floor on §3's parsed activationEvents.

- **[medium]** The plugin-sdk check verifies only that a name is defined somewhere under core/lib/ovallsp/plugins — not on which object, not whether public, not the arity — and its `defined` regex truncates at `!`/`?`/`=`  
  `core/spec/meta/design_doc_drift_spec.rb` — `named` is every `register_[a-z_]+` word anywhere in docs/design/plugin-sdk.md; `defined` is every `^\s*def (register_[a-z_]+)` across the plugins directory, flattened into one set. The comparison `named - defined` therefore cannot distinguish a method reachable on the receiver the document shows from one defined on a different class in the same directory. The document's whole purpose is to tell a plugin author what to call on `context`; the check answers a question about the directory. Two consequences, both verified: (a) the document can instruct an author to call a `RuntimeContext` method, or the module-level `Ovallsp::Plugins.register_static`, on a `StaticContext` — a NoMethodError which the Loader's own failure isolation then rescues and logs, so the author sees a silently disabled plugin rather than a diagnosis; (b) the `[a-z_]+` capture stops before `!`, `?` and `=`, so renaming a documented method to a bang form leaves the extracted name unchanged and the drift check green (loader_spec catches that specific method, but that is another spec's coverage, not this check's). `def self.register_*` is also unmatched by `defined`, which would produce a false failure rather than a false pass, but is the same brittleness. <!-- deleted -->
  *Check:* (a) In docs/design/plugin-sdk.md, inside the `Ovallsp::Plugins.register_static("ovallsp-my-plugin") do |context|` block, add `context.register_static("nested")` and `context.register_reload_hook { }` above the existing `context.register_declarations([`. Then `cd core && bundle exec rspec spec/meta/design_doc_drift_spec.rb` → 6 examples, 0 failures, while both added lines raise NoMethodError against `Ovallsp::Plugins::StaticContext`. (b) Rename `def register_snapshot_section(name, &block)` to `def register_snapshot_section!(name, &block)` in core/lib/ovallsp/plugins/runtime_context.rb → the drift spec is still 6/6 green (spec/ovallsp/plugins/loader_spec.rb fails, which is what actually catches it). Suggested fix: extract the receiver alongside the method from the document (`context.` vs `Ovallsp::Plugins.`) and check `Ovallsp::Plugins::StaticContext.public_instance_methods` / `RuntimeContext` / `Ovallsp::Plugins.singleton_class` respectively, which also removes the regex's suffix and `def self.` blind spots. <!-- deleted -->

- **[low]** DOCUMENTATION_MAP and 046's C5 row both overstate what the drift check enforces  
  `docs/DOCUMENTATION_MAP.md` — Line 41 states the check is "§3, §5, §6, §7 against `package.json` and `clientPresentation.ts` — set equality both ways" and closes with "All five sections the row names are machine-checked". §3 and §6 are set equality both ways. §7 is set equality only over the ids the documented-side regex can express, so the document may list settings that do not exist (finding 2). §5 is set equality between the document and a quote-character-dependent sample of clientPresentation.ts, so the file may define status strings the document does not list (finding 1). docs/design/tasks/046-0.2.14-making-the-record-true.md line 303 records the same as "the exact drift measured". This is the project's own failure mode — a record claiming a guarantee stronger than the mechanism delivers — sitting in the row written to prevent it.  
  *Check:* Apply finding 1's or finding 2's mutation and re-read line 41: the document and the code disagree while the row says they cannot. If the checks are strengthened as suggested, the row becomes true as written; if not, it should say set equality holds over package.json's commands and activationEvents, and is one-directional-modulo-a-regex for the settings and status-bar lists.


#### `doc-links` — 6 confirmed

- **[high]** The check's headline scope — "source comments are in scope" — can be reverted with all four examples green, because SKIP is pinned by nothing  
  `scripts/check_doc_links.rb` — SKIP is an unpinned constant and doc_links_spec.rb asserts nothing about how much of the tree the checker reads. Widening SKIP to `\A(core/|vscode/|site/|scripts/|core/vendor/|vscode/node_modules/|core/spec/fixtures/rails_real/|.*\.(png|ico|svg|lock|vsix|sqlite3)\z)}` drops inspection from 537 files to 117 and reinstates precisely the state the check was built to end: a dangling `docs/design/tasks/*.md` citation in a core/lib source comment passes. Example 1 ("all resolve to a file that exists") is satisfied by exit 0, which is what a gutted checker returns; examples 2-4 run against tmpdir fixtures the widened SKIP does not touch. The one narrower widening that did fail an example failed only because that example's fixture happens to sit at `<probe>.md` — a path coincidence, not an assertion. The counts the script prints (537 files, 786 citations) are asserted by nothing: `docs/design/tasks/046-0.2.14-making-the-record-true.md:155` records "527 tracked files" and "661 documentation citations", both already stale, and neither carries a `<!-- measured: -->` marker.  
  *Check:* Apply the SKIP widening above, add `<probe>.rb` containing `# See <probe>.md`, run `ruby scripts/check_doc_links.rb` (exit 0, "every documentation path resolves") and `cd core && bundle exec rspec spec/meta/doc_links_spec.rb` (4 examples, 0 failures). Restore SKIP and the same probe is reported. A countermeasure of the right shape: have the spec assert a floor on files inspected and citations counted, derived rather than typed, so gutting the input set is a red.

- **[high]** The guarantee is already false in the unmodified tree: CITATION can only name paths under docs/ and three enumerated leaf directories  
  `scripts/check_doc_links.rb` — `CITATION = docs/(?:design/)?(?:tasks/|adrs/|docs/)?[A-Za-z0-9._-]+\.md` cannot match any documentation path that does not begin `docs/`, and cannot match a docs subdirectory outside the three it lists. Twenty tracked Markdown documents live outside `docs/` — CLAUDE.md, AGENTS.md, README(.ja), CONTRIBUTING(.ja), SECURITY(.ja), SUPPORT(.ja), CODE_OF_CONDUCT(.ja), vscode/CHANGELOG(.ja), vscode/PRIVACY(.ja), vscode/README(.ja), vscode/THIRD_PARTY_NOTICES(.ja) — and tracked content names them constantly (CLAUDE.md 207 times, AGENTS.md 39, README.md 26, CONTRIBUTING.md 22, vscode/README.md 18, vscode/PRIVACY.md 13). None of those citations is checked unless it happens to be a relative Markdown link inside a `.md` file; a backticked one in Markdown, or any one in a source comment, is invisible. `docs/design/schemas/` is a real directory in this tree that the regex cannot name at all. The header says "every documentation path named in tracked content"; the register (046 C1) and CI job name repeat it. It is the same class of overclaim round 1 caught for relative links, one level up.  
  *Check:* With the unmodified script, write `<probe>.rb` containing `# See <probe>.md and <probe>.md and <probe>.md and <probe>.md`, and a `.md` file containing a backticked `<probe>.md`. `ruby scripts/check_doc_links.rb` exits 0 with "every documentation path resolves". Fix: match any `[A-Za-z0-9._/-]+\.md` token that names an existing-or-plausible repository path rather than enumerating directories, or at minimum add the root and vscode documents and stop enumerating docs subdirectories.

- **[medium]** A relative link beginning `docs/` is skipped by the relative pass and resolved by the absolute pass from the wrong base  
  `scripts/check_doc_links.rb` — Line 185, `next if raw.start_with?("docs/") # already counted by the pass above`, is only sound for Markdown at the repository root. In a nested document the link resolves against the citing file's own directory, but the CITATION pass resolves the identical text against ROOT. So a Markdown link to the roadmap written with its full repository-relative path, placed inside a task file, really points at that path appended to the task directory, which does not exist, and the checker reports it resolved (neither form is spelled out here — the checker scans this file, so quoting either would make this line the finding) — the exact failure the relative pass was added in round 1 to close, reopened by its own optimisation. I measured zero live instances today, so this is latent rather than broken now; it is the link a writer produces by copying a path out of docs/DOCUMENTATION_MAP.md into a task file.  
  *Check:* Add `<probe>.md` containing such a link; `ruby scripts/check_doc_links.rb` exits 0 while the link is dead on GitHub. Fix: drop the `start_with?("docs/")` shortcut and let a link be checked as relative unless the citing file is at the repository root, or check both interpretations and require one to resolve.

- **[medium]** The unreadable-file failure — the 0.2.14 repair for a swallowed failure — is unpinned, and removing it hides a dangling citation completely  
  `scripts/check_doc_links.rb` — Deleting the seven lines `unless unreadable.empty? … exit 1` leaves doc_links_spec.rb 4/4 green; nothing else in core/spec mentions the behaviour. With the block gone, a tracked file that is not valid UTF-8 is dropped from `inspected` with no output at all, so a citation of a document that has never existed passes and the check prints "every documentation path resolves". This is the block whose own comment says "until 0.2.14 this comment claimed and nothing did" and cites CLAUDE.md's swallowed-failure rule — the repair shipped with no test, which is CLAUDE.md's "unpinned behaviour is a defect in its own right" verbatim. Same for the shallow-clone refusal (line 93): changing `if shallow.strip == "true"` to a string git never returns also leaves the spec 4/4 green, though that one fails safe.  
  *Check:* Delete the block, then `printf '# See <probe>.md \xff\xfe\n' > <probe>.rb` and run `ruby scripts/check_doc_links.rb` (exit 0) and `cd core && bundle exec rspec spec/meta/doc_links_spec.rb` (4/4 green). Add an example that plants an invalid-UTF-8 file in a tmpdir root and requires a non-zero exit naming it, and one that runs against a `git clone --depth 1` and requires the refusal.

- **[medium]** Locally the check holds only by accident of a case-insensitive filesystem  
  `scripts/check_doc_links.rb` — Resolution is `File.file?(File.join(ROOT, target))`, which on macOS APFS is case-insensitive: the roadmap's path with its filename lower-cased, and this register's path with one word of its filename upper-cased, both "resolve" and the check exits 0, while both are dead links on Linux, in a Linux checkout, and in GitHub's web renderer. The CI job runs on ubuntu-latest so CI catches it, which means `scripts/preflight.rb` — the gate whose purpose is to be run before the commit — is strictly weaker than CI on a case-only typo. That is the same local-green/CI-red asymmetry CLAUDE.md records for the real-Rails and capability suites. It also makes `ever_existed?` (git, case-sensitive) and the resolver disagree about the same path.  
  *Check:* `ruby -e 'p File.file?("docs/ROADMAP.md")'` prints true, and so does the same call with the filename lower-cased (not written out: the checker scans this file). Then write a source comment citing the lower-cased spelling and run the checker: exit 0. Fix: after `File.file?` succeeds, confirm the resolved path appears verbatim in the enumerated file list (RepoFiles already has it) rather than trusting the filesystem's idea of equality.

- **[low]** 046's A0 census numbers are stale and unmarked, in the release whose title is making the record true  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — Line 155 states the checker "inspects **527 tracked files** and finds **661 documentation citations**". The script prints 536 and 786 today. Both are claims about this tree, neither carries a `<!-- measured: -->` marker, and nothing re-derives them — exactly the failure mode measured_claims_spec.rb's header enumerates three prior instances of. Separately, the CITATION header comment at line 61 says "Anchors and trailing punctuation are stripped by the caller"; no caller strips anything (the regex's own character class ends the match), so the comment describes code that does not exist.  
  *Check:* `ruby scripts/check_doc_links.rb` and compare its first line against 046:155. Fix: mark both numbers and add a deriver, or restate them as historical with an `@<rev>` binding.


#### `duplicate-headings` — 5 confirmed

- **[high]** A 4-backtick fence silently switches the duplicate-heading check off for the rest of the file  
  `core/spec/meta/duplicate_headings_spec.rb` — `headings_in` tracks fences with `marker[0]` only — the fence CHARACTER, never its LENGTH. CommonMark closes a fence only on a marker of the same char AND at least the opener's length. So in a ````-fenced block that quotes ```-fenced code (exactly the markdown-in-markdown shape this repo writes constantly — 10-ai-execution-guide.md quotes templates, and this very spec's comment quotes fences), the inner ``` is read as the closer, and the real ```` closer then OPENS a fence that never closes. Every heading from that line to EOF is invisible. Nothing anywhere asserts the fence state is closed at EOF, so the check reports a file clean having examined none of it — CLAUDE.md's named pattern, 'a checker that cannot see the thing it checks reports exactly what a working checker reports when nothing is pinned'. The related `\A\s*` on the fence regex means a fence marker shown inside a 4-space-indented code block also toggles state spuriously.  
  *Check:* Create `<probe>.md` containing: `# T`, blank, then a ```` fence line, then ```ruby, then `x = 1`, then a ```` line, blank, then `## Dup`, `a`, `## Dup`, `b`. Run `bundle exec rspec spec/meta/duplicate_headings_spec.rb` from core/ — 3 examples, 0 failures. Remove the file. Fix: close only on `same char && len >= opener len && line is bare marker`, allow `{0,3}` leading spaces on an opener only, and add a fourth example asserting fence state is nil at EOF for every file. I verified that EOF-balance guard passes on all 117 tracked .md files today with no exemptions.

- **[medium]** The exact incident class the check was built for still passes when the pasted block is a `###` subsection  
  `core/spec/meta/duplicate_headings_spec.rb` — The heading regex is `/\A\#{1,2} \S/` — h1 and h2 only. 024's entry states the guarantee as '**no tracked Markdown document states the same heading twice**' and this spec's comment as 'the same question asked of every tracked Markdown document'; both overstate what is checked, and nothing in the file records the h1/h2 scoping or why. The guarantee is false in the tree today at h3+ in five files. More importantly it misses the motivating defect: I took 024.15's own `### Why it is deferred rather than fixed` subsection (23 lines) and pasted it back into the register — precisely 'a scripted edit whose end boundary silently misses' — and the offender count went 0 -> 0. Register entries routinely carry ### subsections, so the file the check was written to protect is protected only at its ## level. Note the fix is not a flag flip: vscode/CHANGELOG.md and CHANGELOG.ja.md legitimately repeat `### Details` 29 times each and 08-implementation-plan.md repeats `### Deliverables` 11 times, so h3 needs a scoped rule (e.g. duplicates within one ## section, or per-document opt-in), which is why the current scope must at minimum be stated and argued in the file rather than left implicit.  
  *Check:* `ruby -e` over docs/design/tasks/024-deferred-review-findings.md: splice lines 715..737 back in after line 737 and run the spec's `headings_in().tally.select { |_,c| c>1 }` — same result before and after. Separately, plant `<probe>.md` with `### Dup` twice and run the spec: 0 failures. For the pre-existing violations: the same tally at levels 3-6 lists 5 files.

- **[medium]** Two behavioural lines in the fence parser are unpinned — including the one the spec's own comment calls load-bearing  
  `core/spec/meta/duplicate_headings_spec.rb` — CLAUDE.md: 'A behavioural line that no test fails on when it is reverted is a defect ... regardless of whether the behaviour itself is correct.' Two mutations of `headings_in` leave all three of the spec's own examples green AND produce zero offenders across all 117 tracked .md files: (1) replacing `elsif marker[0] == fence then fence = nil` with an unconditional `fence = fence.nil? ? marker[0] : nil`; (2) deleting the `~{3,}` alternative from the marker regex. Mutation (1) removes exactly the rule the comment above the method asserts — 'a fence closes on the same marker -- so a ``` inside a ~~~ block is content, which is how this file's own examples stay quotable'. That claim is stated in prose and tested by nothing. The existing fence example uses two separate ``` blocks, which stays balanced under both parsers, so it cannot distinguish them — CLAUDE.md's 'a spec whose fixture cannot distinguish the two candidate behaviours is unpinned even though it passes'.  
  *Check:* Distinguishing fixture the spec is missing: `# Doc` / blank / `~~~markdown` / ``` / `## Quoted` / ``` / `~~~` / blank / `## Real` / x / `## Real` / y. Original returns ["# Doc","## Real","## Real"]; the always-toggle mutant returns ["# Doc","## Quoted","## Real","## Real"]. Add that as an example, plus one tilde-only fence example, and both lines become pinned.

- **[medium]** Concurrent agents left the working tree modified; the dirty set changed three times during one review round  
  `.github/workflows/ci.yml` — Not about my guarantee, reported per instructions. While I ran, other agents mutated tracked files in this shared tree: `.github/workflows/ci.yml` gained `continue-on-error: true` on the 'Fail if the real-Rails or capability suites were skipped instead of run' job (later reverted), `docs/RELEASE_CHECKLIST.md` had test filenames rewritten to non-existent files (`ghost.test.ts`, `neverExisted.test.ts`, `deletedLastYear.test.ts`, later reverted), and the tree currently holds edits to `core/lib/ovallsp/plugins/runtime_context.rb`, `core/spec/meta/sbom_spec.rb`, `scripts/verify_sbom_against_vsix.rb`. CLAUDE.md's mutation-sweep rule already says never to mutate-test a tree another agent is mutating, because it invalidates both results — running several `attack` agents against one checkout is that hazard at round scale, and it made the hunk-level half of my work unsafe to perform.  
  *Check:* `git status --porcelain` repeatedly during a parallel round; the set differs between consecutive calls. Give each attack agent its own worktree (`git worktree add`) or a clone, so 'verify the tree is clean before you finish' is a statement an agent can actually make about its own footprint.

- **[low]** The fence regex tolerates indentation and the heading regex does not, in the same function  
  `core/spec/meta/duplicate_headings_spec.rb` — Fences are matched with `/\A\s*(`{3,}|~{3,})/` (any indentation) but headings with `/\A\#{1,2} \S/` (column zero only). In CommonMark an ATX heading may be indented up to 3 spaces, and a heading nested inside a list item is indented to the list's content column. Such a heading is a heading to every renderer and invisible to this check, so a pasted block that lands indented — which is what a scripted edit inserting into a list context produces — is not seen.  
  *Check:* Plant `<probe>.md` containing `# I`, blank, ` ## Dup` (three leading spaces), `a`, blank, ` ## Dup`, `b`. Run the spec: 3 examples, 0 failures. Fix: use `/\A {0,3}\#{1,2} \S/` and normalise the captured text before tallying, so `## X` and ` ## X` count as the same heading.


#### `encoding` — 6 confirmed

- **[high]** The require is enforced by substring search, so a comment satisfies it  
  `core/spec/meta/script_encoding_spec.rb` — Line 47: the check is `File.read(path, encoding: "UTF-8").include?('require_relative "utf8"')` — a substring test over the whole file, comments and string literals included. `# require_relative "utf8"` passes it. The second example is anchored on the same text (line 58, `lines.index { |l| l.include?(...) }`), so it locks onto the commented line and inspects only what precedes the comment, and passes too. Both examples stay green while the script crashes under LC_ALL=C with the exact `invalid byte sequence in US-ASCII` this countermeasure exists to prevent. The realistic form is not someone commenting a line out: it is a new script written in this repo's own heavily-annotated house style whose header explains the rule. `scripts/utf8.rb` line 26 carries that very paragraph, so copying the explanation and forgetting the code is one careless paste — and catching the fifth call site is the entire reason this check was written instead of a fifth local fix.  
  *Check:* In a clone: (a) `sed -i '' '4s|^require_relative "utf8"$|# require_relative "utf8"|' scripts/documented_counts.rb`, then `env LC_ALL=C LANG=C ruby scripts/documented_counts.rb --check` dies at line 49, while `cd core && bundle exec rspec spec/meta/script_encoding_spec.rb` reports 5 examples, 0 failures. (b) Add a new `scripts/check_ja_headings.rb` that mentions `require_relative "utf8"` only inside a comment and then does `File.read(<docs/KNOWN_LIMITATIONS.ja.md>).scan(/^## /)`: same result. Fix shape: match a real require line — `/^\s*require_relative\s+["']utf8["']\s*$/` per line rather than `include?` over the file — and pick the first line matching that, not the first line containing the text.

- **[high]** "Before anything that reads or shells out" is enforced as three literal names  
  `core/spec/meta/script_encoding_spec.rb` — Line 62: the ordering example decides "does this read or shell out" with `before.match?(/File\.read|IO\.popen|Open3\./)`. That is three names, not the claim. `IO.read`, backticks, `%x()`, `File.foreach`, `IO.foreach`, `File.binread` and `$stdin.read` all read or shell out and all slip past. Backticks are not hypothetical here: `scripts/check_home_paths.rb` line 136 uses them (`git log --all --format=...` in backticks) against this repo's Japanese commit messages, so it is an established idiom in exactly the tree this guards. The check therefore passes for a reason narrower than the one it states, and the guarantee it is quoted as enforcing is false for any script that reads through one of the unlisted names before its require.  
  *Check:* In a clone, insert above line 4 of `scripts/documented_counts.rb`: `JA_HEADINGS = IO.read(File.expand_path("../docs/KNOWN_LIMITATIONS.ja.md", __dir__)).scan(/^# /).length`. Leave the require intact. `env LC_ALL=C LANG=C ruby scripts/documented_counts.rb --check` dies at line 3 with `invalid byte sequence in US-ASCII`; `bundle exec rspec spec/meta/script_encoding_spec.rb` reports 5 examples, 0 failures. Fix shape: stop enumerating method names — require the `require_relative "utf8"` to be the first non-shebang, non-comment, non-`frozen_string_literal` statement in the file. That is what the prose actually claims, and it needs no list to keep current.

- **[medium]** Nothing ever runs a real script under a locale-less shell  
  `core/spec/meta/script_encoding_spec.rb` — The example at line 81, "survives a locale-less shell reading a Japanese document", runs a probe the spec writes into a tmpdir — not any file in `scripts/`. `grep -n 'LC_ALL\|LANG\|locale' .github/workflows/*.yml` returns nothing, and the six `ruby scripts/*.rb` steps in ci.yml inherit the runner's `C.UTF-8`. So the operative half of the guarantee — that the seventeen actual scripts survive LC_ALL=C — rests entirely on the textual require check, which the two findings above walk past. The probe proves `utf8.rb` works, which was never in doubt after the fourth incident; it proves nothing about the scripts. Compounding it: `check_doc_links.rb` and `check_home_paths.rb` get UTF-8 transitively through `require_relative "repo_files"`, so removing their own require changes neither the check nor the behaviour, and a per-file audit reports them clean for the wrong reason.  
  *Check:* `/usr/bin/grep -rn 'LC_ALL' .github/workflows/` — empty. Then confirm the gap is in the checking and not the tree: `for s in check_doc_links check_site_links check_swallowed_failures documented_counts; do env LC_ALL=C LANG=C ruby scripts/$s.rb; done` all succeed at 8f1d4f4 (documented_counts needs `--check`, check_home_paths needs `--tree` or `--messages`). Fix shape: add an example that execs each enumerated script's own file under `{"LC_ALL"=>"C","LANG"=>"C"}` in a no-op/usage mode and asserts none exits with `invalid byte sequence`. Handle the transitive case by checking the require is in the file itself, separately from whether the process ends up UTF-8.

- **[low]** An extension-less executable in scripts/ is not enumerated, and seven exemptions fit under the sentinel  
  `core/spec/meta/script_encoding_spec.rb` — Line 41 enumerates with `RepoFiles.list(SCRIPT_ENCODING_ROOT, "scripts/*.rb")`. Subdirectories are fine — git's `*` crosses `/` in a pathspec without `:(glob)` magic, which I confirmed — but the `.rb` suffix is load-bearing: a `#!/usr/bin/env ruby` file with no extension is invisible. Separately, `SCRIPT_ENCODING_EXEMPT` (line 38) removes any named script from both examples with nothing else noticing, and the `be >= 10` sentinel at line 69 has 7 scripts of slack against the current 17 — so up to seven names could be added to that list before the guard that exists to stop an empty list from passing would say anything.  
  *Check:* In a clone, write `scripts/count-ja-headings` containing a ruby shebang and `puts File.read(File.expand_path("../docs/KNOWN_LIMITATIONS.ja.md", __dir__)).scan(/^## /).length`, chmod +x. `env LC_ALL=C LANG=C ruby scripts/count-ja-headings` dies; `ruby -e 'require_relative "scripts/repo_files"; p RepoFiles.list(Dir.pwd, "scripts/*.rb").grep(/count-ja/)'` prints `[]`. For contrast, `scripts/lib/nested.rb` IS listed and the spec does fail on it. Fix shape: enumerate `scripts/*` and select by `.rb` suffix OR a ruby shebang; and tie the sentinel to the actual count (e.g. assert the exempt list has exactly one entry) instead of a floor with seven names of headroom.

- **[low]** `Encoding.default_internal = nil` is unpinned, and may not earn its place  
  `scripts/utf8.rb` — Deleting line 32 leaves all 5 examples of `script_encoding_spec.rb` green — nothing in the spec sets an internal encoding, so the probe cannot distinguish its presence from its absence. By CLAUDE.md's own rule ("a behavioural line that no test fails on when it is reverted is a defect in its own right") that is a finding. It is worth pairing with a second question before pinning it, though: I could not construct an observable difference. Under `RUBYOPT="-E UTF-8:UTF-8"` reading a file with a deliberately invalid byte, the read succeeded identically with and without the line. Its live range is only a `RUBYOPT`/`-E` that sets an internal encoding differing from UTF-8, which nothing in this repo does. So the decision is either to pin it with an example that sets something like `-E UTF-8:EUC-JP`, or to conclude it is decoration and drop it — but leaving it unpinned and unexplained is the one option the project's rules exclude.  
  *Check:* In a clone: `/usr/bin/sed -i '' '/^Encoding.default_internal = nil$/d' scripts/utf8.rb`, then `cd core && bundle exec rspec spec/meta/script_encoding_spec.rb` — 5 examples, 0 failures.

- **[low]** Stale comment: as_utf8's stated reason was made false by the utf8 require four lines above it  
  `scripts/check_home_paths.rb` — Lines 66-71 say backticks hand back a string tagged with the shell's external encoding, US-ASCII whenever LANG is unset, so this repo's Japanese commit messages raise on the first `split` under a bare local shell while passing in CI's UTF-8 locale. That was true before `scripts/utf8.rb` existed. With `require_relative "utf8"` at line 4 of this same file, `Encoding.default_external` is UTF-8 before any backtick runs, so the described failure cannot occur and the stated reason for `as_utf8` no longer holds. The `.scrub` still earns its keep — a commit message really can carry invalid bytes — but the paragraph explains the wrong hazard. That is the shape CLAUDE.md's revert rule warns about: prose correct when written, with nothing announcing it stale when the fix moved underneath it. Not my assigned guarantee; noticed while surveying backtick usage.  
  *Check:* Read `scripts/check_home_paths.rb` line 4 and lines 66-74 together. Then confirm the claim is dead: in a clone, a probe that requires `scripts/utf8` and then runs `git log -1 --format=%B` in backticks under `env LC_ALL=C LANG=C` gets a UTF-8 string, not US-ASCII. Rewrite the comment around invalid bytes rather than around the locale.


#### `home-paths` — 8 confirmed

- **[high]** A committed symlink's blob is a home path the scanner never reads  
  `scripts/check_home_paths.rb` — offences_in_file (line 90) resolves a path with File.file? and File.binread, both of which dereference symlinks. Git does not: for a symlink it stores the target string as the blob's entire content. So `ln -s /Users/<real>/... x && git add x` commits and pushes the home path verbatim while the scanner either reads the *target file's* content instead (live link) or returns [] at the File.file? guard (broken link). The broken-link case does not even record a skipped_files entry, so the 0.2.5 fix for silent skips — whose whole point was that a file the check could not clear must say so — does not cover it. Nothing else compensates: gitleaks scans for secret patterns, not home paths, and release.sh inspects the VSIX, not the tree.  
  *Check:* cd <repo> && ln -s /Users/$(whoami)/WorkSpace/secret/nope docs/leak_link && git add docs/leak_link && git cat-file -p :docs/leak_link # prints the real home path that would be pushed ruby -e 'require_relative "scripts/check_home_paths"; puts HomePaths.tree_offences.size; puts HomePaths.skipped_files.map { |s| s[:path] }.inspect' # => 0, and the link is not even listed as skipped (cd core && bundle exec rspec spec/meta/home_path_guard_spec.rb) # => 10 examples, 0 failures git rm --cached -q docs/leak_link && rm -f docs/leak_link Fix shape: use File.lstat/File.readlink (or read the blob via git cat-file) so link text is scanned as content, and record a skip for anything not cleared.

- **[high]** The binary/invalid-encoding skip clears a whole file, and nothing pins which files it clears  
  `scripts/check_home_paths.rb` — One NUL byte or one invalid UTF-8 sequence anywhere in a tracked file makes the scanner return [] for the entire file (lines 101-110), including any real home path in its plain-ASCII portion. The compensating story — 'a skip is reported' — is only printed by the CLI's no-offence branch; the spec asserts nothing about which files are skipped or how many, only that the reasons are drawn from %i[binary invalid_encoding], which is true by construction since those are the only two literals in the code. So the skip set is free to grow to any size, containing any file, with the suite green. Same class as CLAUDE.md's 'green because it did not run', arriving through the skip list instead of through RSpec.  
  *Check:* cd <repo> && printf 'built at /Users/'$(whoami)'/WorkSpace/secret\x00trailing\n' > `<probe>.md` ruby -e 'require_relative "scripts/check_home_paths"; puts HomePaths.tree_offences.size' # => 0 (cd core && bundle exec rspec spec/meta/home_path_guard_spec.rb) # => 10 examples, 0 failures rm -f `<probe>.md` Repeat with '\xff\xfe' instead of '\x00' for the invalid_encoding half. Fix shape: scan the file's clean-decodable portion anyway (scrub, then match), and pin the skip *set* — an allowlist of the two icon PNGs — so a new skipped file fails rather than being tolerated.

- **[high]** 'skips compiled payloads' is an assertion that cannot fail  
  `core/spec/meta/home_path_guard_spec.rb` — The example at line 123 asserts HomePaths.offences_in_file("does/not/exist.bundle") is empty. That path does not exist, so the method returns [] at the File.file? guard and the NUL branch it claims to be about is never reached. The example is true whatever the binary-skip logic does — including if it is deleted outright, which I verified. CLAUDE.md's own rule: 'Before writing an example, ask what would have to happen for it to fail; if the answer is "nothing", it is asserting nothing.' It is also the only example that names the binary branch, so that branch is entirely unpinned in the sense of the 'unpinned behaviour is a defect' section.  
  *Check:* cat > /tmp/kill.rb <<'RUBY' require_relative "scripts/check_home_paths" module HomePaths module_function def offences_in_file(rel) abs = File.join(ROOT, rel); return [] unless File.file?(abs) c = File.binread(abs).force_encoding(Encoding::UTF_8) unless c.valid_encoding? then skipped_files << {path: rel, reason: :invalid_encoding}; return [] end c.lines.each_with_index.flat_map { |l,i| names_in(l).map { |n| "#{rel}:#{i+1}: #{n}" } } end end RUBY cd core && bundle exec rspec -r /tmp/kill.rb spec/meta/home_path_guard_spec.rb # => 10 examples, 0 failures with the binary branch deleted Fix shape: write a real fixture — a Dir.mktmpdir file containing a NUL and a planted name — and assert both that it yields no offence AND that it appears in skipped_files with reason :binary.

- **[high]** The CI commit-message scan can be turned off with `if: false` and its guard stays green  
  `core/spec/meta/ci_skip_guard_spec.rb` — The example at line 206 is named 'still runs the message scan as an executed step' but only asserts the step exists in the parsed YAML and that its `run` contains `--messages`. Adding `if: false` (never runs) or `continue-on-error: true` (runs, fails, build stays green) leaves all 13 examples passing. The commit-message half of the guarantee has nowhere else to run — the tree spec deliberately does not check messages, and preflight.rb is not invoked by CI — so this single line is the whole enforcement. The same spec file already knows the fix: ~30 lines above, the 4.0 count step is pinned with expect(count_step["if"].to_s).to include("!cancelled()"). The describe block's comment even states the threat model ('deleting the step leaves every check green while the disclosure path reopens') and then checks only deletion, not disabling. The same gap exists on the suites-ran guard step, which a concurrent agent was mid-way through demonstrating in the working tree while I ran.  
  *Check:* cp .github/workflows/ci.yml /tmp/ci_mut.yml, insert `if: false` and `continue-on-error: true` under `- name: No real home directory path in any commit message`, then copy core/spec/meta/ci_skip_guard_spec.rb to a temp spec with only the ci.yml path swapped to /tmp/ci_mut.yml and run it: 13 examples, 0 failures. Fix shape: assert step["if"] is nil-or-truthy and step["continue-on-error"] is not true, for this step and for the suites-ran guard step.

- **[medium]** Doubled and escaped separators evade the pattern, including the form the scanner's own comment claims to catch  
  `scripts/check_home_paths.rb` — PATTERN (line 45) requires exactly one separator character immediately followed by an alphanumeric, so every doubled or escaped spelling of a real home path slips through. The forms are described rather than spelled, because this file is scanned by that pattern (`024.126`): a Windows drive path with the doubled backslash as JSON, TypeScript or a pasted fenced block stores it; the JSON escape that puts a backslash before the solidus; and a doubled slash. The first is the sharpest — line 31 of the scanner writes that form and says it 'is caught', and the scanner does not flag its own line, so a real username written there would be published undetected. Also missed: the agent-scratchpad mangling, where the path is joined with hyphens instead of slashes, a plausible paste in this repo given how much command output its task documents quote, which discloses both username and directory layout.  
  *Check:* build each spelling from parts rather than typing it and pass it through `HomePaths.names_in`; each returns `[]` while the single-separator spelling built the same way returns the name. `core/spec/meta/home_path_guard_spec.rb` holds the pinned versions.

- **[medium]** Annotated tag messages are a pushed public channel no mode scans  
  `scripts/check_home_paths.rb` — message_offences (line 132) runs `git log --all --format=...%B`, which prints *commit* messages. This repository has 25 annotated tags (git for-each-ref refs/tags --format='%(objecttype)' => 25 tag, 4 commit), whose annotation bodies are pushed to the public remote and are not commit messages. I confirmed the v0.2.12 tag's own subject appears zero times in `git log --all --format=%B`. Release tags are written by hand at release time — exactly the moment 0.2.3 pasted a build machine's home directory into a commit message — so the channel is the same class the check exists for, and it is unscanned. No tag carries a home path today, so this is latent.  
  *Check:* cd <repo> && git for-each-ref refs/tags --format='%(objecttype)' | sort | uniq -c # 25 tag git log --all --format='%B' | grep -c "$(git for-each-ref refs/tags/v0.2.12 --format='%(contents:subject)')" # => 0 Fix shape: in --messages mode also iterate `git for-each-ref refs/tags --format='%(refname:short)%00%(contents)'` and run names_in over each annotation.

- **[medium]** One word added to SYNTHETIC disables the detector with every check green  
  `scripts/check_home_paths.rb` — The comment above SYNTHETIC (line 47) says adding a name 'is meant to be a deliberate edit with a reason: an unknown name fails rather than being quietly tolerated' — but nothing enforces either half for the list itself. Adding the maintainer's real username makes the guarantee unenforceable and all 10 examples still pass, because the planted-name examples use alice/tkato/bob/carol. Compare measured_claims_spec.rb, which this file's own docs cite as the same shape and which does require a two-part deliberate edit. 024's register already calls this list 'already right' — the list is right in shape and unguarded in fact.  
  *Check:* cd <repo> && cat > /tmp/widen.rb <<'RUBY' require_relative "scripts/check_home_paths" HomePaths.send(:remove_const, :SYNTHETIC) HomePaths.const_set(:SYNTHETIC, %w[example exampleuser dev rhc runner runneradmin teruz].freeze) RUBY (cd core && bundle exec rspec -r /tmp/widen.rb spec/meta/home_path_guard_spec.rb) # => 10 examples, 0 failures Fix shape: pin the list's exact contents in the spec so any addition is a two-file edit, and assert the Marketplace publisher id from vscode/package.json is NOT a member.

- **[low]** 'reports what it skipped rather than returning silence' cannot fail either  
  `core/spec/meta/home_path_guard_spec.rb` — In the example at line 95, expect(skipped).to be_an(Array) is true by the memoised `@skipped_files ||= []`, and expect(reasons.uniq - %i[binary invalid_encoding]).to be_empty is true by construction — those two symbols are the only values any `skipped_files <<` site writes. It is also order-dependent: with RSpec's random ordering, if it runs before any tree_offences call the list is empty and both expectations pass vacuously. So the 0.2.5 change it exists to pin — that skips are reported rather than silent — is unpinned, and the reporting could be deleted from the CLI with this example green.  
  *Check:* cd core && bundle exec rspec spec/meta/home_path_guard_spec.rb -e 'reports what it skipped' --seed 1 # passes; the reasons come from the only two literals in the source, so no code change can make the subtraction non-empty Fix shape: assert the concrete set (the two icon PNGs, reason :binary) after an explicit tree_offences call, so a new skipped file or a lost skip record fails.


#### `measured` — 6 confirmed

- **[high]** The `mutex-sites` deriver ignores `@<rev>`, so a dated claim is checked against the present tree — and the failure message tells the author to write the false historical number  
  `core/spec/meta/measured_claims_spec.rb` — `"mutex-sites" => lambda { |_rev = nil| ... }` accepts the revision and discards it (the `_` prefix means no lint notices). `register-entries` and `register-open-defects` pass `rev` to `register(rev)` and genuinely derive from that revision; `mutex-sites` does not. Nothing checks that a deriver invoked with `@rev` actually consumes it, so the guarantee's dated half is true for two derivers by construction and false for the third by accident. The consequence is inverted, which is what makes it dangerous: a historical document that writes the TRUE count at its revision goes red, and the message reads "mutex-sites says 29, the tree has 31 / Re-derive the number rather than editing the prose around it" — instructing the author to put 31 into a sentence about 0.2.7. That is verbatim the failure mode the same file's own comment describes for `register-entries` ("a historical document ended up asserting a number nobody had measured against the thing it describes"), left standing in the deriver directly above it.  
  *Check:* From the repo root: `printf '\n[measured marker for mutex-sites]\n' >> docs/design/tasks/037-0.2.7-concurrency-foundations.md` then `cd core && bundle exec rspec spec/meta/measured_claims_spec.rb` — RED. Change 29 to 31 and rerun — GREEN. The true count at that revision: `git ls-tree -r --name-only f67e743e6eec -- core/lib | grep '\.rb$' | while read f; do git show "f67e743e6eec:$f"; done | grep -c 'Mutex.new'` = 29.

- **[high]** Only the first `<!-- measured: -->` marker on a line is read; a second marker on the same line is invisible to every example in the file  
  `core/spec/meta/measured_claims_spec.rb` — `claims` does `next unless (m = line.match(MARKER))`, and `String#match` returns the first MatchData only. A second marker on the same line is never parsed, so it escapes both "matches what the tree actually says" AND "names a deriver for every claim, so none can be marked and left uncomputed". Two claims on one line is not exotic — the register's own marker line reads "**149 entries below** [measured marker for register-entries]", and a sentence stating two counts ("149 entries, 57 of them open") lands both markers on one line naturally. The file's positive control, "reads a claim out of a document and compares it", does not exercise `claims` at all — it re-implements the scan inline with `lines.filter_map { |line| line.match(MARKER) }`, reproducing the identical single-match-per-line bug, so the control cannot see it. Use `line.scan(MARKER)` / `line.to_enum(:scan, MARKER)` and have the control call `claims` against a tmpdir tree instead of re-writing the scan.  
  *Check:* `printf '\nX [measured marker for mutex-sites] and Y [measured marker for register-entries]\n' >> docs/design/tasks/037-0.2.7-concurrency-foundations.md; cd core && bundle exec rspec spec/meta/measured_claims_spec.rb` — GREEN, though the register has 149 entries. Repeat with `[measured marker for totally-made-up-claim]` as the second marker — also GREEN, so an unknown deriver name is undetected there too.

- **[high]** The claim scanner's scope is a hand-written four-glob list — the exact defect the citation scanner ten lines below it was rewritten to fix, with the lesson written out in the same file  
  `core/spec/meta/measured_claims_spec.rb` — `claims` scans `%w[docs/**/*.md core/lib/**/*.rb core/spec/**/*.rb vscode/src/**/*.ts]`. The citation half of the same file uses `RepoFiles.list(TREE_ROOT)` over every tracked-or-untracked text file, carrying this comment: "The first version of this scanned four globs and missed both changelogs — which `024.67`'s own Area list names — so it reported clean while `024.5` was cited in both. A guard whose scope is a list somebody remembered has the defect it was built to catch." The claims half is still that first version. Everything outside the four globs is a place where a `<!-- measured: -->` marker looks like it is checked and is inert: `README.md`, `README.ja.md`, `CLAUDE.md`, `AGENTS.md`, `CONTRIBUTING.md`, `vscode/CHANGELOG.md` and `.ja.md`, `vscode/README.md`, `scripts/**`, `site/**`, `.github/**`, and any non-`.md` file under `docs/`. CLAUDE.md's own "*The 158 sites were enumerated and argued in 0.2.13*" is precisely a claim about this tree, in a file where marking it would achieve nothing (158 is correct today — I re-derived it: `rescue_verdicts.yml` has 158 keys). Fix by scanning `RepoFiles.list` filtered on text extensions, as the citation half already does.  
  *Check:* Create untracked probes each containing `[measured marker for register-entries]` at `<probe>.md`, `<probe>.md`, `<probe>.rb`, `site/ZZ_probe.html`, `.<probe>.md`, `core/lib/ZZ_probe_toplevel.txt`, `docs/design/ZZ_probe.txt`, then `cd core && bundle exec rspec spec/meta/measured_claims_spec.rb` — GREEN with all seven present (the register has 149). Delete the probes afterwards.

- **[high]** The verified number and the number a reader sees are independent strings, so the marker can be right while the prose beside it is anything at all  
  `docs/design/docs/02-architecture.md` — The system checks the value inside the HTML comment. The number in the sentence next to it is a separate, unchecked hand-typed string. Nothing ties them, so the guarantee as literally stated holds while the thing the guarantee exists for — the number in the document — is free to rot, and rots invisibly because the marker beside it reads as proof that it was checked. This is 'the check passes for a reason other than the one it states': `documented_counts_spec`, the precedent this file cites, compares against the number as written in the prose (`DocumentedCounts.stated(document)`), which is the shape that actually holds. Fix by requiring the marker's value to appear on its own line before the marker, the way `DeferredFindings#documents?` insists an anchor's line carry content in front of it.  
  *Check:* `perl -i -pe 's/現在 31 箇所あり/現在 999 箇所あり/' docs/design/docs/02-architecture.md` (line 269, leaving the marker on 270 at 31), then `cd core && bundle exec rspec spec/meta/measured_claims_spec.rb` — GREEN, with the document now reading "core/lib currently has 999 sites". Restore afterwards.

- **[medium]** `mutex-sites` counts text occurrences, not mutexes: one of the 31 is a prose mention in a comment  
  `core/spec/meta/measured_claims_spec.rb` — The deriver is `File.read(f).scan("Mutex.new").length` — a substring count over the whole file, comments and strings included. `core/lib/ovallsp/signatures/type_converter.rb:112` reads `# path -- \`Mutex.new\` says \`Thread::Mutex\`, which is what it is.` — a sentence about RBS type-name rendering, not a lock. So `core/lib` contains 30 mutexes and the architecture document's threading section says 31 sites observe the lock order. The check is green because the deriver and the prose disagree about what is being counted, not because the count is right, and the coupling runs the wrong way: writing another comment that mentions `Mutex.new` would force the author to raise the documented number of lock sites. Count assignments (`= Mutex.new`) or parse, rather than grepping the file text.  
  *Check:* `grep -rn 'Mutex.new' core/lib --include='*.rb' | wc -l` → 31; `grep -rn 'Mutex.new' core/lib --include='*.rb' | grep -E ':[0-9]+:\s*#'` → the single comment at type_converter.rb:112. Actual mutex constructions: 30.

- **[medium]** Three of the scanner's four globs are inert and can be deleted with the suite green — an unpinned behavioural line by this project's own definition  
  `core/spec/meta/measured_claims_spec.rb` — No marker exists anywhere under `core/lib`, `core/spec` or `vscode/src`; all three live markers are under `docs/`. So the scanner's coverage of the source trees is asserted by nothing: narrowing `patterns` to `%w[docs/**/*.md]` leaves all six examples green. The orphan check ('has every deriver attached to a claim that exists') does defend against emptying the list entirely, which is good, but it cannot see a partial narrowing because the three derivers are all cited under `docs/`. CLAUDE.md: 'A behavioural line that no test fails on when it is reverted is a defect... regardless of whether the behaviour itself is correct.' Fixing finding 3 (scan via `RepoFiles`) dissolves this one; otherwise the file needs a fixture-tree example that proves the scanner reaches a source file.  
  *Check:* Edit line 106 of `core/spec/meta/measured_claims_spec.rb` to `patterns = %w[docs/**/*.md]`, then `cd core && bundle exec rspec spec/meta/measured_claims_spec.rb` — 6 examples, 0 failures. Restore afterwards.


#### `preflight` — 4 confirmed

- **[high]** scripts/preflight.rb is pinned by nothing: the whole gate can be reduced to a no-op with every check in the repository green  
  `scripts/preflight.rb` — No spec anywhere reads preflight.rb's content. Grepping core/spec for "preflight" yields exactly one hit, and it is a failure-message string in untracked_visibility_spec.rb. I replaced the 8-entry CHECKS list with a single `ruby -e true` check; `ruby scripts/preflight.rb` then printed "preflight: all 1 checks passed" in 0.06 seconds having run nothing, and exited 0 - which is precisely what the installed pre-commit hook consults. spec/meta came back 187 examples / 0 failures, identical to the baseline, and all five out-of-suite scripts stayed green. This is the exact defect class the tree already recognised and closed elsewhere. core/spec/meta/ci_skip_guard_spec.rb was written because CI-only text means "deleting the step leaves every check green while the published sentences resting on it stay put", and it pins ci.yml's structure so that cannot happen. preflight.rb is the newer gate from the same release (046's C9) and got no equivalent guard, while CONTRIBUTING.md publishes a sentence enumerating all eight checks by name and RELEASE/CONTRIBUTING both instruct contributors to rely on it. CLAUDE.md's rule that "a behavioural line that no test fails on when it is reverted is a defect in its own right" applies to every line of this file.  
  *Check:* cd <repo> && grep -rn 'preflight' core/spec | grep -v '^\S*: *#' # one hit, a message string. Then in a scratch worktree replace the CHECKS array body with a single Check.new(..., dir: ROOT, command: %w[ruby -e true]) and run: ruby scripts/preflight.rb (prints "all 1 checks passed") and cd core && bundle exec rspec spec/meta (187 examples, 0 failures - unchanged).

- **[high]** The 024.148 repair is unpinned on preflight's side: skip detection can be deleted with suites_ran_spec and ci_skip_guard_spec both green  
  `scripts/preflight.rb` — Register entry 024.148 is about preflight.rb having shipped a weaker rule (a non-zero example count) that could not fail in the one case it existed for, because a skipped example is still an example. The repair extracted scripts/check_suites_ran.rb so both callers share it. ci_skip_guard_spec.rb pins one caller with the example "runs the shared checker rather than a copy of its logic", asserting ci.yml contains the script path. Nothing makes that assertion about preflight.rb - the caller 024.148 is actually about. I replaced both `NON_EMPTY_SUITE` and `SUITES_RAN` with `lambda { |_out| nil }`, so exit status alone decides. suites_ran_spec.rb, ci_skip_guard_spec.rb and script_encoding_spec.rb: 24 examples, 0 failures. They stay green because they test the CheckSuitesRan module and ci.yml, not preflight's use of either. Demonstrated as a live regression, not a hypothetical: in an environment without vscode/node_modules the env-suite command exits 0 reporting "80 examples, 0 failures, 2 pending"; the real checker exits 1 on that JSON naming 2/7 skipped client-behaviour examples; the mutated preflight reports "ok". The guarantee's own second clause - reports a skipped check rather than assuming it passed - is false again, with the register entry that fixed it still reading as closed.  
  *Check:* cd <repo> && grep -n 'shared checker' core/spec/meta/ci_skip_guard_spec.rb # the assertion that exists for ci.yml. Then in a scratch worktree rewrite both lambdas in scripts/preflight.rb as `lambda { |_out| nil }` and run: cd core && bundle exec rspec spec/meta/suites_ran_spec.rb spec/meta/ci_skip_guard_spec.rb (0 failures). Contrast: bundle exec rspec spec/integration/real_rails_spec.rb spec/e2e/capabilities_spec.rb spec/meta/client_behaviour_spec.rb --format json --out tmp/rspec.json && cd .. && ruby scripts/check_suites_ran.rb core/tmp/rspec.json; echo $? (1, when node_modules is absent).

- **[medium]** The hook --install installs rewrites the index and lands a stray commit on the branch when the repository is a linked git worktree  
  `scripts/preflight.rb` — Measured from a real `git commit` in a linked worktree, git exports GIT_DIR and GIT_INDEX_FILE as ABSOLUTE paths to the worktree's gitdir. Those override the `chdir:`/`-C` that scripts/repo_files.rb and the two tmp-repo spec files depend on, so subprocesses that believe they are operating on a throwaway repo operate on the real one. Two spec files git-init throwaway repos inside the full suite preflight runs as check 2: core/spec/meta/doc_links_spec.rb and core/spec/meta/untracked_visibility_spec.rb. Running those two files under exactly those env values gave 1 failure (doc_links_spec's "admits a path that a commit carried and a later commit deleted"), so the hook rejects the commit for a reason unrelated to the change - and the documented remedy is PREFLIGHT_SKIP=1, i.e. turning the gate off. Worse, reproduced in a throwaway victim repo: one spec-shaped snippet replaced the victim's index (its two committed files became untracked, the tmpdir's note.md staged) and wrote a real commit onto the victim's branch. Under the hook that is the index of the commit being made. Bounded honestly: a PLAIN repo is unaffected - git leaves GIT_DIR empty and GIT_INDEX_FILE relative (".git/index"), which resolves after -C, so an end-to-end plain-repo commit through the hook is correct and the same specs pass. The trigger is worktrees only. Separately, `--install` run from a worktree writes to the MAIN repo's .git/hooks (correct git behaviour for a shared hooks dir, but the message names the main repo path and the hook then applies to every worktree). Nothing tests the hook at all - not that it installs, not that it runs, not that it blocks.  
  *Check:* cd <repo> && git worktree add --detach /tmp/wtprobe HEAD && cd /tmp/wtprobe && printf '#!/bin/sh\necho "GIT_DIR=[$GIT_DIR] GIT_INDEX_FILE=[$GIT_INDEX_FILE]"\n' > "$(git rev-parse --git-path hooks)/pre-commit" && chmod 755 "$(git rev-parse --git-path hooks)/pre-commit" && touch x && git add x && git -c user.email=t@e.invalid -c user.name=t commit -m p # both absolute. Then: cd core && GIT_DIR=<that> GIT_INDEX_FILE=<that>/index bundle exec rspec spec/meta/doc_links_spec.rb (1 failure). Remove the hook file and the worktree afterwards; contrast a plain `git init` repo, where GIT_DIR is empty and GIT_INDEX_FILE is relative and nothing breaks.

- **[low]** CONTRIBUTING.md, preflight.rb's header and CHECKS disagree about how many places the checks live in  
  `CONTRIBUTING.md` — CONTRIBUTING.md says "The checks live in seven places"; scripts/preflight.rb's header comment says "the checks are in six places (the suite, three scripts, a git state, a derived number)"; CHECKS actually has eight entries and names five distinct scripts, and no entry corresponds to "a git state". Minor drift, but this is a file whose entire subject is a list nobody was holding together, and the prose describing it has already drifted from the code it describes.  
  *Check:* cd <repo> && grep -n 'seven places' CONTRIBUTING.md && grep -n 'six places' scripts/preflight.rb && ruby scripts/preflight.rb --list | grep -c '^[a-z]' # 8


#### `register` — 7 confirmed

- **[high]** An entry heading the grammar does not recognise is skipped, not failed — `## 024.<n>: Title` with no metadata block passes everything  
  `scripts/deferred_findings.rb` — `parses every entry` computes `headings(deferred) - entries(deferred).keys`. Both sides use the *same* heading shape (`ENTRY_HEADING` and `METADATA_BLOCK` both require `024.N` followed by a literal space), so a heading outside that shape is absent from both and the subtraction is empty for it. The module's own comment states the opposite rule as load-bearing: "An entry with no block is a failure, not a skip. The old guard silently dropped a heading it did not recognise, so an entry could be added and never checked. `parses every entry` compares the block count to the heading count for exactly that reason." It cannot do that. `## 024.<n>: A finding written with a colon after its number` — an entirely natural heading — and bare `## 024.<n>` both carry zero metadata: no status, no kind, no user-visible declaration, hence no KNOWN_LIMITATIONS obligation and no Area-path check. `reindex_findings.rb` splits on the looser `/^(?=## 024\.)/`, so it *does* see the entry and renders it into the index as `| [`024.<n>`](#024200-) | ? | — | |`, and the `register-entries` measured claim still says 149 while the file visibly has 150 entries. The old guard's exact failure mode has returned through the one shape the replacement cannot see.  
  *Check:* In a clone at 8f1d4f4: `printf '\n## 024.<n>: A finding written with a colon after its number\n\nBody prose, no yaml block.\n' >> docs/design/tasks/024-deferred-review-findings.md; ruby scripts/reindex_findings.rb; ruby scripts/reindex_findings.rb --check` (prints "current"); `cd core && bundle exec rspec spec/meta/deferred_findings_spec.rb spec/meta/measured_claims_spec.rb` (36/0 and 6/0). Then `grep -n '024\.200' docs/design/tasks/024-deferred-review-findings.md` to see the `| ? | — | |` index row. Control: give the same entry a normal `## 024.<n> Title` heading and no yaml block — `parses every entry` fails.

- **[high]** "Exactly one parser" is true of the metadata block only; the entry *number* has five readers that disagree, and the index renders garbage for a sub-numbered entry  
  `scripts/reindex_findings.rb` — C4 unified the reading of the yaml block, but the primary key — the entry number — is still read by `ReindexFindings.number_of` (`/\A## (024\.[0-9R]+)/`, no trailing space, no sub-numbers), `ReindexFindings.title_of` (`/\A## 024\.[0-9R]+ (.*)/`), `ReindexFindings.rebuild`'s split (`/^(?=## 024\.)/`), `deferred_findings_spec`'s two scans, `measured_claims_spec#register_numbers` (`/^## (024\.[0-9R]+) /`) and `CITATION`. On the heading `## 024.13.<n> A sub-numbered follow-up` those give, respectively, "024.13", "", a split, and `[]` — while `DeferredFindings.headings`/`entries` correctly give "024.13.<n>". `deferred_findings_spec` explicitly asserts sub-numbered entries work "in both readers", so this is a supported shape. The consequence: the index gains a second row numbered `024.13` with an empty title and the dead anchor `#02413-`, `entry_key` ties it with the real 024.13 so their order is arbitrary, and every check is green including "indexes every entry, so the table cannot silently omit one" (both its scans truncate identically, so they agree while both are wrong). `measured_claims_spec`'s own comment claims round 1 removed the third and fourth readers and made `DeferredFindings` "the single parser of this file" — but `register_numbers` at line 188 is still a hand-rolled heading regex that disagrees with `ENTRY_HEADING`.  
  *Check:* `ruby -r./scripts/deferred_findings -r./scripts/reindex_findings -e 'h="## 024.13.<n> X\n\n```yaml\nstatus: open\nkind: defect\n```\n"; p DeferredFindings.headings(h), ReindexFindings.number_of(h), ReindexFindings.title_of(h), h.scan(/^## (024\.[0-9R]+) /).flatten'. Full round trip: insert a valid sub-numbered entry with no Area line, bump the `register-entries` marker in the register from 149 to 150, `ruby scripts/reindex_findings.rb`, then both meta specs — 36/0 and 6/0 with `| [`024.13`](#02413-) | open | — | |` sitting in the index. (The sub-number is written with this record's `<n>` placeholder rather than a digit: `024.182` widened the citation guard to read a sub-number as itself, so a spelled one here is a pointer at an entry that does not exist. Substitute any digit to run it.)

- **[high]** "Every entry states one Area" is false today and is unenforceable — zero Area lines passes, and deleting the line switches off 046's C3 path-existence guard  
  `core/spec/meta/deferred_findings_spec.rb` — The only Area check is `states each entry's Area exactly once, so a doubled body cannot pass`, and it fires on `count > 1` only. `count == 0` passes. The register has 149 headings and 147 `**Area:**` lines: `024.30` and `024.R3` state no Area at all. Worse, this makes the C3 guard (`names only paths that exist, in every open entry's Area`) opt-out: `area_paths` returns `[]` for an entry with no Area line, so the reject is vacuous. An entry whose Area names a file that does not exist — the exact 024.87 defect C3 was written for — is made green by deleting the Area line rather than correcting the path, and the entry then arrives at whoever picks it up with no starting file, which the guard's own message calls "the first thing whoever picks the entry up will open".  
  *Check:* Live: `ruby -e 'md=File.read("docs/design/tasks/024-deferred-review-findings.md",encoding:"UTF-8"); md[md.index("\n## 024.")+1..].split(/^(?=## 024\.)/).reject{|p|p.strip.empty?}.each{|b| n=b[/\A## (\S+)/,1]; c=b.scan(/^\*\*Area:\*\*/).length; puts "#{n}: #{c}" if c!=1}'` prints `024.30: 0` and `024.R3: 0`. Mutation: delete the `**Area:**` line from open user-visible `024.13` — both specs green. Control: change that Area to a nonexistent path — `names only paths that exist` fails.

- **[high]** The citation guard's scope is a hand-written extension list that excludes the public site — the four site pages citing `024.55` are unchecked  
  `core/spec/meta/measured_claims_spec.rb` — `scanned_files` filters `RepoFiles.list` to `\.(rb|ts|js|md|json|yml|yaml|sh|erb)\z`. Its comment says "Every tracked text file, not a hand-written list of directories. The first version of this scanned four globs and missed both changelogs ... A guard whose scope is a list somebody remembered has the defect it was built to catch." It then hand-writes a list of extensions, which is the same defect one level down. Tracked files outside the list: 11 `.html` (all of `site/`), 4 `.rbs`, and one each of `.txt`, `.toml`, `.xml`, `.ps1`, `.css`, `.gemspec`, `.lock`. `site/index.html`, `site/security.html`, `site/ja/index.html` and `site/ja/security.html` each cite `024.55` right now, so the most user-facing surface in the repo carries live register pointers that nothing verifies. CLAUDE.md separately warns that `site/` "is not generated from the Markdown docs and therefore propagates nothing on its own", which is precisely why it needs to be in scope. `scripts/check_home_paths.rb` scans `RepoFiles.list(ROOT)` with no extension filter, so the omission is this guard's alone.  
  *Check:* `printf '<!-- see 024.<n> -->\n' >> site/index.html; cd core && bundle exec rspec spec/meta/measured_claims_spec.rb` → 6/0. `ruby scripts/check_site_links.rb` and `ruby scripts/check_doc_links.rb` both exit 0. `grep -rn '024\.[0-9]' --include='*.html' .` shows the four live `024.55` citations.

- **[medium]** The doubled-body countermeasure is exactly one line wide: 2,515 characters of an entry pasted twice below its Area line passes  
  `core/spec/meta/deferred_findings_spec.rb` — The check was added after a scripted edit doubled `024.69`'s entire body, and it detects that by counting `**Area:**` lines. It therefore detects duplication only when the duplicated slice happens to contain the Area line. The 024.69 incident's own description — "a `String#find` that returned -1 when its terminator was absent, so a slice meant to end at a paragraph ran to the end of the entry" — is a class of bug whose slice boundary is wherever the terminator search failed, which is far more often *inside* the body than above the Area line. I duplicated 2,515 characters of `024.13` starting immediately below its Area line and every check stayed green, `reindex --check` included. Per CLAUDE.md's own framing this is a countermeasure aimed at the symptom (the one incident's slice boundary) rather than the class (an entry body appearing twice).  
  *Check:* In a clone: take `024.13`'s block, find the newline after its `**Area:**` line, and append everything from there to the end of the block a second time. `cd core && bundle exec rspec spec/meta/deferred_findings_spec.rb` → 36/0. Control: append from just after the heading instead (so the Area line is included) → `states each entry's Area exactly once` fails.

- **[low]** A citation of a sub-numbered entry silently truncates: `024.13.<n>` resolves because `024.13` exists  
  `core/spec/meta/measured_claims_spec.rb` — `CITATION = /\b024\.(?<number>[0-9]+|R[0-9]+)\b/` cannot express a sub-number, so `024.13.<n>` matches as `024.13` and passes whenever `024.13` exists. Sub-numbered entries are a supported shape — `deferred_findings_spec` asserts `024.30.<n>` parses "in both readers" — so a pointer at a sub-entry that was never written, or was renumbered away, reads as resolving. The asymmetry is visible in one run: `024.<n>.1` was correctly reported dangling (because `024.<n>` does not exist) while `024.13.<n>` in the same line was accepted.  
  *Check:* Append `See 024.13.<n> and 024.<n>.1 for the reasoning.` to any scanned `.md`, then `cd core && bundle exec rspec spec/meta/measured_claims_spec.rb`. The failure names only `024.<n>`. (Sub-numbers written with this record's `<n>` placeholder rather than a digit, for the reason the fix itself created — see the note on the previous check.)

- **[low]** The register is exempt from the citation guard, and it is the densest source of `024.N` cross-references  
  `core/spec/meta/measured_claims_spec.rb` — `scanned_files` rejects `024-deferred-review-findings.md`, so a `see 024.NNN` typo inside an entry — the most common place a cross-reference is written — is unverifiable. I planted `024.<n>` in the register and the guard stayed green. A live scan of the register's own internal references finds `024.<n>` cited at lines 85, 95 and 3974 against no entry; that particular gap is deliberate and the legend explains it at length, so it is not a live defect, but it is also indistinguishable by machine from a typo. The exclusion looks like it exists to avoid the entry headings themselves matching, which a scan that ignores `^## ` lines and index rows would handle without giving up the body prose.  
  *Check:* `ruby -rset -e 'reg=File.read("docs/design/tasks/024-deferred-review-findings.md",encoding:"UTF-8"); known=(reg.scan(/^## (024\.[0-9R]+) /).flatten+reg.scan(/^\| `(024\.[0-9R]+)` \|/).flatten).to_set; reg.lines.each_with_index{|l,i| l.scan(/\b024\.([0-9]+|R[0-9]+)\b/).flatten.each{|n| puts "#{i+1}: 024.#{n}" unless known.include?("024.#{n}")}}'`


#### `release-gate` — 5 confirmed

- **[high]** Gate evidence naming TypeScript tests is never checked at all — the regex has no `ts` branch, and the spec contains dead code proving the author thought it did  
  `core/spec/meta/release_gate_spec.rb` — RELEASE_GATE_EXECUTABLE is /`([A-Za-z0-9_./-]+\.(?:rb|sh|js))`|`(test:[a-z:]+)`/ — no `ts`. Gates 7, 8, 9, 10, 18 and 19.1 of the Task 023 table cite TypeScript test files (`versionInfo.ts`, `versionInfo.test.ts`, `clientLifecycle.test.ts`, `vscode/src/test/unit/workspaceTrust.test.ts`) as their sole evidence, and every one is invisible to the check. Line 129's `next if base.end_with?("_spec.rb", ".test.ts")` is unreachable: the regex cannot produce a `.test.ts` capture. That dead branch is the evidence the guarantee was believed to cover TS tests. Consequence: any of those four files can be deleted or renamed and the gate that cites it stays green, which is exactly the `make-final-review-bundle.sh` failure this spec was written for, in a different file extension.  
  *Check:* ruby -e 'RE=/`([A-Za-z0-9_.\/-]+\.(?:rb|sh|js))`|`(test:[a-z:]+)`/; puts "`clientLifecycle.test.ts`".scan(RE).flatten.compact.inspect' # => [] Then in docs/RELEASE_CHECKLIST.md replace `clientLifecycle.test.ts` with `neverExisted.test.ts`, `versionInfo.test.ts` with `ghost.test.ts`, and `vscode/src/test/unit/workspaceTrust.test.ts` with `vscode/src/test/unit/deletedLastYear.test.ts`; `cd core && bundle exec rspec spec/meta/release_gate_spec.rb` => 3 examples, 0 failures.

- **[high]** "Something actually runs it" is implemented as a substring search, so an error-message string, an exemption-list entry or a skip clause all count as an invocation  
  `core/spec/meta/release_gate_spec.rb` — haystack_excluding(base) joins every non-comment line under core/spec and scripts plus the named callers and asks `.include?(base)`. Nothing distinguishes a call from a mention. The spec's own comment says round 1 caught this via `verify_sbom_against_vsix.rb`'s own `fail!("usage: ...")` line, but the fix only excluded the candidate's OWN file — the same kind of string in a DIFFERENT file still counts. Gate 8's `scripts/generate_sbom.rb` is wired today by exactly two lines: a real one in sbom_spec.rb, and `scripts/verify_sbom_against_vsix.rb:27`'s `fail!("#{sbom_path} does not exist -- run scripts/generate_sbom.rb first")`. Remove the real runner and the gate still reports enforced — losing the only enforcement 046 created for the one gate it identified as enforced nowhere. Two more live instances of non-invoking mentions being accepted as wiring: `scripts/utf8.rb` is wired by `SCRIPT_ENCODING_EXEMPT = %w[scripts/utf8.rb].freeze` (a list meaning "do not check this file") and `scripts/repo_files.rb` by `next if rel.end_with?("scripts/repo_files.rb")` (a skip clause). The error runs the other way too: `require_relative "../../../scripts/deferred_findings"` — the canonical Ruby load, no extension — does not match, so a genuinely-loaded library reads as unwired.  
  *Check:* In core/spec/meta/sbom_spec.rb change SBOM_SCRIPT's literal to "../../../scripts/generate_#{%w[sbom].first}.rb" (behaviour identical; both sbom examples still pass). `cd core && bundle exec rspec spec/meta/release_gate_spec.rb spec/meta/sbom_spec.rb` => 5 examples, 0 failures. Then also change scripts/verify_sbom_against_vsix.rb:27's message from "run scripts/generate_sbom.rb first" to "run the generator first" and re-run => 1 failure, "gate 8: scripts/generate_sbom.rb exists but nothing invokes it". The check tracks the string, not the call.

- **[high]** An invented script name that is a substring of a real one passes both the existence and the wiring check  
  `core/spec/meta/release_gate_spec.rb` — Existence is `!RepoFiles.list(ROOT, "*#{base}").empty?` — a suffix glob on the basename — and wiring is `haystack.include?(base)`, a substring test. Both are satisfied by any real file whose name merely ends with the cited one. `scripts/sbom.rb` and `tools/smoke.rb` do not exist anywhere in this repository, yet both are accepted: `*sbom.rb` globs onto `scripts/generate_sbom.rb`, and `include?("smoke.rb")` hits inside `vsix_semantic_smoke.rb`. The cited path is never compared to the real one either, so `../../../../etc/generate_sbom.rb` is accepted as gate 8's evidence. Separately, `cited.start_with?("test:")` is an unconditional existence pass, so the check can never report a nonexistent npm script: gate 4 citing `test:integ` and `test:u` (neither in vscode/package.json) is green, and the one planted npm citation it did catch was reported with the false message "exists but nothing invokes it".  
  *Check:* In docs/RELEASE_CHECKLIST.md replace gate 11's `scripts/verify_sbom_against_vsix.rb` with `scripts/sbom.rb` and `tools/smoke.rb`; `ls scripts/sbom.rb tools/smoke.rb` => No such file; `cd core && bundle exec rspec spec/meta/release_gate_spec.rb` => 0 failures. Separately replace gate 4's `test:unit`・`test:integration` with `test:integ`・`test:u`; `grep -c '"test:integ"' vscode/package.json` => 0; re-run => 0 failures.

- **[high]** The check can be switched off wholesale with an invisible HTML comment, and the example that exists to stop it going inert does not apply the filter it would need to notice  
  `core/spec/meta/release_gate_spec.rb` — The first example skips any row containing `[unwired marker]`. Appending that marker to all 34 rows makes it check nothing — and the marker renders as nothing, so a reader of the rendered checklist cannot see the opt-out. The second example, "finds evidence to check in the first place", is the guard against the check going inert, but it computes evidence_cells and scans for citations WITHOUT applying the unwired filter, so it still counts 34 rows and 15 citations and reports health. Nothing anywhere bounds how many rows may be marked unwired, and nothing enforces the prose requirement that an unwired row "must still say what does enforce the item now". The same example's floors also have large slack (34 rows vs >= 20, 15 citations vs >= 8), and rows are extracted only when cells[1] matches /\A[\d.]+\z/ — so appending a footnote marker to a row number (`| 8* |`), which renders identically, drops the row from the check with no failure and no marker.  
  *Check:* Append " [unwired marker]" to every line of docs/RELEASE_CHECKLIST.md that starts with `|` and whose second cell is numeric; `cd core && bundle exec rspec spec/meta/release_gate_spec.rb` => 3 examples, 0 failures in 0.037s. Separately, change the row numbers of gates 6, 8, 11 and 18 to `6*`, `8*`, `11*`, `18*`: extractor drops 34 rows to 29, still 0 failures.

- **[low]** CI job names cited as gate evidence are outside the regex entirely, and the status column is scanned as if it were evidence  
  `core/spec/meta/release_gate_spec.rb` — Two smaller scope errors. (a) Gates 2, 3, 14 and 15 cite CI job names — `core`, `secret-scan`, `package-contents-inspection` — as the thing that enforces them, and the Task 023 table header explicitly says the evidence column holds "CIのジョブ名、spec、あるいは release.sh のステップ". The regex matches none of these, so a deleted or renamed CI job leaves its gate green here (ci_skip_guard_spec covers some of this, but that is coincidence, not this guarantee). (b) `evidence_cells` returns `cells[2..].join(" ")`, i.e. the 状態 column concatenated with the evidence column, so the check treats the status text as evidence and — more to the point — an `[unwired marker]` placed in the status cell silences the row just as well as one in the evidence cell.  
  *Check:* Read core/spec/meta/release_gate_spec.rb:36 (the regex) against docs/RELEASE_CHECKLIST.md lines 66, 67, 78, 82 (the job-name citations); the probe at scratchpad/probe.rb prints `cited=[]` for rows 2, 3, 14 and 15. For (b), see release_gate_spec.rb:70 — `[cells[1], cells[2..].join(" ")]`.


#### `release-sh` — 8 confirmed

- **[high]** Three of the six guaranteed checks are enforced only by `set -euo pipefail` (line 23), which nothing pins  
  `vscode/scripts/release.sh` — The payload-hash check (line 196), the semantic smoke (line 199) and the SBOM-vs-artifact check (line 206) are bare commands with no `||`, no `if`, no status test. Their refusal is entirely a property of `set -e` on line 23. release_script_guard_spec.rb asserts only that the strings `verify-packaged-payload-hash.js`, `vsix_semantic_smoke.rb` and `verify_sbom_against_vsix.rb` appear in the file — it never pins `set -e`, and nothing else in the repository does either. Deleting one line silently converts three named guarantees from refusals into log lines. The guarantee's own wording, 'runs the payload-hash check, the semantic smoke and the SBOM-vs-artifact check', is satisfied by a script that runs all three and ignores every verdict. The three sub-scripts all exit 1 correctly, so the verdict is real and it is release.sh that throws it away.  
  *Check:* Copy release.sh to a scratch mirror alongside a copy of the spec (the spec resolves SCRIPT from its own __dir__), delete `set -euo pipefail`, and run `bundle exec rspec <mirror>/core/spec/meta/release_script_guard_spec.rb` from core/ — 8 examples, 0 failures. The narrower form is equally green: append `|| echo "note: continuing."` to lines 196, 199 and 206. To see it is load-bearing, run this twice, with and without `set -e`: `echo A; false; echo B; false; echo C; false; echo REACHED_PUBLISH_PROMPT`. Fix: pin `set -euo pipefail` in the spec, and — better, since a text pin cannot see `|| true` — make each of the three an explicit `if ! <cmd>; then echo ...; exit 1; fi` so block_containing can pin the refusal the same way it pins the other three.

- **[high]** The clean-tree refusal can be deleted outright by planting `diff --quiet` as a trailing comment (spec line 24)  
  `core/spec/meta/release_script_guard_spec.rb` — `code` strips only WHOLE-LINE comments (`line.strip.start_with?("#")`), and `block_containing` takes the FIRST line matching the pattern. So a trailing comment on any non-comment line inside an earlier `if…fi` block that already contains `exit 1` captures the window. The PAT block at lines 51-56 is such a block and sits before the clean-tree check. Adding `# unrelated to git diff --quiet` to the end of release.sh line 54 and then deleting the whole clean-tree block leaves zero occurrences of the refusal in release.sh and the spec at 8 examples, 0 failures. The header comment reasons carefully about a commented-out block still containing the strings — it closed that direction and left the mirror-image one open. This is the same place a previous attack round already broke (the whole-file `exit 1` regex); block_containing was the countermeasure, and per CLAUDE.md's two-rounds-same-place rule the next countermeasure has to be a different shape — execute the refusal rather than read it.  
  *Check:* In a scratch mirror, append ` # unrelated to git diff --quiet` to release.sh line 54, then delete lines 78-84 (the whole `if ! git -C "$REPO_ROOT" diff --quiet ... fi` block). `grep -c 'git -C "$REPO_ROOT" diff' release.sh` returns 0; `bundle exec rspec <mirror spec>` returns 8 examples, 0 failures. Minimal fix: strip trailing comments too (`line.sub(/(?<=\s)#.*$/, "")`), and require the matched line to be the block's own opener rather than merely inside the window.

- **[high]** The clean-tree refusal (line 78) can be aimed at a path that never changes; nothing reports what it examined  
  `vscode/scripts/release.sh` — Adding a pathspec — `git -C "$REPO_ROOT" diff --quiet -- LICENSE` on both halves — keeps every text assertion true (the matcher is `/diff --quiet/`, the block still ends in `exit 1`) while the check stops seeing modified tracked content anywhere else. A VSIX then ships from a dirty tree with a `buildCommit` naming a commit whose content it does not match, which is the entire stated reason the gate exists. The artifact check got an `INSPECTED` countermeasure for exactly this 'aimed at nothing' class after a re-derivation round pointed it at a nonexistent subdirectory; the clean-tree check, added in the same release, got none — it prints nothing about what it inspected and has no plausibility floor.  
  *Check:* In a scratch mirror, change line 78 to `if ! git -C "$REPO_ROOT" diff --quiet -- LICENSE || ! git -C "$REPO_ROOT" diff --cached --quiet -- LICENSE; then` — 8 examples, 0 failures. Then, in a throwaway repo: `git init`, commit `LICENSE` and `core/x.rb`, modify `core/x.rb` only, and evaluate both conditions — the shipped one refuses, the mutated one falls through to publish. Fix in the shape the artifact check already uses: have the block echo `git -C "$REPO_ROOT" status --porcelain | wc -l` and the pathspec it used, and pin that echo.

- **[high]** The one refusal the spec executes (spec line 96) passes on a script that does not refuse  
  `core/spec/meta/release_script_guard_spec.rb` — This example exists because 'semantic mutations are invisible to any text match', and it is the only executed one. It can be defeated. Demote the PAT refusal to `if [ "${OVALLSP_STRICT_PAT_MODE:-0}" = "1" ]; then exit 1; fi` and all 8 examples stay green, because: (1) block_containing stops at the FIRST `^\s*fi\s*$` at or after the match, which is now the NESTED `fi`, so the unreachable `exit 1` is still inside the window; and (2) the executed example asserts only `status.success? == false` plus `output.include?("readable beyond its owner")`. The message is still printed — as a warning — and the non-zero status comes from somewhere else entirely: Dir.mktmpdir is not a git repository, so the downstream clean-tree check fires spuriously and `git status --short` aborts with 128. Both assertions are satisfied by a script that continues past a world-readable Marketplace PAT. The comment says an exit-status-only assertion 'would have called a pass' and adds the message assertion as the cure, but the pair still cannot distinguish 'refused' from 'warned, then died later for an unrelated reason'.  
  *Check:* In a scratch mirror, replace release.sh's `exit 1` at line 55 with `if [ "${OVALLSP_STRICT_PAT_MODE:-0}" = "1" ]; then` / ` exit 1` / ` fi`, and soften the preceding echo — 8 examples, 0 failures. Then run the fixture by hand: mktmpdir, `scripts/release.sh`, a mode-644 `.vsce-pat.local` beside it, and diff the two outputs. Pristine: exit 1, three lines, stops. Mutated: same warning, then `warning: Not a git repository`, the whole `git diff --no-index` usage dump, `release.sh: the tracked tree has uncommitted changes.`, `fatal: not a git repository`, exit 128. Fix: `git init` + one commit in the fixture so nothing downstream can fabricate a failure; assert `status.exitstatus == 1` rather than `success? == false`; and assert the script stopped there — `expect(output).not_to include("release.sh: building")`. Independently, make block_containing match `fi` at the opener's own indentation so a nested block cannot truncate the window.

- **[medium]** The INSPECTED countermeasure (line 187) guards the directory only — the grep's pattern and exclusions are unguarded  
  `vscode/scripts/release.sh` — `find "$INSPECT_ROOT"` and the grep share only INSPECT_ROOT. 'Aimed at nothing' has three dimensions — the directory, the pattern, and the exclusion set — and the count covers one. Changing the pattern to `"$HOME/.ovallsp-never-exists"`, or widening exclusions with `--exclude='*.map' --exclude='*.json' --exclude='*.js'`, makes the check match nothing while the count stays at its healthy value and the log still prints PASS with a four-figure file count. The spec's comment states the count 'converts "aimed at nothing" from silent into visible', and release.sh's says 'A count that is not derived from what was actually searched guards the variable rather than the search' — the count is still not derived from what was actually searched, only from where.  
  *Check:* Build a fake artifact: 150 ordinary files plus one containing "$HOME", under a temp root. Run the shipped grep (refuses, lists the leaking file) and the pattern-mutated grep (no match) — both then print `PASS: ... (151 files inspected)`. In a scratch mirror either mutation leaves the spec at 8 examples, 0 failures. Fix: derive the reported number from grep itself using the same option string, or plant a known decoy file containing $HOME inside UNPACK_DIR and require the grep to find exactly that one — a positive control, so a search that finds nothing fails instead of passing. Also note the floor is `-lt 100` while the comment says 'over a thousand'.

- **[medium]** The "calls the grep it means" pin (spec line 126) is satisfied by the advisory grep, and its scan misses indented calls  
  `core/spec/meta/release_script_guard_spec.rb` — Two independent holes. (a) `expect(code).to include("/usr/bin/grep -rlF")` is satisfied by release.sh line 179 — the ADVISORY grep whose output goes to /dev/null and which only prints a note — so the hard-failure grep on line 174 does not have to be absolute at all. (b) The scan `/(?:^|[|&;(]\s*|\bif\s+|!\s*)grep\s/` requires `grep` at column 0, or immediately after one of `| & ; (`, `if `, or `!`. In Ruby `^` matches a line start, so `grep` must follow it with no whitespace — any INDENTED bare `grep` escapes, as does `; then grep`, a backticked grep, and a continuation line after `&& \`. Since almost every grep inside an `if` or a function body is indented, the scan misses the common case. Moving line 174's grep into a function body as ` grep -rlF --exclude=... "$HOME" "$INSPECT_ROOT"` reinstates the exact ugrep hazard the check was written for, with all 8 examples green.  
  *Check:* In a scratch mirror, replace line 174 with `artifact_carries_home_path() {` / ` grep -rlF --exclude='*.bundle' --exclude='*.so' --exclude='*.dylib' "$HOME" "$INSPECT_ROOT"` / `}` / `if artifact_carries_home_path; then` — 8 examples, 0 failures, and the hard-failure grep is now shell-resolved. (On this machine a non-interactive bash currently resolves `grep` to /usr/bin/grep, so the hazard is latent here, not live — the check exists so nobody has to depend on that.) Fix: allow leading whitespace in the anchor, and assert that EVERY grep invocation in the file is absolute rather than that one absolute one exists — e.g. `code.scan(/\bgrep\s+-/).length == code.scan(%r{/usr/bin/grep\s+-}).length`.

- **[medium]** release_gate_spec's C6 fix closed the self-naming hole and left the caller-naming one  
  `core/spec/meta/release_gate_spec.rb` — Round 1 fixed C6 by excluding the candidate's OWN file from the haystack, because verify_sbom_against_vsix.rb's usage string names itself. But the haystack is still a plain `include?(basename)` over the full non-comment text of every caller, and release.sh line 205 is `echo "-- ruby scripts/verify_sbom_against_vsix.rb --"` — a non-comment line in a DIFFERENT file, so it is not excluded. RELEASE_CHECKLIST gates 8 and 11 are therefore reported 'wired' by an echo string alone. That is the same defect the spec's own comment names — 'A check whose evidence is that a string appears somewhere is the same mistake as the table it is checking' — displaced one file outward. Combined with the set -e finding, appending `|| echo` to release.sh line 206 keeps both specs green while gate 8/11 cannot refuse anything.  
  *Check:* Run: ruby -e 'src=File.read("vscode/scripts/release.sh"); nc=src.each_line.reject{|l| l.strip.start_with?("#","//")}; eo=nc.reject{|l| l.start_with?(%q{ruby "$REPO_ROOT/scripts/verify_sbom_against_vsix.rb"})}; puts eo.join.include?("verify_sbom_against_vsix.rb")' — prints true, i.e. with the real invocation removed the echo alone still counts as wiring. Fix: require the basename to appear in a line that plausibly INVOKES it (leading ruby/node/bash/sh/npm run, or a `run:` key in YAML) rather than anywhere in the text, and exclude quoted echo/printf arguments.

- **[low]** release.sh can be left syntactically invalid with every check green — nothing runs `bash -n`  
  `vscode/scripts/release.sh` — Nothing anywhere runs `bash -n` on release.sh — not the guard spec, not CI (there is no shellcheck or shell-syntax job in .github/workflows). Every assertion in the guard spec is a text match, and the one executed example exits at the PAT check on line 55, long before bash's parser reaches a later error. So an unterminated `if` introduced anywhere after line 55 leaves the release path unrunnable while the suite is green, and it is discovered only by the person attempting the release.  
  *Check:* In a scratch mirror, replace `echo "-- SHA-256 --"` with `if [ -n "$VSIX_PATH" ]; then` followed by the original echo (no closing `fi`). `bash -n release.sh` reports 'line 256: syntax error: unexpected end of file'; `bundle exec rspec <mirror spec>` reports 8 examples, 0 failures — the executed example passes too. Fix is one line in the spec: `expect(system("bash", "-n", SCRIPT)).to be(true)`.


#### `suites-ran` — 5 confirmed

- **[high]** The CI skip-guard step can be made non-gating, or made never to run, with every check green  
  `.github/workflows/ci.yml` — core/spec/meta/ci_skip_guard_spec.rb pins that the step "Fail if the real-Rails or capability suites were skipped instead of run" exists in the parsed YAML of the core job and that its `run` text names scripts/check_suites_ran.rb. It never pins that the step's failure fails the job. Three one-line edits each disable the gate while the whole of spec/meta stays green: `continue-on-error: true` on the step (187 examples, 0 failures); `if: false` on the step; `|| true` appended to the run text (the assertion is `include?`, so a suffix is invisible). The spec's own header comment records the 0.2.3 merge-round-6 lesson that a text slice stays green when a step is commented out, and fixes that by locating steps in parsed YAML — but `continue-on-error` and `if` are parsed-YAML structure GitHub executes too, and neither is asserted. The same gap covers every other CI-only guard that file pins: "Fail if a documented-count check skipped", and secret-scan's `--messages` home-path step. Note the file already asserts `job.fetch("continue-on-error")` is true for core-ruby-4, so the key is known to the authors; its absence is just never asserted where it gates. A per-step assertion would be a regression test for three instances; the countermeasure shape is one helper applied to every gating step of the core job (falsy `continue-on-error`, no `if` that can be false, and run text free of `||`, `; true`, `set +e`).  
  *Check:* cd <repo>; insert a line ` continue-on-error: true` between the `- name: Fail if the real-Rails or capability suites were skipped instead of run` line and its `run:` line in .github/workflows/ci.yml; then `cd core && bundle exec rspec spec/meta` -> 187 examples, 0 failures. Repeat with ` if: false` in the same position, and with `|| true` appended to the run line: `bundle exec rspec spec/meta/ci_skip_guard_spec.rb spec/meta/suites_ran_spec.rb` -> 19 examples, 0 failures in both cases. Revert.

- **[high]** The NOT YET exemption is unguarded for the third suite, client_behaviour_spec.rb  
  `core/spec/meta/ci_skip_guard_spec.rb` — The example "does not exempt the environment skip it exists to catch" iterates only spec/integration/real_rails_spec.rb and spec/e2e/capabilities_spec.rb. CheckSuitesRan::SUITES names three files. Changing core/spec/meta/client_behaviour_spec.rb's `skip("vscode/node_modules is not installed")` to `skip("NOT YET -- vscode/node_modules is not installed")` leaves every guard green, and check_suites_ran.rb then reports `all 2 client-behaviour examples ran.` and exits 0 for a run in which both were skipped for want of node_modules — the checker states the opposite of the truth in its own success line. The docs/CLIENT_BEHAVIOUR.md rows marked **checked** would again run nowhere, which is the exact regression that spec file's header comment says cost a release. Secondly, even adding the third path to that loop would not work as written: the scan is /^\s*skip\s+"([^"]+)"/, which requires a paren-less `skip "..."`; client_behaviour_spec uses `skip("...")`, so `skip_messages` would come back empty and the example would fail on `not_to be_empty` rather than check anything. The regex needs to accept `skip(` — and while there, the check belongs in one place iterating CheckSuitesRan::SUITES rather than a hand-copied two-element array that can drift from the three-element table it is meant to cover.  
  *Check:* cd <repo> && sed -i '' 's/skip("vscode\/node_modules is not installed")/skip("NOT YET -- vscode\/node_modules is not installed")/' core/spec/meta/client_behaviour_spec.rb && cd core && bundle exec rspec spec/meta/ci_skip_guard_spec.rb spec/meta/suites_ran_spec.rb spec/meta/client_behaviour_spec.rb -> 26 examples, 0 failures. Then build a JSON report with two client_behaviour_spec examples at status "pending" and pending_message containing "NOT YET", and run `ruby scripts/check_suites_ran.rb <that file>` -> prints "all 2 client-behaviour examples ran.", exit 0. Revert.

- **[high]** release_artifacts_spec's tag-accounting invariant skips on every CI run and nothing watches it  
  `core/spec/meta/release_artifacts_spec.rb` — Two examples — "accounts for every tag, in one table or the other" and "records no version that was never tagged" — call `skip "no tags in this checkout (CI fetches --no-tags; a tarball has none)"`. The spec's own comment states that actions/checkout@v4 fetches --no-tags, and the core job's checkout is a bare `- uses: actions/checkout@v4` (ci.yml:23) with no fetch-depth or fetch-tags. So both examples are pending in every CI run, rspec exits 0, and the file is not in CheckSuitesRan::SUITES, so the skip-guard has no opinion on it. This is precisely the guaranteed-against shape — a suite that skipped for want of an environment reported as a suite that passed — and the invariant it protects is the one written because 0.1.14 and 0.1.15 were tagged, never built, and noticed only by someone looking at the Marketplace by eye. It has therefore been unenforced anywhere continuous since it was written. Fix by giving the core job's checkout `fetch-tags: true` (or fetch-depth: 0) and adding the path to CheckSuitesRan::SUITES, or by moving the tag examples to a job that has tags (secret-scan already checks out with fetch-depth: 0).  
  *Check:* cd <repo> && mkdir -p /tmp/stub && printf '#!/bin/sh\ncase "$*" in *tag*) exit 0 ;; esac\nexec /usr/bin/git "$@"\n' > /tmp/stub/git && chmod +x /tmp/stub/git && cd core && PATH=/tmp/stub:$PATH bundle exec rspec spec/meta/release_artifacts_spec.rb -> "4 examples, 0 failures, 2 pending", exit 0. Locally without the stub it is 4 examples, 0 failures (this checkout has 29 v* tags), which is why the gap is invisible to a developer. Confirm the core job's checkout takes no `with:` block: sed -n '23,25p' .github/workflows/ci.yml

- **[medium]** preflight.rb's environment-dependent-suites check is pinned by nothing, and its freshness coupling is implicit  
  `scripts/preflight.rb` — Deleting the entire `Check.new(name: "environment-dependent suites actually ran", ...)` entry from CHECKS leaves spec/meta at 187 examples, 0 failures. No spec reads preflight's CHECKS list, so the local half of the guarantee — the half the guarantee's wording claims ("locally or in CI") — can be removed silently. Related and also unpinned: the SUITES_RAN lambda reads core/tmp/rspec.json unconditionally, with no check that the report it reads is the one this run produced. The only thing making it fresh is `--out tmp/rspec.json` inside that same Check's command; drop the flag and the lambda reads a stale, gitignored report from an earlier run and passes. core/tmp/rspec.json exists on this machine right now, so the stale read is not hypothetical. The cheap fix for the second half is to write the report to a Dir.mktmpdir path the lambda is handed, so a stale file cannot be read at all; the fix for the first is a spec asserting CHECKS names this check and that its command carries --out.  
  *Check:* cd <repo>; delete the eight-line `Check.new(name: "environment-dependent suites actually ran", ...)` entry from scripts/preflight.rb; `cd core && bundle exec rspec spec/meta` -> 187 examples, 0 failures. `ruby scripts/preflight.rb --list` then prints 7 checks instead of 8 and nothing compares the number. Revert.

- **[low]** suites_ran_spec's ci.yml link is a whole-file text include, the exact form ci_skip_guard_spec's comment calls insufficient  
  `core/spec/meta/suites_ran_spec.rb` — The example "is what ci.yml runs, not a second implementation" asserts `File.read(ci.yml).include?("scripts/check_suites_ran.rb")`. That string also appears in the step's surrounding comment block (ci.yml:112-120), so the example passes with the step deleted, commented out, or disabled — it passed under all three of my disabling mutations. ci_skip_guard_spec's header comment says exactly why that form is wrong (0.2.3 merge round 6: a text slice stays green when the step is commented out) and that file does it correctly, so this one is currently backstopped rather than load-bearing. But it reads as an independent guard, and a reader who trusts it gets no protection from it. Either assert the parsed step (as ci_skip_guard_spec does) or delete it and reference the file that already checks this.  
  *Check:* cd <repo>; comment out the two lines of the guard step in .github/workflows/ci.yml (leaving the comment block above it intact); `cd core && bundle exec rspec spec/meta/suites_ran_spec.rb` -> green. Only spec/meta/ci_skip_guard_spec.rb catches it. Revert.


#### `untracked` — 5 confirmed

- **[high]** The countermeasure's third example passes while scanning zero files — an assertion that cannot fail  
  `core/spec/meta/untracked_visibility_spec.rb` — `it "is the only way this tree enumerates its own files"` builds `offenders` from `RepoFiles.list(UNTRACKED_ROOT, "scripts/*.rb", "core/spec/meta/*.rb")` and asserts it is empty. Nothing asserts that the list was non-empty in the first place, and nothing asserts the needle logic would flag a real offender. Both controls exist elsewhere in this tree for exactly this reason: `core/spec/meta/script_encoding_spec.rb:68` is `it "finds scripts to check, so an empty list cannot pass"` with `expect(self.class.scripts.length).to be >= 10`, and `analysis_stack_spec.rb` carries `it "would catch a harness that assembled its own"`, and `release_gate_spec.rb` carries `it "finds evidence to check in the first place"`. The guard for 024.147 — the entry whose whole subject is a check that reports clean because it examined the wrong set of files — has neither. This is CLAUDE.md's "an assertion that could not fail" and "a checker that cannot see the thing it checks reports exactly what a working checker reports when nothing is pinned", inside the countermeasure.  
  *Check:* In core/spec/meta/untracked_visibility_spec.rb line 78, change the pathspecs to `"scriptz/*.rb", "core/spec/metz/*.rb"`. Run `bundle exec rspec spec/meta/untracked_visibility_spec.rb` from core/: 3 examples, 0 failures. Fix: assert the scanned list length (>= 40, it is 49 today) and add a distinguishing example that feeds the filter a line containing the assembled needle and expects it to be flagged.

- **[high]** The guard scans only two globs, so a check living anywhere else may enumerate tracked-only forever  
  `core/spec/meta/untracked_visibility_spec.rb` — The scope is `scripts/*.rb` and `core/spec/meta/*.rb` — 49 files. git's `*` crosses `/`, so `scripts/*.rb` does reach subdirectories, but `core/spec/meta/*.rb` does not reach `core/spec/support/`, `core/spec/spec_helper.rb`, `core/spec/ovallsp/**`, `core/spec/e2e/**` or `core/spec/integration/**`, and neither glob reaches `.github/workflows/*.yml`, `vscode/**`, `Rakefile`, or any shell script. `core/spec/support/` already holds two shared helpers that meta specs depend on (`unspellable.rb`, `analysis_stack_helper.rb`), so moving an enumeration there is an ordinary refactor, not a contrived one. This is the same shape as the defect `measured_claims_spec` records about itself: "The first version of this scanned four globs and missed both changelogs... A guard whose scope is a list somebody remembered has the defect it was built to catch."  
  *Check:* Create core/spec/support/repo_scan.rb containing `IO.popen(["git", "ls-files", "-z", *pathspec], chdir: root, err: %i[child out], &:read)`. Run `bundle exec rspec spec/meta/untracked_visibility_spec.rb`: 3 examples, 0 failures. Then `ruby -r./scripts/repo_files -e 'p RepoFiles.list(".", "scripts/*.rb", "core/spec/meta/*.rb").include?("core/spec/support/repo_scan.rb")'` => false, so committing it does not help either. Fix: scan every tracked-and-untracked text file, the way check_home_paths.rb and measured_claims_spec.rb already do with a bare `RepoFiles.list(ROOT)`.

- **[high]** The needle is a literal substring, so the privacy check can be re-blinded inside the guard's own scope  
  `scripts/check_home_paths.rb` — The guard matches the literal `ls-files` on non-comment lines. Two ways past it, both in a file the guard does scan: (a) `git ls-tree -r --name-only -z HEAD` — a natural alternative that is strictly blinder than ls-files, since it cannot see staged files either; (b) `["git", "ls" "-files", "-z"]` — Ruby adjacent-literal concatenation, which runs exactly `git ls-files -z` while leaving no `ls-files` substring in the source. With (a) in place, `ruby scripts/check_home_paths.rb --tree` reported "clean" and exited 0 against a working tree containing the maintainer's real home path in an uncommitted file. This is the one rule CLAUDE.md says was machine-checked precisely because the prose form failed twice, and the machine check can be switched off with every spec green.  
  *Check:* Replace `def tracked_files; RepoFiles.list(ROOT); end` (scripts/check_home_paths.rb:77) with `out = IO.popen(%w[git ls-tree -r --name-only -z HEAD], chdir: ROOT, err: %i[child out], &:read); out.split("\0").reject(&:empty?).sort`. Then `printf '# %s/x\n' "$HOME" > `<probe>.md`; ruby scripts/check_home_paths.rb --tree` => "clean", exit 0. `bundle exec rspec spec/meta/untracked_visibility_spec.rb spec/meta/home_path_guard_spec.rb spec/meta/script_encoding_spec.rb` => 18 examples, 0 failures. Revert and re-run the script: exit 1 naming `<probe>.md`. Fix: match the enumeration site rather than one spelling — e.g. flag any `IO.popen`/`Open3`/backtick invocation of `git` whose argv starts with a tree-listing subcommand, or invert the check to require that every file which shells out to git for a file list requires scripts/repo_files.

- **[medium]** The guard's self-exemption is a suffix match, so any directory ending in "scripts" is exempt  
  `core/spec/meta/untracked_visibility_spec.rb` — Line 79 is `next if rel.end_with?("scripts/repo_files.rb")`. Because git pathspec `scripts/*.rb` reaches into subdirectories, a file at `scripts/xscripts/repo_files.rb` or `scripts/sub_scripts/repo_files.rb` is listed and then silently exempted — `"scripts/xscripts/repo_files.rb".end_with?("scripts/repo_files.rb")` is true. The one file that legitimately needs exempting has a known exact path, so a suffix match buys nothing and gives away a hole. Contrived on its own, but it compounds with the two findings above: the exemption widens the same scope the guard already fails to cover.  
  *Check:* `ruby -e 'p "scripts/xscripts/repo_files.rb".end_with?("scripts/repo_files.rb")'` => true. Fix: `next if rel == "scripts/repo_files.rb"`.

- **[medium]** Including untracked files makes meta-spec verdicts depend on unversioned local state, and release_gate_spec's "something invokes this" evidence is now satisfiable by a scratch file  
  `core/spec/meta/release_gate_spec.rb` — `haystack_excluding` builds its corpus from `RepoFiles.list(RELEASE_GATE_ROOT, "core/spec", "scripts")`, which since 024.147 includes untracked files of every extension under those two directories. The example decides a cited gate script is "wired" if its basename appears on a non-comment line anywhere in that corpus. An uncommitted scratch file under core/spec or scripts that happens to name a script basename therefore flips a gate from "nothing invokes this" to "wired" — the check passing for a reason other than the one it states, which is the exact failure the surrounding comment says round 1 already caught once (the verify_sbom_against_vsix.rb self-naming case). More generally, every meta check now reads working-tree residue, so two reviewers with the same commit can get different suite results — which cuts against CLAUDE.md's rule that a measurement must be reproducible and that both sides of a comparison must be given the identical corpus. The trade-off may still be right, but it is not argued anywhere and the release_gate consequence looks unintended.  
  *Check:* Read core/spec/meta/release_gate_spec.rb:92. Add an untracked file `scripts/notes.txt` whose text names a gate script that nothing invokes, and the `cites only executables that exist and that something actually runs` example stops being able to report it. Consider restricting the haystack to files a commit would carry, or to a fixed extension set, while leaving the tree-wide scans (check_home_paths, check_doc_links, duplicate_headings) untracked-inclusive as intended.


### Round 3 — `reproduce`

Six agents, each re-deriving a class of claim rather than reading it:
every number, every entry marked `released-in: 0.2.14`, every "verified
by planting" claim, every factual assertion in the 17 commit messages,
the round records themselves, and every guarantee stated in a file
header. **63 findings.** Every agent reported the tree clean.

**This round is the one that found the release lying about itself**, and
the two worst are mine:

- **Round 1's finding list was never written down**, in the file whose
  own sentence three lines above says it must be, citing `024.109` —
  what the other arrangement cost. Three of the six agents found it
  independently. Now written — **truncated, which the note above that
  list records**: the counts and the refutations are there, the bodies
  are cut at ~600 characters. `024.154`.
- **Two commits shipped a red suite, not one.** `024.147` said
  `release_gate_spec` was "the one that had actually been affected".
  Round 3 checked out every commit and ran the meta suite at each.
  `26243e0` was red too, from the same cause, and its own message quotes
  file and citation counts taken inside that blind window. Corrected in
  the entry.

*This is the third round to find defects. `CLAUDE.md` bounds the loop
there and says to ship with what is open recorded — so what follows is
the record, and the fixes below it are the ones taken before that bound
applied.*


#### `checks-fire` — 5

- **[high]** release.sh's clean-tree refusal can still be deleted outright with the whole suite green (round 2's finding reproduces)  
  `core/spec/meta/release_script_guard_spec.rb` — 046 line 667 records this as open [high]; I confirmed it. `block_containing(/diff --quiet/)` finds the first line matching the pattern and walks back to the enclosing `if`, and `code` strips only lines that *start* with `#`. Replacing the whole uncommitted-changes block with an unrelated `if` block containing `echo "no-op" # diff --quiet` and an `exit 1` leaves release_script_guard_spec at 8 examples, 0 failures. The commit message for dc9b044 says the gate was "mutation-tested by replacing the condition with `if false`" — that mutation is caught (I verified it), but it is the one a text match cannot miss; the cheaper deletion is not. copy-core.js bakes buildCommit into the packaged Core, so this is the gate that stops a VSIX naming a commit whose content it does not match.  
  *Check:* In a clone, delete the `if ! git -C "$REPO_ROOT" diff --quiet ...` block from vscode/scripts/release.sh and insert `if [ -n "$SKIP_NOTHING" ]; then\n echo "no-op" # diff --quiet\n exit 1\nfi`, then run `bundle exec rspec spec/meta/release_script_guard_spec.rb`: 8 examples, 0 failures.

- **[medium]** "All 19 dangling citations live in source comments" is false — one was in a Markdown document, and three tracked places say otherwise  
  `scripts/check_doc_links.rb` — Lines 25-27 state: "Source comments are in scope, deliberately. All 19 dangling citations live in them. A checker that read only Markdown would have reported this tree clean." 046 repeats it twice: line 157-158 ("19 citations, 5 names, 17 files, every one in a source comment") and line 180 ("source comments included, since all 19 dangling ones live there"). I re-ran the A0-era checker against the pre-A0 tree: the census reproduces exactly — 19 citations, 17 files, 5 never-committed names — but 18 are in .rb comments and the 19th is prose in docs/design/plugin-sdk.md:5 (`<the mis-spelled 018 filename>`). A Markdown-only checker would have reported one dangling citation, not a clean tree. The conclusion (source comments must be in scope) survives; the absolute quantifier that carries it does not, and the same file contradicts it ten lines earlier (line 15-17: "t <!-- deleted -->
  *Check:* Clone the repo twice, `git checkout 6bc31b9` in one and `26243e0` in the other; copy 26243e0's scripts/check_doc_links.rb into the 6bc31b9 clone and run it. It prints "19 citation(s) resolve to nothing, naming 5 path(s)"; piping the indented hit lines through `sed 's/:[0-9].*//' | sed 's/.*\.//' | sort | uniq -c` gives 18 rb, 1 md, the md being docs/design/plugin-sdk.md. <!-- deleted -->

- **[medium]** C3's Area check silently skips any path it cannot parse, so two open entries are already outside the guarantee and the check is opt-out by typo  
  `scripts/deferred_findings.rb` — AREA_PATH (line 126) matches only a backticked path rooted at core|vscode|scripts|docs|site|.github, and area_paths returns [] for anything else — with no signal. deferred_findings_spec.rb:405 ("names only paths that exist, in every open entry's Area") therefore checks 55 of 57 open entries: 024.150's Area names `AGENTS.md`, `CLAUDE.md` (real root-level files the matcher cannot express — the exact blind spot round 2 widened CITATION to fix, not applied here) and 024.102's Area is prose; a path with a line suffix such as 024.73's `core/lib/ovallsp/plugins/loader.rb:446` is also unmatched. Round 2 closed the neighbouring hole (zero Area lines) with `!= 1`; the successor hole — one Area line, zero parseable paths — is uncovered, and unlike doc_links_spec (which now asserts per-root coverage is non-zero) nothing asserts this check reads anything. It also makes 046's line 191 claim, "the meas  
  *Check:* In a clone at HEAD, replace an open entry's Area line with an unbackticked nonexistent path, e.g. `**Area:** core/lib/ovallsp/diagnostics/never_committed_file.rb` in 024.13, and run `bundle exec rspec spec/meta/deferred_findings_spec.rb` — 36 examples, 0 failures. With backticks it fails at :405. Live state: `ruby -e 'require "./scripts/deferred_findings"; b=File.read("docs/design/tasks/024-deferr

- **[low]** `check_pinned_mutations.rb --verify-only` prints "every one caught by the example that names it" without applying a single mutation  
  `scripts/check_pinned_mutations.rb` — In --verify-only mode the loop `next`s after the dry-run selection check, so no file is written and no example is ever run to failure; the final line still prints "check-pinned-mutations: 21 mutation(s), every one caught by the example that names it." That is the mode the ordinary suite runs (pinned_mutations_spec.rb), so the message a reader sees from the suite asserts something the run did not establish — the exact shape the script's own header warns about ("a checker that cannot see the thing it checks reports exactly what a working checker reports when nothing is pinned"). Coverage itself is fine: ci.yml's "Pinned mutations" job runs the real applier, and I confirmed 21 of 21 are genuinely caught. The defect is the claim, not the coverage.  
  *Check:* `ruby scripts/check_pinned_mutations.rb --verify-only` completes in seconds (versus minutes for the real run) and prints the "every one caught" line; the two refusal paths I planted (an entry naming a spec file, an entry whose `from` matches 7 times) do fail correctly in that mode.

- **[low]** "is the only way this tree enumerates its own files" scans two globs, not the tree  
  `core/spec/meta/untracked_visibility_spec.rb` — The example at line 76 enumerates `scripts/*.rb` and `core/spec/meta/*.rb` only, while its stated guarantee is that nothing anywhere enumerates the repository the old way. A `git ls-files` enumeration reintroduced in any other spec directory (core/spec/ovallsp/**, core/spec/integration/**, core/spec/support/**) or in a workflow is invisible to it. The check works — planting `git ls-files` in core/spec/meta/spec_constants_spec.rb and in scripts/check_site_links.rb both fire — but its coverage is a scope decision that is neither stated at the site nor asserted, which is the class round 2 fixed for check_doc_links by making per-root coverage an assertion.  
  *Check:* Append `PLANTED = \`git ls-files docs\` if false` to core/spec/ovallsp/cold_indexer_spec.rb and run `bundle exec rspec spec/meta/untracked_visibility_spec.rb`: 3 examples, 0 failures. The same line in core/spec/meta/spec_constants_spec.rb fails.


#### `commit-claims` — 14

- **[high]** A second commit on this branch shipped a red suite, from the same cause, and the record says there was only one  
  `docs/design/tasks/024-deferred-review-findings.md` — 024.147 and commit 23196a8 both end with "Each was re-run after this fix; `release_gate_spec` is the one that had actually been affected." Two commits on this branch shipped a failing meta suite, not one. Commit 26243e0 ("046 A0") is red: `check_doc_links.rb` reports its own two comment lines (`<the two-digit design-doc shorthand>`, `<the two-digit design-doc shorthand>`) as citations resolving to nothing, and `doc_links_spec.rb` shells out to it, so `1 example, 1 failure`. The cause is identical to 024.147's: at 26243e0 the script enumerated with `git ls-files`, it was untracked when preflight ran, so it did not scan itself; `git commit` put it in its own input and it went red. It stayed red for one commit and was repaired at 1bf897b (024.126) without either the commit message or the register saying HEAD had been red. 26243e0's own message also states "inspects 527 tracked files and 661 citations", numbers taken ins  
  *Check:* git clone --no-hardlinks <repo> w && cd w && for c in $(git log --format=%h main..feat/0.2.14 --reverse); do git checkout -q $c; printf '%s ' $c; (cd core && bundle exec rspec spec/meta 2>&1 | grep -E '^[0-9]+ examples'); done -> 0 failures everywhere except 26243e0 (1) and 7c92b05 (1). Then: git checkout -q 26243e0 && ruby scripts/check_doc_links.rb; echo $? -> exit 1, '529 tracked file(s) inspec

- **[high]** CLAUDE.md's new preflight paragraph describes the arrangement 024.148 records as the defect, and miscounts the checks  
  `CLAUDE.md` — The paragraph added at 7c92b05 says preflight "runs the full suite, the two real-Rails suites separately with a non-zero example-count assertion, and the five scripted checks." Three things are wrong at HEAD. (1) It is three spec files, not two: `spec/integration/real_rails_spec.rb`, `spec/e2e/capabilities_spec.rb` and `spec/meta/client_behaviour_spec.rb`. (2) It is not a non-zero example-count assertion — that is exactly 024.148, the check that "could not fail in the case it existed for" because a skipped example is still an example; 8f1d4f4 replaced it with `CheckSuitesRan.complaints`, which reads per-example status. So the project's operating document now instructs a reader to rely on the rule the release fixed. (3) `CHECKS.length` is 8, of which six are scripted (documented_counts, check_home_paths, check_doc_links, reindex_findings, check_swallowed_failures, check_site_links), not f  
  *Check:* ruby scripts/preflight.rb --list -> 8 checks, the third naming three spec files. grep -n 'SUITES_RAN\|NON_EMPTY_SUITE' scripts/preflight.rb. git log --oneline -S 'the five scripted checks' main..HEAD -- CLAUDE.md -> 7c92b05; git log --oneline -S 'client_behaviour_spec.rb --format json' main..HEAD -- scripts/preflight.rb -> 8f1d4f4. git show 7c92b05:scripts/preflight.rb | grep -c 'Check.new' -> 8.

- **[high]** agents_pointer_spec writes its matcher twice and its own comment forbids that; gutting the real assertion leaves every example green  
  `core/spec/meta/agents_pointer_spec.rb` — The file's header comment states the rule — "One pattern, read by the assertion and by the example that proves the assertion fires. Written twice, they can drift, and the drift shows up as the planted example passing while the real one has stopped matching anything — which is this release's own subject" — and then does exactly that one method later. The bullet pair correctly shares `AGENTS_TASK_FILE`, but the goal-paragraph pair writes the regex `/`feat\/[0-9]+\.[0-9]+\.[0-9]+`|being prepared now is \*\*[0-9]/` separately in "does not assert which release is being prepared" and in "would catch the claim coming back". Replacing the assertion's copy with a regex that matches nothing leaves 5 examples, 0 failures — the guarantee 024.150 says is "pinned by agents_pointer_spec.rb" is one edit away from unenforced. This is 024.151's exact class, and it is unrecorded: round 2's twelve attacked   
  *Check:* In a clone: ruby -e 'p="core/spec/meta/agents_pointer_spec.rb"; s=File.read(p,encoding:"UTF-8"); File.write(p, s.sub("claims = goal_paragraph.scan(/`feat\\/[0-9]+\\.[0-9]+\\.[0-9]+`|being prepared now is \\*\\*[0-9]/)", "claims = goal_paragraph.scan(/zzz-never-matches/)"))' && (cd core && bundle exec rspec spec/meta/agents_pointer_spec.rb) -> 5 examples, 0 failures. Then git checkout -- the file.

- **[medium]** Round 2's stated method is contradicted by a finding recorded in the same document, and its headline numbers do not re-derive  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — 046's round-2 header and commit 54b7274 both assert "Every attacker worked in a clone or reverted, and the tree was verified clean and at the same HEAD afterwards." 046's own `untracked` section records the opposite, at [medium]: "Concurrent agents left the working tree modified; the dirty set changed three times during one review round" — `.github/workflows/ci.yml` gained `continue-on-error: true` on the suites-ran job (later reverted) and `docs/RELEASE_CHECKLIST.md` had test filenames rewritten to files that do not exist, while an attacker was measuring. CLAUDE.md's measurement rule names this specifically ("Never run this ... while another agent is mutating the same working tree. Concurrent mutation invalidates both results. Sequence them."). Consistent with that, round 2's two headline numbers do not reproduce from clean checkouts: "Citations 794 -> 1,503" derives as 786 -> 1504, and  
  *Check:* In a clone: git checkout -q 8f1d4f4 && ruby scripts/check_doc_links.rb | head -1 -> '536 file(s) inspected, 786 documentation citation(s)'. git checkout -q 54b7274 && ruby scripts/check_doc_links.rb | head -1 -> '535 file(s) inspected, 1504 documentation citation(s)'. grep -n 'dirty set changed three times' docs/design/tasks/046-0.2.14-making-the-record-true.md.

- **[medium]** The SKIP mutation manifest entry claims to replay round 2's break verbatim; the mutation it encodes inspects zero files  
  `core/spec/meta/pinned_mutations.yml` — The entry's `why` reads "check_doc_links' SKIP -- widening it to exclude core/ drops inspection from 537 files to 117 ... which is round 2's break verbatim." Neither half derives. The encoded replacement `"|core/spec/fixtures/rails_real/" -> "|core/|"` introduces an empty alternative into the regex, so SKIP matches every path and the checker inspects 0 files, not 117 (it still exits 0 and reports "every documentation path resolves", which is why the coverage floor catches it). The `why` line's own literal description — exclude `core/` — gives 209 files. Round 2's actual mutation, recorded verbatim in 046's `doc-links` section as `\A(core/|vscode/|site/|scripts/|core/vendor/|...)`, is what gives 117. The example does catch the mutation, so the pin works; what is wrong is that the manifest's stated reason is a claim about this tree that the manifest's own mutation refutes — the same shape   
  *Check:* In a clone at HEAD: ruby -e 'p="scripts/check_doc_links.rb"; s=File.read(p,encoding:"UTF-8"); File.write(p, s.sub("|core/spec/fixtures/rails_real/", "|core/|"))' && ruby scripts/check_doc_links.rb | head -2 -> '0 file(s) inspected'. Repeat with sub("core/spec/fixtures/rails_real/", "core/") -> '209 file(s) inspected'. Repeat with the four-root regex from 046 at 8f1d4f4 -> '117 file(s) inspected'.

- **[medium]** "Ten checks -- two scripts and eight specs -- enumerated their input with git ls-files" is nine  
  `docs/design/tasks/024-deferred-review-findings.md` — 024.147's opening count, repeated in commit 23196a8, is off by one. At 23196a8^ exactly nine files used `git ls-files`: seven specs (analysis_stack, client_behaviour, design_doc_drift, duplicate_headings, measured_claims, release_gate, script_engine... i.e. script_encoding) and two scripts (check_doc_links, check_home_paths). The tenth `RepoFiles` site at HEAD is `core/spec/meta/untracked_visibility_spec.rb`, which was created by that same commit and therefore cannot have enumerated anything the old way. The companion sentence "all ten sites use it" is true at HEAD; the premise sentence is not. This is an unmarked count about this tree in the entry whose subject is checks that answer a different question than they claim.  
  *Check:* git grep -l 'ls-files' 23196a8^ -- 'scripts/*' 'core/spec/*' | wc -l -> 9. git show 23196a8 --name-only --format= | grep -c '^core/spec/meta/.*_spec.rb' -> 8, of which untracked_visibility_spec.rb is new (git log --oneline -- core/spec/meta/untracked_visibility_spec.rb).

- **[medium]** The recorded departure from the review cadence describes a core/lib diff that is not this change set's  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — The departure that excuses skipping the `drive` method reads: "This change set alters no engine answer — the only `core/lib` edits in the range are two comment rewrites and one deleted tombstone comment." Derived: `git diff --stat main..HEAD -- core/lib` is 16 files, 38 insertions, 47 deletions. Thirteen of those are A0's citation repoints (plugins.rb, loader.rb, manifest.rb, runtime_context.rb, static_context.rb, plan.rb, planner.rb, generated_method_fact.rb, parser_service.rb, generated_method_index.rb, query_context.rb, server.rb, workspace_index.rb), plus query_service.rb, receiver_resolution.rb's comment rewrite and engine.rb's tombstone. The conclusion survives — zero non-comment lines change in core/lib — but the evidence as written is not what a reader checking it would find, and a wrong description of the diff is precisely what would let a behavioural line ride along under a dep  
  *Check:* git diff --stat main..HEAD -- core/lib -> 16 files. git diff main..HEAD -- core/lib | grep -E '^[+-]' | grep -v '^[+-][+-]' | grep -vE '^[+-]\s*#' | grep -vE '^[+-]\s*$' | wc -l -> 0.

- **[medium]** 046's Round 1 section is an empty placeholder promising results that were never written there  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — Line 455-459 reads "### Round 1 — `diff` / Five readers over the change set ... *Results below as they land.*" and the next non-blank line is "### Round 2 — `attack`". Round 2's 70 findings are recorded in full immediately after, under the standing instruction that "raising a finding without recording it is not allowed"; round 1's 84-raised / 18-survived list exists only in commit 8f1d4f4's message. Line 430 of the same file states the rule and its cost: "The finding list goes here as each round reports it. `024.109` is what the other arrangement cost — findings that lived only in a session and were lost when it ended." The sentence "Results below as they land" is itself now a record that stopped being true, in the release named for making the record true.  
  *Check:* sed -n '455,462p' docs/design/tasks/046-0.2.14-making-the-record-true.md -> the placeholder is immediately followed by the Round 2 heading. awk 'NR>=461' <same file> | grep -c '^- \*\*\[' -> 70, all under Round 2's twelve guarantee sections.

- **[low]** "four source comments lean on §7" — one does; the other three cite a different design document  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — 046's deletion ledger (line 234) justifies keeping `05-protocol.md` §6 and §8 rather than deleting them with "The file stays — four source comments lean on §7", and the rewritten §6 repeats it in Japanese ("番号を詰めると `section 7` を指すソースコメントが壊れる"). Derived: only `core/lib/ovallsp/agent_process_manager.rb:16` cites `docs/05-protocol.md section 7`. The other three occurrences of the string are `models/model_registry.rb:25` and `types.rb:38` and `local_inferencer.rb:980`, all citing `docs/03-semantic-engine.md` §7.1/§7.3 — a different document whose numbering `05-protocol.md` cannot break. The decision may still be right on one citation; the measurement offered for it is a grep over an undifferentiated string, which is the same shape as C6's first version that this release records as "the mistake it audits".  
  *Check:* grep -rn 'section 7' core/lib | head -> four hits, one naming 05-protocol.md and three naming 03-semantic-engine.md.

- **[low]** 024.141: "kept the pre-fix command for eleven releases" — twenty-five releases shipped with it  
  `docs/design/tasks/024-deferred-review-findings.md` — The entry's "What makes this its own entry rather than a typo" paragraph rests on the number: "The document describing the script kept the pre-fix command for eleven releases." The `--packagePath` fix landed in e9d09d5 with `vscode/package.json` at version 0.1.3. `docs/RELEASE_ARTIFACTS.md` records 0.1.3 through 0.1.13 (11) and 0.2.0 through 0.2.13 (14) as published — 25 releases, not eleven. Eleven is the count of the 0.1.x line alone. The entry's argument is stronger than the figure it cites, so nothing depends on it being small, which is exactly why nobody re-derived it.  
  *Check:* git log --format=%h -S packagePath -- vscode/scripts/release.sh | tail -1 -> e9d09d5; git show e9d09d5:vscode/package.json | ruby -rjson -e 'puts JSON.parse(STDIN.read)["version"]' -> 0.1.3. grep -c '^| 0\.' docs/RELEASE_ARTIFACTS.md and read the table.

- **[low]** 024.150's "It grew by 15 words" is 99 words, undated, in the release that invented the dated marker for this  
  `docs/design/tasks/024-deferred-review-findings.md` — 024.150 says "`046` asserted that the paraphrase would shrink in 0.2.14. It grew by 15 words." Derived word counts of AGENTS.md: `main` 1463, HEAD 1562 — a growth of 99. The +15 was accurate at 8f1d4f4^ (1478), i.e. at the instant round 1 measured it, and round 1's own AGENTS.md rewrite in the same commit took it to 1562. The claim is present-tense, carries no `<!-- measured: -->` marker, and is not dated — while the same commit's headline fix was to date `037`'s register count as `register-open-defects@f67e743e6eec` for precisely this failure mode. Nothing re-derives it, so the entry that exists because a record drifted has already drifted.  
  *Check:* git show main:AGENTS.md | wc -w -> 1463; git show HEAD:AGENTS.md | wc -w -> 1562; git show 8f1d4f4^:AGENTS.md | wc -w -> 1478.

- **[low]** "seven numbered gates" contradicts the same release's three other statements of five  
  `docs/design/tasks/024-deferred-review-findings.md` — Commit dc9b044 opens "make-final-review-bundle.sh was 919 lines that RELEASE_CHECKLIST named as the enforcement for seven numbered gates". Derived at dc9b044^: the script is cited in five numbered rows — 1 (clean tree), 2 (Core RSpec), 14 (secret scan), 15 (vsce ls --tree), 16 (darwin-arm64) — plus one prose line at 33. `fe05e3f`, `0e84e1a` and `046` line 94 all say five, so the release contradicts itself and the outlier is the commit that performed the deletion and reassigned the gates. Since that commit's whole argument is a gate-by-gate account of what still covers each row, the count of rows is the load-bearing number in it.  
  *Check:* git show dc9b044^:docs/RELEASE_CHECKLIST.md | grep -n 'make-final-review-bundle' -> lines 33, 38, 39, 51, 53, 54: one prose line and five table rows. sed -n '90,95p' docs/design/tasks/046-0.2.14-making-the-record-true.md -> 'five gate rows'.

- **[low]** "Every one lived in a source comment" is false of the census it describes, and contradicts its own preceding sentence  
  `core/spec/meta/doc_links_spec.rb` — The spec's header (and commit 26243e0) says of the 19-citation census: "The whole `plugins/` subsystem and the public SDK document pointed at one of them. Every one lived in a *source comment*, so a checker that read only Markdown would have called this tree clean." Reproducing the census at 6bc31b9 gives 19 citations in 17 files, of which 18 are source comments and one is `docs/design/plugin-sdk.md:5` — a Markdown document, the very "public SDK document" the previous sentence names. A Markdown-only checker would therefore have found one of the nineteen, and the conclusion drawn from the claim is wrong. `fe05e3f` states it correctly ("the whole `plugins/` subsystem and the public SDK document point at one of them"); the tightened version in the spec header is where it broke. <!-- deleted -->
  *Check:* In a clone: git checkout -q 6bc31b9 && git show 26243e0:scripts/check_doc_links.rb > scripts/check_doc_links.rb && ruby scripts/check_doc_links.rb -> 19 citations, 5 paths, 17 distinct files, the last of which is docs/design/plugin-sdk.md:5. <!-- deleted -->

- **[low]** check_doc_links' CITATION comment enumerates twenty root documents as fifteen  
  `scripts/check_doc_links.rb` — The comment reads: "twenty tracked Markdown files -- `CLAUDE.md`, `AGENTS.md`, both READMEs, both CONTRIBUTINGs, `SECURITY`, `SUPPORT`, `CODE_OF_CONDUCT`, and six under `vscode/`". The total 20 is right, but the enumeration sums to 15: SECURITY, SUPPORT and CODE_OF_CONDUCT each have a `.ja` half (6, not 3) and there are eight files under `vscode/` (CHANGELOG, PRIVACY, README, THIRD_PARTY_NOTICES, each with a `.ja`), not six. Commit 54b7274 states a shorter and worse version of the same list, omitting SECURITY/SUPPORT/CODE_OF_CONDUCT entirely. The comment is what a reader consults to decide whether the structural pattern still covers what it claims, so the list is the part that has to be right.  
  *Check:* git ls-files | ruby -ne 'p=$_.chomp; puts p if p =~ %r{\A(?:vscode/)?[A-Z][A-Z0-9_]*(?:\.ja)?\.md\z}' -> 20 files, 8 of them under vscode/.


#### `fixed-entries` — 8

- **[high]** 024.130's reproduction does not reproduce, on main or at HEAD, and both KNOWN_LIMITATIONS publish it to users  
  `docs/design/tasks/024-deferred-review-findings.md` — 024.130 (line 7190, created by this release's split of 024.90; open, user-visible: yes) claims at line 7201: "In `Billing::Invoice`, `Order.new` hovers `Order` while `Shipping::Order.new` hovers `Shipping::Order` -- so with two `Order`s in the workspace the label does not say which was meant." I drove the real semantic stack (AnalysisStack.build + Semantic::QueryService#type_at, the same call Server#hover_result makes) over four shapes of that exact scenario and the label is fully qualified every time: Billing::Order and Shipping::Order in separate files -> `Billing::Order`; both nested in one file -> `Billing::Order`; compact `class Billing::Order` -> `Billing::Order`; hovering the constant itself -> `ClassOf[Billing::Order]`. Identical results at HEAD and on main, so it is not a fix this release made silently. The only way to get a bare `Order` is when nothing named `Order` is reachabl  
  *Check:* From core/: bundle exec ruby -e with AnalysisStack.build(signatures:, workspace_index:, model_registry:), index three buffers -- `module Billing; class Order; end; end` / `module Shipping; class Order; end; end` / `module Billing; class Invoice; def go; o = Order.new; o; end; end; end` -- then Semantic::QueryService#type_at at the `o` on the last line. It prints Billing::Order. Repeat in a git wor

- **[medium]** 024.131 says "hovers nothing"; it hovers `nil` -- a wrong answer, not an absent one  
  `docs/design/tasks/024-deferred-review-findings.md` — 024.131 (line 7210) and its body at line 7221 say `b = nil; b ||= "x"` "hovers nothing" and that "the local has no type at the position after it". Measured: type_at on the line after returns Types::NilType, whose #to_s is "nil", and Server#hover_lines (core/lib/ovallsp/server.rb:3891) returns [] only for Types::UNKNOWN -- so the hover reads `nil` for a local that is a String. The variant that really does hover nothing is `b ||= "x"` with no preceding assignment (Unknown). So the entry, the register index (line 303), the 024.90 split table (line 5201), docs/KNOWN_LIMITATIONS.md:652 and docs/KNOWN_LIMITATIONS.ja.md all describe an absent answer where the engine gives a wrong one. Section 0 ranks those the other way round, so the triage this text supports is the wrong triage, and the stated mechanism ("the local has no type") is false -- the `nil` assignment wins outright rather than the `|  
  *Check:* From core/, build the stack as above over a single buffer "b = nil\nb ||= \"x\"\nb\n" and call type_at at line 2, character 0: Ovallsp::Types::NilType, to_s "nil". `Ovallsp::Types::NIL == Ovallsp::Types::UNKNOWN` is false, so server.rb:3891 yields ["nil"], a non-empty hover. Contrast "b ||= \"x\"\nb\n", which yields Unknown.

- **[medium]** 024.139 says both trailing findings sections were deleted; both headings are still in the tree, and the countermeasure it names was never built  
  `docs/design/tasks/024-deferred-review-findings.md` — The entry (status: fixed) states at line 7441: "Both sections are deleted, and this entry is where they went." At HEAD `## 残課題` is still at docs/design/tasks/008.5-runtime-and-index-corrections.md:104 and `## 残っているKnown Issue` at docs/design/tasks/008.6-agent-and-index-hardening.md:89 -- their six items were replaced with a pointer to the register, which is a reasonable outcome but is not what the entry says. The entry then names its countermeasure: "046's C4 is the countermeasure -- the register's parser moving to scripts/ so a check can assert that docs/design/tasks/*.md other than 024 carry no findings section of their own." C4 as executed only extracts DeferredFindings into scripts/; nothing in core/spec/meta or scripts asserts anything about other task files' sections, so a findings section added to a third task document would still be invisible. (The six verdicts in the entry's own  
  *Check:* grep -n '^## 残課題' docs/design/tasks/008.5-runtime-and-index-corrections.md and '^## 残っているKnown Issue' docs/design/tasks/008.6-agent-and-index-hardening.md -- both hit. Then grep -rn 'findings section' core/spec/meta scripts -- no matches.

- **[medium]** 024.141 is marked fixed against a countermeasure that addresses a different class; the release-procedure row it says is missing is still missing  
  `docs/design/tasks/024-deferred-review-findings.md` — The instance is genuinely fixed -- main's docs/PUBLISHING.md:184 and PUBLISHING.ja.md:166 both said `vsce publish --target darwin-arm64 --pre-release`, HEAD says `--packagePath`, matching vscode/scripts/release.sh:239 and the never-use comment at release.sh:223. But the entry's diagnosis is that the class is "a fix applied at the place that runs and not at the place that tells a person what to run", and its countermeasure sentence at line 7575 is: "`DOCUMENTATION_MAP` has no row for 'the release procedure changed', which is why nothing pointed at it. `046`'s C6 is where that goes." C6 (046 line 304) is a different check entirely -- every script in RELEASE_CHECKLIST's evidence column must be invoked by release.sh, a workflow or a spec. DOCUMENTATION_MAP.md gained three rows in this release (VS Code manifest, Core<->Agent protocol, plugin API), none about the release or publish procedure,   
  *Check:* grep -n 'release' docs/DOCUMENTATION_MAP.md -- the only hit naming the release process is the generic "A working agreement" row. Read docs/design/tasks/046-0.2.14-making-the-record-true.md:304 for what C6 is, and `git diff main..HEAD -- core/spec/meta/release_script_guard_spec.rb` for what was added.

- **[medium]** 024.143 presents as its fix the exact guard 024.148 records, in the same release, as unable to fail  
  `docs/design/tasks/024-deferred-review-findings.md` — 024.143's first bullet (line 7676) reads: "**A skipped check is reported, never assumed passed.** ... It asserts a non-zero example count rather than reading the exit status -- `CLAUDE.md` already said to do this by hand." 024.148 (line 7915), also released-in 0.2.14, is the entry saying that assertion "cannot work" because a skipped example is still an example, and that preflight was fixed by delegating to scripts/check_suites_ran.rb. preflight.rb at HEAD does the latter (SUITES_RAN at line 61, calling CheckSuitesRan.complaints; NON_EMPTY_SUITE survives but guards only the full-suite check). 024.143 was never corrected, so a reader who lands on it is told the superseded rule is the shipped one -- and that rule is this project's canonical example of an assertion that cannot fail.  
  *Check:* Read 024.143's bullet at line 7676 beside 024.148 at line 7915, then scripts/preflight.rb lines 44-67 and 85-92.

- **[low]** 024.124's guard is narrower than the entry says: it checks only `kind: defect`, and nothing checks `released-in` at all  
  `scripts/deferred_findings.rb` — 024.124 says the check "fails on an **open** entry whose target is in that table". `open_entries_targeting_a_shipped_release` (line 114) is built on `open_defects`, which filters `kind == "defect"` -- an open `friction` or `roadmap` entry naming a shipped release passes silently. No entry is in that state today, so this is latent, not live. The mirror direction is unguarded outright: `released-in` is in KNOWN_KEYS (line 62) and read by nothing. Fourteen entries currently assert `released-in: 0.2.14` while core/lib/ovallsp/version.rb still says 0.2.13 and docs/RELEASE_ARTIFACTS.md has no 0.2.14 row -- so if this branch ships under any other number, the register re-creates 024.124's situation from the other side, which is the third-occurrence class the entry exists to close.  
  *Check:* Read scripts/deferred_findings.rb:112-119. Then grep -n 'released-in' core/spec/meta/deferred_findings_spec.rb scripts/deferred_findings.rb: the only hits are a comment and the KNOWN_KEYS list.

- **[low]** 024.143's "seven things", and preflight.rb's own header, both miscount the gate they describe  
  `scripts/preflight.rb` — 024.143 opens at line 7656 with "Seven things must be true before a commit here" and enumerates seven. preflight.rb's CHECKS array has eight -- the documented-example-counts check added by 024.145 is the eighth, and this branch's own commit 7c92b05 message says "preflight: all 8 checks passed". Separately, preflight.rb's header comment at line 17 says "the checks are in six places (the suite, three scripts, a git state, a derived number)": it runs rspec twice and six distinct scripts, and there is no git-state check in the file at all. Nothing pins either number -- 024.151 already records that no spec reads preflight.rb -- so both drifted inside the release that added the eighth check.  
  *Check:* `ruby scripts/preflight.rb --list` prints 8 checks, against "Seven things" at docs/design/tasks/024-deferred-review-findings.md:7656 and "six places ... three scripts, a git state" at scripts/preflight.rb:17.

- **[low]** A comment in corpus_diagnostics.rb ends mid-sentence -- the residue of a botched edit, in the file 024.142 rewrote  
  `scripts/corpus_diagnostics.rb` — Lines 169-174 read: "...That is not a smaller measurement, it is a measurement of something else, and it is why a" / "**Assembled, not wired here** (`042`'s D8)." The clause after "why a" was cut and a new sentence starts on the next line. This is 024.140's class exactly -- a scripted edit whose end boundary silently missed -- and it survives in the file 024.142 rewrote and re-verified in this release. It predates the change set (byte-identical on main at lines 75-80), which is the point: 0.2.14 is a whole-repository audit whose subject is the record matching the tree, and duplicate_headings_spec and check_doc_links cannot see a sentence that simply stops. Minor of the same shape: core/spec/support/unspellable.rb:16 spells "rembering".  
  *Check:* sed -n '169,175p' scripts/corpus_diagnostics.rb, and the same range in a worktree of main to confirm it is pre-existing rather than introduced here.


#### `guarantees-doc` — 10

- **[medium]** pinned_mutations.yml's header states the opposite safety property from the script that applies it  
  `core/spec/meta/pinned_mutations.yml` — Line 29 says: "Run by `scripts/check_pinned_mutations.rb`, which applies each mutation to a throwaway copy of `core/lib` and runs the named example against it. The live tree is never modified: the copy goes first on the load path." The script's own header (line 17) says that arrangement DOES NOT WORK and was abandoned: "The mutation is applied to the real file and restored. A copy on the load path does not work: the Gemfile has a `gemspec`, so Bundler puts the real `core/lib` at the front of `$LOAD_PATH`... the first version of this script did that and reported all four mutations uncaught." The code confirms it: `File.write(source, original.sub(entry["from"], entry["to"]))` writes into the real tracked file. The manifest also says `file` is "a path under `core/lib`" (line 18), but 7 of the 21 entries — added in the very last commit, e100388 — are under `scripts/`, and the script was wide  
  *Check:* `sed -n '16,31p' core/spec/meta/pinned_mutations.yml` beside `sed -n '15,30p;110,122p' scripts/check_pinned_mutations.rb`. Then `grep -n '^ file:' core/spec/meta/pinned_mutations.yml` — seven entries name `scripts/…`. Then `git show e100388 --stat` shows the yml gained those entries without its header being touched.

- **[medium]** `--verify-only` prints "every one caught by the example that names it" without applying a single mutation  
  `scripts/check_pinned_mutations.rb` — In `--verify-only` mode every entry takes the `next` branch at line 104 after only a `--dry-run` selection check; no file is written and no example is run. Control still falls through to line 139, which prints `check-pinned-mutations: 21 mutation(s), every one caught by the example that names it.` and exits 0. The failure branch is symmetric: it warns `N of M mutation(s) not caught` for what are actually manifest-shape problems. `core/spec/meta/pinned_mutations_spec.rb` shells out in exactly this mode, so the sentence a suite run produces asserts a property nothing tested. This is the project's own "the answer that would be right if nothing had gone wrong" shape, in the checker built to detect that shape.  
  *Check:* `time ruby scripts/check_pinned_mutations.rb --verify-only` — it prints "21 mutation(s), every one caught by the example that names it" in about 4 seconds, while the real applier runs 21 full rspec invocations against mutated source. Compare lines 96–110 (the `verify_only` early `next`) with lines 137–145.

- **[medium]** generate_sbom.rb still says it is "not run automatically by CI/tests" — this release made that false  
  `scripts/generate_sbom.rb` — Header lines 17–20: "Run manually via `ruby scripts/generate_sbom.rb` whenever ... changes; not run automatically by CI/tests (RELEASE_CHECKLIST.md item 8)." The same change set added `core/spec/meta/sbom_spec.rb` (046's C7), which runs `ruby scripts/generate_sbom.rb --check` on every suite run and a second time into a tmpdir to plant a divergence — and the `--check` block added a few lines below in the same diff carries the comment "046's C7". So the file's own header contradicts the mechanism its own commit installed, and it does so in the direction that matters: a reader is told a stale SBOM will not be caught until somebody remembers to run this by hand, which is precisely the state sbom_spec.rb was written to end.  
  *Check:* `sed -n '14,21p' scripts/generate_sbom.rb` against `core/spec/meta/sbom_spec.rb`; `git diff main..HEAD -- scripts/generate_sbom.rb` shows the `--check` path and the untouched header in one diff.

- **[medium]** rescue_verdicts.yml's header describes a first pass in which "the rest are marked swallows" — no entry has carried that verdict since 0.2.13  
  `core/spec/meta/rescue_verdicts.yml` — Lines 12–16 say the verdicts are "a first pass: every site whose handler raises or reports was marked `surfaces` mechanically, and the rest are marked `swallows` — which is the safe default and the honest one, since nothing has yet been argued. Moving one to `contained` is the review work `024.122` describes." Derived: zero entries carry `swallows`; the only three occurrences of the word in the file are in this header. `ruby scripts/check_swallowed_failures.rb` reports 60 surface, 98 contained, none swallowing, and CLAUDE.md records that all 158 were enumerated and argued in 0.2.13. The header therefore tells a reader that the file's contents are unargued defaults and that the review work is still to be started, when it is finished — the opposite of what a reviewer needs to know, which is that 98 `contained` arguments are one author's and are the thing to audit.  
  *Check:* `grep -n swallows core/spec/meta/rescue_verdicts.yml` returns only lines 4, 9 and 14, all header prose. `ruby scripts/check_swallowed_failures.rb` prints the counts.

- **[medium]** check_doc_links' deletion marker admits a pointer to a renamed file, which its header says is still a failure  
  `scripts/check_doc_links.rb` — The paragraph at lines 130–142 defines what `[deletion marker]` admits and ends: "A path that has never existed in any commit is still a failure with the marker present ... A pointer to a *renamed* file is also still a failure, because the marker is a deliberate edit on the line that needs it rather than a mode the file is in." The first half holds; the second does not. `ever_existed?` asks `git log --all -1 -- <path>`, which is true for the pre-rename path, so a marked citation of a file that was renamed away passes and is counted as a recorded deletion. The stated reason does not support the stated claim either — per-line marking says nothing about renames. This matters because renames are the common case in this tree: task files get renumbered and the design documents were moved wholesale, and a renamed target is exactly the pointer that still leads somewhere real for the writer and n  
  *Check:* In a tmpdir: `git init`; commit `<probe>.md`; `git mv `<probe>.md` `<probe>.md` and commit; commit a note containing "`<probe>.md` [deletion marker]" and "`<probe>.md` [deletion marker]"; run `CHECK_DOC_LINKS_ROOT=<tmpdir> ruby scripts/check_doc_links.rb`. It reports only the never-existed path and counts the renamed one under "naming a deleted file on a line marked as recording t

- **[medium]** The measured-claim scanner's scope is still four hand-written globs, so the header's guarantee is false outside them  
  `core/spec/meta/measured_claims_spec.rb` — The header states: "Adding a claim means adding its deriver below. A marker naming a deriver that does not exist fails, so a claim cannot be marked and left uncomputed." `claims` (line 106) scans `%w[docs/**/*.md core/lib/**/*.rb core/spec/**/*.rb vscode/src/**/*.ts]`. A `<!-- measured: … -->` marker in CLAUDE.md, AGENTS.md, README, CONTRIBUTING, `scripts/*.rb`, `vscode/CHANGELOG.md` or `site/` is invisible to both examples — including one naming a deriver that does not exist. The citation half of this very file was rewritten to `RepoFiles.list(TREE_ROOT)` over every tracked-or-untracked text file, under a comment saying "The first version of this scanned four globs and missed both changelogs ... A guard whose scope is a list somebody remembered has the defect it was built to catch." The lesson is written out ten lines from the code that still has the defect. Recorded in 046 round 2 and   
  *Check:* `sed -n '104,112p' core/spec/meta/measured_claims_spec.rb` against `sed -n '196,208p'` (the `scanned_files` comment). Add "[measured marker for no-such-deriver]" to CLAUDE.md or vscode/CHANGELOG.md and run `bundle exec rspec spec/meta/measured_claims_spec.rb` from `core/` — 6 examples, 0 failures.

- **[low]** "used 91 times across 39 files" — the shorthand census does not reproduce under any reading  
  `scripts/check_doc_links.rb` — Line 40: "`docs/<NN>-<name>.md` is an established shorthand for `docs/design/docs/<NN>-<name>.md`, used 91 times across 39 files since the design documents moved. It is a shorthand, not an error: rewriting 91 lines would cost more than it buys." Recomputed with the file's own `CITATION` and `SHORTHAND` regexes over `RepoFiles.list`, with the file's own `SKIP` applied: 45 hits across 22 files at HEAD; 59 across 24 at `6bc31b9` and at `26243e0`, the commit that introduced the sentence. Never 91/39. No alternative reading reaches it either: bare `NN-name.md` occurrences are 12 across 6 files, and every `NN-name.md` anywhere in the tree is 288 across 123. The number is load-bearing for the decision the paragraph records (not normalising the citations), and the release's stated subject is that a number in a document is a claim about this tree.  
  *Check:* Run the file's own `SHORTHAND`/`CITATION`/`SKIP` constants over `RepoFiles.list(root)` counting matches where `raw.match(SHORTHAND)`; compare HEAD against worktrees at `26243e0` and `6bc31b9`. Note the number carries no `<!-- measured: -->` marker, so nothing re-derives it.

- **[low]** preflight.rb attributes the "45 examples, 41 pending" skip measurement to the wrong suite, and the figure is stale on both sides  
  `scripts/preflight.rb` — Line 53–56: "a skipped example is still an example, so a fully skipped `real_rails_spec.rb` reports '45 examples, 0 failures, 41 pending' and satisfies any count-based rule." `spec/integration/real_rails_spec.rb` has 16 examples, and register entry 024.148 — the entry this comment cites — itself says "all 16 `real_rails` examples marked pending". The 45/41 figure belongs to the e2e capability suite, where `scripts/check_suites_ran.rb` line 19 correctly attributes it ("the e2e suite gives..."); but that suite now has 57 examples, so the figure is stale there too. The two new headers in this change set state the same measurement about two different files, and neither file has 45 examples. The three environment-dependent suites together are 80, which is the number the release's own run recorded.  
  *Check:* From `core/`: `bundle exec rspec --dry-run spec/integration/real_rails_spec.rb` → 16 examples; `... spec/e2e/capabilities_spec.rb` → 57; all three together → 80. Then `sed -n '50,58p' scripts/preflight.rb` and `sed -n '17,24p' scripts/check_suites_ran.rb`, and `sed -n '7915,7970p' docs/design/tasks/024-deferred-review-findings.md` for the register's own "16".

- **[low]** "flags 37 lines in this repository" — the case-insensitivity measurement is 35 today and was 36 when written  
  `scripts/check_home_paths.rb` — Lines 31–45 record a deliberate decision (keep PATTERN case-sensitive) and justify it with a measurement: adding `/i` "flags 37 lines in this repository ... A check that cries wolf 37 times is a check people switch off." Recomputed with the file's own PATTERN plus `/i` and its own SYNTHETIC list, over the same file set and the same binary/invalid-encoding skips: 35 lines at HEAD, 36 at `54104b4` (the commit that wrote the sentence). Never 37. The decision is still the right one — 33 of the hits are one Rails fixture view path — but the sentence is a claim about this tree that nobody re-ran, and this release deleted two of the lines it counted without the number moving.  
  *Check:* Load `scripts/check_home_paths.rb`, scan `HomePaths.tracked_files` with `%r{[/\\](?:Users|home)[/\\](?=[A-Za-z0-9._-]*[A-Za-z0-9])([A-Za-z0-9._-]+)}i`, reject `HomePaths::SYNTHETIC`, count lines with at least one hit → 35. Repeat in a worktree at `54104b4` → 36.

- **[low]** preflight.rb, CONTRIBUTING.md and the CHECKS list disagree about how many places the checks live in  
  `scripts/preflight.rb` — Header line 17: "the checks are in six places (the suite, three scripts, a git state, a derived number)". `CONTRIBUTING.md:147`: "The checks live in seven places". The `CHECKS` constant has eight entries naming five distinct scripts, and no entry corresponds to "a git state" — nothing in preflight checks whether the tree is clean or staged. This is a file whose entire subject is a list nobody was holding together, and the three statements of that list's size were not held together. Raised in 046 round 2 as [low] and not fixed; re-derived here independently.  
  *Check:* `ruby scripts/preflight.rb --list` prints the eight checks and their five scripts; compare with `sed -n '13,20p' scripts/preflight.rb` and `sed -n '145,150p' CONTRIBUTING.md`.


#### `numbers` — 15

- **[high]** RC-2's "156 citation lines in core/lib point into the package" is 37  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — RC-2 states "`docs/design/docs/04`–`11` are pre-implementation briefs from Task 001, and 156 citation lines in `core/lib` point into the package." Re-derived at 6bc31b9, the number of core/lib lines citing any file under docs/design/docs/ (including the docs/NN-*.md shorthand) is 37, in 6 files, naming 6 of the 12 documents (02:11, 03:12, 04:8, 05:4, 10:1, 11:1). The 156 looks like a count of every core/lib line mentioning `docs/` at all — that is 158 — of which 115 cite `docs/design/tasks/`, i.e. task records, not the design package. RC-2 is the stated justification for rewriting 05/06/07 and for adding three DOCUMENTATION_MAP rows, and it overstates the exposure by more than four times. This is a number about the tree with no `<!-- measured: -->` marker.  
  *Check:* git worktree add --detach /tmp/w 6bc31b9; cd /tmp/w; ruby -e 'C=%r{docs/(?:design/)?(?:tasks/|adrs/|docs/|schemas/)?[A-Za-z0-9._-]+\.md}; n=0; Dir.glob("core/lib/**/*.rb").each{|f| File.read(f,encoding:"UTF-8").each_line{|l| n+=1 if l.scan(C).any?{|p| p.start_with?("docs/design/docs/")||p.match?(%r{\Adocs/\d{2}-})}}}; puts n' # => 37; compare `grep -rn "docs/" core/lib | wc -l` => 158 and `grep -r

- **[high]** The 4,006-line deletion ledger counts 281 lines that were rewritten in place, not deleted  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — The "What is deleted" table's Lines column sums to exactly 4,006 and the sentence beneath reads "**≈4,006 lines and 5 fixture files deleted**". Three of its rows are section sizes that were replaced, not removed. `05-protocol.md §6, §8 | 16`: both sections still exist; the diff is +25/-8 and the file grew 227→244. `07-vscode-extension.md §3,5,6,7,10,11,12 | ~110 of 191`: all seven sections still exist; the diff is +79/-68 and the file grew 191→202. `06-plugin-system.md §§2–8 | 155`: §2 (30 lines, main lines 9–38) is byte-identical and was kept — only §§3–8 went, and the diff is +21/-124. The `008.5`/`008.6` row's 11 is likewise section size against 6 lines actually removed. Actual deletion attributable to the ledger's rows is 3,921 lines plus 54 fixture lines = 3,975, and the change set as a whole is +5,707/-4,588, i.e. net **+1,119 lines** — so the closing line "this change set is 4,000  
  *Check:* git diff --numstat main..HEAD -- docs/design/docs/05-protocol.md docs/design/docs/06-plugin-system.md docs/design/docs/07-vscode-extension.md docs/design/tasks/008.5-runtime-and-index-corrections.md docs/design/tasks/008.6-agent-and-index-hardening.md; git diff --shortstat main..HEAD; diff <(git show main:docs/design/docs/06-plugin-system.md | sed -n '9,38p') <(sed -n '9,38p' docs/design/docs/06-p <!-- deleted -->

- **[high]** Round 1's findings were never written down, in the file whose own rule says they must be  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — 046 states, immediately above the round sections: "**The finding list goes here as each round reports it.** `024.109` is what the other arrangement cost — findings that lived only in a session and were lost when it ended." The register legend adds "raising one without recording it is not allowed." Round 1's section is four sentences ending "*Results below as they land.*" and nothing landed; round 2's 70 findings are listed in full, round 1's are not. The only record of round 1 is commit 8f1d4f4's subject line — "84 raised, 30 deduplicated, 18 survived verification, 18 fixed" — which states counts and no content, so neither the 18 confirmed findings nor the 36 that were neither duplicates nor confirmed can be re-derived, re-checked, or re-opened. Five references to "round 1 measured X" elsewhere in 046 point at a list that does not exist. Round 2's dispositions have the same gap in smalle  
  *Check:* sed -n '/^### Round 1/,/^### Round 2/p' docs/design/tasks/046-0.2.14-making-the-record-true.md # the whole section; then `git log --oneline main..HEAD | grep 'round 1'` for the counts that exist only there

- **[medium]** "4 section-0 headings against the source's 8" counts release milestones, not section 0  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — RC-1 justifies deleting `<the deleted concatenated design document>` partly because it "**omits part of section 0**, the trusted root every rule in this repository is subordinate to (4 section-0 headings against the source's 8)". The source has 8 (`## 0.` plus `### 0.1`–`0.7`) — that half checks out. The file's four are `### 0.1-alpha`, `### 0.2-alpha`, `### 0.3-alpha`, `### 0.5-beta`, at lines 2109–2121, which are release-milestone headings in a roadmap list and have nothing to do with section 0. The file's `# 01. Product Requirements` block starts at `## 1. 背景`: it contains none of section 0 — no 開発理由, no 「完成」の定義, no 精度と出荷のあいだの原則, no この 0 節の位置づけ. The measurement is a regex artefact, and it understates the real case ("omits part of" where "omits all of" is true), in the root-cause section of a release whose subject is statements that stopped being true.  
  *Check:* git show 6bc31b9:`<the deleted concatenated design document>` | grep -nE '^#+ *0\.' # => 0.1-alpha / 0.2-alpha / 0.3-alpha / 0.5-beta; git show 6bc31b9:`<the deleted concatenated design document>` | grep -c '開発理由\|「完成」の定義\|精度と出荷のあいだの原則' # => 0

- **[medium]** "≈2,300 lines of the design package rewritten to match the code" is unsupported by any measurement  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — The sentence closing the deletion ledger reads "**≈4,006 lines and 5 fixture files deleted**, against ≈2,300 lines of the design package rewritten to match the code." The change set touches nine files under docs/design/docs/ for +155/-200; four of those nine files (03, 04, 08–11 minus the two rewritten) got exactly 5 added lines each — a one-sentence "this is a pre-implementation brief" header. Only 05, 06 and 07 were materially rewritten, and they total 539 lines at HEAD (244+93+202). The whole package is 2,794 lines. 2,300 is close to the size of docs 02–11 (2,323), but 02 was not touched at all in this change set. Nothing in the tree or the diff yields ~2,300 lines rewritten, and the figure is the half of a work-volume comparison a reader would use to judge whether the pass was proportionate.  
  *Check:* git diff --numstat main..HEAD -- 'docs/design/docs/' # => +155 / -200 across 9 files; wc -l docs/design/docs/*.md # => 2794 total, 539 for 05+06+07

- **[medium]** The docs/NN-*.md shorthand count of "91 across 39 files" double-counts fully-qualified paths  
  `scripts/check_doc_links.rb` — The SHORTHAND comment says the form is "used 91 times across 39 files since the design documents moved" and argues from it: "rewriting 91 lines would cost more than it buys". 046's A0 repeats the figure. The number reproduces only from a raw grep of `docs/[0-9]{2}-[a-z0-9-]+\.md`, which also matches the tail of the *fully-qualified* `<the two-digit design-doc shorthand>` — the form that is not a shorthand and needs no rewriting. Counted the way the script's own SHORTHAND regex counts (anchored `\Adocs/(\d{2}-...)\z` against a CITATION match), it is 59 occurrences across 24 files at 6bc31b9 and 45 across 22 files at HEAD. So the cost side of the argument is roughly doubled, and the number is stated in the header of the file that decides whether the shorthand is normalised.  
  *Check:* echo 'see docs/design/docs/03-semantic-engine.md' | grep -oE 'docs/[0-9]{2}-[a-z0-9-]+\.md' # matches, though it is not a shorthand; then count with the script's own SHORTHAND applied to CITATION matches over RepoFiles.list minus SKIP => 45 across 22 files at HEAD

- **[medium]** "No box has ever been ticked in this repository's history" — 56 are ticked, 13 of them inside the claimed range  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — The "What is kept" section keeps all 138 unticked acceptance boxes on the grounds that "No box has ever been ticked in this repository's history; they are stage milestones, not tracked obligations." The 138 re-derives exactly. The justification does not: `docs/design/tasks/008.5-runtime-and-index-corrections.md` lines 90–102 carry 13 ticked boxes under `## 完了基準`, and 008.5 is inside the stated 001–022 range — it is also one of the two files this very change set edited (its `## 残課題` section was removed). Another 43 ticked boxes sit across eight 023.* files, 56 in total. So ticking is an established practice in this repository, and the argument for keeping 138 empty boxes rests on a fact that is false.  
  *Check:* grep -rc -- '- \[x\]' docs/ | grep -v ':0' # 9 files, 56 total, 13 in 008.5; grep -h -- '- \[ \]' docs/design/tasks/00*.md docs/design/tasks/01*.md docs/design/tasks/02[0-2]*.md | wc -l # => 138

- **[medium]** A0's census "527 tracked files, 661 documentation citations" matches no revision, present or past  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — A0 states in the present tense that "`scripts/check_doc_links.rb` inspects **527 tracked files** and finds **661 documentation citations**", and the header says every count in the file was re-derived at 6bc31b9. Running the A0-era checker (as committed in 26243e0) against the 6bc31b9 tree gives 526 files / 655 citations; running it at its own commit 26243e0 gives 529 / 663; running the current checker at HEAD gives 535 files / 1509 citations, 107 of them relative. So the figure is not the 6bc31b9 number the header promises, is not the number at the commit that introduced the checker, and is off by a factor of 2.3 on citations against HEAD after C1 was widened to cover relative links and root-level documents. The C1 table row itself records that widening ("round 1 measured 105 links outside the check") without updating the census two pages above it. Round 2 raised this as a low `doc-links  
  *Check:* ruby scripts/check_doc_links.rb | head -1 # => 535 file(s), 1509 citation(s), 107 relative; then run the checker from 26243e0 inside a detached worktree at 6bc31b9 => 526 / 655, and at 26243e0 => 529 / 663

- **[medium]** "61 of its 74 resolved entries are cited from code" recomputes to 59  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — The evidence bullet that refutes the maintainer's volume hypothesis says "61 of its 74 resolved entries are cited from `core/lib`, `core/spec`, `vscode/src` or `scripts/`. Exactly **one** is cited from nowhere outside it." At 6bc31b9 the register holds 74 resolved entries (correct), 73 of them are cited somewhere outside the register, exactly one (`024.54`) is cited nowhere outside it (correct) — and 59, not 61, are cited from those four roots. The 15 that are not: 024.36, 024.48, 024.50, 024.52, 024.53, 024.54, 024.58, 024.59, 024.66, 024.92–024.96, 024.123. Widening the roots to any tracked path does not produce 61 either; it produces 73.  
  *Check:* git worktree add --detach /tmp/w 6bc31b9; cd /tmp/w; ruby -r./scripts/deferred_findings -e 'md=File.read("docs/design/tasks/024-deferred-review-findings.md",encoding:"UTF-8"); r=DeferredFindings.resolved(md).keys; roots=%w[core/lib core/spec vscode/src scripts]; hit=r.select{|n| `git ls-files`.split("\n").any?{|f| roots.any?{|x| f.start_with?(x+"/")} && File.file?(f) && File.read(f,encoding:"UTF-8

- **[low]** "Four source comments lean on §7" — one does  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — The deletion ledger's row for `05-protocol.md` gives as its reason for keeping the file and its section numbering: "The file stays — four source comments lean on §7." core/lib holds exactly four citations of 05-protocol.md, and only one of them names a section: `core/lib/ovallsp/agent_process_manager.rb:16` ("docs/05-protocol.md section 7"). The other three — `runtime_agent/agent.rb` lines 9, 107 and 165 — cite the file generally and then two of them name `agent/snapshot` and `agent/model`, which are §4 subsections. The shipped 05-protocol.md now repeats the reasoning in its own §6 ("番号を詰めると `section 7` を指すソースコメントが壊れる"), so a single call site is carrying an argument stated as four.  
  *Check:* grep -rn '05-protocol' core/lib # 4 hits, one of which says 'section 7'

- **[low]** "All 19 dangling citations live in source comments" — one is in a Markdown document  
  `scripts/check_doc_links.rb` — The checker's header states "**Source comments are in scope, deliberately.** All 19 dangling citations live in them. A checker that read only Markdown would have reported this tree clean." 046's A0 repeats it as "every one in a source comment." 18 of the 19 are in .rb comments; the nineteenth is `docs/design/plugin-sdk.md:5`, a public Markdown document — which 046's own RC-3 names in the same breath ("The whole `plugins/` subsystem and the public SDK document point at one of them"). The design conclusion is unaffected — 18 of 19 still require source-comment scanning — but the sentence as written is false and contradicts the paragraph three pages earlier. <!-- deleted -->
  *Check:* git worktree add --detach /tmp/w 6bc31b9; cd /tmp/w; ruby scripts/check_doc_links.rb (or the HEAD checker with CHECK_DOC_LINKS_ROOT=/tmp/w) — the 19th hit is docs/design/plugin-sdk.md:5 <!-- deleted -->

- **[low]** Six numbers about the tree carry no stated basis and cannot be re-derived  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — None of these carries a `<!-- measured: -->` marker or a recorded method, and none reproduces: (a) "The 1,753 lines an agent must read at session start" — CLAUDE.md 509 + AGENTS.md 141 + 01-product-requirements 325 + README 285 + DOCUMENTATION_MAP 146 = 1,406, and no combination of the plausible session-start set lands on 1,753; (b) "only **7 sentences** repeat across files" in core/lib — a sentence-level scan of core/lib comment text finds 0 repeats at any length threshold I tried, so the splitting rule matters and is unrecorded; (c) "`CLAUDE.md`'s incident narratives (47% of the file)" — no definition of what counts as narrative; (d) "**249 edits across 41 files**" for the 0.3.0 renumber — `0.3.0` occurs 196 times on 158 lines in 32 files at 6bc31b9; (e) "Nine auditors … produced 122 findings … **15 survived adversarial check, 9 were refuted, 28 low-severity carried forward**" — 52 of   
  *Check:* grep -rn 'measured:' docs/design/tasks/046-0.2.14-making-the-record-true.md # no markers in the file; wc -l CLAUDE.md AGENTS.md README.md docs/design/docs/01-product-requirements.md docs/DOCUMENTATION_MAP.md; git worktree add --detach /tmp/w 6bc31b9 && cd /tmp/w && grep -rohF '0.3.0' --exclude-dir=vendor --exclude-dir=node_modules . | wc -l

- **[low]** C4's table row states an action that was not taken, and the "changed shape" list omits it  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — The checks table says C4 is "extract `DeferredFindings` into `scripts/`; delete `metadata_of` **and its false comment**". The extraction happened and the false comment went, but `metadata_of` is still present at `scripts/reindex_findings.rb:54`, still called at line 66, and `core/spec/meta/deferred_findings_spec.rb:111-112` asserts its behaviour — it was rewritten as a one-line delegation to `DeferredFindings.entries`, which is a better outcome than deletion but is not what the row says. The section "What they turned out to be" opens "Four changed shape once built, and the changes are the part worth reading" and lists C2, C6, C7, C8; C4 also changed shape and is not there. A reader auditing the checks against their table will look for a deleted method and find a live one.  
  *Check:* grep -rn 'metadata_of' scripts/ core/spec/ | grep -v vendor # 4 hits, definition at scripts/reindex_findings.rb:54

- **[low]** The recorded departure from a `drive` round enumerates core/lib edits incorrectly  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — The departure from `041`'s request for a `drive` round is justified with "the only `core/lib` edits in the range are two comment rewrites and one deleted tombstone comment, and the corpus is not consulted by anything that changed". `git diff --numstat main..HEAD -- core/lib` lists 16 files: engine.rb (-11, the tombstone), receiver_resolution.rb (+22/-20, the comment rewrite), query_service.rb (+3/-3), and thirteen further files each +1/-1 — the A0 citation repointings, which the same document counts as 19 fixes across 17 files elsewhere. The conclusion survives — reverse-reading the diff, exactly one changed non-comment line exists in core/lib and it is a removed blank line, so no engine answer moves — but the enumeration a reviewer would use to check the departure is wrong, and it is the sentence that decides a mandated review method does not apply.  
  *Check:* git diff --numstat main..HEAD -- core/lib # 16 files; git diff main..HEAD -- core/lib | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[+-]\s*#' | wc -l # => 1, a blank line

- **[low]** 024.25's Area names a spec file no commit has ever carried, and C3 is scoped so it cannot see it  
  `docs/design/tasks/024-deferred-review-findings.md` — 046's RC-3 is "pointers with no resolver", and C3 answers it for `**Area:**` lines — but only for open entries: "every path in an **open** entry's `**Area:**` must exist". Running the same check over all 150 entries finds one survivor: `024.25`'s Area names `core/spec/meta/known_limitations_parity_spec.rb`, and `git log --all -- <path>` returns nothing, so no commit in this history has carried it. It is the identical class the release opened with — 19 citations to five names no commit ever carried — surviving inside the register the release spent most of its lines making true, because the countermeasure's scope excludes resolved entries and `check_doc_links.rb`'s CITATION pattern matches only `.md`. Two open entries (024.102, 024.150) also carry no Area line at all, which round 2 already raised.  
  *Check:* ruby -r./scripts/deferred_findings -e 'md=File.read("docs/design/tasks/024-deferred-review-findings.md",encoding:"UTF-8"); DeferredFindings.area_paths(md).each{|n,ps| ps.each{|p| puts "#{n}: #{p}" unless File.exist?(p)}}' # => 024.25; git log --all --oneline -- core/spec/meta/known_limitations_parity_spec.rb # => empty


#### `round-records` — 11

- **[high]** Round 1's finding list was never written into 046 — the section is still an unfilled placeholder  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — Lines 455-459 are the whole of `### Round 1 — diff`: five readers described, then "*Results below as they land.*" and nothing else. The round is finished (commit 8f1d4f4, "84 raised, 30 deduplicated, 18 survived verification, 18 fixed") and a later round has since been recorded in full below it, so the sentence is stale in the release whose title is making the record true. Two paragraphs above it the file states the standing instruction — "The finding list goes here as each round reports it", citing 024.109 as what the other arrangement cost — and round 2's section repeats that "raising a finding without recording it is not allowed". 041, the task this release takes its cadence from, says the same: "The round's finding list goes in this file as the round reports it." All 18 surviving round-1 findings exist only in a commit message; 12 refuted ones exist nowhere. Concretely unrecoverable:  
  *Check:* sed -n '429,460p' docs/design/tasks/046-0.2.14-making-the-record-true.md — no findings under Round 1. git log -1 --format=%B 8f1d4f4 — the 18 are only here. grep -n '^## 024.150' -A6 docs/design/tasks/024-deferred-review-findings.md shows status: open, so "18 fixed" counts an entry that was opened, not fixed.

- **[high]** All 70 round-2 findings are recorded truncated, every one cut mid-sentence  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — The round-2 list (lines 479-715) is introduced as "here in full because the standing instruction for this pass is that raising a finding without recording it is not allowed", and 024.151 says "The individual breaks are recorded in 046". They are not: every one of the 70 body lines is between 424 and 480 characters and 70 of 70 end without terminal punctuation, cut mid-word or mid-token. Examples of the recorded text's final characters: "RELEASE_CH", "and set equality against the manifes", "returns `[]` for an entry with no Ar", "`/^## (024\.[0-9R]+" (unclosed regex), "An entry with no block is a failure," (unclosed quote), "§5 is the secti". The cut lands on the consequence clause — the part that says what the break costs — so for the 55 findings deferred to 0.3.0 under 024.151 the recorded text is the only thing a future session has, and it stops before the point.  
  *Check:* awk 'NR>=461' docs/design/tasks/046-0.2.14-making-the-record-true.md | grep '^ `' | awk '{print length($0)}' | sort -n | uniq -c → all 424-480; then | grep -cvE '[.!?)"]$' → 70 of 70. awk 'NR>=461' … | grep '^ `' | awk '{print substr($0,length($0)-40)}' shows the cuts.

- **[high]** 046 records no disposition per finding, so the register's "55 open" cannot be reconciled with 046's 70  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — Commit 54b7274 says "55 of the 70 remain open and recorded" and 024.151's body says "55 confirmed findings" open. 046 lists all 70 with a severity tag and a body and nothing else — no fixed/open marker, no strike-through, no back-reference — and 024.151 does not enumerate either. So the arithmetic 70 − 15 = 55 is unverifiable from the record, and a reader picking up 024.151 in 0.3.0 has 70 items of which 15 are already done and no way to tell which. I reproduced findings on both sides: still live — the relative-link pass still skips any raw link beginning `docs/` (scripts/check_doc_links.rb:239) and resolves it from the wrong base; §5's status-bar comparison still has no non-empty floor (core/spec/meta/design_doc_drift_spec.rb:103-108 asserts floors for §6 and the manifest only); already fixed — the settings regex is now `[A-Za-z0-9._-]` and the Area check now fires on `!= 1` with 024.30  
  *Check:* awk 'NR>=461' docs/design/tasks/046-0.2.14-making-the-record-true.md | grep -c '^- \*\*\[' → 70; grep -in 'fixed\|~~' on the same range → no disposition markers. grep -n '55 confirmed findings' docs/design/tasks/024-deferred-review-findings.md.

- **[medium]** The recorded departure from the `drive` round rests on a false enumeration of the change set  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — Lines 436-441 justify skipping the `drive` round 041 requires: "the only `core/lib` edits in the range are two comment rewrites and one deleted tombstone comment, and the corpus is not consulted by anything that changed". Both halves are wrong as stated. `git diff main..HEAD -- core/lib` touches 16 files: one comment rewrite (receiver_resolution.rb), one deleted comment (engine.rb:1107-1117), and 16 repointed citations across the other 14 files. And the corpus consumer itself changed: scripts/corpus_diagnostics.rb gained 129 lines (provenance to stderr, an empty-corpus refusal, a missing-path refusal, `--expect-control`) plus a new 119-line spec. The paragraph landed at 23196a8, after the citation repoints landed at 26243e0, and it says of itself that it "must be re-checked each round" — it was not re-checked at either round. The conclusion survives (every core/lib line changed is inside  
  *Check:* git diff main..HEAD --stat -- core/lib → 16 files changed. git diff main..HEAD -- core/lib | grep -c '^+.*docs/design/tasks/0' → 16 citation repoints. git diff main..HEAD --stat -- scripts/corpus_diagnostics.rb core/spec/meta/corpus_diagnostics_spec.rb → 129 and 119 lines.

- **[medium]** RC-2's headline number counts a different question: 37 core/lib citations point into the design package, not 156  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — Line 60: "`docs/design/docs/04`–`11` are pre-implementation briefs from Task 001, and 156 citation lines in `core/lib` point into the package." At 6bc31b9, the revision the file's header says every count was re-derived at, core/lib carries 157 lines citing any `docs/**.md` path; only 37 of them point into `docs/design/docs/` (all via the `docs/NN-*.md` shorthand — the full-path form appears zero times), while 115 point into `docs/design/tasks/`. So 156/157 is the count of all documentation citations in core/lib, attributed here to the briefs package, inflating RC-2's evidence roughly fourfold. RC-2 is the root cause that justified rewriting ~2,300 lines of the design package, so this is the number a reader would check the diagnosis against. Note that the neighbouring counts do hold when re-derived (the A0 census's 19 citations / 5 names / 17 files, the shorthand's 91 occurrences across 3  
  *Check:* git grep -o -E 'docs/design/docs/[0-9]{2}-[a-z0-9-]+\.md|docs/[0-9]{2}-[a-z0-9-]+\.md' 6bc31b9 -- core/lib | wc -l → 37. git grep -o -E 'docs/[A-Za-z0-9._/-]+\.md' 6bc31b9 -- core/lib | wc -l → 157. git grep -o -E 'docs/design/tasks/[A-Za-z0-9._-]+\.md' 6bc31b9 -- core/lib | wc -l → 115.

- **[medium]** The audit's opening arithmetic leaves 70 of its 122 findings unaccounted for  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — Lines 10-13: "Nine auditors over disjoint areas produced 122 findings; each non-trivial one was then given to a second agent whose instructions were to *refute* it. **15 survived adversarial check, 9 were refuted, 28 low-severity carried forward.**" 15 + 9 + 28 = 52. The remaining 70 of the 122 have no stated disposition anywhere in the document — they are not the 15, not the 9, not the 28, and the sentence's own structure ("each non-trivial one" was checked) says they were not simply skipped as trivial, since the trivial ones are the 28 carried forward. This is the paragraph a reader uses to size the audit, in a release whose thesis is that a number nobody recomputes is a belief.  
  *Check:* sed -n '8,14p' docs/design/tasks/046-0.2.14-making-the-record-true.md and add the three figures.

- **[medium]** Two consecutive rounds found the same place and CLAUDE.md's same-place rule was neither invoked nor satisfied  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — CLAUDE.md's "Two rounds in a row on the same place" section requires tracking, per round, which code each finding is about, and — the first time a place is found twice in a row — a mechanical countermeasure rather than a third hand-fix. 046 records no per-place tracking, and the rule is never mentioned in the review-rounds section, yet at least one place qualifies plainly. Round 1 fixed release_gate_spec's wiring check ("C6 called a gate wired because the cited script's own usage string names it… The haystack is built per candidate now, excluding the candidate's own file"); round 2 then filed three findings against the same decision in the same method, one of them explicitly self-referential: "release_gate_spec's C6 fix closed the self-naming hole and left the caller-naming one". check_doc_links.rb is the same shape — round 1 (105 relative links outside the check, the rescue that reporte  
  *Check:* In a clone at HEAD: sed -i '' '206d' vscode/scripts/release.sh; cd core && bundle exec rspec spec/meta/release_gate_spec.rb → 3 examples, 0 failures; add spec/meta/release_script_guard_spec.rb → 1 failure. Then grep -in 'same place\|same-place' docs/design/tasks/046-0.2.14-making-the-record-true.md → no hit in the review-rounds section.

- **[medium]** 046's A0 census is stale by 2.3x, under a header stating every count was re-derived  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — Line 3-4: "Measured at `6bc31b9`. Every count below was re-derived at that revision, and where one differs from an auditor's, the one here is the one that was run." Line 155: "`scripts/check_doc_links.rb` inspects **527 tracked files** and finds **661 documentation citations**." Running the script at HEAD prints 535 files and 1509 citations — the citation figure moved by more than a factor of two when round 2 widened CITATION (the commit itself records "Citations 794 -> 1,503"), and neither number was updated, neither carries a `<!-- measured: -->` marker, and nothing re-derives them. Round 2 raised exactly this as a [low] finding (against then-current figures of 536/786), and it remains open and unmarked, which means the file's own header claim is the newly false statement: these two counts were not re-derived at 6bc31b9 or anywhere else.  
  *Check:* ruby scripts/check_doc_links.rb → "535 file(s) inspected, 1509 documentation citation(s)"; compare with sed -n '155,156p' of the task file.

- **[medium]** The standard the rounds were judged against changed twice inside the loop, and "fix; do not add" was applied asymmetrically  
  `CLAUDE.md` — 046 says each round was given "`CLAUDE.md` and `AGENTS.md` … as the standard to hold it to". CLAUDE.md was itself edited inside the loop: round 1's commit (8f1d4f4) and round 2's commit (54b7274) between them added two new mandatory sections ("Writing a check means writing bait for the other checks", "Two working-practice traps") and softened the trusted-root paragraph, so rounds 1, 2 and 3 were each held to a different standard. After round 2, e100388 added a new capability to the checking machinery (042's D7 mutation manifest extended to `scripts/`, +18 lines in check_pinned_mutations.rb, 7 new manifest entries) and refactored scripts/documented_counts.rb to extract a pure function. CLAUDE.md's cadence rule says "During a review loop, fix; do not add… every addition between rounds resets it" — and 046 line 286-290 invokes that exact rule to decline an AGENTS.md restructure. Whichever r  
  *Check:* git log --oneline main..HEAD -- CLAUDE.md → 7c92b05, 8f1d4f4, 54b7274 (two of them are round commits). git show --stat e100388 -- scripts/check_pinned_mutations.rb core/spec/meta/pinned_mutations.yml scripts/documented_counts.rb. sed -n '284,292p' docs/design/tasks/046-0.2.14-making-the-record-true.md for the contrasting use of the rule.

- **[low]** The deletion ledger's line count for make-final-review-bundle.sh is short by one, and the total sums it  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — Line 229 records `make-final-review-bundle.sh` at **918** lines. The file has 919 lines; `wc -l` says 918 only because the last line carries no trailing newline, and `git diff --stat main..HEAD` reports 919 deletions for it. The ledger's stated total ("≈4,006 lines … deleted", line 241) is the exact sum of the rows — 2680+918+63+42+155+110+16+11+11 = 4006 — so it inherits the off-by-one and should read 4,007. Small, but this total was itself the subject of a round-1 correction (4,040 → 4,006) and is presented as measured; the other four line counts I re-derived at 6bc31b9 (2,680, 63, 42, 11) are exact.  
  *Check:* git show main:make-final-review-bundle.sh | grep -c '' → 919, while wc -l → 918; git diff main..HEAD --stat -- make-final-review-bundle.sh → 919 deletions.

- **[low]** C8's row says 026 records three false corpus results; the script it describes, written in the same change set, says five  
  `docs/design/tasks/046-0.2.14-making-the-record-true.md` — Line 306, the C8 row's "Fails on" column, reads "`026`'s three recorded false results". The header comment this release added to scripts/corpus_diagnostics.rb (lines 79-87) says "`026-0.2.1-review-loop.md` lists five" and enumerates them, and CLAUDE.md's "A measurement is a claim" section says three plus "0.2.1's last day added two more to the list". Two statements of the same count, in one change set, disagreeing — the class the release exists to remove, and in the row that defines what the new check is for.  
  *Check:* sed -n '306p' docs/design/tasks/046-0.2.14-making-the-record-true.md against sed -n '79,88p' scripts/corpus_diagnostics.rb and the "A measurement is a claim" section of CLAUDE.md.



#### Incidental findings from the `024.90` split verification — 37

Eight agents drove one entry each and were told to report anything else
they noticed. This is that, unfiltered and unverified — recorded because
the standing rule is that raising without recording is not allowed, and
triaging them is 0.3.0's work.

- The empty-string shape is PINNED by an existing example, so fixing 024.127 is not a one-line change to `empty_hover`. core/spec/ovallsp/server_spec.rb:48 asserts `expect(messages[1][:result][:contents][:value]).to eq("")` for a hover on a document that was never opened, with a comment justifying it as "an empty, non-committal result rather than a guess (Task 013)". Whoever takes 024.127 must change that expectation to `result: nil`; the entry does not mention it. It is also the only place in `core/spec` that pins the empty hover — grep for `empty_hover` finds nothing outside `server.rb`, and t

- A hover positioned past the end of a line answers as if the cursor were on the last token of that line, which is an assertion made where there is nothing. Same fixture, same live server, probing character positions beyond the line length: line 5 (len 19) char 19 -> {contents: {kind: "plaintext", value: "String"}} line 5 (len 19) char 20 -> {contents: {kind: "plaintext", value: "String"}} line 5 (len 19) char 25 -> {contents: {kind: "plaintext", value: "String"}} line 5 (len 19) char 300 -> {contents: {kind: "plaintext", value: "String"}} line 7 (len 16) char 100 -> {contents: {kind: "plaintext

- The entry's justification is a claim about client behaviour with no citation: "a client is entitled to treat an empty-string hover as a hover that exists." CLAUDE.md requires claims about the client, the editor, or the LSP specification to go through docs/CLIENT_BEHAVIOUR.md, and that file has no hover entry at all (`grep -i hover docs/CLIENT_BEHAVIOUR.md` is empty). The wire-level fact is verified above and the spec-conformance half is checkable from the specification, but whether VS Code actually renders an empty popup — the part that makes this `user-visible: yes` and puts it in docs/KNOWN_

- Unrelated, in the harness rather than the product: `wait_until_ready` in core/spec/e2e/lsp_client.rb:64-74 accepts only `%w[ready ready-rails]`. Neither string is a state the server can produce for a non-Rails or untrusted workspace — server.rb:1014-1027 emits `indexing`, `ready-rails`, `agent-unavailable`, or `ready-static`, and there is no plain `ready`. For a static workspace the helper therefore polls for the whole 120-second timeout and then returns `"ready-static"` as if it had timed out, which is what my first run did. The capability suite only ever drives a real Rails app so it does no

- The Area line is imprecise. It names `core/lib/ovallsp/signatures/environment.rb` and `core/lib/ovallsp/local_inferencer.rb`, but the code that would have to change is `core/lib/ovallsp/signatures/overload_resolver.rb` — `OverloadResolver.matches?` is the selector that ignores argument types, and it is named in neither Area path. `environment.rb` only converts RBS method_types into Overload structs (line 208) and `local_inferencer.rb` only calls the resolver (line 1158). A fixer following the Area would open two files and not find the decision. Worth correcting on the entry.

- The completion half of the claim is narrower in impact than it reads. 209 members come from the union, but Integer alone answers 205 — the union adds only four: ["nan?", "next_float", "prev_float", "reminder"]. "209 members drawn from all four" is literally true and the count is right, but the practical inflation over the honest answer is +4, not +209. (`rectangular`, `polar`, `numerator`, `infinite?`, `to_r`, `gcd`, `bit_length` are all genuinely on Integer.) The hover half — an unusable label — is the real user harm here; the completion half is close to harmless.

- Two arithmetic operators are worse than `*` in a way the entry does not cover, and both leak an ABSTRACT supertype into the answer, which the entry's "the union contains the truth" defence does not cover as cleanly: `a = 2 ** 3` => `Complex | Numeric`, and `a = 10 % 3` => `Float | Integer | Numeric | Rational`. `Numeric` as a hover label for `2 ** 3` is not just imprecise, it is a name no value ever has. If 024.128 is the entry that covers integer arithmetic, these belong in its reproduction; if not, they are unrecorded.

- The defect is not confined to Integer receivers, which the entry's title ("Integer arithmetic") implies. Float arithmetic is hit too: `x = 1.5; y = 2; z = x * y` => `Complex | Float`. The honest control that it is overload-count and not arithmetic generally: `s = "a"; t = s * 3` => `Nominal "String"` (String#* has one overload, so the resolver answers precisely). So the trigger is "more than one overload of matching arity", not "arithmetic".

- docs/KNOWN_LIMITATIONS.md:645 and docs/KNOWN_LIMITATIONS.ja.md:591 both write the example as `price * quantity` while the register entry writes `price * qty`. Cosmetic — both reproduce identically — but the two records of the same defect do not quote the same reproduction.

- The register entry says "`024.13` is why". That is accurate for the array literal (`Generic` never enters `reportable_branches`) but not for `"hello".upcse` / `42.upcase`, which are stopped by `MethodResolver#availability` answering `reason=:ancestor_not_identified` because `hierarchy_index.ancestors("String")` is `["String"]` and never reaches `BasicObject`. 024.13's own prose still describes the pre-inversion `closed_nominal?` ("calls a receiver closed when every ancestor is workspace-declared or RBS-known"), which contradicts the comment now sitting on the method in core/lib/ovallsp/diagnos

- 024.129 says "nothing, in either mode" — `Engine::MODES` is `%i[safe standard strict]`, three modes. Silent in all three, so the finding is if anything stronger than written, but the phrasing is stale.

- UNRELATED, possibly new: dot-completion offers Kernel's private conversion methods, which cannot be called with an explicit receiver. Completion at `[1, 2].` returns these capitalised labels: ["Array", "Complex", "Float", "Hash", "Integer", "Pathname", "Rational", "String"]. Asked the interpreter: `ruby -e 'p [1,2].Array([3])'` => `NoMethodError: private method 'Array' called for an instance of Array`, and `Kernel.private_instance_methods(false).include?(:Array)` => `true` (ruby 3.4.10). Accepting one of these completions produces code that raises. Likely cause: RBS declares them with `def sel

- PROCESS, not about the entry: `git status --porcelain` is NOT clean in this working tree, and none of it is mine. The session-start snapshot recorded "(clean)"; at the end of my run it shows ` M CLAUDE.md`, ` M core/spec/meta/agents_pointer_spec.rb`, ` M core/spec/meta/release_script_guard_spec.rb`, ` M docs/design/tasks/046-0.2.14-making-the-record-true.md`, ` M scripts/check_doc_links.rb`. A concurrent session is mutating the tree while these verification agents run. I wrote only to the scratchpad and deleted those files, but any other agent in this batch that reads `git status` to check its

- The correct restatement of 024.131 is two entries, not one, and neither is the sentence currently published. (a) A defect: every operator-assignment write is invisible to LocalInferencer#eval_type, so the type from the last plain `=` survives it — wrong answers for `b = nil; b ||= "x"` (says nil, is String) and `c = "s"; c &&= 1` (says String, is Integer), right answers by luck for `+=`. (b) A separate, milder one: `@m ||= "x"` with no preceding assignment — the ordinary Ruby memoization idiom, far more common in real code than the entry's example — is Unknown and hovers nothing. The entry's t

- `docs/KNOWN_LIMITATIONS.md:652` and `docs/KNOWN_LIMITATIONS.ja.md:597` both publish the wrong sentence to end users, and they under-report: a user told "hovers nothing" will not go looking for why hover said `nil`. Both need rewriting when the entry is corrected, and the `<!-- documents: 024.131 -->` marker means the doc-link check will keep them in step only if the entry itself is fixed first.

- Nothing in `core/spec` pins any operator-write inference behaviour. `/usr/bin/grep -rln 'OrWrite|or_write' spec/` hits only `spec/meta/spec_constants_spec.rb`, and neither `local_inferencer_spec.rb` nor `spec/ovallsp/semantic/` contains a `||=` fixture. So the `nil` answer is unpinned in both directions: it is not asserted today, and when the fix lands there is no existing example that will notice the change. Worth a fixture that distinguishes the branches — `b = nil; b ||= "x"` must yield `String` while `b = 1; b ||= "x"` must yield `Integer`, which are different observable values, so one fix

- This is the second of the nine 024.90 split entries shown not to reproduce as written (024.130 was the first). Both are `user-visible: yes` and both are published in KNOWN_LIMITATIONS. Two out of nine, both in the same direction — text promoted to a register entry without being re-driven — is a rate worth acting on: the remaining seven should be treated as unverified until each has been driven, and the split itself is the countermeasure candidate (a promotion from bullet to entry ought to require a reproduction, the way any other entry does).

- 046 round 3 already recorded a `[medium]` finding on 024.131 (docs/design/tasks/046-0.2.14-making-the-record-true.md:1103) reaching the same conclusion by the same measurement. I drove it independently before reading past its first lines; the agreement is genuine corroboration, not a copy. My run adds the end-to-end server hover (that finding measured `type_at` only, so it inferred the popup contents from `hover_lines`; I confirmed the actual `contents.value` is the string "nil"), the absence of any resulting diagnostic, the `&&=` second wrong answer, the `@m ||=` memoization case, and the fac

- The working tree had five modified files (CLAUDE.md, core/spec/meta/agents_pointer_spec.rb, core/spec/meta/release_script_guard_spec.rb, docs/design/tasks/046-0.2.14-making-the-record-true.md, scripts/check_doc_links.rb) when this task started, and the same five when it finished — concurrent work from another session, none of it mine. Flagging it because CLAUDE.md warns against running a hunk-by-hunk pinning sweep while another agent is mutating the same tree; the same caution applies to anyone reading a `git status` from this branch right now.

- THE ENTRY AND THE PUBLISHED LIMITATION BOTH UNDERSTATE THIS BADLY. `docs/KNOWN_LIMITATIONS.md:653` lists it under **Smaller things** as "A scope defined inside a concern's `included do` has no type" (and `.ja.md:598` likewise). It is not a missing type — it is a **false `unknown-method` report on ordinary Rails code**: `Post has no method named `recent`` on a scope that works at runtime. Section 0's "a wrong answer is worse than no answer" makes this a different severity class from the four hover niceties it is bulleted next to. Both the register entry's body and the two KNOWN_LIMITATIONS bull

- The engine also **invents** a member in the other direction: `Trashable.recent` answers `Relation[Trashable]`, and `recent` is offered on the concern module in completion. At runtime `Trashable.recent` raises `NoMethodError` — `included do` never runs against the module (Ruby session pasted in the evidence). So one fact produces both halves of the failure at once: denied where it exists, asserted where it does not. That half is recorded nowhere I could find.

- The mechanism is the singleton/instance split, not concerns generally. `included do attr_accessor :x end` and `included do def m; end end` both work, because they land on the concern's *instance* side and `include` propagates that. Everything a concern's `included do` puts on the *class* side is affected the same way as `scope` — this entry's fix should cover `enum` (which records `#{value}?` instance predicates, so it happens to be fine) and any future class-side DSL, not just `scope`. Worth widening the entry's title/body from "a scope" to "a class-side macro".

- The register's `024.31` entry (lines ~1680-1790) already names the needed direction — "the visitor to carry a *receiver* for a block ... `included do` opens the includer" — and records that `Cref#in_eval_block(owner)` was built in 0.2.13 to do exactly that, closing the `Class.new`/`instance_eval` halves. `included do` was left on the plain `#in_block` path (`core/lib/ovallsp/parser_service.rb:749-753`, `eval_block_owner` at line 703 covers only `EVAL_BLOCK_CALLS = %i[instance_eval instance_exec class_eval class_exec module_eval module_exec]`). The infrastructure for the fix therefore exists; w

- `AnalysisStack#replace_file` (`core/lib/ovallsp/analysis_stack.rb:96`) updates `workspace_index` and `hierarchy_index` but **not** `generated_method_index`, while `Server` updates all three (`core/lib/ovallsp/server.rb:1447`). The comment above `#replace_file` says it is "Every per-file store that has to be told about a parsed file, so a caller cannot update one and forget another" — and it forgets one. Any spec calling `stack.replace_file` alone gets a stack that knows no `scope`/`enum`/`delegate` facts, i.e. a different program from the server's, which is precisely the `024.109`/`024.119` ca

- The `evidence` hash is as uninformative as the sentence: `{required: 0, maximum: 0, passed: 1}` is identical for `def kwargs(name:, size: 1, **rest)` and for `def z`. A fix that only rewords `expected_arity` would leave any downstream consumer (a code action, a client that formats its own message) equally unable to tell the two apart. Whatever the fix, the keyword counts should reach `evidence` too.

- The same wording defect covers three shapes the entry does not name: `def kw(**rest)` (keyrest only), `def b(&blk)` (block parameter only), and the plural form `kw("a", "b")` -> "takes 0 arguments, but 2 given". All are true positives with the same unreadable sentence. Any fix should be scoped to the whole class, not to the `name:, size: 1, **rest` example.

- Adjacent gap, not covered by 024.133 and I found no register entry for it: the engine says nothing at all about keyword arity errors that are not positional-count errors. `def kw(name:); end` called as bare `kw` -> no findings (Ruby: `missing keyword: :name`), and called as `kw(bogus: 1)` against a signature with no `**rest` -> no findings (Ruby: `missing keyword: :name`). So the one keyword mistake the check does report is the one it reports confusingly, and the two it could report unambiguously it does not report at all. Worth a register entry if one is wanted.

- 024.89 (signature help strips parameter kinds, so `key:` presents as the fourth positional) is the same root theme from another surface: the user-facing layers do not distinguish keyword from positional anywhere. If 024.133 is fixed by teaching the diagnostic about parameter kinds, 024.89 is the second reader of the same missing distinction and the two are probably one change.

- Working-tree hygiene: `git status --porcelain` at the start of my session reported clean, and at the end reports five modified files -- CLAUDE.md, core/spec/meta/agents_pointer_spec.rb, core/spec/meta/release_script_guard_spec.rb, docs/design/tasks/046-0.2.14-making-the-record-true.md, scripts/check_doc_links.rb. None of them are mine; I wrote only to the scratchpad outside the repo and touched no repo file. Flagging it because concurrent agents are mutating the tree, which the CLAUDE.md rule about not running a hunk sweep during concurrent mutation exists for -- and because it means my run an

- The entry is narrower than the defect: `ready-static` is not the only unaccepted terminal state. `agent-unavailable` is too, and that one is reachable from a *Rails* workspace — precisely the kind every e2e fixture is. Drove it with a Rails-looking tmpdir (`bin/rails` + `config/environment.rb` present, so `RailsBootstrap.rails_app?` is true, but the boot raises): `distinct states seen: ["ready-static", "agent-unavailable"]`, `last poll: ["0.55", "agent-unavailable"]`, `wait_until_ready(timeout: 5) -> "agent-unavailable" after 5.27s`. So the entry's reassurance that "no example hits it today be

- The accept list contains a value the server never emits. `%w[ready ready-rails]` — `"ready"` appears nowhere in `core/lib`, and the extension's own status map (`vscode/src/clientPresentation.ts:82-83`) knows only `ready-static` and `ready-rails`. Half the helper's accept list is dead. Whatever the fix is, `"ready"` should not survive it.

- Shape of the fix, if it helps: the helper is asking "is the server settled?" and answering it with a hardcoded subset of state names, which is the same seam this repo has been bitten by before — two places that must agree about one vocabulary, with nothing making them agree. The server already knows which of its four states are terminal (`indexing` is the only one that is not); a terminal/settled predicate owned next to `#status_result` and read by the helper would make the helper correct for all four, including states added later. As written, the helper also cannot distinguish "settled but no

- Working-tree note: `git status --porcelain` was already dirty when this session started — `CLAUDE.md`, `core/spec/meta/agents_pointer_spec.rb`, `core/spec/meta/release_script_guard_spec.rb`, `docs/design/tasks/046-0.2.14-making-the-record-true.md`, `scripts/check_doc_links.rb`. I touched none of them and the list is byte-identical at finish. Flagging it because several verifiers are running concurrently against this tree, and any of them reading `git status` as their own footprint would report a false one. My scratch specs were written under the session scratchpad and deleted.

- Working tree was NOT clean when I finished, and none of it is mine. `git status --porcelain` shows ` M CLAUDE.md`, ` M core/spec/meta/agents_pointer_spec.rb`, ` M core/spec/meta/release_script_guard_spec.rb`, ` M docs/design/tasks/046-0.2.14-making-the-record-true.md`, ` M scripts/check_doc_links.rb`. I only ever wrote to the scratchpad outside the repo (and deleted that file); these are from a concurrent session. Worth flagging because CLAUDE.md's measurement rules say the hunk-by-hunk pinning sweep must never run while another agent is mutating the same tree — that condition is currently tru

- `024.135`'s `status: open` / `target: 0.3.0` and its `Area:` line are all correct as written; no metadata correction needed.

- The rescue in `#read_results` is recorded as `surfaces` in `core/spec/meta/rescue_verdicts.yml:84`. That is defensible (it logs `@logger.error` to the Output channel) but note it also returns `nil`, and `nil` is a value a caller acts on — `Server#run_observed_tests_result` reports `{sampleCount: 0, methodCount: 0}` and deliberately leaves the store intact. So it is really `surfaces` *and* a contained fallback; the contained half is the one the round-7/round-8 comments in `runner.rb` spend forty lines arguing for. Not a defect, but the verdict column records only half of what that site does.

- If `024.135` is ever fixed, the sibling write site `core/lib/ovallsp/observation/harness.rb:75` (`Marshal.dump`) has to change in the same commit — it is in the file injected into the workspace's process via `RUBYOPT`, so a parent/child format skew there is silent (the parent just logs "unreadable results" and reports a zero run). The entry names only the `Runner` half in its `Area:` line.

**Review cadence.** Three rounds, fresh reviewer each, methods from
`{diff, reproduce, attack}`. **Recorded departure:** the `drive` round,
written out once under "Review rounds" above with the enumeration it
rests on — not restated here, because it was, and both copies were
wrong (`024.162`). Written down at all because 0.2.12 and 0.2.13 shipped
with no recorded rounds and no written departure, which is itself one of
this audit's findings.

**Two rules over the whole thing.** One measurement at a time, in the
foreground, each side printing its own working directory and revision.
And **grep the tree for the thing being deleted before committing the
deletion, not after** — `024.47` records what a revert leaves behind when
that happens in the wrong order.

# Task 041: 0.2.11 — the loop's own leavings

What 0.2.10's three review rounds recorded rather than fixed. The branch
is **`feat/0.2.11`**, and `AGENTS.md` names it. `036` records where this
sits on the way to 1.0.0.

## Why this is a release and not an appendix

Nine entries, and they are not a list of odds and ends. Six of them are
one sentence: **this engine asserts absence from evidence that does not
establish absence.** A macro it could not read; a module it has seen one
file of; a `method_missing` on the wrong side; a `ClassMethods` that
belongs to no concern; a constant lookup that stops one step short of
Ruby's. Each was found separately and each is the same mistake.

0.2.10 is why they are here rather than in it. Its `drive` round measured
one attempt at the module half — 41 findings added, 0 removed, every one
false — and the release rolled it back rather than refine a rule that
bought nothing. That is the shape to avoid repeating: a completeness
*heuristic* is worse than no answer, and what these need is a
completeness *proof*.

## What it holds

| | What it is | Why it is here |
|---|---|---|
| `024.110` | the unreadable macro is reported as a missing method, while what it might define is correctly left alone | the engine contradicts itself about one fact, and it is a false report on ordinary code |
| `024.116` | `def self.method_missing` and `define_singleton_method` do not open a surface | same family, pre-existing on classes, in no document until 0.2.10 |
| `024.106` (second half) | nothing checks a module's class-level calls | needs the index to prove it has seen every file that reopens the module |
| `024.114` | `module_function :name` cannot see a method declared in another file | needs a fact applied after both files are indexed, the way `AncestorFact` is |
| `024.115` | `include M` reaches `M::ClassMethods` whether or not M is a concern | narrow it to something with a marker; 0.2.10's round showed the pre-Rails-4 shape is detectable |
| `024.112` | a bare constant is not looked up through the enclosing class's ancestors — Ruby's second step | `024.103` implemented the first step and stopped |
| `024.111` | a visibility section written inside a block does not reach the body it runs in | the block frame is right for `included do` and wrong for an iterator |
| `024.113` | the publish funnel's memory is keyed by uri, not by buffer | the last place a version integer is compared across buffers |
| `024.109` | two examples of 0.2.9's change set whose fixtures may not distinguish | mechanical to re-derive; two of the four are named and fixed |

## `024.110`, and a decision reversed on a measurement

**Fixed.** A bare call in a class or module body that this engine does
not recognise now opens the owner's *class* surface as well as its
instance one — because whatever supplies the macro is exactly the thing
that could not be read, and the engine was giving two contradictory
answers about one fact: declining to report anything the macro might
define, and reporting the macro itself.

**This reverses a decision `024.23` recorded as deliberate.** The
argument for it, in `open_surface_spec.rb`'s own words, was that opening
both surfaces "would make every unreadable macro silence its own report".
It does, and the measurement is why that is the right trade:

| actioncable + activejob + activemodel + activesupport | before | after |
|---|---|---|
| `unresolved-constant` (control) | 1,099 | 1,099 |
| `unknown-method` | **84** | **25** |
| added / removed | — | **0 / 59** |

Every removal sampled was confirmed against the interpreter as a false
report: `ActiveJob::Serializers::SymbolSerializer.instance` (from
`include Singleton`), `Hash.ruby2_keywords_hash`,
`ActionCable::Server::Base.config` (from `cattr_accessor`). What is lost
is the genuinely *mistyped* macro — and this engine has never been able
to tell one from a gem's DSL, so reporting one meant reporting both.

**Seven examples had to change, and six of them were `024.109`'s
category.** `engine_spec` and `server_ancestry_spec` both used a bare
`fixtures :all` as the thing that should be reported, in examples about
an entirely different rule — the Agent-confirmed ancestry gate. With the
class body silenced they would have passed whatever that rule decided.
Both now put the call inside a `def`, which opens nothing, so the rule
under test is again the thing that decides.

## `024.112`, and an expectation that was wrong before it was written

**Fixed.** A bare constant is now looked up through Ruby's second step —
the ancestors of the innermost cref — after the nesting and before Object.
`LocalInferencer` takes a `hierarchy_index` for it; without one the lookup
stops after the nesting, which is what 0.2.10 shipped.

**The first draft of the examples asserted something Ruby does not do.**
They were written in `module App`, and the file's own `before` already
declares `App::Config` — so Ruby's *first* step finds it and the
superclass's namespace never gets a turn. Asked with the fixture the
examples actually build:

```
$ ruby -e '
class Config; def top_only; end; end
module App; class Config; def app_only; end; end; end
class Zbase; class Config; def zbase_only; end; end; end
module App;   class Runner < Zbase; def which = Config; end; end
module Other; class Runner < Zbase; def which = Config; end; end
p App::Runner.new.which     # => App::Config
p Other::Runner.new.which   # => Zbase::Config
'
# ruby 3.4.10
```

Which is the difference between step 1 and step 2, and exactly what
`CLAUDE.md`'s "establish where the expected value comes from" section is
about — the expectation has to be taken from the interpreter *with the
fixture in front of it*, not from the shape of the rule in the abstract.
Two of the three examples failed for the right reason and one for the
wrong one, which is how it was caught.

Corpus, same four gems, control identical at 1,099: **0 added, 0
removed** — the rule only fires where a superclass's namespace declares a
same-named class, which none of these gems do.

## `024.116`, and what the two fixes share

**Fixed.** `declares_method_missing?` asked the index for
`kind: :instance_method` whichever side the lookup was on, so a class
answering `CWithMM.anything` through `def self.method_missing` was judged
closed. And `define_method`/`define_singleton_method` written inside a
block — `%w[a b].each { |n| define_singleton_method(n) { … } }`, which is
how a class generates a family of methods — were dropped by the
block guard in `#record_open_surface`. Those two calls name what they do;
unlike an arbitrary macro they are not a guess, so they open the surface
wherever they are written.

**`024.110` and `024.116` are the same sentence twice.** The engine had
evidence that it could not enumerate an owner's class-level surface — an
unreadable macro in the body, a `method_missing` on that side, a
`define_singleton_method` in a loop — and asserted absence anyway. That
is this release's whole theme, and the corpus says what it was worth:

| actioncable + activejob + activemodel + activesupport | main `9aafed0` | branch |
|---|---|---|
| `unresolved-constant` (control) | 1,099 | 1,099 |
| `unknown-method` | **84** | **25** |
| added / removed | — | **0 / 59** |

Fifty-nine fewer false reports and not one new finding, on four gems
nobody wrote for this engine.

## What closed, and what did not

Six of the nine. `024.110`, `024.112`, `024.113`, `024.114`, `024.115`
and `024.116`.

Five of the six are the release's own sentence — *this engine asserts
from evidence that does not establish what it asserts*, in both
directions:

- **asserting absence** it had not earned: an unreadable macro
  (`024.110`), a `method_missing` on the side being looked up and a
  `define_singleton_method` in a loop (`024.116`), a `module_function`
  naming a method in another file (`024.114`), a constant lookup that
  stopped one step short of Ruby's (`024.112`);
- **asserting presence** it had not earned: `include M` reaching
  `M::ClassMethods` for any module that merely nested one, so completion
  offered a name that raises (`024.115`).

`024.113` is the odd one and the smallest: the publish funnel took the
document in 0.2.10 and left the memory it compares against keyed by uri,
so a version from one buffer was still being compared against a version
from another.

**Three are left open and each for a stated reason**, not for want of
time:

- `024.106`'s second half needs the index to prove it has seen every file
  that reopens a module — a property of the index, not of a name. 0.2.10
  measured the name-shaped version at 41 false findings and rolled it
  back; the same shape must not be tried again.
- `024.111` is a change to `#block_self_is_module`, a rule three
  releases have adjusted, inside a release that is reviewing itself.
- `024.109`'s remaining two examples are mechanical to re-derive and
  belong with whoever next runs the sweep.

`024.55`'s second half — the post-start version handshake — is a
behaviour change with its own failure mode and its own measurement, and
0.2.10 shipped the half that could be argued without one.

### Round 1 (`attack`) — 11 findings, three of them regressions from this release

| # | What it was | Disposition |
|---|---|---|
| 1 | **`024.116` regression.** `declares_method_missing?` gained a `singleton` argument but not the `:extend` origin remapping `#open_surface?` applies three lines below it. `extend M` puts M's *instance* methods on the class-level chain, so `ExtC.anything` was reported on a class Ruby answers. Baseline was correct | Fixed — the same side computation, in both places |
| 2 | **`024.115` regression, and the justification for it was untrue.** The pre-Rails-4 `def self.included(base); base.extend(ClassMethods); end` is **not** "an ordinary `extend` this index has always followed" — the receiver is a method *parameter*. `UOld.old_cm` became a false report on a whole generation of real concerns | Fixed — the hook is recognised as its own relation, `:concern_class_methods`, and is the second marker. **The claim was written from another round's summary rather than checked**, which is the failure `docs/CLIENT_BEHAVIOUR.md` exists to prevent, committed inside the release that added that document's own guard |
| 3 | **`024.116` regression.** `METHOD_DEFINING_CALLS` hardcoded `define_method → :instance` and returned before consulting `Cref#surface_kind`, so inside `class << self` both directions inverted — and both were right in 0.2.10 | Fixed — `define_method` asks `surface_kind`; `define_singleton_method` inside `class << self` opens nothing, because Ruby puts that method on a singleton class this index cannot name |
| 4 | **The `024.112` corpus figure was a diff of two runs of the same program** | Fixed — see above |
| 5 | `024.112` discarded `prepend`: `ancestors.drop(1)` assumes self is first, and with `prepend M` it is second | Fixed — the self entry is dropped by *name* |
| 6 | `024.114` recorded `module_function :name` in a class body and inside `class << self`, where Ruby raises `NameError`. Now a cross-file fact, so the mistake spread | Fixed — gated on `module_owner?` and not `declares_singleton?` |
| 7 | `024.112` over-reached inside `class << self`, where the innermost cref is the singleton class and its ancestors are not the enclosing class's | Fixed |
| 8 | A concern that includes a concern lost the inner `ClassMethods` — pre-existing, undocumented, and the commonest way a large Rails app composes concerns | Fixed — the marker is followed transitively, and only through modules that are themselves concerns |
| 9 | `024.110` does not hold for a call inside a block: `%i[a b].each { |f| validates f }` is still reported while the bare spelling is not | **`024.117`**, 0.2.12 |
| 10 | `engine.rb` carried three dead copies of rules that moved to the resolver in 0.2.9, and the last still stated the *pre*-`024.116` rule | Fixed — deleted |
| 11 | `041` understated `024.110`'s cost: what is lost is every class-level report on an owner whose body contains an unreadable call, and it makes the Agent's ancestry gate unreachable for those receivers | Recorded below |

**Could not break: `024.113`.** Every sequence the round tried — reopen
without close, close then reopen below, an in-flight answer from the old
buffer, two uris, an interleaved workspace publish, out-of-order within
one buffer — behaved correctly, and its four specs distinguish the two
candidate behaviours.

### What `024.110` actually costs

Not "the genuinely mistyped macro", which is what this file said. It is
**every class-level `unknown-method` report on any owner whose body
contains a call this parser cannot read** — and for a Rails-shaped class,
any `validates`/`has_many`/`cattr_accessor` in the body is enough. The
Runtime Agent's ancestry gate in `closed_nominal?` also becomes
unreachable for those receivers, since the availability answer is no
longer `absent` by the time the gate is consulted.

The four-gem measurement shows the 59 removals were false reports. It
says nothing about the genuine class-level reports that can no longer be
produced, and no measurement here does. That is a real gap in the
argument for a change this release made, recorded rather than papered
over.

## Review

Three rounds finding defects, then ship with what is open recorded. **The
round's finding list goes in this file as the round reports it.**

**And at least one round is `drive`.** 0.2.10's two reading rounds missed
41 false reports between them, including a defect one of them had just
introduced; the round that ran the product against real Ruby found every
one. A release about what the engine may assert is not reviewed by
reading it.

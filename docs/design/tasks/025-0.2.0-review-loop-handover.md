# Task 025: 0.2.0 review loop — handover

State of `feat/0.2.0` at the tip of the branch, written for whoever picks
the loop up next. Facts and open questions only; the decisions themselves
live where they belong — in `024-deferred-review-findings.md`, in
`KNOWN_LIMITATIONS.md`, and in the commit messages.

Verify the numbers below rather than trusting them. This document has
been stale about its own tree three times: it was written at `659c5dc`,
then revised by a change set that kept growing under review, and the
count below was wrong on each occasion before a reviewer re-ran it. It is
cheaper to re-run the suite than to trust this line.

## Where the branch is

`feat/0.2.0`. `main` is fully merged in (merge-base == `main`'s tip), so
`git diff main...HEAD` is the complete change set and nothing from
0.1.11–0.1.15 can be lost except where 0.2.0 edited the same lines —
each such site was checked.

**Re-run the counts rather than reading them here.** Every number this
section has ever carried has gone stale, including after a revision that
was made specifically to correct it:

```
git rev-list --count main..HEAD
cd core && bundle exec rspec
cd vscode && npm ci && npm test
gitleaks detect --config .gitleaks.toml
grep -c '^## 024\.' docs/design/tasks/024-deferred-review-findings.md
```

The last one is not the open count. For that, read the `yaml` blocks the
way `core/spec/meta/deferred_findings_spec.rb` does — `status` other than
`fixed`/`done`, `kind: defect` — because a hand count of that table has
been wrong every time it has been checked, most recently by ten.

`npm ci` first: `node_modules` is not in the tree, and two reviewers lost
time to `npm test` failing for that reason before reaching anything real.

Not pushed. **0.2.0 is a minor release, so publishing needs the user's
permission** — the patch-release autonomy they granted covers patches
only. Nothing has been pushed, tagged or published from this branch.

## What the release contains

Six features, six capability rows (H7, C12, C13, G15, G16, T1), plus the
merge of three patch releases. `docs/EXTENSION_CAPABILITIES.md` and both
changelogs carry the rows; the workspace-diagnostics feature ships
*without* a row, because its E2E example does not pass (024.14) — README
marks it ⚠️ with a footnote and both changelogs say so.

## The review loop

Twenty-one independent rounds have run. Every round's findings were
verified before acting on them; two were rejected as wrong.

**Round 21 was the first run after `main` (0.1.15) was merged, and the
first written to `CLAUDE.md`'s "How to ask for an independent review".**
Three reviewers, same broad instructions, no exclusion list, no
"concentrate on", no corpus told to skip — the corpora were given as a
record of *when* each was last measured, with the note that nothing had
been measured at the tip. All three found the same most-severe defect
independently, and it was one a narrowed round would plausibly have
missed: it lived in the merge itself.

**Round 22** ran three reviewers on the same broad instructions. Two of
its four code defects were in round 21's own fixes, which makes it the
first self-referential round in `CLAUDE.md`'s sense -- round 21 was not
-- so the counter stands at one. If round 23 is again mostly about round
22's changes, the rule fires and this thread rolls back.

Round 22's own measurement, both sides over one corpus (Ruby 3.4.7's
stdlib, five Rails 8.1.3 gems, minitest 6.0.6), diffed by position:

| code | `ca66774` | after round 22 |
|---|---|---|
| `unknown-method` | 5,831 | **3,362** |
| `argument-count` | 17 | **15** |
| `unresolved-constant` | 9,408 | 9,408 |

Nothing introduced. The 2,469 removed are reports built on a substituted
ancestor -- a method inherited from a superclass whose name resolved to a
different class -- plus the 525 `alias_method` reports in prism's
`translation/ripper.rb`. 23 were read individually; that is a sample,
not a proof.

**Two of round 22's own numbers were wrong before they were right**, and
both failures are worth carrying: a diff between two runs over *different
corpora* (one included this repository's `core/lib`), which invented ten
introduced findings; and a diff computed from a file still being written,
which invented seventy-nine. Compare the corpus definitions before
reading a diff, and wait for the run to finish.

What round 21 changed, measured at `ca66774` against the pre-merge tip
`db9bcfa`, both sides over one fixed corpus (Ruby 3.4.7's stdlib, five
Rails 8.1.3 gems, minitest 6.0.6), diffed by position:

| code | before | after |
|---|---|---|
| `argument-type` | 795 | **0** |
| `unknown-method` | 6,339 | 5,932 |
| `argument-count` | 148 | 17 |
| `unknown-route-helper` | 46 | **0** |

**Nothing introduced.** Separately, prism 1.6.0 with its own `sig/`
loaded went 151 `argument-type` to 0, also introducing nothing, and this
repository's own `core/lib` went 60 `unknown-method` to 53.

The `argument-type` line is not only good news and 024.37 records why:
that check's entire measured output over every corpus was false
positives, and with them fixed it reports nothing at all.

Reproducing this comparison is `scripts/corpus_diagnostics.rb`, which was
an ad-hoc script through round 20 and is committed now — the method this
document recommends was, until then, not runnable by whoever read it.
Run it once per side, pointing both at the *same* corpus, and diff:

```
cd core && bundle exec ruby ../scripts/corpus_diagnostics.rb <corpus> > /tmp/branch.txt
cd <worktree-of-main>/core && bundle exec ruby <this-branch>/scripts/corpus_diagnostics.rb <corpus> > /tmp/main.txt
sort /tmp/main.txt > /tmp/m && sort /tmp/branch.txt > /tmp/b && comm -13 /tmp/m /tmp/b
```

The second invocation runs *this* branch's copy of the script against the
other revision's engine — the script keys off `Dir.pwd`, and a worktree
of `main` has no copy of its own. `comm -13` is the half that matters
most: a line only the new side produces is a report the change
introduced.

### What the rounds found, in one line each

Rounds 1–8 were mostly unpinned lines and false documentation claims.
Rounds 9–20 were increasingly about wrong reports on real code:

- **9** — a view helper's `@ivar` reported as unassigned; the 2,000-file
  cap reported to nobody.
- **10** — the pass's traversal had no order (024.15's shape, new
  reader); helpers re-parsed on every keystroke (17.3ms → 0.26ms).
- **11** — a cache keyed on `document.version`, which restarts at 1 per
  open session.
- **12** — `defined?(@x)` counted as a read; `encoding` matched as a bare
  prefix and ate ordinary prose.
- **13** — a 200-file `didChangeWatchedFiles` blocked the dispatch thread
  13.5s (→ 0.14s); the first pass published the pre-Agent static reading
  project-wide.
- **14** — the batch pass took the *generation*, ending the workspace
  pass at file 1.
- **15** — `parameter_env`'s seed value unpinned (`nil` for every
  parameter on hover, with the suite green).
- **16** — the last two ranking bands unpinned; the batch never exercised
  as a batch.
- **17** — an RBI-declared type reported **incompatible with itself** (11
  instances in the `rbs` gem); a block's body hidden behind the enclosing
  call's type.
- **18** — **every `true`/`false`** reported against a plain-class
  parameter; an operator call's receiver mis-resolved.
- **19** — round 17's own fix produced **230 new `unknown-method` reports
  across the stdlib**; `summarize` 30% slower.
- **20** — a `Data.define`/`Struct.new` class reported incompatible with
  `Object`; the pass restarted itself on its own ancestry answers (9×
  amplification); a whole regex literal coloured as a variable.

### The method that finds these

Fixtures stopped finding defects around round 16. What works is **driving
the engine over large real corpora** — the Ruby stdlib, every installed
gem, this repository's own `core/lib` — and diffing the branch's findings
against `main`'s, then reading every difference. Rounds 17, 18, 19 and 20
each found a wrong report that way and none of them from a fixture. Keep
doing that.

## What is open, and where it is recorded

| entry | what |
|---|---|
| **024.14** | Workspace diagnostics has no E2E row; the example produced nothing in 45s. Cause diagnosed (restart-without-resume); the *coalescing* half of the direction is now implemented (round 20), the resume half is not. |
| **024.18** | The unassigned-`@ivar` check cannot enumerate what it needs to. Blocked on 024.R7's gem index for the precise form; two wrong-report shapes remain (a view rendered by another controller's action; a chain three deep whose top is unread). |
| **024.19** | The argument-type check can judge against a class the receiver is not, via the index's simple-name fallback. Reported against prism's source; **not reproduced from a fixture here**, which is why it is recorded rather than fixed. |
| **024.20** | `contains?` treats Prism's exclusive `end_offset` as inclusive. Making it exclusive is the right rule and **fails 39 examples**, because every caller handing it an LSP range end depends on the current one. `locate_in_block` answers `Unknown` meanwhile. |
| **024.13** | A workspace that reopens a core class makes its chain look closed, so a gem's addition to that class is reported unknown. Predates this release. |
| **024.21** | Semantic highlighting colours only the first segment of a qualified constant. |
| **024.22** | The unassigned-`@ivar` check is silent in an application `rails new` produces, because the generated `ApplicationController` calls `allow_browser`. The G16 row passes against a hand-written empty one. |
| **024.24** | An empty route table answers "no such route", so a `*_path`/`*_url` call the workspace does not declare is reported when no routes are loaded — an untrusted workspace, or any non-Rails project. Pre-existing; 0.1.14 cut its stdlib count from 48 to 8 without touching the cause. |
| **024.25** | Two specs written to enforce the KNOWN_LIMITATIONS pairing were rolled back: each round found another Markdown shape their regexes mishandled, and three documents came to claim they enforced rules they did not. The entry is the deliverable. |

**This table is not the list.** It was written before the 0.1.15 merge
brought 024.26–024.36 onto this branch, and a hand count of it has been
wrong every time anyone has checked — most recently by ten. Parse the
`yaml` blocks the way `core/spec/meta/deferred_findings_spec.rb` does and
read that. Two of the rows above are also out of date in the other
direction: **024.24 is fixed in 0.2.0** (round 21), and 024.19's cause —
the index answering a bare constant by picking among same-named classes —
is now refused for every diagnostic, though the entry stays open because
the entry is about the index's answer, not about the checks that read it.

024.21–024.24 were found by driving the engine over real code, which is
the method the section above recommends. 024.23 is not in the table
because it is no longer open: it was the largest source of wrong reports
the engine produced, and it was fixed and released as 0.1.14 ahead of
this branch rather than carried into it.

`docs/KNOWN_LIMITATIONS.md` and `.ja.md` carry the user-facing half of
all of these — checked by hand, as of this document's last revision.
That sentence was false when this document first claimed it: 024.19 and
024.20 were in neither language, and so was 024.13, which predates this
release. Two rounds were then spent trying to make a spec enforce it
rather than a person, and the attempt was rolled back; **024.25** records
what went wrong and what the right shape would be.

## Process notes worth carrying forward

**The mutation sweep needs three guards, each learned by losing a
verdict to its absence.**

1. Check the edit landed by comparing against the **pre-mutation text**,
   not against `git diff` — a mutation that reverts an *uncommitted* fix
   produces exactly HEAD, and the git check then calls it "did not land"
   and skips the restore, leaving the fix reverted in the tree.
2. Verify every file is byte-identical to its baseline **after each
   mutation**, before scoring the next. A verdict measured against a tree
   a previous mutation failed to restore is not a verdict; this cost one
   ("the delegation gate reads as unpinned" — its predicate was still
   `false` from the run before).
3. Require a real `N examples, M failures` line. Never score off a blank
   string.
4. **Trap the exit.** A sweep killed part-way — a timeout, an
   interrupt — leaves the tree mutated, and the next run scores against
   it. `trap 'restore_all' EXIT INT TERM`. Learned twice: once in 0.1.15,
   once in round 21, where a ten-minute tool timeout left the flag under
   test flipped.
5. **Verify the mutation landed where you meant.** `sed -i '' 's/X/Y/'`
   rewrites the *first* match in the file, which in round 21 was a call
   site rather than the definition being tested — and the "restore" then
   rewrote a third line. Assert the pattern is present and unique before
   substituting, and diff after.

**Never `git checkout -- lib/` to clean up after a sweep.** It reverts
the round's own uncommitted work, not just the residue. This happened in
round 20 and cost four fixes that had to be re-applied from the
transcript.

**A spec that passes both ways pins nothing.** Several rounds' findings
were fixtures that could not distinguish the two candidate behaviours —
a band number where ASCII ordering makes both orders identical, a
fixture whose file names and line numbers sort the same way, an
`include(...)` that a *different* guard rejects on its own. When a
mutation survives, ask whether the fixture can see the difference at all
before assuming the line is dead.

**Some decisions are unpinnable and should be deleted rather than
tested.** Four were removed this way — a `data[:receiver] && data[:name]`
guard no input reaches, a `simple_name` map the signature environment
makes redundant, a `[name] +` seed `HierarchyIndex#ancestors` already
contains, and (round 21) a `signature.direct` requirement that looked
necessary for a subclass overriding an RBS-declared method and turned out
to be reached by an earlier refusal in every case.

**"Unpinned" and "unobservable" are different answers.** Round 21 found a
keyword whose two values produce byte-identical output over 12,859
reports — but the two callers are asking genuinely different questions,
and the conservative value is the one that avoids a false report. That is
not a decision to delete; it is one to pin at the level where the
difference exists, by calling the method directly. Deleting it would have
made the engine report more, which is the unsafe direction.

**Cost decisions get a source assertion, not a behavioural one.**
`spec/meta/workspace_index_cost_spec.rb` is the pattern: filter before
sort, `min_by(limit)` over `sort_by.first(limit)`, `touched.uniq`, and
the `SourceLocation` ASCII fast path all live there, because reversing
any of them leaves every answer unchanged and multiplies what a keystroke
costs.

## The two-rounds rule, considered

`CLAUDE.md` says to stop and roll back when a round's findings are mostly
about the previous round's own changes, twice running. Rounds 17–20 all
touch the same two files — `diagnostics/engine.rb`'s argument-type check
and `argument_type_spec.rb` — so the rule has to be answered rather than
left unmentioned, which is what the first version of this document did.

**It does not fire, on this reading.** Round 19 is plainly
self-referential: it exists because round 17's fix produced 230 new
reports. Round 20 is not. Its three findings — a `Data`/`Struct` class
judged incompatible with `Object`, the workspace pass restarting on its
own ancestry answers, a regex literal coloured as a variable — are about
the release's own features (G15, the workspace pass, T1), not repairs of
round 19. One self-referential round in a row is not two.

Whoever continues should re-ask this at each round rather than treat it
as settled. Round 20 reset the counter, so it now takes two *consecutive*
self-referential rounds to fire — but the rule is about a fix aimed at a
symptom, and rounds 17–20 sat on the same check for four rounds. Count
the rounds the rule asks about, and separately notice if the same check
keeps coming back.

**Round 21 sat on it a fifth time, and the "same check keeps coming
back" reading is the one to take seriously.** Its most severe finding was
G15's again — a call judged against a signature it does not bind to. It
is not self-referential in the rule's sense: it is a defect the merge
carried in from 0.1.14's ancestry model, not a repair of round 20. But
five rounds on one check, ending in a measurement that the check reports
nothing at all on real code, is the shape 024.37 exists to put a decision
under.

## Judgement call for whoever continues

The loop has not converged: rounds 17, 18 and 20 each found a *new*
wrong-report shape in G15, and each was real. That is not a sign the
rounds are unproductive — each fix was a genuine defect on shipped Ruby —
but it does mean the corpus method will keep finding shapes for as long
as it is pointed at new corpora, and "no findings" may not be reachable
by continuing to widen the search.

The question worth putting to the user is whether the bar for shipping
0.2.0 is "a round comes back clean" or "the known shapes are fixed or
documented and the remaining search is bounded". Both are defensible; the
first is what the standing instruction says, and the second is what the
evidence suggests will end sooner.

Round 21 sharpens it rather than settling it, in two directions at once.
Against continuing: its four code defects were each a *wrong report on
shipped Ruby*, three reviewers found the worst one independently, and the
round removed 1,372 reports while introducing none — that is a productive
round by the only measure `CLAUDE.md` says is worth reading, user impact.
Against the same: two of those defects were in the merge itself and one
was in a method added the same day, so the round was partly measuring the
change set's own freshness rather than the release's residue.

The second question this round adds is not about the loop at all.
**024.37 asks whether G15 should ship as a capability**, given that its
measured yield on real Ruby is zero and its entire measured output before
this round was wrong. That is a product decision, and it does not need
another round to answer.

Performance, for what it is worth, is now **better than `main`**:
`summarize` over 400 stdlib files went 2.25s → 1.01s, because the
ASCII fast path speeds up every range conversion and not only the new
ones.

# Task 028: 0.2.3

State of this release, written for whoever continues. 0.2.1 shipped on
2026-08-06 as `b759fc111a32b441164d9796f31a94947dcf73c0d77e817ea045c5f155141e22`.

> **Renumbered from 0.2.2 to 0.2.3 on 2026-08-11**, and this file from
> `027-0.2.2-review-loop.md`. The number was given to a single-defect
> patch that had to ship immediately — see
> [`027-0.2.2.md`](027-0.2.2.md). Everything below was written while this
> was going to be 0.2.2; where it says so, read 0.2.3. The branch name
> `fix/0.2.2` likewise refers to the work now called 0.2.3.
>
> **The "hardware fault" recorded below was this repository.** The
> pause on 2026-08-09 was diagnosed as a failing machine — applications
> vanishing, a reinstall that did not help. The cause was
> `store_spec.rb` handing a fabricated absolute path to cache pruning, so
> every `bundle exec rspec` swept the filesystem root and removed all but
> the newest top-level directory. Reinstalling macOS did not help because
> re-cloning restores the cause. That is what 0.2.2 fixes, and it is why
> this release was interrupted rather than finished. The paragraphs below
> are left as they were written, because a record that quietly corrects
> its own past reasoning stops being a record.

Read [`026-0.2.1-review-loop.md`](026-0.2.1-review-loop.md) first — it
holds rounds 23–31 and, at its end, the two things this release exists to
act on.

## Where this stands, paused 2026-08-09

Work stopped mid-round because the machine it was running on has a
hardware fault. Nothing here is blocked on a decision; it is blocked on a
working computer.

**The branch is `fix/0.2.2` at `bb506bf`, one commit ahead of
`origin/fix/0.2.2`.** Everything below is in that commit unless it says
otherwise.

**Round 37 produced nothing at all.** Three reviewers were launched; two
were stopped a few seconds in when the maintainer said one was enough,
and the third stalled with no output after ten minutes. That is a harness
failure, not a result — *no* reviewer read *any* of this change set. The
round has not run. Do not read the absence of findings as a clean round;
it is an empty one.

**So the release gate is not met.** The order recorded in the next
section says 0.2.2 ships once a review confirms it is better than 0.2.1.
No review has been asked and answered. Neither release nor publish has
happened, and the tag does not exist.

### What the pause added, and how it was found

None of it came from a reviewer. All three were found by setting up a
second Ruby and running the suites, which is worth noting: the
configuration `SUPPORT_MATRIX` asks a contributor to create is one no
contributor had created since the document started asking.

| what | how it showed |
|---|---|
| `bin/ovallsp` globbed `vendor/bundle/**/gems/*/lib`, so a checkout bundled under two Rubies put the *other* ABI's native extensions on `$LOAD_PATH` — the crash ADR-0005 exists to prevent, arriving from inside the payload it permits | `stdio_spec` failed under 3.4 the first time 4.0 had also been bundled |
| `documented_counts_spec` has never run — its glob was rooted at `core/`, not `core/spec/`, so `diff-lcs`' twenty vendored spec files made its file count disagree and it skipped. CI sets the same layout with `bundler-cache: true`, so it has been inert there too, and the figure it guards had gone stale to 1,934 against 1,941 | noticed because the suite grew and nothing complained |
| Three `coreProcess` tests failed four runs in six, in three different combinations: macOS charges the first execution of a newly written executable 2.62 s (0.04 s thereafter), and the fixtures are written fresh each run, inside a 1 s snapshot timeout | ran the unit suite ten times rather than once |

The bootstrap moved into `Ovallsp::VendorBootstrap` so it could be tested
at all — `bin/ovallsp` only ever runs as a subprocess — and the
executable-fixture warming lives in one place,
`vscode/src/test/support/executableFixture.ts`, because two suites need
it.

### The state of the checks themselves

> **Superseded, and left visible.** The paragraph below was written on
> 2026-08-09 and is no longer true of this machine: measured at
> `d602e20` under Homebrew's `ruby@3.4` (3.4.10), the Core suite is
> **1,950 examples, 0 failures, 0 pending** and the real-Rails suites run
> — **73 examples, 0 failures** — because Rails and sqlite3 *are*
> resolvable for 3.4 here now. `core/.bundle/config` and `core/vendor/`
> do not exist either, so the toolchain note below overstates the setup.
> Round 39 found this: AGENTS.md makes this file the authority on release
> state, and a stale "the decisive suites cannot run here" is precisely
> the belief `CLAUDE.md`'s "a green suite is a measurement too" section
> exists to remove.

- `core`: **1,941 examples, 0 failures** under Ruby 3.4.10 *and* 4.0.6.
  76 pending both times, all of them the real-Rails suites skipping
  because Rails and sqlite3 are not installed for either Ruby here.
- `vscode`: **186 passing**, ten consecutive runs, under Node 20.20.2.
- Ruby 4.0 now has an **informational, non-blocking** CI job, and
  `SUPPORT_MATRIX`'s claim that nothing continuously verifies 4.0 was
  corrected in both languages. Deliberately non-blocking: the maintainer's
  instruction is that 4.0-specific failures are recorded, not fixed.

### What has not been done

1. **Round 37**, from the beginning. One reviewer.
2. ~~The corpus baseline.~~ **Taken 2026-08-11** — see "Measurement,
   before anything else in this release" below. The two sides are
   identical, verified with a positive control rather than trusting the
   version strings.
3. Release and publish. `vscode/.vsce-pat.local` now exists on this
   machine; 0.2.2 was published through `release.sh` with it on
   2026-08-11.

Note that 1 is now the only thing between this branch and a release
decision. `027-0.2.2.md` records a waiver of the same gate for 0.2.2 and
the terms it was judged on; those terms do not transfer — 0.2.2 was one
defect wide with no engine change, and this release is neither.

### The toolchain, if it has to be rebuilt

Homebrew, all keg-only, matching CI: `ruby@3.4` (3.4.10), `ruby@4`
(4.0.6), `node@20` (20.20.2), bundler 2.7.2 in both Rubies,
`core/.bundle/config` setting `BUNDLE_PATH: vendor/bundle`.

**Installing the `ruby` formula repoints `/opt/homebrew/bin/ruby` at
4.0.6.** Never invoke a bare `ruby` or `bundle` on a machine set up this
way — prefix the interpreter you mean and have each side print `ruby -v`
before it runs. This file already carries three corpus comparisons that
were confidently wrong because a side ran something other than what was
assumed; two Rubies sharing one checkout is a fresh way to make that
mistake, and the `bin/ovallsp` defect above is what it looks like.

## The rule this release is being run under

**During a review loop, fix; do not add.** 0.2.1 ran nine rounds and
seven of them found a defect in code the *previous* round had written.
Not the loop failing — the loop being asked to review a change set that
kept growing. Three capabilities were added mid-loop and all three went
back to the roadmap before shipping, and the machinery they justified
produced most of rounds 25–31's findings.

So: a capability a reviewer asks for is an entry in `024`, not work to do
before the next round. This is now in `CLAUDE.md`; it is repeated here
because it is the single most expensive thing 0.2.1 learned.

## The order the next three releases run in

Decided by the maintainer on 2026-08-09, before round 37, and written
here because it is a scoping rule and a scoping rule kept in someone's
head is what 0.2.1 spent nine rounds paying for.

1. **0.2.2 ships once a review confirms it is already better than
   0.2.1.** The question a round is being asked is that one — not
   "is anything still open", which the register answers yes to and will
   keep answering yes to. What is open and unaddressed here (`024.45`,
   `024.47`, `024.56`, `024.57`) is open on 0.2.1 as well, so it cannot
   be a reason to hold a release that is strictly ahead of it.

   This sets the *bar for shipping*; it does not by itself waive
   `CLAUDE.md`'s clean-round gate. A round that confirms the improvement
   and reports nothing satisfies both at once. A round that confirms the
   improvement and also finds a defect is the ordinary case: fix it and
   re-review. Waiving the gate is still a separate, explicit call that
   gets written down where the release is recorded.

2. **0.2.3 is scoped to bugs users actually hit, and to nothing else.**
   An entry qualifies if it shows up while editing an ordinary Rails or
   Ruby file — no contrived construction, no fixture built to expose it.
   An entry that needs explaining before it can be reproduced does not
   qualify, however real it is. The selection happens when 0.2.3 opens,
   against the register as it stands then; today's plausible candidates
   are `024.45` (seconds of lag on a large file), `024.47` (`"hello".`
   losing every String method next to a workspace `String`), `024.56`
   (errors left in the panel for a closed file), `024.43` and `024.44`.
   `024.57` is the direction `024.56` needs, so they travel together or
   neither does.

   The narrowness is the point. 0.2.2's own history is a release that
   grew a thread across four rounds and had to roll it back; a stated
   scope is the cheapest countermeasure available for that, and it is
   the one thing 0.2.1's write-up asks for that has not yet been tried.

3. **Then 0.3.x.** No capability work before that — a capability a
   reviewer or a roadmap row asks for during 0.2.2 or 0.2.3 is an entry
   in `024`, exactly as the rule above says.

Neither 0.2.2 nor 0.2.3 gets a `ROADMAP.md` section: patch releases
announce nothing new, which is why that document has none. They are
where promises already made get kept.

## What 0.2.2 inherits, in the order a user notices it

Every item has a `024.*` entry with the detail; this is the ordering, not
a substitute for reading them.

| # | what | where |
|---|---|---|
| ~~`024.41`~~ | Typing `.` reports a method on the *next* line — **out of 0.x scope**, retargeted to 0.4.0; Ruby reads the text the same way | `server.rb#did_change` |
| `024.45` | Re-analysis after a keystroke is 2–5 s on a 1,600–2,700-line file, against a stated 300 ms — **not addressed**: the debounce that reduced it was rolled back (024.57) | `server.rb#reindex` |
| ~~`024.49`~~ | The red toast 0.2.1 removed is still shown by a second version check — **fixed** | `vscode/src/versionInfo.ts` |
| ~~`024.51`~~ | First launch after upgrading blocks while it sweeps the old cache — **fixed** | `cache/store.rb` |
| `024.47` | A literal's type is answered by a same-named workspace class — `"hello".` loses every String method | `hierarchy_index.rb#canonical_name` |
| ~~`024.50`~~ | The Marketplace description promises the behaviour 0.2.1 removed — **fixed** | `vscode/README.md` + `.ja.md` |
| `024.43` | `puts(` gets no signature help while completion offers `puts` | `query_service.rb#signature_owners` |
| `024.42` | An RBS label says `Unknown` where RBS says `self`; type variables leak | `signatures/type_converter.rb` |
| `024.44` | A partial's local resolves to nothing | `server.rb#ivars_for_view` |

`024.41` and `024.45` were read as wanting the same thing — **debouncing
`didChange`** — and 0.2.2 did it. It answers `024.45`, whose problem *is*
the repeated work. It does not answer `024.41`, and reading them as one
item produced this release's worst mistake: three documents claiming a
fix that had not happened, and a spec that could not tell.

**And then `024.41` turned out not to be a defect at all.** The
maintainer asked what Ruby actually does with

```ruby
a.
b = "str"
```

and the answer settles it: `ruby -c` says `Syntax OK`, running it raises
`undefined method 'b='`, Prism parses `CallNode name=b=`, and OvalLSP
reports the same name on the same line the interpreter points at. A
trailing `.` is Ruby's line continuation. The engine is not misreading
anything — the text genuinely means that until you finish typing.

Four rounds classified it as an engine defect without anyone running the
two lines through Ruby. It had been filed alongside the `a.end` half,
which *was* a real recovery defect, and inherited its classification.
**The lesson is not about this entry.** When a finding says "the engine
reads X wrongly", the cheapest possible check is to ask the language, and
it was never made because the finding arrived already labelled.

It is retargeted to **0.4.0** as a refinement, and out of 0.x's scope:
catching a misreading the language itself calls `Syntax OK` is not
something the 0.x line promises. See `ROADMAP.md`.

## The three capabilities 0.2.1 built and moved back

They are on the roadmap again (`documentHighlight` and `@` completion at
0.3.0, `activeParameter` at 0.4.0) and **their code was removed, not
disabled**. Rebuilding them is not a revert of a revert: what was removed
had known defects, listed here so the next attempt does not rediscover
them.

- **`documentHighlight`.** Calls `ensure_reference_index_current`, and
  the comment on that method says to defer index rebuilding until Find
  References or Rename needs it because it is O(workspace). The editor
  asks for highlights on every cursor move. Measured: 183 ms on a
  300-file workspace, 625 ms on 1,000, after every edit. Whatever version
  ships needs a per-file answer that does not rebuild the workspace
  index.
- **Completion after `@`.** The environment has to be scoped to the class
  the cursor is in, and the owner name comes from
  `declaration.symbol_id.name` — which is *already* fully qualified.
  Joining the owner onto it silently produces `::Admin::::Admin::Importer`
  and every `@ivar` feature goes silent in a namespaced class. Test the
  positive direction: 0.2.1's first two specs both asserted `not_to
  include`, which an empty answer satisfies.
- **`activeParameter`.** Needs `activeSignature` with it, or the index
  points into an overload that does not have that parameter (RBS orders
  overloads shortest-first). And the token map must cover every token
  Prism gives the same character: `BRACKET_LEFT_ARRAY`, `LAMBDA_BEGIN`,
  `PARENTHESIS_LEFT_PARENTHESES`. An unmapped opener still meets a mapped
  closer.

## Round 32

**One reviewer, not three.** 0.2.1's rounds used two or three in
parallel; the maintainer's instruction during this round was that
parallel reviewers given the same neutral instructions converge on the
same defects, so the extra runs buy repetition rather than coverage. The
way to vary method is *between* rounds. The one that ran was the one with
no method suggested to it — the other two had been given a starting point
(end-to-end driving, and the hunk sweep), and a starting point narrows
what one reviewer looks at.

`CLAUDE.md`'s instruction rules were kept: no exclusion list, no
"concentrate on X", no "a clean report is expected", corpus list given as
coverage with the revision each was measured at, and every finding
required to state what a user would see differently with "nothing" a
valid answer.

Ten findings. Nothing was found twice in a row in the same place, so the
same-place rule did not fire.

| what | where it went |
|---|---|
| The debounce does not fix 024.41, and three documents said it did | 024.41 rewritten; both changelogs; the spec replaced |
| A publish could outlive `didClose` and leave findings on a closed file | 024.52, fixed |
| The 30-day cache grace read a clock unrelated to the workspace being away | 024.53, fixed |
| 024.49/50/51 fixed but still published as current limitations | retired; `deferred_findings_spec` now checks the other direction |
| The cache-retention change was never walked through `DOCUMENTATION_MAP` row 34 | PRIVACY ×2, `site/security.html` ×2, both READMEs |
| `Show Version Information` did not print the new note | `extension.ts`; the note is now named by folder |
| A dead recovery branch on a truthy return, guarding a latch | `#schedule_diagnostics` rewritten around what can actually fail |
| `flush_pending_diagnostics` gave each thread its own timeout | a shared deadline — which round 33 found still additive; see below |
| Five behaviours with no test that fails when reverted | four pinned; see below for the fifth |
| Four comments claiming what the code no longer does | corrected in place |

**The findings that mattered most were about this release's own headline
change**, and none of them was the change being wrong — they were the
claims around it being wrong. That is a different failure from 0.2.1's,
where the loop kept reviewing new capabilities. Worth watching, not yet
worth a rule.

### Found while retargeting 024.41, not by the round

Moving one entry to 0.4.0 touches `ROADMAP.md`, README's matrix and the
site, and walking that row turned up drift nobody had reported:

- **`site/roadmap.html` and `site/ja/roadmap.html` were a whole release
  behind, on two separate moves.** 0.2.1 sent `activeParameter` to 0.4.0
  and brought `documentHighlight` and `@ivar` completion back to 0.3.0.
  The Markdown and README were updated both times; the site neither time.
- `roadmap_parity_spec` pairs README with `ROADMAP.md`, and
  `check_site_links.rb` pairs `capabilities.html` with README's matrix.
  **Nothing paired the roadmap with its own site pages** — the one edge
  of that triangle that was missing.

The site drifting from a Markdown document it mirrors is the *second*
occurrence of one shape. 0.2.1's was `capabilities.html`, and its
countermeasure was the row-count check — aimed at capabilities alone.
Per `CLAUDE.md`'s same-place rule this got the same countermeasure aimed
at the pair it missed: `check_site_links.rb` now compares roadmap items
per version, in both languages. It found both drifts the moment it was
written, before it was ever run in anger.

`DOCUMENTATION_MAP`'s own tail claimed in one section that the site is
machine-checked and in another that "the site has no parity guard".
Both are corrected, in both languages.

## Round 33

One reviewer, hunk sweep as the starting point rather than end-to-end
driving — the angle round 32 had not used. Twelve findings, and the
distribution is the interesting part: **one user-visible regression, one
tool bug, two documentation errors, and eight behaviours with no test
that fails when they are reverted.** A sweep is very good at the last
category and round 32's method found none of them.

| what | where it went |
|---|---|
| A `didChange` whose text is unchanged discarded the previous edit's pending report | fixed in `#reindex`; `server_publish_invariant_spec` |
| `DEFAULT_DIAGNOSTICS_DEBOUNCE` unpinned below 0.3 — `0.002` left 1,929 green | bounded at both ends |
| `ABSENT_WORKSPACE_GRACE` unpinned below 31 days — one second left the file green | a 29-day example |
| The cache sweep's `track_thread` unpinned — removing it left the suite green | a reclaim example |
| `cancel_pending_diagnostics` unpinned; round 32's fixture could not distinguish it | a second fixture, held at the write |
| Both `extension.ts` note loops unpinned — nothing in `vscode/src/test` reaches that file | formatting moved to `versionInfo.ts`; five tests |
| The supersession latch unpinned — `first = true` left the burst example green | a waiter count |
| `flush_pending_diagnostics` additive with the budget that follows it, and its call-site comment inverted | the join removed; a bound on the whole of `#run` |
| A `Thread.new` failure in `build_cache_store` cost the entire session's cache | rescued separately from the store |
| `roadmap_items`' `.compact` on the wrong side of the Hash | `filter_map` on the sections |
| The changelog sold a 0.2.2-introduced hazard as a fixed pre-existing bug | reworded, in both languages |
| EN and JA gave different figures for one measurement (a minute vs 30 s; measured 26 s) | 26 s everywhere, including the code comment |

## Round 39

Nine findings. The one that matters, and the rule it fires:

**Three rounds running found the same place: one bad entry abandoning the
whole cache sweep.** Round 37 found it in `prune_workspaces` (an
unreadable sibling), round 38 in `prune_generations_of`'s sort (a
generation vanishing mid-walk), round 39 in the same method's *removal*
(a generation that cannot be removed at all). Each of the first two was
fixed where it was found, which is exactly what `CLAUDE.md` says buys a
third: "not a third hand fix... something that makes that class of defect
fail a check rather than wait for a reviewer."

**The rule's remedy is a rollback, and here it would be wrong, so it was
not done.** The thread those rounds produced *is* the containment and
tolerance that stops the sweep destroying or abandoning things; reverting
it restores the abort. What was actually wrong is smaller and stated in
the rule's own words: tolerance was being added per call site, one reader
at a time, while containment already lived in one place. So tolerance
moved to join it — `remove_within` now swallows `SystemCallError`, and
there is no second site left to get it wrong. Recorded here rather than
done quietly, because a rule set aside in silence stops being a rule.

The rest:

| what | outcome |
|---|---|
| An unremovable generation permanently pins every older one | fixed in `remove_within`; the abort mechanism was verified directly after two end-to-end fixtures failed to distinguish |
| `spec_constants_spec`'s Prism walker had no test — blinding it left the guard green | pinned with the two bugs its previous implementations had |
| `age_for_sort`'s fallback direction unpinned; the example asserted a count both directions satisfy | asserts *which* generation survives now |
| `VendorBootstrap` took a RubyGems dependency (`Gem.ruby_engine`) it does not need | `RUBY_ENGINE` |
| 024.65 described the reverted code in the present tense | past tense, and says so |
| `incompatibilityReason` left with one caller by the revert | kept, with the reason written down |
| The new CI step took the previous step's comment | comment restored to its own step |
| 028's "state of the checks" said the decisive suites cannot run here — they do | superseded note, original left visible |
| `SUPPORT_MATRIX`'s 3.4 row named two Rubies nobody has run beside a machine-checked count | provenance corrected, and the row now says which half is enforced |

**Two fixtures I wrote did not distinguish the behaviours they were
supposed to pin**, both on the sweep, and both were caught by
reverse-applying rather than by reading. One had the blocked directory
end up newest, where it is never a removal candidate; the other asserted
a count that both candidate behaviours satisfy. That is the same fixture
defect `CLAUDE.md` names, twice in one round, from the person applying
the rule.

## Round 40

Eleven findings. Fixed: the Output channel said the same thing twice on a
Ruby the payload was not built for (the handshake is the single writer
now); the changelog bullet that made 024.65 read as a clean distinction;
`VendorBootstrap`'s redundant `Dir.exist?` guard, which no fixture could
distinguish and which is now deleted rather than pinned; `AGENTS.md`
naming 027 as the current loop file and keeping a five-item list that had
drifted out of agreement with `CLAUDE.md`'s; a packaging comment citing
the old glob; the CI comment whose other half round 39 restored; and the
Ruby 4.0 claims in four documents.

**The 4.0 job could not tell green from never-ran.** `continue-on-error`
makes it green whatever happens, and it emitted no count — so three
published documents cited it as continuous verification of something
nobody could confirm had executed. It reports its example count now and
fails on zero. Non-gating was a decision about *failures*; it was never a
decision to accept nothing.

Recorded rather than fixed: **024.64 gains a second file.** `bin/ovallsp`'s
call into `VendorBootstrap` is unpinned for the same reason
`extension.ts`'s wiring is — nothing CI runs executes it. Reverting the
script to `main`'s glob leaves the suite green.

### The count is not falling, and that is the thing to look at

| round | findings |
|---|---|
| 37 | 8 |
| 38 | 12 |
| 39 | 9 |
| 40 | 11 |

`CLAUDE.md` says a falling count proves little; a flat one over four
rounds says something specific. Several findings each round are about the
previous round's own fixes: 40's duplicate Output note came from 38's
revert, 40's CI comment from 39's half-restore, 40's 4.0 row from 39
correcting the row above it. That is 0.2.1's pattern — nine rounds, seven
finding the previous round's code — and 0.2.1's recorded answer was not
more rounds. It was to cut the change set back.

This release is 61 files. Nothing in it is a capability added mid-loop,
so the "fix; do not add" rule is being kept; what is growing is the
*repair surface*. That is a scoping question and it belongs to the
maintainer, which is why it is written here rather than acted on.

## Round 39, first attempt — stalled, not clean

Launched and died with no findings: the agent printed one line ("let me
establish the baseline by running the suites") and produced nothing for
ten minutes, at which point the harness gave up. **No reviewer read any
of this change set.**

Recorded because this has happened before, to the first attempt at round
37, and the record then said the thing worth repeating: *do not read the
absence of findings as a clean round; it is an empty one.* Two empty
rounds and a loop that stops when the count reaches zero would ship on a
harness failure.

The likely cause is cost rather than anything about the tree: the full
Core suite takes about three minutes and the real-Rails suites another
three, and a reviewer that opens by running both sequentially can exceed
a no-output watchdog before it has said anything. The relaunch says so —
which is guidance about how to avoid a timeout, not about what counts as
a finding, and the finding scope is unchanged.

## Round 37

One reviewer, round-one instructions: no exclusion list, no "concentrate
on X", no "a clean report is expected", the corpus list given as a record
of what was measured and at which revision. Eight findings, ranked by
what a user would see.

| what | outcome |
|---|---|
| The new `site-parity` job is red at HEAD; both index pages say 0.2.1 and neither roadmap page has a 0.2.2 panel | fixed |
| Ten user-facing documents credit this release's changes to 0.2.2 | fixed |
| `extension.ts`'s handshake wiring still unpinned — rounds 33, 36, 37 | **024.64**, re-scoped |
| One unreadable sibling scope aborts the whole cache sweep | fixed, with a fixture watched failing at 11 generations |
| `documented_counts_spec`'s new guard cannot fail | fixed, watched failing with a planted file |
| Two register entries numbered `024.60` | fixed, plus the guard that would have caught it |
| The sweep-start rescue and two other lines survive reverse-application | open, below |
| Two error toasts on a non-CRuby engine | **024.65** |

**Round 36's countermeasure was red at HEAD.** The roadmap-panel check
was written as the mechanical countermeasure the same-place rule asks
for, and never run against the tree before committing. A countermeasure
nobody ran is indistinguishable from none, and it is worth saying plainly
because this loop has now produced two of those in a row — round 36's
check, and round 36's claim about `extension.ts` that round 37 disproved
in one edit.

**The third hit fired the rule.** 024.64 records the root cause: both
countermeasures moved code *out of* `extension.ts`, which pins the code
and never the wiring, because nothing CI runs executes `activate()`. The
entry is the deliverable; the direction needed is a seam or an
integration job, and neither belongs in a loop.

### Still open after round 37

- **024.64**, **024.65** — recorded, re-scoped.
- **Three unpinned lines.** `server.rb`'s sweep-start `begin/rescue`
  (round 33's fix) survives reverse-application with the whole suite
  green; `hierarchy_index.rb`'s `@signatures` is assigned and read
  nowhere; `store.rb`'s `seconds_since_write` fail-safe has no coverage
  in either direction. Each is a defect by this repository's own rule and
  none was fixed in this pass.
- **`ruby@4` is not installed on this machine**, so 028's Ruby 4.0 claims
  were not re-measured by this round.

## Round 36 — the rollback reviewed as a change

The reviewer was told explicitly that a rollback is a change like any
other and that a half-removed one is a defect. That framing produced the
round's two best findings, and both were things a diff review would not
have raised.

**The rollback was half-removed in four user-facing documents.** Both
`KNOWN_LIMITATIONS` still said "0.2.2 made diagnostics wait 300 ms after
you stop typing" in the 024.41 paragraph, 244 lines above a new paragraph
saying the opposite; both `ROADMAP`s said "0.2.2's debounce delays it".
`dea715c` edited `KNOWN_LIMITATIONS` and missed a second paragraph inside
it, and did not open `ROADMAP` at all. `deferred_findings_spec`'s
`wrongly_documented` guard cannot see this: it checks `status`, and these
are paragraphs about an entry that is *still open* whose text describes
reverted code.

**The hunk kept from the rolled-back thread was wrong to keep.** 0.2.2
published outside `if apply_file_summary(...)` and kept that when the
debounce went, reasoning that it fixed the synchronous path too.
Measured, on a 2,574-line file: a byte-identical `didChange` went from
0.015 s to **2.098 s**, under the lock hover and completion need, with
the control (text genuinely changed) agreeing to 1%. On the synchronous
path there was nothing to fix -- the panel keeps correct diagnostics for
identical text, and the only difference is a `version` field VS Code does
not read. It is reverted, and `server_publish_invariant_spec` is restated
about the document's *text* rather than its version, which is the claim
the server actually needs to make and still fails on round 33's defect.

That is worth naming as a rule: **"kept deliberately" needs the same
evidence as "changed deliberately".** The keep was a judgement made while
removing something else, and nothing measured it.

| the rest | where it went |
|---|---|
| `spec_constants_spec` blinded by any `class X; end` in a fixture, plus one live false positive | rewritten on Prism; both directions checked by hand |
| `check_site_links.rb` only runs on `site/**`, so the drift it exists for never triggers it | now a `ci.yml` job, unfiltered |
| The roadmap check was one-directional; the site's shipped panel had no 0.2.1 | paired against the changelog's released versions; the panel added |
| `UNMARKED_SCOPE_GRACE` unpinned (1 second left the file green) | a half-hour example, aged absolutely |
| `mark_workspace` before `Store.new` unpinned | pinned by call order |
| The `extension.ts` note loops still unpinned — round 33's finding, one level out | the *condition* moved into `writeHandshakeLines`, so the mutation cannot be written at the call site |
| 024.52/54/56 describing code that no longer exists | corrected; 024.56 retitled and re-scoped to the republish path |
| The republish race was in no user-facing document | `KNOWN_LIMITATIONS`, both languages |

### The one that is still open

`024.56` is real, reproduced twice, and **every build has it** including
0.2.1 — `#republish_open_diagnostics` publishes without re-reading the
store, so a `didClose` during that pass leaves errors in the panel for a
file nobody has open. It is documented now rather than fixed, because the
fix is 024.57's direction (one writer that remembers what it last
published) and that is the change 0.2.2 just rolled back a thread for
attempting piecemeal.

## Round 35, and the rollback

One reviewer, method left to it. Seven findings, and two of them ended
the debounce:

- **`#republish_open_diagnostics` has round 32's race** — it snapshots
  the open documents and publishes without re-reading the store, so a
  `didClose` for one file lands between another's analysis and its write.
  Reproduced three times identically. Round 32 fixed that symptom on the
  waiter path and left the older path alone, and the invariant spec
  written as the countermeasure has no row containing a republish.
- **The debounce cannot bound concurrent analyses of one file.** The
  pending slot is released before the analysis, so the next edit starts a
  second waiter while the first computes. Measured: 12 edits 0.4 s apart
  → 12 analyses, 5 concurrent; 32 edits → 32 analyses, 13 concurrent.

`CLAUDE.md`'s same-place rule fired. Four rounds in a row had found a
defect in one change, and the countermeasure round 33 put in was aimed at
the symptom — it pins the property for the *debounce* path and the same
property was being violated by a different publisher the whole time.

**The maintainer chose to follow the rule.** The whole thread was rolled
back and `024.57` written: the root cause is that four publishers write
to one stream and ordering was added pairwise at call sites, one round at
a time; the direction needed is one writer that remembers what it last
published for each uri, plus a deferral that holds its slot until the
publish completes.

Round 35's other five findings stand on their own and were fixed:
`spec_constants_spec` could not see constants one nesting level in
(including one in the file it was written for), `mark_workspace`'s
`mkdir_p` was unpinned, its comment claimed to close a window it only
narrows (now closed by `UNMARKED_SCOPE_GRACE`), a dead `ThreadError`
clause, and a claim about `KNOWN_LIMITATIONS`' own history that was wrong
in both languages.

### What 0.2.2 is, after the rollback

The release it was originally about: the toast 0.2.1 did not remove, the
cache sweep blocking the first launch after an upgrade, the absent-
workspace grace, the engine split between the two version deciders, and
the documents those changes made stale — plus six mechanical
countermeasures and one property spec that outlived the change it was
written for.

`024.45` goes back to open and unaddressed. `024.41` stays retargeted to
0.4.0, because that was a re-classification and not a fix.

## Round 34

One reviewer, method left to it; it chose end-to-end driving plus a walk
of the documents. Ten findings, and the two that mattered most were
**documents describing behaviour that has never existed**, not code.

| what | where it went |
|---|---|
| Four documents promised OvalLSP *stops* on a version mismatch; it reports and carries on | documents corrected; the behaviour is 024.55 |
| `KNOWN_LIMITATIONS`' re-analysis paragraph still described the per-keystroke cost this release changed | rewritten in both languages |
| An engine mismatch was fatal in one decider and probed-and-forgiven in the other | fatal in both; pinned |
| `SOURCE` collided between two spec files, so one silently used the other's fixture | renamed; `spec_constants_spec` now fails on any such pair |
| `@publish_threads` written in four places and read by none | removed, with the three comments describing its reader |
| The 50 ms sleep cap unpinned — `sleep(done.last)` left 1,945 green | pinned, with a rendezvous, because the first attempt passed either way |
| Four comments still describing the dispatch thread doing what a waiter now does | corrected |
| `prune_workspaces` could delete another window's scope in the gap before its marker is written | the marker is written first |
| `RELEASE_CHECKLIST` row 2 still said an unmatched Ruby is refused before start | corrected |
| A debounced publish can overwrite the Agent's route republish | 024.56 |

### A document can be wrong about behaviour that never existed

`024.55` is the one worth reading twice. Four documents — both
getting-started pages, both Marketplace READMEs — said OvalLSP "stops
before sending any feature request" on a version, protocol, build or
payload mismatch. `.stop(` occurs once in `extension.ts` and it is inside
a comment. So a Core whose *payload hash does not match* — a corrupted or
tampered build — has always gone on answering hover and completion, while
the page the user was sent to said they were protected from exactly that.

Not a regression, and not something a review of the diff would find:
nothing in this release touched it. It surfaced because
`DOCUMENTATION_MAP`'s Ruby/platform row names those pages and
`versionInfo.ts` was changed, so a reviewer walked the row and read what
was on the other end. **That is the argument for the map being a
checklist rather than a lookup**: the pages it sends you to are worth
reading even when your change is not about them.

0.2.2 corrected the documents and left the behaviour to `024.55`,
because stopping has a failure mode of its own — a false positive locks
the user out of the extension entirely, and this project has shipped a
version check that was wrong about a working combination twice.

### Two spec files, one constant

`SOURCE` in the new debounce spec landed on `Object`, where
`server_receiverless_spec.rb` had already put one. Files load in sorted
order, so the debounce spec ran two review rounds against *another
file's* heredoc, and the only symptom was a Ruby warning nobody read.

Second occurrence: `server_declaration_position_spec.rb` did the same
thing earlier in this release and was caught only because it happened to
break an example outright rather than quietly swap a fixture. So
`spec/meta/spec_constants_spec.rb` now fails on any constant defined in
more than one spec file.

### The debounce, found twice in a row

`CLAUDE.md`'s same-place rule fired for the first time in this release.
Round 32 found `didClose` racing an in-flight publish; round 33 found a
no-op `didChange` discarding a pending one. Different bugs, same three
pieces of state disagreeing — `@pending_publish`'s captured version,
`@document_store`'s current one, and whether a waiter is alive to
reconcile them.

The countermeasure is `server_publish_invariant_spec.rb`: one property —
*an open document's last published diagnostics are for its current
version, and a closed document's are empty* — asserted over a table of
notification sequences. A regression test pins the sequence someone
thought of; this pins the property, and the next defect adds a row.

`flush_pending_diagnostics` was the *second* place found twice: round 32
found a per-thread deadline, round 33 found the shared one that replaced
it still additive with `BackgroundTasks#shutdown`'s. Both were the same
mistake — those threads are tracked, so something already joins them
correctly and a second joiner had nothing to add but its own budget. The
method now only releases the waiters, and the countermeasure is a bound
on the whole of `#run` rather than on any one join: five hung waiters
against a 0.2 s background budget, asserted under 1.5 s. Both rounds'
shapes were checked against it by hand and both fail it.

### What round 33 raised and 0.2.2 did not do

- **`@pending_publish_mutex` is held across `publish_findings`**, which
  writes to the client. A background waiter blocked in `@output.write`
  holds it, and the next `didClose` blocks the dispatch thread on
  `cancel_pending_diagnostics`. Left alone deliberately: the check and
  the write have to be atomic against the close, and a stalled stdout
  already stalls the dispatch thread on its own writes.
- **`seconds_since_write`'s `rescue StandardError; 0`** — the fail-safe
  "keep it" branch — has no coverage in either direction.

### What round 32 raised and 0.2.2 did not do

- **`await_and_publish`'s `return if stopping` is still not pinned by
  outcome.** Removing it fails nothing: the flush and then
  `BackgroundTasks#shutdown` kill the thread anyway, so it is pinned in
  wall-clock duration only. It was rewritten in this round (the abandon
  branch now also releases the latch, which *is* pinned), but a spec that
  fails when the early return goes does not exist.
- **Nothing bounds the number of scope directories** now that absent ones
  survive for thirty days. `DEFAULT_MAX_GENERATIONS` bounds generations
  within one workspace and nothing bounds workspaces. Before 0.2.2 an
  absent workspace's scope was removed whole on the next sweep, so the
  question did not arise. A machine that opens many short-lived
  workspaces accumulates up to thirty days of them.

## What is left of round 31 that has no entry

- **`#bound_prefix_before?` still carries its own copy of the name-start
  rule**, without the `word_char?(text[left - 2])` guard that
  `#name_start_offset` has. No input was found where the two disagree —
  it is called at a `(` offset, where the unguarded step is what the
  `!`/`?` case needs — so it is a claim the code does not literally
  support rather than a defect. The countermeasure's premise is "one
  place decides"; there are still two.
- **`scripts/corpus_diagnostics.rb` was fixed to pass `signatures:`**,
  which means **every figure quoted before 0.2.1's last day was measured
  against a configuration no user runs**. The numbers in the 0.2.1
  changelog were taken before that fix. They are not known to be wrong —
  the shadow rule was reverted out of resolution too, which is the same
  direction — but the first corpus run of 0.2.2 should re-establish a
  baseline rather than diff against a recorded one.

## Measurement, before anything else in this release

The baseline to compare against is **0.2.1's tag**, not `main` at some
later commit, and it must be re-taken with the fixed
`corpus_diagnostics.rb`. `CLAUDE.md`'s measurement section carries the
five ways this release got it wrong; the short form is: one run at a
time, in the foreground, each side printing its own directory and
version, with a control category in the diff that the change cannot
affect.

### Taken 2026-08-11: the engine's answers are unchanged

**v0.2.1 against 0.2.3, and the two sides are identical.** Zero reports
introduced, zero removed.

| | baseline | current |
|---|---|---|
| revision | `v0.2.1` (worktree) | `8a3350e` (fix/0.2.3) |
| version printed by the run | 0.2.1 | 0.2.3 |
| working directory printed | `…/scratchpad/baseline/core` | `…/OvalLSP/core` |
| total reports | 11,527 | 11,527 |
| `unresolved-constant` (control) | 9,495 | 9,495 |
| `unknown-method` | 2,020 | 2,020 |
| `argument-count` | 12 | 12 |

Corpus, identical on both sides, given as a record of *what was measured
and when* rather than as a list to avoid: Ruby 3.4.10's standard library
(`/opt/homebrew/Cellar/ruby@3.4/3.4.10/lib/ruby/3.4.0`) and the `lib`
directories of `actionpack`, `activemodel`, `activerecord`,
`activesupport` and `railties` 8.1.3.1 and `minitest` 5.25.4. Note the
Ruby is 3.4.10 where earlier rounds used 3.4.7, so absolute counts here
are not comparable with the figures 0.2.1's changelog quotes — only the
two sides of *this* diff are comparable with each other, which is what a
baseline is for.

**Identical is the expected answer, and it was still checked rather than
assumed.** The engine differs across these revisions in three files, but
every hunk is a comment or the deletion of `TypeNameResolution.canonical`,
which had no caller. Nothing was expected to move.

`CLAUDE.md` names "the two sides came out identical" as one of the ways a
measurement in this project has been wrong before — both sides had been
running the same tree. So a **positive control** was run rather than
trusting the printed version strings: `code: "unknown-method"` was
changed to `"unknown-method-CONTROL"` in the *baseline worktree only*,
and both sides were re-run over `minitest`. Baseline reported 3
`unknown-method-CONTROL`; current reported 3 `unknown-method`; the
`unresolved-constant` control stayed at 112 on both. Each side therefore
runs its own tree's engine, and the identical full-corpus result is a
fact about the code rather than an artefact of the harness. The
perturbation was reverted and the worktree confirmed clean.

What this settles for the release: **0.2.3 changes nothing the engine
reports.** Its user-visible content is the extension-side toast, the
background cache sweep, the grace periods, and documentation — all of
which this measurement is blind to by construction, and none of which it
needed to see. What it rules out is a silent diagnostics regression
riding along with them.

## A correction worth repeating, not just recording

`024.47` described the *opposite* of the defect the release actually has,
in the register and in both languages' `KNOWN_LIMITATIONS`, from the day
it was written until 0.2.2 checked it. The entry was written mid-loop,
the behaviour it named was reverted three commits later as part of
returning 0.2.1 to its scope, and nothing went back to the entry.

A published limitation naming a defect the release does not have is worse
than no limitation at all: it sends a reader looking for something that
is not there, and it hides the one that is. The lesson is narrower than
"keep documents current" — it is that **a revert has the same
documentation trigger as a change**. `DOCUMENTATION_MAP`'s table is
written in terms of what you changed; reverting is changing.

# Task 028: 0.2.3 — the cleanup 0.2.1's revert owed, written from measurement

The work that was being prepared as 0.2.2 before the cache sweep
incident (`027-0.2.2.md`), renumbered. Its opening rule is inherited
from 026 and 027: **anything not fixed exists only in this file.**

027 deferred three things here: "The 0.2.1 documentation cleanup, the
register entries and the hover/completion countermeasure." The first
two ship in this release. The third is re-scoped by this release's own
record — see "The countermeasure moved again, and why" below — because
evaluating it concretely showed the recorded costing was wrong in every
line, and the roll-back rule's step 3 ("the problem goes to its own
release or its own task") already named the destination.

## What this release is

A patch in `PUBLISHING.md`'s sense: nothing new is announced, and every
change makes the tree or the published record say what is actually
true. No engine behaviour changes. The one spec that changed behaviourally
(`store_spec`'s unusable-directory example) tests the same property
through a fixture that cannot be defeated by the runner's uid.

## Scope was confirmed against the tree, not against the documents

Established before writing anything (the maintainer's standing
instruction: no guarantee existing implementation or documents are
correct). Three published documents said things the shipped build does
not do — the limitations document, 0.2.1's release notes, and the
site's version badge, which has its own section below — and a fourth,
`CLAUDE.md`, promised a register list that did not exist. The first
two and the fourth, in detail:

- **`KNOWN_LIMITATIONS` (both languages) described the opposite of the
  shipped behaviour** for a namespaced class named after a core class:
  it said hover/definition/completion stopped answering in 0.2.1. Run
  against this tree with controls, all three answer — the paragraph
  described the mid-loop resolution-side arrangement that `ea6cc80`
  rolled back before 0.2.1 shipped. What the shipped build actually
  does, and the paragraph never said: **diagnostics are silently
  withheld for such receivers** — `r.tagg` on a `Billing::Range` is
  never reported while the identical typo on a `Pricing::Tariff` is.
  Both files rewritten from the measurement; 024.47 records it.
- **The 0.2.1 changelog carried two bullets under one heading
  contradicting each other** about the completion half of the shadow
  rule, in both languages. The bullet claiming the reverted change as
  shipped ("no longer answers for the core one in **completion**") was
  false; the bullet saying completion behaves as 0.2.0's did was true.
  The false bullet is deleted in both languages; this release's
  changelog entry carries the notice, and the true bullet — which
  already pointed at 024.47 — stands.
- **`CLAUDE.md` promised "024.47 records the full list" of the revert's
  leftovers, and 024.47 contained no such list.** It does now, and the
  list is longer than `CLAUDE.md`'s own inventory said ("three
  comments" — a re-grep found six, plus an inert constructor
  parameter). The `CLAUDE.md` paragraph is corrected to match.

## What this release changes

| what | where |
|---|---|
| The unusable-cache-directory example constructs its unusable path (a regular file as parent, inside `mktmpdir`) instead of assuming `/` is unwritable — under root the old fixture created directories at the filesystem root and failed against correct code | `core/spec/ovallsp/cache/store_spec.rb` |
| Dead `TypeNameResolution.canonical` removed; header rewritten to describe the one live reader (the diagnostics engine) and the rolled-back placement | `core/lib/ovallsp/index/type_name_resolution.rb` |
| Inert `signatures:`/`@signatures` removed from `HierarchyIndex`, with its three construction sites | `semantic/hierarchy_index.rb`, `server.rb`, `scripts/corpus_diagnostics.rb`, `spec/.../query_service_spec.rb` |
| Stale comments describing the reverted arrangement as current: four rewritten to the shipped one; two deleted with the dead code they described | rewritten: `engine.rb` (`shadowed_declared_type?`), `type_name_resolution.rb` (header), `scripts/corpus_diagnostics.rb`, `CLAUDE.md` (countermeasure exemplar); deleted with their subjects: `hierarchy_index.rb` (`initialize`), `server.rb` (`@signatures` note) |
| `activeParameter` deferral comment named the wrong release (0.3.0; the roadmap and README say 0.4.0) | `server.rb` |
| Dead e2e client helper from the same rollback's capability removal | `core/spec/e2e/lsp_client.rb` (`document_highlights`) |
| False 0.2.1 changelog bullet deleted, both languages | `vscode/CHANGELOG.md`, `vscode/CHANGELOG.ja.md` |
| `KNOWN_LIMITATIONS` shadow-class section rewritten from measurement, both languages, with a note saying what it used to claim | `docs/KNOWN_LIMITATIONS.md`, `.ja.md` |
| 024.47 rewritten: corrected example, the revert-leftover inventory `CLAUDE.md` points at, the re-costed direction, the re-scoping | `docs/design/tasks/024-deferred-review-findings.md` |
| Site version badges said `Preview 0.2.1` against a 0.2.2 `package.json` — `check_site_links.rb` was red on `main` | `site/index.html`, `site/ja/index.html` |
| The site check now runs where the input that invalidates it changes (see below) | `.github/workflows/ci.yml`, `.github/workflows/pages.yml` |
| Version 0.2.2 → 0.2.3 in the four version files, changelog entries in both languages | per `DOCUMENTATION_MAP.md`'s version row |
| `TypeNameResolution.substitution?` gets the direct unit spec it never had, pinning the qualified-name contract a full-suite sweep showed nothing else could see (round 1) | `core/spec/ovallsp/index/type_name_resolution_spec.rb` |
| The site/version guard's wiring pinned, both halves watched failing against their deletion (round 1) | `core/spec/meta/site_version_guard_spec.rb` |
| `corpus_diagnostics.rb`'s `MethodAnalyzer`/`LocalInferencer` constructions matched to `Server#initialize`'s — `model_registry:`, `generated_method_index:`, `observation_store:` — with a one-file A/B confirming byte-identical output (round 1) | `scripts/corpus_diagnostics.rb` |
| `DOCUMENTATION_MAP`'s version row names the site badge and its checkers; the "found by a person, nothing would have caught them" list stops naming the badge; EN/JA aligned (round 1) | `docs/DOCUMENTATION_MAP.md`, `.ja.md` |
| 024.48 re-tensed against the tree 0.2.3 leaves behind; 024.47's inventory gains the seventh leftover (round 1) | `docs/design/tasks/024-deferred-review-findings.md` |
| `AGENTS.md`'s current-loop pointer moved off "026 for 0.2.1"; a JA site sentence that stated the tested/verified distinction as the same predicate twice fixed (round 1) | `AGENTS.md`, `site/ja/getting-started.html` |
| The dated guard narrative deduplicated into this file; shipped comments keep the ageless mechanism and a pointer (round 2), and the record itself states witnessed events only (round 3, 024.49) | `.github/workflows/ci.yml`, `.github/workflows/pages.yml`, `core/spec/meta/site_version_guard_spec.rb`, this file |
| The deploy gate pinned — the check job runs the script and the deploy `needs` it (round 3) | `core/spec/meta/site_version_guard_spec.rb` |
| `corpus_diagnostics.rb`'s `SemanticContext` gains the `ancestry_registry:` the server passes; A/B byte-identical (round 3) | `scripts/corpus_diagnostics.rb` |
| A map sentence trimmed to what the base revision supports, and a JA hover sentence names its expression (round 4) | `docs/DOCUMENTATION_MAP.md` + `.ja.md`, `docs/KNOWN_LIMITATIONS.ja.md` |
| A second dead e2e client helper removed (`stderr_output`), then its dangling write removed with it — the drain keeps no state (rounds 5–6) | `core/spec/e2e/lsp_client.rb` |
| "Six comments rewritten" recounted as four-rewritten, two-deleted-with-their-subjects, in the four places stating it (round 5) | `024-…md`, both changelogs, this file |
| The literal-side sentence re-bound to the class that shares the literal's name — `(1..5).` pays the cost under a `Billing::Range`, `"hello".` does not — in both limitation documents, both changelog bullets, and 024.47 (round 6) | `docs/KNOWN_LIMITATIONS.md` + `.ja.md`, both changelogs, `024-…md` |
| The supported-Ruby row's attestation decoupled from any one preparation machine; the 3.4.x runs named as the release PR's CI and the pre-publish gate (round 6) | `docs/SUPPORT_MATRIX.md` + `.ja.md` |

## The countermeasure moved again, and why

027 deferred "the hover/completion countermeasure" to this release. The
concrete candidate was 024.47's second shape — let the ancestor chain
hold both the workspace class and the RBS type — whose recorded cost
was "one spurious completion candidate on a literal".

It was evaluated against this tree before being built into it: a
monkeypatched simulation of the twin chain, plus a reading of every
engine check that consumes the chain. The recorded costing understated
every line of the bill:

- `"hello".upcase(:ascii)` — legal Ruby — becomes an `argument-count`
  false positive whenever the shadow class declares its own `upcase()`:
  `sole_source_declaration` keeps each source's answer singular while
  the receiver's identity is not, and `sole_declared_overload` has the
  symmetric RBS-side family. This is the exact false-report class 0.2.1
  reverted `self` inference over (024.46).
- A written `Billing::Range` gains the whole core `Range` API as
  spurious completion candidates — the "one candidate" costing was
  written from the literal side only.
- One hover popup mixes identities: an RBS overload label over a
  workspace `Defined:` link. The readers stop disagreeing about
  membership and start disagreeing about identity.
- Making literal receivers closed re-opens 024.13's family — `"".squish`
  under ActiveSupport-style direct additions becomes `unknown-method` in
  any workspace containing a shadow class.
- And the project's usual arbiter cannot arbitrate: gems that reopen
  core classes do it at the *top level*, where `substitution?` never
  fires, so a corpus diff is blind exactly where collisions live. If
  this shape is ever built, constructed fixtures watched failing are
  the gate, not corpus deltas.

024.47's own sentence — "neither is a patch" — was already the recorded
judgment, and the roll-back rule's step 3 already re-scoped this
problem once. Attempting it inside the release that owes the
documentation cleanup would have held the urgent half (published
documents that are false today) hostage to a review loop on the risky
half. So: the fix goes to its own task, with the honest bill recorded
in 024.47; this release delivers the record, not the code. What this
release *does* carry as a mechanical countermeasure is the site/version
guard below.

## A guard that could not see its input

`check_site_links.rb` compares the version the site's index pages
advertise against `vscode/package.json` — and it ran only from
`pages.yml`, which triggers only on `site/**` changes. A release that
bumps `package.json` without touching `site/` (0.2.2, whose review gate
was waived) never runs the check. The countermeasure is the same shape
as `024.41`'s: give the guard the input it could not see. The check now
also runs in `ci.yml` — on every pull request and every push to `main`,
which is what that workflow triggers on — and `pages.yml` additionally
triggers on `vscode/package.json` so the deployed badge refreshes with
a version bump instead of waiting for the next site edit. Round 1 of
the review loop found the wiring itself unpinned — deleting either half
left every check green, the exact situation `ci_skip_guard_spec.rb`
exists for — so `core/spec/meta/site_version_guard_spec.rb` now pins
both halves, each watched failing against its deletion.

**The timeline, as witnessed events only** — rewritten a third time in
round 3, and the third rewrite changed the *shape* rather than the
numbers, because the numbers kept failing review (024.49 records why).
Every statement below is an event with a timestamp, from GitHub's push
and deploy history or this repository; no durations of conditions this
record cannot watch end:

- 2026-08-06: 0.2.1 merged to `main` (84c7dcb; the push history is
  then empty until the 16th); `pages.yml` deploys the site for what
  is, as of this record, the last time. Badge and `package.json` both
  say 0.2.1; the check is green there — reproduced from an archive.
- 2026-08-11: the 0.2.2 VSIX is published to the Marketplace (027).
  `main` contains no trace of 0.2.2, so no in-repo check could see
  anything about it — a publish-before-push gap in the release process
  itself, which wiring inside the repository cannot reach, recorded
  rather than papered over.
- 2026-08-16T03:55–04:00Z: 0.2.2's three commits land on `main` with
  no site change. From that push the check is red on `main` —
  reproduced from an archive of c4c0be2 — and all three pushes went
  through green CI while it was red, which is the mechanism this
  guard closes.
- 2026-08-17T00:07Z: this branch carries the badge fix (3f9babd), and
  the check is green at that very commit — measured from an archive of
  it in round 5 — and at every branch state since. `main` stays red
  until this release lands on it; that end is not this record's to
  date.

The same-place rule ran its full course on this narrative: round 1
corrected its trigger claim, round 2 its duration attribution — with a
dedup countermeasure that did stop the four-way copying — and round 3
found the deduplicated text still asserting a completed duration for a
condition that had not ended. Third finding, same place: per the rule,
the thread is rolled back rather than hand-fixed again. What was rolled
back is the claim *shape* — durations and completions are gone;
witnessed, timestamped events remain — and 024.49 is the entry the
rule requires, naming the three attempts and why each was the wrong
shape.

## Out of scope, deliberately

- **024.42 and 024.43.** Both fit a patch on `PUBLISHING.md`'s terms
  (S2's row carries no receiver qualifier, so both are the extension
  not doing what the row already claims). They are excluded on scope
  discipline — 0.2.1's lesson about change sets that grow — and because
  each touches a resolution path this release otherwise leaves alone:
  024.42 changes the shape a signature is stored in (its entry's own
  structural clause), 024.43 the receiverless lookup chain. Their
  entries and `KNOWN_LIMITATIONS` paragraphs stand; nothing is carried
  on memory.
- **024.41, 024.44, 024.45.** Their entries already say "their own
  task" / "not a correction" in terms.
- **The GitHub Security Advisory for the 0.2.2 incident.** Still the
  maintainer's call, exactly as 027 left it.
- **024.48 stays in the register** (fixed entries are kept; 024.47
  cites it by number, and the register's own legend forbids deleting a
  cited entry).

## Verified

Environment note, because it differs from the maintainer's: this
release was prepared on x86_64 Linux, Ruby 3.3.6, running as root in a
container whose `ps` reports pgid/sid 0 for every process. Three
environment-specific results follow from that and are *not* defects in
the tree — each was cross-checked against CI on `main` (green at
`c4c0be2`):

- `vscode` unit: 174 of 175 pass locally; the process-group
  termination test needs a kernel whose `ps` reports process groups.
  CI (`ubuntu-latest`) runs it and is green.
- The old `store_spec` fixture *failed* here (and littered `/`) —
  which is the finding, not an obstacle: root is exactly the uid the
  fabricated-path assumption breaks under. The rewritten example
  passes under every uid.
- `test:integration:packaged` and VSIX packaging are Apple Silicon
  steps and were not run here; they remain on the maintainer's
  pre-publish list (`PUBLISHING.md`), along with recording the
  artifact's SHA-256 in `docs/RELEASE_ARTIFACTS.md` once published —
  the step `release_artifacts_spec` audits per tag — and the full
  suite under Ruby 3.4.x, the venue `SUPPORT_MATRIX`'s supported row
  attests: this branch was prepared under 3.3.6, so the 3.4 runs are
  the release PR's CI matrix and the maintainer's gate
  (`RELEASE_CHECKLIST` #1).

The record, all on this machine unless stated:

- `bundle exec rspec` (core, full): 1,917 examples, **1 failure at the
  0.2.2 baseline** — the `store_spec` fixture above, i.e. the tree's own
  defect — then **1,917 examples, 0 failures** on the change set as
  first pushed. Round 1 added two spec files (seven `TypeNameResolution`
  examples and three wiring examples, each run and green on its own),
  and the three documents `documented_counts_spec` audits moved to
  1,927 with them (then to 1,928 with round 3's fourth guard example);
  each figure's confirming full-suite run is recorded in its round's
  note below.
- `spec/e2e/capabilities_spec.rb` + `spec/integration/real_rails_spec.rb`
  run explicitly with local `rails 8.1.3.1`/`sqlite3 2.9.6`, at the
  base and again on the change set: **73 examples, 0 failures** both
  times — non-zero, matching 027's recorded count, so the suites ran
  rather than skipped.
- 2 pending both runs: `release_artifacts_spec`'s two tag-dependent
  examples, self-skipping in a tagless clone with the reason printed.
- The new unusable-directory example was watched failing in both
  directions: against the un-rescued `ensure_directory` (the error
  propagates) and against an `ensure_directory` that skips `mkdir_p`
  (the cache wrongly activates; caught by the example's `enabled?`
  assertion). The old fixture's failure under root was itself observed
  at baseline (seed 38500).
- `ruby scripts/check_site_links.rb`: red before (both index badges),
  green after.
- `find lib spec -name '*.rb' -print0 | xargs -0 -n1 ruby -c`: clean.
- `gitleaks` 8.24.3 over the working tree (`gitleaks dir`, repo config)
  and over the outgoing commit range (`gitleaks git --log-opts
  c4c0be2..HEAD`): no leaks, both before the first push and re-run at
  the end of the review loop. CI's gitleaks-action re-scans the full
  history on every PR/push besides.

## Two preparations, one release

After this branch's loop closed, a second 0.2.3 surfaced:
`origin/fix/0.2.3`, 25 commits on the same base — **the renumbered
original**. It is the branch 027 meant by "the work that was being
prepared as 0.2.2 … renumbered to 0.2.3": paused 2026-08-09 on what was
then thought a hardware fault (the store_spec sweep, as 027 records),
renamed from `fix/0.2.2` on 2026-08-11, resumed after 0.2.2 shipped,
and carrying the maintainer's own review rounds 32–40 (numbering
continuing 0.2.1's loop) plus engine work: cache-sweep tolerance and a
background sweep, `VendorBootstrap` (a real ABI-mixing defect in
`bin/ovallsp`), a vscode handshake rework, a Ruby 4.0 CI job.

**Why two preparations existed.** This branch's session started from
`main`, where 027 says the work "continues in
`028-0.2.3-review-loop.md`" — a file that existed only on
`fix/0.2.3`, which nothing on `main` named. The pointer pointed at a
file, not at a branch, so a session that could not see the branch
rebuilt the release from the pointer. The rule that prevents the
recurrence is codified with this release (CLAUDE.md, AGENTS.md, the
map): **work in progress lives on a pushed branch, and the record on
`main` names the branch**; starting or resuming a release begins with
`git fetch --all` and reading the highest-numbered task file on every
branch that names the release, not only on `main`.

**Their loop's final state, in its own words.** Rounds 37–40 found 8,
12, 9, 11 — "The count is not falling … This release is 61 files …
what is growing is the *repair surface*. That is a scoping question
and it belongs to the maintainer." Two pieces are recorded there as
unpinned rather than fixed (the `bin/ovallsp`→`VendorBootstrap` call;
the extension wiring). The loop was stopped there, without a clean
round.

**The maintainer's convergence decision (2026-08-17):** the unified
0.2.3 is this branch plus `fix/0.2.3`'s converged, current-tree parts;
the engine thread stays on `fix/0.2.3` and becomes **0.2.4**. The
criterion applied is the maintainer's own: combine what is
demonstrated to work; what its own record does not yet call
demonstrated continues under its own number.

**What was imported, all verified against this tree before adoption**
(each spec run green here; behavioural claims probed):

- `documented_counts_spec`'s glob fix — the guard had **never run**
  anywhere gems are vendored (`core/**` glob caught `diff-lcs`' spec
  files; CI's `bundler-cache: true` layout included), so the audited
  figures were unguarded on every CI run to date — plus ci.yml's
  fail-if-skipped step (024.59).
- `spec_constants_spec` — no two spec files may define the same
  top-level constant; the class of defect struck twice in the 0.2.x
  work (024.60's sibling; the entry is their round 34's).
- `server_publish_invariant_spec` — the property that outlived their
  rolled-back debounce: the last publish for an open document
  describes its current text, a closed document's are empty.
- `server_views_spec`'s agreement example — hover and completion asked
  about one template `@ivar` must agree (024.63's countermeasure).
- The strengthened shadowed-literal pin in `query_service_spec`
  (`not_to include("upcase")` — the distinguishing assertion).
- `deferred_findings_spec`'s two new guards: a *resolved* entry still
  carrying a KL marker fails (the revert lesson mechanised), and
  duplicate entry numbers fail.
- `executableFixture` warming for the vscode unit suite (024.60's
  first-execution races; 175 passing here now).
- ci.yml: the `core-ruby-4` informational job (fails on zero examples
  — "a green suite is a measurement too").
- Register entries documenting the **current** tree: 024.50, 024.55,
  024.56 (with their KNOWN_LIMITATIONS sections, both languages),
  024.59, 024.60, 024.62, 024.63, 024.R9 — re-stamped where their
  `released-in: 0.2.2` named a branch that never shipped.
- Honesty fixes to published prose the current build disproves:
  `vscode/README` (no hard stop on a foreign Ruby; a version mismatch
  is reported, not fatal), the site's getting-started answer,
  `RELEASE_CHECKLIST` row 2, `SUPPORT_MATRIX`'s 4.0 row, the
  DOCUMENTATION_MAP trigger rows they widened.
- The roadmap⇔site parity check in `check_site_links.rb`, with the
  site roadmap pages it makes true (this branch's own site roadmap was
  measurably stale against `ROADMAP.md` — 0.3.0 showed 7 of 9 items).

**What stayed on `fix/0.2.3` for 0.2.4**, with its register entries
(their 024.49, .51–.54, .57, .58, .64, .65): the cache-sweep
behaviour changes and grace, `VendorBootstrap` and the `bin/ovallsp`
rewrite, the vscode handshake/toast rework, the thirty-day retention
prose (PRIVACY, site security), their changelog bullets describing all
of it, and their 028 — which should be renamed (029) when that branch
resumes. Known for that resume, relayed from this merge's analysis:
their tip's 024.41/024.45 paragraphs still describe the rolled-back
debounce as present; their 024.59's countermeasure paragraph describes
an in-suite guard their own rounds deleted; their `store_spec` still
carries the fabricated absolute path this branch's round 0 replaced —
their branch must take the unified fixture on rebase; and their
024.49 collides with this register's and renumbers then.

**Branch environment, recorded for the maintainer:** after this PR
merges, `fix/0.2.3` should be rebased onto the merged `main`,
renumbered to 0.2.4 (version files and its 028→029), and ideally
renamed (`fix/0.2.4`); the old name should not keep claiming a shipped
version. None of that is done from this branch — it edits nothing
outside itself.

**The unified tree, measured after the import:** full core suite
**1,943 examples, 0 failures, 2 pending** (the tag-dependent pair; the
three audited documents moved to 1,943 after a dry-run count and the
figure was then confirmed by the run — measure first, write second);
e2e + real-Rails explicitly, **73 examples, 0 failures**; vscode unit
**175 passing** (the +1 is the imported `platformCompatibility` probe
example) with the one documented environment failure; `check_site_links` green including the
imported roadmap-parity check, which was watched failing against a
deleted roadmap item before being trusted; both workflows parse;
gitleaks clean over tree and the full outgoing range.

## Review loop

Recorded per round, per `CLAUDE.md`. Instructions to every reviewer:
un-narrowed ("report anything you consider a defect, whether or not it
looks already known or deliberate; if a decision recorded as deliberate
is the wrong decision, say so"), plus "do not modify this repository",
plus the measured environment facts and the coverage list (what was
measured, at which state — never as an exclusion). Reviewers run
concurrently against a tree nothing mutates until the round's reports
are all in.

### Round 1 — three reviewers, eleven distinct findings, all fixed

Two reviewers independently drove a live Core over stdio and confirmed
every behavioural claim the rewritten documents make (twelve checks
each; all held). One re-derived the base-red/HEAD-green site check from
`git archive` copies and reproduced the store fixture's root failure
and both of its mutation directions. The change set's substance
survived; the findings, deduplicated across the three reports:

1. **The new CI job was inserted between the gitleaks comment and the
   job it documents** (found by all three). The 023.7 fetch-depth
   paragraph read as being about the site job. Jobs reordered; each
   comment sits above its own job again.
2. **The new guard was itself unpinned** — deleting the ci.yml job or
   the pages.yml paths left every check green, which is
   `ci_skip_guard_spec.rb`'s stated reason for existing.
   `site_version_guard_spec.rb` added; both examples watched failing
   against the respective deletion.
3. **"The check now runs on every push" overstated the trigger** (all
   three; ci.yml fires on pull requests and pushes to `main`).
   Corrected in the changelog (both languages), this file, and the
   ci.yml comment.
4. **`substitution?`'s qualified-name guard was unpinned** — one
   reviewer deleted the line in a scratch copy and ran the entire
   suite: 1,917 examples, 0 failures. The engine, its only caller,
   blanks qualified receivers one line earlier, so the module's
   documented contract held only as an emergent property of the sole
   caller — 0.2.2's containment lesson. Pinned by a direct unit spec
   with a fixture whose every other branch would answer true; the
   example fails with the line deleted. 024.47's inventory now carries
   it as the seventh leftover.
5. **024.48 described the rolled-back arrangement in the present
   tense**, and this change set's own edits (removing `signatures:`,
   re-tensing 024.47's tail) had sharpened the contradiction without
   touching the entry that states it. Re-tensed with a parenthetical;
   the entry's lesson is unchanged.
6. **`corpus_diagnostics.rb`'s rewritten comment claimed constructor
   equality with `Server#initialize` while two constructors below it
   diverged** (`model_registry:`/`generated_method_index:` absent from
   `MethodAnalyzer`, `observation_store:` from `LocalInferencer`).
   All three passed now, the way the comment instructs; a one-file A/B
   (stash/unstash, same corpus, foreground, sequential) came out
   byte-identical, confirming the divergence was inert.
7. **`DOCUMENTATION_MAP`'s version row still omitted the site badge**
   — the exact omission behind the 0.2.2 drift — and its "found by a
   person, nothing would have caught them" list still named the badge
   as unchecked while the paragraph above said the opposite; EN and JA
   also disagreed about whether index pages are covered. All three
   fixed, both languages.
8. **`AGENTS.md` pointed the current-loop pointer at "026 for 0.2.1"**,
   two releases stale, in the file agents read mid-handoff. Now 028.
9. **This file's own dangling forward references** — a gitleaks record
   "at the final gate below" that did not exist, and the trio sentence
   ("three published documents") resolving to a different trio than the
   changelog's. The gitleaks record is written out above; the trio
   sentence now names its members and counts the register
   contradiction separately.
10. **A pre-existing JA site sentence** stated the tested/verified
    distinction as the same predicate twice
    (`site/ja/getting-started.html`; outside the change set, fixed in
    place per the working agreement).
11. Considered and declined, recorded rather than silently dropped:
    one reviewer argued `AGENTS.md`'s stale pointer was out of scope
    (overruled — the map's staleness standard applies at release time);
    the same reviewer noted the store example's `not_to raise_error`
    line is near-vacuous alone (true, and deliberate — the example's
    teeth are the `enabled?` assertion and the constructed fixture,
    both mutation-verified).

After the fixes, measured so far: both new spec files green in
isolation (7 + 3 examples, each watched failing under its mutation
first); meta + coverage guards green; `check_site_links` green; vscode
unit 174/175 (the documented environment artifact); YAML of both
workflows parses; the corpus-script A/B byte-identical. The full-suite
re-run and the range gitleaks re-run land with the round's closing
commit — the number below is written after the run finishes, not
before.

Full-suite re-run, after the round's fixes: **1,927 examples, 0
failures, 2 pending** (`--order random`; the two pendings are the
tag-dependent pair throughout) — matching the three audited documents.
gitleaks re-run over the tree and the full outgoing range after the
round's commit: no leaks.

### Round 2 — one reviewer, one finding, and the same-place rule fires

One reviewer this round and from here on (the maintainer's
instruction: one per round unless several with genuinely different
centres would overlap little; round 1's three overlapped on four of
eleven findings). Two others had been started with identical briefs
and were stopped before reporting when the instruction arrived.

The reviewer re-reproduced the round-1 record (suite counts, live-Core
probes of every rewritten claim, all five mutation checks, dead-code
completeness, cross-document consistency, the previous-version grep,
gitleaks) and reported **one defect**: the claim "the check was red on
`main` for five days", stated in four files, is attached to the wrong
fact. Verified against GitHub's push and deploy history and reproduced
locally (the check is green at the 0.2.1-merge tree, red at the 0.2.2
tree): the redness on `main` began with 0.2.2's push, not five days
earlier; five days is the publish-before-push gap between the
Marketplace and the repository, which no in-repo check can see. (This
round's own restatement — a completed "under 21 hours" — was itself
corrected by round 3; see below and 024.49.) The timeline — and the
untreated half it exposes — is in "A guard that could not see its
input" above.

Same-place accounting: round 1 had already corrected these comments'
trigger claim ("on every push"), so a second consecutive round finding
the same comments wrong fired the same-place rule. The countermeasure,
not a third hand fix: the dated narrative was deduplicated out of the
three shipped files (ci.yml, pages.yml, the guard spec's header) into
this section alone, with the shipped files keeping only the ageless
mechanism sentence and a pointer. A fact restated in four places was
wrong in three of them, which is the map's opening lesson arriving in
this release's own additions.

Two reviewer observations, recorded: the new CI job's first real
execution in GitHub Actions will be the release PR (this branch has no
PR, and ci.yml fires on PRs and pushes to `main` — locally the script
passes and the wiring is spec-pinned); and rounds must not overlap
their own measurements — two reviewers' `rspec` runs briefly shared
the real-Rails fixture during round 1's tail, the concurrent-
measurement shape `CLAUDE.md` warns about. One reviewer per round also
retires that risk.

### Round 3 — one reviewer, three findings, and the roll-back threshold

The reviewer reproduced the full round-2 record (suite, live-Core
probes, all mutation checks, dead-code completeness, versions, the
previous-version grep, gitleaks; plus a live fetch showing the
deployed badge still 0.2.1 against a Marketplace 0.2.2 — the
narrative's premise is real). Three findings:

1. **The corrected timeline still asserted a completed duration**
   ("red-on-`main` lasted under 21 hours") **for a condition that had
   not ended** — `main` is red until this release lands, and the claim
   was arithmetically stale by its own commit time besides. Third
   consecutive round on the same narrative, so the roll-back threshold
   fired: the claim *shape* was rolled back (witnessed, timestamped
   events only; no durations of unwatched conditions), and **024.49**
   is the entry the rule requires, naming all three attempts and why
   each was the wrong shape.
2. **The deploy-gating half of the site guard was unpinned** — the
   reviewer replaced the pages.yml check job's run line with an `echo`
   in a scratch clone and the whole meta suite stayed green while the
   deploy would publish unchecked. A fourth guard example now pins
   both links (the check job runs the script; the deploy `needs` the
   check), watched failing against each mutation separately.
3. **The corpus script's `SemanticContext` omitted `ancestry_registry:`**
   which the server passes — inert today (its one consumer requires an
   active Runtime Agent the script never has; the one-file A/B is
   byte-identical before/after), but the exact divergence class 024.48
   is about, in the file 024.48 is about. Fixed by passing it. This
   file's constructor parity has now produced findings in two
   non-consecutive rounds (1 and 3); if a fourth round finds another
   missing keyword, the same-place rule's answer is a shared
   construction the script and the server both call, not a fifth
   keyword.

Also in this round: the round-2 section's own restatement was
re-tensed (its "under 21 hours" is what finding 1 corrected), and the
guard-spec example count moves the audited suite figure to 1,928.

Full-suite re-run, after the round's fixes: **1,928 examples, 0
failures, 2 pending** (`--order random`; the tag-dependent pair) —
matching the three audited documents. `check_site_links` green; both
workflows parse; gitleaks re-run over tree and range after the round's
commits: no leaks.

### Round 4 — one reviewer, two findings, both prose

The reviewer re-measured the whole record independently (suite 1,928/0
with the documented-counts audit in-run; e2e + real-Rails 73/0; all
six mutation checks; live-Core probes with controls; a fresh base-vs-
HEAD corpus A/B over `net/`, byte-identical at 118 findings a side;
live fetches confirming the deployed badge still trails the
Marketplace). Findings:

1. **"The version badge used to head this list" was false** — at the
   0.2.2 base the badge was the third of six items in
   `DOCUMENTATION_MAP`'s found-by-a-person list, not its head, in both
   languages. A round-1 sentence of this release's own; corrected to
   "used to be on this list" / 「この一覧にあり」.
2. **`pages.yml`'s PR-exclusion line (`if: github.event_name !=
   'pull_request'`) is deletable with every check green.** Considered,
   and deliberately not pinned — recorded here per the reviewer's own
   framing rather than silently dropped: the line predates this
   release and is deploy-publishing *policy* (the live site reflects
   `main` only), not part of the badge-honesty wiring this release
   added and pinned; round 3's `needs:` pin was the guard's own
   integrity, which is the distinction. Deleting it would still
   deploy only checked content (`needs: check` holds). Pinning every
   workflow line regresses without limit; the guard spec pins the
   mechanism it was built for.

Also from this round, one JA ambiguity tightened (`KNOWN_LIMITATIONS.ja`
now names `"hello".upcase` where "同じ位置" could be read as the
completion position), and one observation stands as written (the
timeline's 03:55–04:00Z entries are push times, per the cited push
history; the first commit's author time is 03:22Z).

Round close: prose-only delta, so the round was closed on the meta and
parity guards plus the site check; the full suite stands at round 3's
measured 1,928 and runs again at the final gate.

### Round 5 — one reviewer, three findings, all prose or dead code

The reviewer re-measured the whole record again (suite 1,928/0/2; e2e
+ real-Rails 73/0; all six mutations; live-Core probes; a fresh
base-vs-HEAD corpus A/B over stdlib `net/`, byte-identical at 118
findings a side; the base fixture's root failure and its litter
reproduced and cleaned; live fetch of the still-trailing deployed
badge). Findings:

1. **A second dead e2e client helper** — `LspClient#stderr_output`,
   no caller anywhere (the `@stderr_thread` that fills its ivar is
   live and stays; it drains the child's stderr pipe). Pre-existing;
   removed, per the fix-in-place agreement, exactly as its
   `document_highlights` twin was.
2. **"Six comments rewritten" was inexact for two of the six** — the
   `hierarchy_index.rb` `initialize` note and the `server.rb` ordering
   note were deleted with their dead subjects, not rewritten. The four
   places stating it (024.47's inventory, both changelogs, this file's
   table) now say four-rewritten, two-deleted-with-their-subjects.
3. **The timeline understated its own fix by one commit** — the check
   is green at 3f9babd itself (measured from an archive this round),
   not merely from 15f6d76 onward. The bullet now credits the witnessed
   commit.

Round close: the `lsp_client.rb` change touches the e2e harness, so
the two real-Rails-backed files were re-run explicitly — 73 examples,
0 failures — plus the meta and parity guards and the site check; the
example count is unchanged at 1,928.

### Round 6 — one reviewer, four findings, one of them in scope's centre

The reviewer closed the coverage gap themselves (full suite at the
round-5 tree: 1,928/0/2) and re-measured the record end to end.
Findings:

1. **The literal-side sentence was false as bound** — "while your
   workspace contains such a class [`Billing::Range`…], `"hello".`
   completes to that class's members" reads as if any core-named
   namespaced class poisons every literal. Measured both directions:
   with only `Billing::Range` indexed, `"hello".` completes String
   (upcase offered, `tag` not); the cost falls on **a literal of the
   shared name** — `(1..5).` offers `Billing::Range`'s `tag` and not
   `each`, while signature help for `(1..5).each` answers from RBS.
   Re-verified independently before rewording. Corrected in both
   limitation documents, both 0.2.3 changelog bullets, and 024.47,
   using the section's own Range pair; the unshared-literal boundary
   is now stated too. This was the release's centre — a published
   claim the build does not honour — introduced by the release's own
   rewrite; the round-1-established live-probe practice caught the
   receivers it probed, and nobody had probed a literal of an
   *unshared* name against the sentence's quantifier until this round.
2. **Round 5's fix left `@stderr_output` assigned-never-read** — the
   reader was removed, the write stayed. Same place, consecutive
   rounds, so the same-place rule's first stage applies: the fix
   removes the *state* rather than re-pointing it (`Thread.new {
   @stderr.read }` — the drain owns nothing), so the place can no
   longer hold half a removal. `@stderr_thread`, also never read, went
   the same way.
3. **The supported-Ruby row's attestation was silently re-dated by the
   count bump** — "1,928 … actually run … under 3.4.7 and 3.4.5" named
   a run that has not happened anywhere (this container has no 3.4;
   the branch has no PR). 024.49's claim-shape, in a row that
   predates the release. The row now separates the audited count from
   the venue attestation and names where the 3.4 runs happen (release
   PR CI, pre-publish gate); 028's remaining-steps list carries it.
4. **This table had stopped tracking the loop after round 3** — rounds
   4–6's shipped changes now have rows.

Round close: the harness edit re-ran e2e + real-Rails explicitly (73
examples, 0 failures) plus meta/parity guards, deferred-findings guard,
and the site check; example count unchanged at 1,928.

### Round 7 — one reviewer, no findings; the gate's clean round

The reviewer re-verified every load-bearing claim independently rather
than from the record: full suite at the round-6 tree itself
(**1,928 examples, 0 failures, 2 pending**, with the run's JSON
confirming the skip-prone suites contributed 73 non-skipped examples
and `documented_counts_spec` compared rather than skipped); vscode
174/175 with the documented environment artifact; all six wiring/guard
mutations plus both store-fixture directions watched failing; live-Core
probes with controls of every rewritten behavioural sentence, including
round 6's re-bound quantifier in both directions and the
`…::String`-indexed branch; a sequential foreground base-vs-HEAD corpus
A/B over stdlib `net/`, byte-identical with the control category equal
at 101 a side; the 028 timeline reproduced from archives; gitleaks over
the working tree, the outgoing range, and the full history; the
previous-version grep; the DOCUMENTATION_MAP trigger walk row by row.

Verdict, quoted: "A full careful pass … found nothing I consider a
defect. … the round-reports-clean condition of the review gate is met
by this pass."

Three items the reviewer considered and declined to count, recorded so
they are not silently dropped: the shipped comments' shorthand
"triggers only on site/ changes" (the load-bearing half — a version
bump alone could never fire it — is exactly true and was reproduced);
DOCUMENTATION_MAP's revert row asking for a CLAUDE.md section 024.49
did not add (the same-place rule names the register entry as the
deliverable, and no CLAUDE.md sentence became false); and the
supported-Ruby row's residual grammatical ambiguity (the cell now
names the attestation as re-earned per release, which is the honest
best under the count's pinning).

**The loop closes here: seven rounds — 11, 1, 3, 2, 3, 4, 0 findings —
with the clean round required by `CLAUDE.md` observed, not waived.**
What remains for the release is environment-bound and listed in
"Verified" above: the release PR's CI (including this branch's first
real execution of the new site-consistency job and the 3.4.x matrix),
and the maintainer's Apple Silicon packaging, pre-publish gate, publish
approval, and post-publish SHA-256 row.

**The unification reopened the loop** — a clean round covers the tree
it reviewed, and the import changed the tree — so the rounds continue
below over the unified 0.2.3.

### Merge round 1 — one reviewer over the unified tree, five findings, all prose

The reviewer re-measured the unified tree end to end (suite 1,943/0/2;
e2e 73/0; vscode 175 + the documented environment failure; site check
green and watched failing against a deleted roadmap item; five
mutation checks; live-Core probes of every rewritten claim with
controls — including `(1..5).` offering `tag` and not `each`, and
`"hello".` completing String with only `Billing::Range` indexed) and
confirmed the substance holds. Findings, all fixed this round:

1. **The imported 4.0 prose said the CI job runs "on every push"**, in
   four places (both vscode READMEs, both SUPPORT_MATRIX rows) — the
   exact trigger overstatement round 1 corrected for the
   site-consistency job, re-imported. All four now say "every pull
   request and every push to `main`".
2. **024.59 and the imported spec comment stated the other branch's
   history as this tree's** — "skipped on every full run since it was
   written" is disproved by this branch's own record (its full runs
   compared; nothing is vendored under `core/` here), and "1,934" is
   the 0.2.4-bound branch's figure, uncredited; the drift was also the
   fourth of its kind, not the third. Both re-scoped to the vendored
   layout and attributed; `deferred_findings_spec`'s bare "Round 37"
   attributed likewise.
3. **The pre-0.2.1 "refuses / clear diagnostic instead of a degraded
   session" claim survived in the KNOWN_LIMITATIONS platform section
   (both languages) and the site's index callouts (both languages)** —
   contradicting the same document's own "Ruby version scope" section
   and the READMEs this release rewrote. All four now describe the
   probe path (runs unverified with an Output note, or an error naming
   the gems); the map's Ruby/platform row now names the index callout —
   the same row-omission shape the version badge once was.
4. **This file's vscode count credited the wrong example** — the +1 is
   the `platformCompatibility` probe example, not the core-side
   agreement example. Corrected.
5. **CLAUDE.md's exemplar named `#code_offsets`**, a method replaced by
   `#structural_tokens` inside 0.2.1's own loop and existing nowhere in
   the tree — an un-followable pointer in the mandatory instructions.
   Corrected to the method that ships.

Also recorded from this round, not counted: the `continue-on-error`
rendering of the 4.0 job deserves one look on the release PR; the
shared scratchpad is an "identical corpus" hazard for A/B runs (a
stale fixture from an earlier agent was found there — future runs use
a fresh `mktemp -d`); `spec_constants_spec`'s walker misses `::CONST =`
(ConstantPathWriteNode) and over-collects a constant inside
`class << self` onto Object where Ruby scopes it to the singleton —
correct on today's corpus, where neither form occurs.

### Merge round 2 — one reviewer, nine findings, and a family that fired the same-place rule

The reviewer re-measured the record end to end (suite 1,943/0/2 at the
round-1 tree; vscode 175 + the documented failure; the four
site-version-guard mutations; live-Core probes incl. `runVersionHandshake`
returning rather than stopping) and confirmed the substance holds. Nine
findings, all prose; all fixed this round:

1–2–6 were **one family — published stop/refusal claims the build
disproves** — surviving on site pages the honesty pass had covered one
card at a time: the index's handshake card ("stops and explains"), the
capabilities page's x86_64 cell ("is rejected"), and the security
page's "every protocol boundary … refused" quantifier, in both
languages each. Merge round 1 had fixed the same family's platform
callout on the same index pages, so the second consecutive round on
this family fired the same-place rule. The countermeasure, not a third
per-card fix: a **verb-level sweep** (`reject|refus|stop|拒否|停止|縮退`)
across every published page, classifying every hit — the three false
pairs fixed (index handshake, capabilities cell, security quantifier —
the security bullet now names the two Core-side boundaries where
refusal is true and states the Extension↔Core difference), the accurate
hits recorded as accurate (payload-refusal, rename-refusal, cache-root
refusal, trust degradation, resolver accept/reject). The map's
Ruby/platform row had already been widened twice one page at a time;
the sweep is the form that does not need a third widening.

The rest: **3** — the imported documented-count step landed between
024.16's comment block and the real-Rails step it documents (the
round-1 comment-adjacency shape at step level; reordered so each
comment sits above its own step). **4** — 024.R9's "nineteen files"
was wrong in number and membership on this tree; the paragraph now
prescribes the grep instead of carrying a count, and records that its
count was wrong on arrival. **5** — the JA limitations bullet pointed
at a section title that does not exist (「範囲」 for 「scope」).
**7** — "(Ruby 3.3+ ships both as default gems)" mis-stated rbs's
category (bundled, not default, on stock 3.3.6 — measured); both
languages now make the loadability claim without the category claim.
**8** — this file's note about `spec_constants_spec`'s walker was
backwards for `class << self` (it over-collects onto Object, not
misses; measured by running the walker). **9** — the changelog bullet
called 024.50 a "limitation entry"; it is `status: fixed` and the
imported guard forbids a KL marker for it — the bullet now separates
the fixed record from the two open pins.

Recorded, not counted: the roadmap pages mark 0.2.3 shipped from the
moment this merges, before the Marketplace publish — the mirror image
of the publish-before-push gap, inherent to deploy-on-merge and
accepted; getting-started's "fail loudly rather than degrade quietly"
read as "degradations are announced".

### Merge round 3 — one reviewer, two findings in one sentence, and the cards' third round

The reviewer re-measured everything again — including an engine-
neutrality corpus A/B (byte-identical, control equal at 101), eleven
mutation checks, live-Core probes with an argument-count control, and
the register's structural invariants — and found **two defects, both
in one sentence**: merge round 2's replacement text on the index
handshake cards claimed the mismatch error "names both versions" (the
build's notification names neither; the versions are Output-channel
reason lines), and the JA edit closed an em dash directly with 「。」.

That is the **third consecutive round** finding these cards wrong —
round 1's platform callout, round 2's "stops and explains", round 3's
overclaimed replacement detail — and round 2's verb-sweep
countermeasure could not see round 3's instance (no refusal verb in
it), which is the rule's definition of a countermeasure aimed at the
symptom. Per the rule, the thread is rolled back rather than hand-
fixed a third time. **Root cause:** a marketing card kept carrying
micro-claims about behaviour detail (what the error text says) that
must be re-verified against `extension.ts` on every edit. **The
rollback:** the cards now state only what does not need per-edit
verification — the exchange happens, a mismatch is reported and the
session keeps running, the specifics live in the Output channel, and
024.55 tracks the follow-through. No error-text claims remain on
either card; both fixes land in the same sentence replacement.

Four reviewer observations recorded, not counted: the two imported
spec files' own generic Object constants (the guard would catch any
actual collision; a census shows none); `SUPPORT_MATRIX`'s OS row
still attests from one machine (untouched by this change set;
packaging genuinely is an Apple Silicon step); the reason-guard is
satisfiable by an empty `>` note (no instance exists);
the README 4.0 rows omit the real-Rails-skip qualifier the matrix
carries (their own example-count sentence keeps them honest).

### Merge round 4 — one reviewer, five findings, and the entry the rule was owed

The reviewer re-measured the full battery on 69dbbd0 — suite
1,943/0/2 with the count-guard comparing, e2e + real-Rails 73/0
explicitly, vscode 175 plus the one environment failure, the site
check green at HEAD and red at the base, seven mutation checks each
failing exactly its pinned example, live-Core probes over stdio
confirming every rewritten `KNOWN_LIMITATIONS` behavioural claim with
controls, rollback-cleanup completeness, register structure, and
noreply-only commit metadata across the whole range — and reported
the substance holds. Five findings, all in prose or the register; all
fixed this round:

**1 — ten published sentences carried the claim-shape merge round 3
rolled a thread back over.** The platform callout on the index pages
("shows an error naming the gems", both languages) and the
`gem install prism rbs` sentences in `KNOWN_LIMITATIONS`,
`SUPPORT_MATRIX` and the READMEs (both languages each) attributed the
gem line to the error the user sees. The build's notification names
no gems and points at the Output channel, where the reason line does
(`extension.ts` on `compatible: false`;
`platformCompatibility.ts`'s reason text, the half its unit test
pins). Two adjudications of one claim-shape cannot both stand. Fixed
to the split the rollback drew — cards carry no error-content claims;
documentation states the notification → Output-channel split — and
the READMEs' adjacent no-Ruby sentence ("explaining what's wrong and
what to do rather than half-starting") rewritten to the build in the
same pass: the search falls back to plain `ruby`, that path's reason
carries no remedy, and the session still attempts to start (024.55).
Provenance measured against the base: four of the ten predate 0.2.3
on `main`, six were introduced by this change set's own honesty pass
and caught here. Same-place accounting: the platform callout was
found in merge round 1 (refusal verb) and now — non-consecutive, so
the counting rule does not fire; the fix is the rollback's
adjudication applied tree-wide, with the `gem install prism rbs` grep
as the sweep and every hit classified. Recorded as **024.66**.

**2 — merge round 3's rollback had no register entry**, and the rule
it invoked names the entry as the deliverable — a reading this
release had already applied once (024.49, its other rollback).
**024.66** now records the cards thread: the three attempts, the root
cause, the split the rollback drew, and round 4's extension of it.

**3 — `KNOWN_LIMITATIONS`' close-race bound was narrower than the
mechanism** (both languages): "only on a large file", where the real
precondition is a republish pass still running — a window that grows
with the *number* of open files, since the pass snapshots the open
list before starting and `#publish_diagnostics` never re-checks
openness — so twenty-odd ordinary tabs give a second-plus window with
no large file involved, by the document's own per-file figure. The
lead now bounds by the pass and names what the window grows with.
Round 6 of the main loop counted this claim-shape once before (a
bound in a limitation document the mechanism disproves at the
margin), at a different place.

**4 — 024.R9's opening counts were stale on the tree that ships
them**: "roughly 2,800 lines across fifty entries", measured 3,403/57
at HEAD against 2,727/48 at the base — accurate when written, made
~25% stale by this change set. Merge round 2 fixed the same entry's
files-count clause and left this one; non-consecutive rounds, and the
fix this time is the entry's own prescription: the sentence now names
the two commands instead of carrying the numbers, so it cannot go
stale a third time.

**5 — typo in 024.55**: "a integrity failure" → "an integrity
failure".

After the fixes, re-measured on this tree, one at a time: full suite
**1,943 / 0 / 2** (the register guards parsing 024.66 included), e2e
+ real-Rails explicitly **73 / 0**, `check_site_links.rb` **OK** (255
internal references across 11 pages), gitleaks tree scan clean.

Recorded, not counted, as the reviewer filed them: the `core-ruby-4`
fail-on-zero step sits inside `continue-on-error: true` and how
GitHub renders that at the PR-checks level deserves one look on the
release PR (already on the record); `check_site_links.rb`'s
`released_versions` filter matches only a title exactly equal to
"unreleased", so a future variant heading would count as released (no
instance today); the index cards cite 024.55 as bare text a site
reader cannot resolve (the traceability trade the rollback made
deliberately); getting-started's "fail loudly rather than degrade
quietly" preamble (adjudicated in merge round 2, left standing); the
e2e stderr drain thread dies by IOError at `stop` under default
`report_on_exception` (pre-existing shape, no observable effect on
the 73/0 run).

### Merge round 5 — one reviewer, three findings, and the guards' own guard

The reviewer reproduced everything independently at 626d652 — suite
1,943/0/2 with per-file counts from the run's JSON, e2e + real-Rails
73/0 explicitly, vscode 175 + the one environment failure, the site
check green at HEAD / red at a c4c0be2 archive / green at a 3f9babd
archive, gitleaks over tree and range and `--all` metadata, an
engine-neutrality A/B on stdlib `net/` (byte-identical, 118 findings
a side, control equal at 101), the full mutation battery including
both new register guards and both directions of the store fixture
(reproducing the old fixture's root-littering at the base), 11/11
live-Core probes with controls, both Core-side protocol refusals in
source, the register's line/entry counts at all three revisions, and
a verb-level sweep of every published page classifying every hit —
and reported the substance holds. Three findings:

**1 — the two imported CI-only guards were themselves unpinned — the
release's own defect class, one level up.** Deleting ci.yml's
documented-count fail-if-skipped step whole, or the `core-ruby-4` job
whole, left every check in the repository green (verified by the
reviewer in a scratch clone: meta suite 72/0 both times) while the
published sentences resting on them stayed put — `SUPPORT_MATRIX`'s
4.0 row, both READMEs' "a non-gating CI job runs…", the changelog's
"actually runs now". Round 1's finding 2 established exactly this
class for the site job; these two arrived in the import round and
never got the same treatment. **Fixed here**: two examples appended
to `ci_skip_guard_spec.rb` — the step sliced by name and pinned to
its mechanism (`documented_counts_spec.rb`, the `"pending"`
predicate, `exit 1`), the job YAML-parsed and pinned to its decisions
(`continue-on-error: true`, runs the suite, reports `example_count`,
fails on `count.zero?`) — each watched failing under exactly its
deletion and passing on the restored tree. The suite grows 1,943 →
**1,945**, and the three audited documents moved with it in the same
commit, which is the count-guard mechanism working as designed.

**2 — ci.yml's site-consistency comment described its sibling
workflow falsely, in the present tense, in the release that made it
false**: "The deploy workflow that also runs it triggers only on
site/ changes" — untrue at HEAD, where pages.yml watches
`vscode/package.json` on both triggers (this change set's own fix),
making both halves of the sentence wrong of the shipped tree.
Rewritten: the 0.2.2 history in past tense, and the job's real
current justification stated — ci.yml is path-unfiltered, so it alone
catches drift introduced from outside pages.yml's watch list (a
roadmap or changelog edit, both inputs of the parity half). This also
corrects the record on round 7, which declined the same shorthand as
"exactly true": that was true of the pre-0.2.3 arrangement the round
reproduced from archives, not of the tree it was reviewing — the
recorded decline rested on a wrong tense.

**3 — seven register numbers are cited from the tree and resolve to
no entry** (`024.2`/.3/.7/.9/.12 from live source and spec comments,
`024.4`/.5 from both changelogs; entries deleted around
0.1.9–0.1.10, predating the legend's grep-before-delete rule, and
never re-checked once the rule existed). Verified by full
cross-reference: 58 entries vs 74 distinct number-shaped citations —
two of them synthetic fixture strings in the guard spec's own format
examples, excluded; of the real 72, the remainder is the legend's
documented reservations and gap.
Pre-existing at the base and **recorded, not fixed** — the loop runs
under fix-don't-add, the accurate fix means resurrecting seven
entries from history plus a guard, and the durable guard shape
belongs with 024.R9's register move. **024.67** (open, target 0.3.0)
carries the inventory and the direction; the round record here is not
the register entry, per the rule.

After the fixes, re-measured on this tree, one at a time: full suite
**1,945 / 0 / 2** with the count-guard comparing at the new figure,
e2e + real-Rails explicitly **73 / 0**, `check_site_links.rb` **OK**
(255 / 11), gitleaks tree scan clean, both workflows re-parsed.

Recorded, not counted, as the reviewer filed them: the `(S4; …)`
pointer in server.rb:2399 (its row went with the deferral, second
pointer resolves); pages.yml's paths omit the parity check's other
inputs (ci.yml's path-unfiltered job covers them — now stated in its
comment); the push-time bullets not independently verifiable this
session (committer times consistent); the `continue-on-error`
rendering question (already on the record for the release PR);
ja/getting-started's preamble (adjudicated twice); the roadmap
panels' plus-sign icon (site-wide style); `released_versions`'s exact
"unreleased" match (no variant exists); the stderr drain IOError
(recorded, no observable effect); KL's 0.2.0-era "refuses to start"
sentence (accurate of its era); `store_spec`'s near-vacuous
`.not_to raise_error` (round 1 item 11; its teeth verified by
mutation again this round).

### 0.2.4 re-scoped to design risk (maintainer's directive, during merge round 6)

While merge round 6 ran, the maintainer directed: 0.2.4's scope is the
places where the existing code's design is excessively complex or the
design itself harbors a bug class — whole-tree, not limited to what
0.2.x had planned — with 0.2.3's change set frozen as it stands, and
design-class findings from this loop routed to 0.2.4 rather than fixed
here. Accordingly, the four open design entries this loop touched —
**024.55**, **024.56** (0.3.0 → 0.2.4), **024.62**, **024.63** (no
target → 0.2.4) — now carry `target: 0.2.4`. 024.67 and 024.R9 stay
0.3.0: they are register/process work, not code design. The scope
itself is being drafted from a whole-code design audit (two rounds run
so far) and will be reviewed externally before it lands as a task
file; until then the engine thread's branch (`fix/0.2.3`, to be
renamed) and this record's dispositions section remain 0.2.4's
pointer, per "Where a release's work lives".

### Merge round 6 — one reviewer, four findings, and the pin that a comment could dodge

The reviewer reproduced the record at 189ae01 in full — suite
1,945/0/2 with per-file counts from JSON, e2e + real-Rails 73/0 with
zero skipped, meta 74/0/5, vscode 175 + the one environment failure,
the site check green at HEAD / red at a c4c0be2 archive / green at a
3f9babd archive, gitleaks over tree, range and full history, the
engine-neutrality A/B byte-identical with the control equal at 101,
eleven mutation checks, 11/11 live-Core probes, both Core-side
protocol refusals, and the timeline against commit metadata — and
found four defects, all fixed this round:

**1 — merge round 5's documented-count pin was text-based, and
commenting the step out dodged it.** The slice kept matching the
commented text while parsed YAML — what GitHub Actions executes —
no longer contained the step; the sibling core-ruby-4 pin in the same
commit read parsed YAML and caught the identical mutation. The pin
also never asserted the step lives in the `core` job whose
`tmp/rspec.json` it reads. **Fixed by rewriting
`ci_skip_guard_spec.rb` to locate steps in the parsed workflow**:
both the documented-count pin and — same pass, same blindness,
pre-existing — 024.16's own guard slice now resolve their step from
`jobs.core.steps` and assert on the located step's run text, plus an
order assertion (the count step must follow the full-suite step that
writes the JSON it reads). Watched failing in a scratch clone:
commenting out the documented-count step fails exactly its pin (8/1);
commenting out the 024.16 guard step fails the four examples that
read it (8/4); the deletion mutations from round 5 still fail.

**2 — the site-check chain bottomed out in an unpinned script.**
Gutting `check_site_links.rb`'s badge comparison, or the
roadmap-parity section this release added, left the script green, the
guard specs green, and the full meta directory green — CI, the deploy
gate and every spec passing with the countermeasure's logic deleted.
By the project's own rule the parity hunk was an unpinned behavioural
hunk of this change set. **Fixed with "the site check's own teeth"**
(`site_version_guard_spec.rb`): three examples that run the real
script against a copy of the shipped site — a pristine-copy control
that must pass, a badge mutated to 9.9.9 that must fail naming
package.json, and an item inserted into the first *planned* roadmap
panel (read from `ROADMAP.md`, so the fixture targets the compared
branch, not the shipped-panel branch) that must fail with the parity
message. The fixture is always a copy of the live site, never a
hand-built miniature, so the examples do not drift as the site grows;
copies live under `Dir.mktmpdir` per the containment rule. Watched
failing: gutting the badge comparison fails exactly the badge
example; gutting the parity section fails exactly the parity example.

**3 — 024.67's cross-reference arithmetic held only under an
unstated exclusion** (74 number-shaped citations, two of them
synthetic fixture strings in the guard spec's own format examples;
the entry said 72 without saying why). The entry now states the
exclusion, and prescribes re-running the cross-reference over
trusting its figures — 024.R9's own lesson, which merge round 4
applied to R9 and this entry's first draft then failed to apply to
itself. 028's merge-round-5 paragraph corrected to match.

**4 — a two-line ATX heading in `KNOWN_LIMITATIONS.md`** ("## Static
analysis limitations (not Apple-Silicon-specific, but worth\n
restating for a Preview)") rendered its second line as a stray body
paragraph. Pre-existing at the base, in a document this release
rewrote; joined to one line. The JA counterpart was already correct.

The suite grows 1,945 → **1,949** (one step-presence example, three
teeth examples), and the three audited documents moved with it in the
same commit. After the fixes, re-measured one at a time: full suite
**1,949 / 0 / 2**, e2e + real-Rails explicitly **73 / 0**,
`check_site_links.rb` **OK** (255 / 11), gitleaks tree scan clean,
both workflows re-parsed.

Recorded, not counted, as the reviewer filed them: the `(S4; …)`
pointer (previously adjudicated; concurred); the index cards' bare
024.55 citation (the rollback's recorded trade); RELEASE_CHECKLIST
row 1's vscode suites being the maintainer's gate; `store_spec`'s
teeth re-verified by mutation; `gitleaks detect` being deprecated but
working (ran clean over full history); pages.yml's paths residual
(ci.yml's path-unfiltered job covers it); 024.50's present-tense
symptom under a `fixed` block (register convention); the core-ruby-4
two-step JSON path divergence failing visibly rather than silently;
the walker's two documented blind spots reproduced exactly as
recorded; the roadmap pages marking 0.2.3 shipped at merge
(deploy-on-merge, accepted).

### Merge round 7 — one reviewer, three findings, all of them numbers the documents miscounted

The reviewer reproduced the record at 4fdf17e in full — suite
1,949/0/2, e2e + real-Rails 73/0, meta 78/0/5, vscode 175 + the one
environment failure, the dry-run count trajectory across four
revisions (1,917 → 1,928 → 1,943 → 1,945, every figure matching 028),
the site check at HEAD and both archives, engine-neutrality A/B
byte-identical (118/side, control 101), **fourteen** mutation checks
each failing exactly its pinned example (both comment-out directions,
both script-gutting directions, and the ten prior pins), live-Core
probes with controls, the register cross-reference re-run per
024.67's own prescription, gitleaks over tree/range/history with
noreply-only metadata, and DOCUMENTATION_MAP's own "seventeen files"
claim — and found three defects, all record/document count or deixis
errors, none in code, and no new design-class findings beyond what
the register already routes to 0.2.4. All fixed this round:

**1 — "Six more are older than this release" headed a list of
seven**, in both languages (`KNOWN_LIMITATIONS.md`, `.ja.md`). The
list has been seven since 0.2.0 added the loop-defined-methods bullet
without moving the count — the count-vs-its-own-list class this
release fixed four times elsewhere, surviving in the document it
rewrote. Recounted (seven `- **` bullets verified) and corrected.

**2 — 024.59 and the spec comment it explains both said the vendored
glob matched "twenty" diff-lcs spec files**; the layout both
sentences describe yields **ten** (diff-lcs 1.6.2 ships exactly ten
`*_spec.rb`; a real vendored install at this revision measured 125 =
115 + 10 under the old glob). The mechanism the number decorates is
correct and re-verified; the figure was wrong for the venue it names.
Both sentences now say ten, and the entry records that it arrived
saying twenty — a first draft of this correction speculated about
where twenty could be true (two vendored ABIs) and was cut back to
what was measured, per the measurement rule.

**3 — "Two are gone since the last release" was true when written
(0.2.0) and false at 0.2.3**, in both languages: the two fixes
shipped in 0.2.0 (024.24) and 0.1.14 (024.23), and "the last
release" now means 0.2.2. Stale deixis — the class the release
rewrote elsewhere — re-anchored to absolute versions ("fixed in
earlier releases, not this one"; 024.24 now carries "fixed in
0.2.0" inline).

Same-place accounting: all three are first findings about their
places; every watched place from rounds 4–6 (the documented-count
pin, 024.67's paragraph, the platform callout, R9's opening, the
site-consistency comment) held clean this round.

After the fixes, re-measured one at a time: full suite **1,949 / 0 /
2** (the count is unchanged — one spec comment and prose only), e2e
+ real-Rails explicitly **73 / 0**, `check_site_links.rb` **OK**
(255 / 11), gitleaks tree scan clean.

Recorded, not counted, as the reviewer filed them (abridged to the
non-repeats): `SUPPORT_MATRIX`'s preamble "showing an actionable
diagnostic" reads loose for the loadable-probe case (borderline
024.66 family; the table's own 3.3/4.0 rows state the precise
split); 028's round-1 heading arithmetic has a consistent reading
(item 9 contains two defects); the corpus script's parity hunks are
spec-unpinned but verified inert by the byte-identical A/B, with the
shared-construction countermeasure already named if the place
recurs; G12's row vs 024.56 is the re-scope directive working as
directed; the reviewer's own citation-count differing from 024.67's
(different file sets at different revisions) — the entry's
re-run-over-trust prescription handled it; and the standing
adjudicated items (S4 pointer, README 4.0 rows, maintainer's-machine
attestations, deploy-on-merge, exact-"unreleased" match, stderr
drain, the process-group environment failure).

### Merge round 8 — one reviewer, no findings: the clean round

The reviewer ran the full battery at 925e144 and reported **nothing it
considers a defect**. What that verdict rests on, in its own
reproduction: full suite 1,949/0/2 with the count-guard comparing and
the e2e/real-Rails counts taken from the run's JSON (57 + 16 = 73
non-skipped); vscode 175 + the one documented environment failure;
the site check green at HEAD, red at a c4c0be2 archive with exactly
the two badge lines, green at a 3f9babd archive; the dry-run
trajectory matching this record at every revision it names;
**sixteen** mutation checks each failing exactly its pinned example
and restored; engine-neutrality A/B from two archives over one
shared corpus, byte-identical at 118/side with the control equal at
101; live-Core probes of every rewritten `KNOWN_LIMITATIONS`
behavioural sentence with controls in both directions — including
the diagnostics silence on `Billing::Range` beside the reported
`Pricing::Tariff` typo; all eight `gem install prism rbs` sentences
attributing the line to the Output-channel detail, against source;
both Core-side protocol refusals; the base store defect reproduced
against correct code (root litter created and cleaned); the register
cross-reference finding exactly the seven dangling numbers 024.67
names; diff-lcs 1.6.2's ten spec files and the 125 = 115 + 10
arithmetic; `git ls-files site` = seventeen; EN/JA agreement in
every changed passage; patch semantics against `PUBLISHING.md`
(capabilities, publishing and roadmap untouched); gitleaks over
tree, the 21-commit range and the 221-commit history, noreply-only.

Seventeen considered-and-not-counted items were filed, every one a
previously adjudicated decision or a census-clean observation; the
round adds no new ones.

**The unification loop closes here: eight rounds, findings
5 → 9 → 2 → 5 → 3 → 4 → 3 → 0, with the clean round observed — not
waived** — the same standard the pre-unification loop met at its
round 7. Two same-place escalations ran their course inside the loop
(the verb-sweep countermeasure at round 2; the cards rollback at
round 3, recorded as 024.66), one countermeasure was itself repaired
one round later (the comment-out-blind pin, round 6), and the
maintainer's 0.2.4 re-scope landed mid-loop (round 6) without
widening the change set. What the release ships is what rounds 1–8
measured.

**Not verified by this loop, stated plainly** (the maintainer's
environment-bound gate, unchanged from the pre-unification list):
VSIX packaging and `test:integration:packaged` on Apple Silicon; the
`RELEASE_CHECKLIST` walk against the packaged artifact; the
Marketplace publish itself and the `RELEASE_ARTIFACTS` row it
produces; and the deployed site's first green `main` run, which this
merge itself triggers.

### Merge round 9 — the external review, and two guards that skipped their own case

After the loop closed, an external review (GPT-5.6 Sol, posted on the
release PR from the maintainer's account) reported no new runtime
regressions and two places where a mechanical check skips exactly the
case it exists for. Both verified here empirically before fixing, and
both fixed test-first:

**1 — the spec-constants walker was blind to a fourth spelling.**
Prism parses `::FOO = ...` and `Object::FOO = ...` as
`ConstantPathWriteNode` — not the `ConstantWriteNode` the walker
matched — and `class ::Foo`'s `constant_path` is a
`ConstantPathNode` the class branch's `ConstantReadNode` guard
skipped; all of these land on `Object`, so two spec files could
collide in that spelling with the census green. (The `::CONST =`
blindness had been on the record as a documented blind spot with no
instance in the corpus; the external review's point — a guard whose
purpose is syntax-independent prevention should not carry a known
syntax hole — stands, and the disposition changed from documented to
fixed.) Node shapes probed against the installed Prism first;
two distinguishing fixtures added and **watched failing** (5/2:
rooted and Object-prefixed assignments collected, `Ns::BAR =` and
`class Ns::In` still excluded); the walker now handles rooted and
`Object::`-prefixed path writes and class/module paths.

**2 — the `core-ruby-4` count step never ran on a red run.** Without
an `if:`, GitHub Actions applies the default `success()`, so the
step that exists to distinguish "failed" from "never started" by
count was skipped exactly when the suite failed or RSpec died before
writing the JSON. The step now carries `if: ${{ !cancelled() }}`
(not `always()` — a cancelled workflow should not fabricate a
report) and reports explicitly when `tmp/rspec-4.json` is missing or
unreadable, exiting 1 in both cases. The pin in
`ci_skip_guard_spec.rb` grew the matching assertions first and was
**watched failing** against the unfixed workflow (8/1).

The suite grows 1,949 → **1,951** (the two walker fixtures), and the
three audited documents moved with it in the same commit. After the
fixes, re-measured one at a time: full suite **1,951 / 0 / 2**, e2e +
real-Rails explicitly **73 / 0**, `check_site_links.rb` **OK**
(255 / 11), gitleaks tree scan clean, ci.yml re-parsed.

Same-place accounting: the count step's `if` is the same *file* as
round 5's pins and round 6's rewrite but a new defect class at a new
line (a skip condition, not pin blindness); the walker was last
touched by the import round and its blind spot was a recorded
observation, not a prior fix — neither place fires the counting rule.
Since the external round found defects, the gate applies as always:
one more independent round runs after these fixes, and the loop
re-closes only on a clean report.

### Merge round 10 — five findings, and the walker's second round in a row

The reviewer reproduced the record at 5f99c18 in full — suite
1,951/0/2 with JSON per-file counts, vscode 175 + the one environment
failure, both watched-failing numbers from round 9 re-reproduced (the
walker fixtures 5/2, the workflow pin 8/1), the engine-neutrality A/B
byte-identical with the control equal, sixteen-odd mutation checks,
ten live-Core probes with controls, the base store defect under root,
and the register cross-reference — and found five defects, none
engine-behavioural. All fixed this round:

**1 — `RELEASE_CHECKLIST.md` carried a section false at HEAD and in
direct contradiction with `SUPPORT_MATRIX.md`**: "Public
distributionの状態" still said no git remote, GitHub Actions never
run, Marketplace publish incomplete — each true when written,
pre-existing at the base, and unre-read after the repository went
public, while the same file's own gate row 21 records the
publication. Rewritten to the current state (public repository,
repository URL in package.json — with the nuance that the
`private: true` *field* remains and guards npm, not the Marketplace
— publishes recorded per release, Actions on every PR and push), with
the stale-when-unre-read history noted. The stale next-action #3
("Actions never executed") struck through to what remains true:
real-hardware Windows/Linux VSIX installs.

**2 — the spec-constants walker missed three more defining
spellings** (`MEMO ||=` — ConstantOrWriteNode; multiple assignment —
ConstantTargetNode/ConstantPathTargetNode; `::ROOTED ||=` —
ConstantPathOrWriteNode), one round after the external review's
rooted-write fix: **the same place two rounds in a row, which is the
counting rule's threshold — so the fix is a mechanical countermeasure,
not a third set of branches.** The walker now reads a
`CONSTANT_NODE_KINDS` classification table placing *every* constant
node kind Prism ships (reads; writes that cannot create a constant —
`&&=`/`+=` raise without a prior plain write; defining plain, path,
and path-target forms), and a new example reads Prism itself and
fails on any kind the table does not place — a Prism upgrade cannot
add a constant-defining spelling this census silently ignores.
Fixtures watched failing before the widening (7/1: `||=` and
multiple-assignment targets collected, `Ns::`-scoped and
non-defining forms excluded); the classification example verified by
mutation (removing a kind from the table fails 8/2, naming the kind).

**3 — the `target:` key was load-bearing but undocumented and
unguarded**: five entries route releases through it, the legend
defined every key but it, and the guard ignored unknown keys — so a
typo'd `taget:` would silently route nothing, the exact value-typo
class the `status` field defends against. The legend now documents
`target`, and `deferred_findings_spec` gains `KNOWN_KEYS` with a
format example (a planted `taget:` detected) and a real-file example;
verified by mutation — planting `taget:` on 024.55 in a scratch clone
fails the guard naming the entry and key.

**4 — a teeth example's title said "loses an item" while its fixture
inserts one**; title corrected to the fixture ("gains an item the
markdown does not plan"). The pinned mechanism is symmetric and was
already mutation-verified.

**5 — `vscode/README.ja.md`'s limitations link named the EN file
while targeting the JA URL** (pre-existing, byte-identical at base);
link text now names `KNOWN_LIMITATIONS.ja.md`.

The suite grows 1,951 → **1,956** (two walker fixtures, the
classification example, the two key-guard examples), and the three
audited documents moved with it — after a first sed carrying an
unmeasured 1,955 failed on a wrong working directory before writing
anything, and the number was taken from the dry run instead, which is
the measure-first rule earning its keep twice in one step. After the
fixes, re-measured one at a time: full suite **1,956 / 0 / 2**, e2e +
real-Rails explicitly **73 / 0**, `check_site_links.rb` **OK**
(255 / 11), gitleaks tree scan clean.

Recorded, not counted, as the reviewer filed them: the standing
adjudicated items (pages.yml PR-exclusion, store example's teeth,
process-group failure, tagless pendings, deploy-on-merge, exact
"unreleased" match, stderr-drain IOError, S4 pointer,
`class << self` over-collection — safe direction, SUPPORT_MATRIX
preamble wording, `continue-on-error` rendering, RELEASE_CHECKLIST
having no `.ja` pair by construction, imported specs' census-clean
constants, the vendored-layout glob re-verified structurally).

Per the gate, the loop re-closes only on a clean report: one more
independent round follows.

### Merge round 11 — one finding: the key guard's own character class

The reviewer reproduced the record at 232a73f in full — suite
1,956/0/2 with JSON per-file counts, meta 85/0/5, vscode 175 + the
one environment failure, the site check at HEAD and both archives
(redoing one archive run after hitting the persisted-`cd` trap
CLAUDE.md records), the engine-neutrality A/B byte-identical with the
control equal (and a method note recorded: both sides load the outer
tree's `version.rb` once via the Gemfile `gemspec` directive —
verified symmetric and inert), **sixteen** mutation checks including
both round-10 countermeasures, **thirteen** live-Core probes with
paired controls (adding the parenthetical branch: with
`Serializer::Elements::String` indexed, `"hello".` offers the
workspace class's method and not `upcase`), the round-10 document
rewrites against reality, the register cross-reference, and hygiene
over tree/range/history — and found **one defect, in round 10's own
countermeasure**:

**The unknown-key guard filtered *parsed* fields, and the field
parser's grammar is `[a-z-]+`** — so `Target: 0.2.4` or
`user_visible: no` never parsed as a field at all and the guard was
blind to exactly the typo family it exists to catch: planting
`Target: 0.2.4` on 024.55 left the whole register suite green
(24/0) with the entry silently un-routed. By round 9's own standard —
a guard whose purpose is syntax-independent prevention should not
carry a known syntax hole — the same defect one level down. **Fixed
by flagging stray lines instead of filtering parsed fields**: any
non-blank, non-indented line in a yaml block that is not a
`KNOWN_KEYS` field is reported, whatever characters the typo used
(indented lines are the folded note's continuation). Two fixtures
watched failing against the round-10 guard (25/2: the mis-cased
`Target:` and a hand-built underscored `user_visible:`, which the
`entry` helper cannot express — its own underscore-to-hyphen `tr` is
why that slip is likely); the mutation re-run now fails naming
`024.55 (Target: 0.2.4)`.

The suite grows 1,956 → **1,957**, the three audited documents moved
with it. After the fix, re-measured one at a time: full suite
**1,957 / 0 / 2**, e2e + real-Rails explicitly **73 / 0**,
`check_site_links.rb` **OK** (255 / 11), gitleaks tree scan clean.

Recorded, not counted, as the reviewer filed them: the e2e harness's
`wait_until_ready` not recognizing `"ready-static"`, so a plain-Ruby
caller burns the timeout (pre-existing, harness-internal, no in-tree
caller affected — the reviewer's own probes paid it); getting-started's
"it tells you to install them" (subject is the extension, not the
error text; consistent with the 024.66 split); the standing
adjudicated items (the `class << self` safe direction, deploy-on-merge
roadmap timing, SUPPORT_MATRIX preamble wording, pages.yml
PR-exclusion, exact-"unreleased" match, stderr-drain IOError, tagless
pendings, the process-group failure); and the corpus A/B method note
above, recorded so the next reviewer does not rediscover it.

Same-place accounting: the guard was introduced in round 10 and
repaired here — the first repair of that place, the round-6 shape
(a countermeasure fixed one round after it shipped), not the
two-in-a-row escalation. The walker — round 11's chief watch item —
held clean. Per the gate, one more independent round follows.

### Merge round 12 — the entry grammar's readers, two rounds in a row: the countermeasure stage

The reviewer reproduced the record at eeacaa2 in full — suite
1,957/0/2 with JSON per-file counts, meta 86, vscode 175 + the one
environment failure, the site check at HEAD and both archives plus a
**live fetch** of the deployed site (still advertising 0.2.1 against
`main`'s 0.2.2 — the release narrative's premise, current as of that
session), the engine-neutrality A/B byte-identical with the control
equal, **seventeen** mutation checks, **fourteen** live-Core probes
with controls (extending the shadow-section evidence: a top-level
reopened `class Range` behaves identically to pure core, and the
receiverless-in-reopening shape still reports, 024.13's case), the
register cross-reference, and hygiene over tree/range/history — and
found **two defects, both in the register entry-grammar's readers**:

**1 — the round-11 stray-line guard skipped every indented line as
"continuation"**, so ` target: 0.2.4` — one leading space — was
neither parsed as a field nor flagged, and the entry silently lost
its routing through one character (planted on 024.55: 25/0, silent).
The guard's own comment ("whatever characters the typo used") was
disproved by the build.

**2 — both heading readers require a title**, so `## 024.99` with no
title is invisible to `ENTRY_HEADING` and `METADATA_BLOCK`
*symmetrically*: "parses every entry" compares two identical sets and
an open, uncited entry rides through every guard unchecked — verbatim
the failure this spec's header names as its reason to exist,
surviving for that one shape because both readers shared the
blindness.

**The counting rule fires: the entry-grammar readers were repaired in
round 11 and found wanting again in round 12 — the same place two
rounds in a row — so this round's fix is the mechanical
countermeasure, not a third hand-patch.** Two moves, each making the
readers' assumptions explicit instead of silent: the stray walk now
carries the grammar's one piece of state (an indented line is
continuation only while a folded `>` value is open; every other line
is a known field or a stray), and a loose pre-scan
(`unreadable_headings`) compares everything that *claims* to be an
entry heading against what the grammar parsed, breaking the
symmetric-blindness configuration. Fixtures watched failing against
the round-11 code (27/2); both of round 12's exact probes now fail in
the clone — the space-indented key naming `024.55 ( target: 0.2.4)`,
the appended title-less `## 024.99` naming the heading — and the
`Target:`/`taget:` plants still fail as before.

**And the escalation is armed, in writing: if a future round finds
these readers wrong again, the countermeasure was aimed at the
symptom too, and the rule's rollback stage applies — the whole
key-guard thread (rounds 10 → 11 → 12) comes out, the root cause
goes to the register, and the register-grammar guard re-scopes to
its own task.** The alternative reading — that round 11's rework was
already the countermeasure and this round should have been the
rollback — was considered and set aside deliberately: round 11 was
recorded at the time as the round-6-shaped first repair, and this
round's fix is the first that makes the readers' assumptions
mechanically total rather than patching one assumption. That
judgement is recorded here so the next round can hold it to account.

The suite grows 1,957 → **1,960** (the indented-key fixture, the
unreadable-heading fixture, and the real-file pre-scan example — a
first draft of this sentence said 1,959 from arithmetic and was
corrected from the dry run), the three audited documents moved with
it. After the fixes, re-measured one at a time: full suite
**1,960 / 0 / 2**,
e2e + real-Rails explicitly **73 / 0**, `check_site_links.rb` **OK**
(255 / 11), gitleaks tree scan clean.

Recorded, not counted, as the reviewer filed them: signature help
absent at a paren-less block call (trigger-context behaviour, no
document claims it); the top-level-reopening probes above (consistent
with "unaffected", now on the record as measured); the reviewer's
citation count differing from 024.67's (the entry's own
re-run-over-trust prescription, third occurrence); the push-time
bullets (not fetchable; committer times consistent, rounds 5–7's
disposition); and the standing adjudicated items re-confirmed where
cheap (pages.yml PR-exclusion, store teeth, `class << self` safe
direction, corpus-script hunks A/B-inert, getting-started preamble,
exact-"unreleased" match, stderr-drain, deploy-on-merge,
`private: true` documented, README 4.0 rows, environment failures).

Per the gate, one more independent round follows.

### Merge round 13 — the armed escalation fires: the key-guard thread rolls back

The reviewer reproduced the record at 0e7e8eb in full — suite
1,960/0/2 with JSON per-file counts, meta 89, vscode 175 + the one
environment failure, the site check at HEAD and the base archive plus
a live fetch (deployed site still 0.2.1 — the premise, still
current), the mutation pins re-run, ten live-Core probes with
controls (its own first probe run had three failures from its own
character arithmetic, corrected and re-run before believing
anything), the register cross-reference, EN/JA agreement, and
hygiene — and found **two defects, both in the register
entry-grammar's readers, both the round-12 class one state deeper**:

**1 — the stateful walk swallows an indented typo wherever a folded
note precedes it**: `folded_open` stays true from
`user-visible-note: >` to the next non-indented line, and 024.62 and
024.63 both end note → continuation → `target: 0.2.4` — so
space-indenting that line reads as continuation: not a field, not a
stray, routing silently gone (28/0; the same plant on entries with
no folded note fails naming them, isolating the hole). Twelve
entries carry folded notes — the exposure surface. Psych rejects the
identical text ("did not find expected key"): the guard was more
permissive than YAML at exactly the typo.

**2 — a heading indented 1–3 spaces renders as a real `h2` under
CommonMark and is invisible to all three column-0-anchored readers
symmetrically** — an open, user-visible, uncited entry rides through
every guard (28/0), while the identical entry without the leading
space fails the citation guard by name (28/1). One space is the
whole difference between guarded and invisible.

**This is the third consecutive round on the entry-grammar readers,
after round 12's armed escalation — so the rollback stage executes,
as written:**

1. **Rolled back:** the whole key-guard thread, rounds 10 → 12 —
   `KNOWN_KEYS`, `unknown_keys`, `unreadable_headings` and their six
   examples; `deferred_findings_spec.rb` restored to its
   pre-round-10 state (verified: the diff since 5f99c18 was purely
   additive — 131 lines by `--numstat`; a first draft said 132 from
   `grep -c '^+'`, which counts the `+++` file header, and round 14
   caught it — so the restoration removes exactly the thread). The
   suite returns 1,960 → **1,954**, and the three
   audited documents moved with it.
2. **The register entry is the deliverable:** **024.68** records the
   original hole, the three attempts and why each was the wrong
   shape, the root cause (every guard re-derives the hand-rolled
   grammar's assumptions; the supply of assumptions is the parser,
   not any patch), and the direction actually needed — parse the
   blocks with Psych and fail on its errors, one loose
   heading-shaped scan beside the strict reader — hosted with
   024.R9's register move, target 0.3.0.
3. **What survived, deliberately:** the legend's `target:`
   documentation line. It fixes the *undocumented* half of round
   10's finding, never failed a round, and removing it would
   recreate a recorded defect. Recorded in 024.68 so the partial
   survival is a written decision, not an accident.
4. **The reopened hole is on the record:** until 024.68's direction
   lands, a typo'd key in the register is once again caught by
   nothing. The four `target: 0.2.4` routings were re-read by eye
   against 028's re-scope section after the rollback — correct at
   this revision.

After the rollback, re-measured one at a time: full suite
**1,954 / 0 / 2** (the restored guard file's 22 examples green), e2e
+ real-Rails explicitly **73 / 0**, `check_site_links.rb` **OK**
(255 / 11), gitleaks tree scan clean.

Recorded, not counted, as the reviewer filed them: the `### 024.99` /
`##024.99` heading variants (visibly broken when rendered, folded
into finding 2 as adjacent shapes); number-typos inside the prefix
(any anchored scan must bound somewhere); the fold-marker
trailing-space false-positive direction (loud, no instance); and the
standing adjudicated items re-confirmed where cheap (the reason
guard's empty-note satisfiability, `wait_until_ready`, the
unparsable-file census blindness being unreachable through suite
load, deploy-on-merge, the site check's narrow OK message, the
comment-only lib hunks).

The loop resumes on the rolled-back tree: one more independent round
follows.

### Merge round 14 — one finding: one word in the rollback's own record

The reviewer verified the rollback itself and everything around it at
439b396 — the restoration byte-identical to 5f99c18 with the forward
thread purely additive and the reverse removal exact; no live
leftovers of the guard machinery anywhere (the grep-before-revert
rule followed this time); 024.68's empirical claims reproduced
(Psych rejects the indented-key text with "did not find expected
key"; the restored hand parser swallows it silently — the reopened
hole is real and recorded); a Psych audit of all 60 entry blocks
showing every block parses with only known keys and canonical
statuses (no live typo despite the reopened hole); the six
`target:` routings correct — seven `target:` *lines*, the seventh
being the legend's documentation example, which routes nothing; a
first draft of this sentence said "seven routings" and round 15
caught the conflation; suite 1,954/0/2 with the arithmetic
reconciling (28 − 6 = 22, meta 89 → 83); e2e 73/0; vscode 175 + the
one environment failure; the site check; seven live-Core probes with
a control; six guard mutations each failing exactly its pin; patch
semantics; and hygiene over tree and the 27-commit range — and found
**one defect: the round-13 record's "132 lines" is 131**. The 132
came from `grep -c '^+'` counting the `+++` file header — a
reproducible mis-measurement of a correct claim (the reviewer
re-derived the mechanism). Fixed to the `--numstat` figure, with the
mis-measurement's origin recorded inline, per the same standard the
record applied to itself in rounds 10 and 12.

The reviewer also recorded, per the measurement rule, that its own
first probe run had two defects (an unparsed fixture and an
off-by-one position), caught by its control and corrected before
believing anything — the loop's discipline working on the reviewer's
own side.

Recorded, not counted: the absent key-typo guard (024.68's recorded
deliberate state, currently unexploited per the Psych audit);
"twelve folded notes" being correct at its measured revision (HEAD
has 13 — 024.68's own note is the 13th); and the standing
adjudicated items (the S4 pointer with both halves resolving,
`wait_until_ready`, deploy-on-merge, `private: true`, the
environment failures, 024.67's re-run-over-trust).

After the fix, re-measured: full suite **1,954 / 0 / 2**, e2e +
real-Rails explicitly **73 / 0**, `check_site_links.rb` **OK**,
gitleaks tree scan clean. One more independent round follows.

### Merge round 15 — one finding: lines are not routings

The reviewer reproduced the record at 0fccbae in full — the rollback
end to end again (byte-identical restoration, numstat 131/0 and
0/131, the "132" mechanism re-derived, the reopened hole real with
both plants riding through the restored suite, Psych rejecting the
folded-note shape the hand parser swallows), an independent Psych
audit of all 60 register blocks, fourteen mutations each failing
exactly its pin, twelve live-Core probes with controls in both
directions, the engine-neutrality A/B byte-identical with the
control equal, the dry-run trajectory at four revisions, EN/JA
agreement, patch semantics, and hygiene over tree and the 28-commit
range — and found **one defect: the round-14 record said "seven
`target:` routings"; the register has six** (024.55/.56/.62/.63 →
0.2.4, 024.67/.68 → 0.3.0). The seventh `target:` line is the
legend's documentation example, which routes nothing — the very line
round 13 deliberately kept as documentation. The record's own
previous paragraph uses "routings" for routed entries, so the
sentence conflated lines with routings — the second instance of the
count-includes-a-non-member class (round 13's "132" was the first;
a first draft of this sentence said "third", conflating it with
rounds 10 and 12's one-word count errors, which were self-caught
arithmetic *undercounts* — the opposite mechanism, fixed from the
dry run before commit — and round 16 caught the conflation). Fixed
with the origin recorded inline.

One methodological note the reviewer filed for future readers: a
simplified Psych probe of an indented key *without* a preceding
folded note yields a different Psych error ("mapping values are not
allowed in this context") than 024.68's recorded "did not find
expected key", which is exact for the folded-note shape the entry
describes — recorded so a future reader testing the wrong shape does
not wrongly doubt the entry.

Recorded, not counted: the standing adjudicated items, re-confirmed
where cheap (the absent key guard as 024.68's deliberate state, KL's
0.2.0-era sentence, the S4 pointer with both halves resolving, the
loop-defined-methods bullet needing no entry citation by the
guard's grammar, the roadmap pages as parity-checked documentation,
RELEASE_CHECKLIST's single language by construction).

After the fix, re-measured: full suite **1,954 / 0 / 2**, e2e +
real-Rails explicitly **73 / 0**, `check_site_links.rb` **OK**,
gitleaks tree scan clean. One more independent round follows.

### Merge round 16 — one finding, in the round-15 paragraph

The reviewer reproduced the record at 388c56d — among its checks:
nineteen mutations each failing exactly its pin, including a
full-suite run against a mutated audited document failing exactly on
the count guard; thirteen live-Core probes with controls; the
rollback byte-identical again, with both reopened-hole plants riding
through green as 024.68 records and both Psych error shapes
reproduced; an all-blocks Psych audit; the engine-neutrality A/B
byte-identical on its own corpus slice; and a mechanism-chase that
*cleared* a suspect record figure (round 8's "221-commit history" is
exactly what gitleaks reports over all refs minus merges at that
revision's ref set) — and found one defect: the round-15 paragraph
called its finding "the third" instance of a class that has two
(rounds 10 and 12's one-word count errors were self-caught
arithmetic undercounts, the opposite mechanism). Corrected, with the
distinction now stated in the paragraph itself.

Three rounds running, the sole finding has been one derived word in
the newest record paragraph. The countermeasure applied from this
round on is in the writing, not the tree: a round record carries
measured numbers and plain description only — no novel derived
tallies or class arithmetic for the next round to audit. This
paragraph practises it.

Recorded, not counted, as the reviewer filed them: the changelog
lead's "three published documents" (scoped to the original trio,
internally consistent, left standing by fifteen rounds); its A/B
absolutes differing from the record's on a different corpus slice
(the property, byte-identical with control equal, is what
reproduces); 024.67's stamped figures drifted at HEAD per its own
re-run-over-trust prescription (the seven dangling numbers still
isolate exactly); and the standing adjudications, each re-confirmed.

After the fix, re-measured: full suite **1,954 / 0 / 2**, e2e +
real-Rails explicitly **73 / 0**, `check_site_links.rb` **OK**,
gitleaks tree scan clean. One more independent round follows.

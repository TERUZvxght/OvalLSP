{
  "name": "rslsp",
  "displayName": "Ruby Semantic LSP",
  "description": "Semantic Ruby/Rails language features backed by a standalone Core Language Server.",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": "limited",
      "description": "Static analysis (parsing, completion, definition) works in an untrusted workspace. The Runtime Agent, which executes the workspace's own Rails/Bundler code, only starts once the workspace is trusted."
    }
  },
  "activationEvents": [
    "onLanguage:ruby",
    "workspaceContains:Gemfile",
    "workspaceContains:**/*.erb"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Ruby Semantic LSP",
      "properties": {
        "rslsp.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable the RSLSP Core Language Server."
        },
        "rslsp.ruby.command": {
          "type": [
            "string",
            "null"
          ],
          "default": null,
          "description": "Deprecated synonym for rslsp.rubyExecutablePath, kept for compatibility. Explicit Ruby interpreter used to launch the Core Server, skipping the mise/asdf/rbenv/chruby/PATH search entirely."
        },
        "rslsp.rubyExecutablePath": {
          "type": [
            "string",
            "null"
          ],
          "default": null,
          "description": "Explicit Ruby interpreter used to launch the Core Server, skipping the mise/asdf/rbenv/chruby/PATH search entirely. When unset, RSLSP searches mise, asdf, rbenv, chruby, PATH, then (on Windows) common RubyInstaller locations -- see 'RSLSP: Show Environment Diagnostics' for what was actually picked and why."
        },
        "rslsp.server.path": {
          "type": [
            "string",
            "null"
          ],
          "default": null,
          "description": "Path to the rslsp Core Server entrypoint. Defaults to the bundled core/bin/rslsp script."
        },
        "rslsp.observation.testCommand": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "default": null,
          "description": "Command used by 'RSLSP: Run Tests with Type Observation', e.g. [\"bundle\", \"exec\", \"rspec\"]. Defaults to the Core Server's own default (bundle exec rspec)."
        }
      }
    },
    "commands": [
      {
        "command": "rslsp.observation.runTests",
        "title": "RSLSP: Run Tests with Type Observation"
      },
      {
        "command": "rslsp.observation.clearTypes",
        "title": "RSLSP: Clear Observed Types"
      },
      {
        "command": "rslsp.observation.showEvidence",
        "title": "RSLSP: Show Type Evidence"
      },
      {
        "command": "rslsp.restartServer",
        "title": "RSLSP: Restart Server"
      },
      {
        "command": "rslsp.restartAgent",
        "title": "RSLSP: Restart Rails Agent"
      },
      {
        "command": "rslsp.showLogs",
        "title": "RSLSP: Show Logs"
      },
      {
        "command": "rslsp.showEnvironmentDiagnostics",
        "title": "RSLSP: Show Environment Diagnostics"
      },
      {
        "command": "rslsp.reindexWorkspace",
        "title": "RSLSP: Re-index Workspace"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -p . --watch",
    "test:unit": "npm run compile && mocha out/test/unit/**/*.js",
    "test:integration": "npm run compile && node ./out/test/integration/runTest.js",
    "test": "npm run test:unit",
    "copy-core": "node scripts/copy-core.js",
    "package": "npm run copy-core && npm run compile && vsce package"
  },
  "dependencies": {
    "vscode-languageclient": "^9.0.1"
  },
  "devDependencies": {
    "@types/glob": "^8.1.0",
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/test-electron": "^2.3.9",
    "@vscode/vsce": "^2.24.0",
    "glob": "^10.3.10",
    "mocha": "^10.3.0",
    "typescript": "^5.4.0"
  }
}
